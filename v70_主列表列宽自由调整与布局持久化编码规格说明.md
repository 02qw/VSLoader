# v70 主列表列宽自由调整与布局持久化编码规格说明

## 1. 需求背景

当前 VSLoader 主界面快捷项列表包含 4 列：

```text
名称 | 备注 | 原始模块名 | 更新时间
```

目前 `MainWindow.xaml` 中主列表 `DataGrid` 明确设置了：

```xml
CanUserResizeColumns="False"
```

同时 4 列使用固定比例宽度：

```xml
名称       Width="4*"
备注       Width="5*"
原始模块名 Width="5*"
更新时间   Width="4*"
```

这导致用户无法拖动列头调整列宽。  
当名称或原始模块名较长时，文本会因为列宽不够显示为：

```text
...
```

用户希望可以自由拖动这 4 列的列宽，并且把调整后的列宽保存到现有窗口布局配置文件中，下次启动自动恢复。

## 2. 当前源码结论

### 2.1 主列表位置

文件：

```text
C:\Users\shee_\OneDrive\Desktop\VSLoader\VSLoader\MainWindow.xaml
```

当前主列表：

```xml
<DataGrid x:Name="ShortcutsGrid"
          ...
          CanUserReorderColumns="False"
          CanUserResizeColumns="False"
          ...>
```

当前 4 列：

```xml
<DataGridTextColumn Width="4*" SortMemberPath="Name" />
<DataGridTextColumn Width="5*" SortMemberPath="Description" />
<DataGridTextColumn Width="5*" SortMemberPath="SourceModuleName" />
<DataGridTextColumn Width="4*" SortMemberPath="UpdatedAt" />
```

### 2.2 窗口布局配置文件

现有布局配置服务：

```text
C:\Users\shee_\OneDrive\Desktop\VSLoader\VSLoader\Models\Services\WindowLayoutService.cs
```

默认保存路径：

```text
%AppData%\VSLoader\window-layout.json
```

当前保存内容包括：

```text
1. 主窗口位置和大小
2. 地图窗口位置和大小
3. 地图窗口是否打开
4. 地图视图缩放/偏移
```

对应模型：

```text
C:\Users\shee_\OneDrive\Desktop\VSLoader\VSLoader\Models\WindowLayoutConfig.cs
C:\Users\shee_\OneDrive\Desktop\VSLoader\VSLoader\Models\RuntimeLayoutState.cs
```

### 2.3 现有保存机制

`MainWindow.xaml.cs` 中已有：

```csharp
ScheduleWindowLayoutSave()
```

该方法使用 500ms debounce：

```text
1. 先构造当前布局快照。
2. 取消上一次未完成保存。
3. 延迟 500ms。
4. 后台写入 window-layout.json。
```

本需求应复用这套机制，不新增独立保存文件。

## 3. 需求目标

完成后：

```text
1. 主列表 4 列允许用户拖动列头调整宽度。
2. 用户调整列宽后，列宽写入 %AppData%\VSLoader\window-layout.json。
3. 写入机制复用现有 500ms debounce 保存逻辑。
4. 下次启动程序时，自动恢复上一次用户调整后的列宽。
5. 不允许用户拖动改变列顺序。
6. 不改变排序、搜索、右键菜单、双击打开等现有功能。
7. 文本仍可保留省略号，但用户可以通过调宽列减少省略。
```

## 4. 非目标范围

本次不实现：

```text
1. 不新增单独的列宽配置文件。
2. 不增加“重置列宽”按钮。
3. 不允许拖拽改变列顺序。
4. 不保存列显示/隐藏状态。
5. 不做列宽按分辨率比例缩放算法。
6. 不修改批量新增识别、地图、AdminUI、WebUI 逻辑。
7. 不取消 TextTrimming=CharacterEllipsis。
```

## 5. 推荐方案

### 5.1 列宽保存格式

在 `WindowLayoutConfig` 中新增：

```csharp
public ShortcutGridColumnLayoutConfig? ShortcutGridColumns { get; set; }
```

新增模型：

```text
C:\Users\shee_\OneDrive\Desktop\VSLoader\VSLoader\Models\ShortcutGridColumnLayoutConfig.cs
```

字段：

```csharp
namespace VSLoader.Models;

public sealed class ShortcutGridColumnLayoutConfig
{
    public double? Name { get; set; }

    public double? Description { get; set; }

    public double? SourceModuleName { get; set; }

    public double? UpdatedAt { get; set; }
}
```

保存为像素宽度，例如：

```json
{
  "MainWindow": {
    "Left": 19.2,
    "Top": 102.6,
    "Width": 960,
    "Height": 670
  },
  "ShortcutGridColumns": {
    "Name": 260,
    "Description": 330,
    "SourceModuleName": 420,
    "UpdatedAt": 210
  }
}
```

选择像素宽度的原因：

```text
1. DataGrid 用户拖拽后的列宽天然是像素值。
2. 保存/恢复简单稳定。
3. 不需要复杂比例换算。
4. 当前窗口位置大小本身也是像素配置，同一文件风格一致。
```

### 5.2 RuntimeLayoutState 新增字段

文件：

```text
C:\Users\shee_\OneDrive\Desktop\VSLoader\VSLoader\Models\RuntimeLayoutState.cs
```

新增：

```csharp
public ShortcutGridColumnLayoutConfig? ShortcutGridColumns { get; set; }
```

用途：

```text
作为运行期布局状态的一部分，参与 BuildWindowLayoutConfigSnapshot() 生成保存快照。
```

### 5.3 启用列宽拖拽

文件：

```text
C:\Users\shee_\OneDrive\Desktop\VSLoader\VSLoader\MainWindow.xaml
```

修改：

```xml
CanUserResizeColumns="True"
```

保持：

```xml
CanUserReorderColumns="False"
```

说明：

```text
允许调宽度，但不允许改变列顺序，避免排序、保存映射、视觉结构复杂化。
```

### 5.4 给列设置最小宽度

建议给 4 列设置 `MinWidth`，避免用户拖得过窄导致列不可用：

```xml
<DataGridTextColumn Width="4*" MinWidth="120" SortMemberPath="Name" />
<DataGridTextColumn Width="5*" MinWidth="160" SortMemberPath="Description" />
<DataGridTextColumn Width="5*" MinWidth="180" SortMemberPath="SourceModuleName" />
<DataGridTextColumn Width="4*" MinWidth="150" SortMemberPath="UpdatedAt" />
```

推荐最小宽度：

```text
名称：120
备注：160
原始模块名：180
更新时间：150
```

### 5.5 列宽恢复时机

列宽恢复不能太早。  
如果在 `InitializeComponent()` 前或 DataGrid 列未完成初始化时恢复，会无效。

推荐时机：

```text
MainWindow_ContentRendered 中，ApplyDefaultWindowLayoutOnce 后，同步恢复列宽。
```

当前已有：

```csharp
private void MainWindow_ContentRendered(object? sender, EventArgs e)
{
    Dispatcher.BeginInvoke(new Action(ApplyDefaultWindowLayoutOnce), DispatcherPriority.ApplicationIdle);
}
```

建议调整为：

```csharp
private void MainWindow_ContentRendered(object? sender, EventArgs e)
{
    Dispatcher.BeginInvoke(new Action(() =>
    {
        ApplyDefaultWindowLayoutOnce();
        RestoreShortcutGridColumnWidths();
        AttachShortcutGridColumnWidthTracking();
    }), DispatcherPriority.ApplicationIdle);
}
```

注意：

```text
1. 先恢复窗口大小，再恢复列宽。
2. 恢复列宽期间不要触发保存。
3. 监听列宽变化必须在恢复完成后再挂。
```

## 6. 主窗口代码修改

文件：

```text
C:\Users\shee_\OneDrive\Desktop\VSLoader\VSLoader\MainWindow.xaml.cs
```

### 6.1 新增状态字段

新增：

```csharp
private bool _isRestoringShortcutGridColumns;
private bool _isShortcutGridColumnTrackingAttached;
```

用途：

```text
避免恢复列宽时误保存。
避免重复绑定列宽变化事件。
```

### 6.2 列宽映射

建议新增静态列字段顺序：

```csharp
private static readonly string[] ShortcutGridColumnKeys =
[
    "Name",
    "Description",
    "SourceModuleName",
    "UpdatedAt"
];
```

或者直接按 `SortMemberPath` 判断。

推荐用 `SortMemberPath`：

```text
因为当前 4 列的 SortMemberPath 正好是稳定字段名。
```

### 6.3 恢复列宽

新增方法：

```csharp
private void RestoreShortcutGridColumnWidths()
{
    var layout = _runtimeLayoutState.ShortcutGridColumns;
    if (layout is null)
    {
        return;
    }

    _isRestoringShortcutGridColumns = true;
    try
    {
        ApplyColumnWidth("Name", layout.Name);
        ApplyColumnWidth("Description", layout.Description);
        ApplyColumnWidth("SourceModuleName", layout.SourceModuleName);
        ApplyColumnWidth("UpdatedAt", layout.UpdatedAt);
    }
    finally
    {
        _isRestoringShortcutGridColumns = false;
    }
}
```

`ApplyColumnWidth`：

```csharp
private void ApplyColumnWidth(string sortMemberPath, double? width)
{
    if (width is null || width <= 0)
    {
        return;
    }

    var column = ShortcutsGrid.Columns.FirstOrDefault(column => column.SortMemberPath == sortMemberPath);
    if (column is null)
    {
        return;
    }

    column.Width = new DataGridLength(width.Value, DataGridLengthUnitType.Pixel);
}
```

### 6.4 捕获列宽

新增方法：

```csharp
private ShortcutGridColumnLayoutConfig CaptureShortcutGridColumnWidths()
{
    return new ShortcutGridColumnLayoutConfig
    {
        Name = GetColumnActualWidth("Name"),
        Description = GetColumnActualWidth("Description"),
        SourceModuleName = GetColumnActualWidth("SourceModuleName"),
        UpdatedAt = GetColumnActualWidth("UpdatedAt")
    };
}
```

`GetColumnActualWidth`：

```csharp
private double? GetColumnActualWidth(string sortMemberPath)
{
    var column = ShortcutsGrid.Columns.FirstOrDefault(column => column.SortMemberPath == sortMemberPath);
    if (column is null || column.ActualWidth <= 0)
    {
        return null;
    }

    return Math.Round(column.ActualWidth, 2);
}
```

### 6.5 监听列宽变化

WPF `DataGridColumn` 不是普通 `FrameworkElement`，不能直接像控件那样绑定普通事件。  
推荐使用 `DependencyPropertyDescriptor` 监听 `DataGridColumn.WidthProperty`。

需要引用：

```csharp
using System.ComponentModel;
```

当前 `MainWindow.xaml.cs` 已经有：

```csharp
using System.ComponentModel;
```

新增方法：

```csharp
private void AttachShortcutGridColumnWidthTracking()
{
    if (_isShortcutGridColumnTrackingAttached)
    {
        return;
    }

    var descriptor = DependencyPropertyDescriptor.FromProperty(
        DataGridColumn.WidthProperty,
        typeof(DataGridColumn));

    if (descriptor is null)
    {
        return;
    }

    foreach (var column in ShortcutsGrid.Columns)
    {
        if (IsShortcutGridPersistedColumn(column))
        {
            descriptor.AddValueChanged(column, ShortcutGridColumnWidthChanged);
        }
    }

    _isShortcutGridColumnTrackingAttached = true;
}
```

列判断：

```csharp
private static bool IsShortcutGridPersistedColumn(DataGridColumn column)
{
    return column.SortMemberPath is "Name" or "Description" or "SourceModuleName" or "UpdatedAt";
}
```

变化处理：

```csharp
private void ShortcutGridColumnWidthChanged(object? sender, EventArgs e)
{
    if (_isRestoringShortcutGridColumns)
    {
        return;
    }

    SaveShortcutGridColumnWidthsToSession();
}
```

### 6.6 保存列宽到 RuntimeLayoutState

新增：

```csharp
private void SaveShortcutGridColumnWidthsToSession()
{
    _runtimeLayoutState.ShortcutGridColumns = CaptureShortcutGridColumnWidths();
    ScheduleWindowLayoutSave();
}
```

说明：

```text
复用现有 500ms debounce。
```

### 6.7 关闭时可选立即保存

当前 `MainWindow_Closed` 中会调用：

```csharp
SaveWindowLayoutImmediately();
```

为了确保用户刚拖完列宽立刻退出时不丢失，建议在 `SaveWindowLayoutImmediately()` 前先同步捕获一次列宽：

```csharp
SaveShortcutGridColumnWidthsToSessionWithoutDebounce();
```

或者在 `BuildWindowLayoutConfigSnapshot()` 内读取当前列宽。

推荐方案：

```text
BuildWindowLayoutConfigSnapshot() 优先使用当前 DataGrid 实际列宽。
如果 DataGrid 尚不可用，再 fallback 到 _runtimeLayoutState.ShortcutGridColumns。
```

这样不需要额外保存方法，快照永远尽量最新。

## 7. Layout 配置读写修改

### 7.1 WindowLayoutConfig

文件：

```text
C:\Users\shee_\OneDrive\Desktop\VSLoader\VSLoader\Models\WindowLayoutConfig.cs
```

新增：

```csharp
public ShortcutGridColumnLayoutConfig? ShortcutGridColumns { get; set; }
```

### 7.2 RuntimeLayoutState

文件：

```text
C:\Users\shee_\OneDrive\Desktop\VSLoader\VSLoader\Models\RuntimeLayoutState.cs
```

新增：

```csharp
public ShortcutGridColumnLayoutConfig? ShortcutGridColumns { get; set; }
```

### 7.3 ApplyWindowLayoutConfigToRuntimeState

在 `MainWindow.xaml.cs` 中：

```csharp
private void ApplyWindowLayoutConfigToRuntimeState(WindowLayoutConfig config)
```

新增：

```csharp
_runtimeLayoutState.ShortcutGridColumns = config.ShortcutGridColumns;
```

建议只接受合理宽度：

```text
宽度 > 0 才恢复。
```

可在 `RestoreShortcutGridColumnWidths()` 时过滤。

### 7.4 BuildWindowLayoutConfigSnapshot

当前返回：

```csharp
return new WindowLayoutConfig
{
    MainWindow = ...,
    FactoryMapWindow = ...,
    WasFactoryMapOpen = ...,
    FactoryMapView = ...
};
```

新增：

```csharp
ShortcutGridColumns = CaptureShortcutGridColumnWidthsOrFallback()
```

建议实现：

```csharp
private ShortcutGridColumnLayoutConfig? CaptureShortcutGridColumnWidthsOrFallback()
{
    var captured = CaptureShortcutGridColumnWidths();
    return HasAnyColumnWidth(captured)
        ? captured
        : _runtimeLayoutState.ShortcutGridColumns;
}
```

`HasAnyColumnWidth`：

```csharp
private static bool HasAnyColumnWidth(ShortcutGridColumnLayoutConfig config)
{
    return config.Name > 0
        || config.Description > 0
        || config.SourceModuleName > 0
        || config.UpdatedAt > 0;
}
```

## 8. XAML 修改要求

文件：

```text
C:\Users\shee_\OneDrive\Desktop\VSLoader\VSLoader\MainWindow.xaml
```

### 8.1 开启列宽拖拽

修改：

```xml
CanUserResizeColumns="True"
```

保持：

```xml
CanUserReorderColumns="False"
```

### 8.2 设置列最小宽度

建议：

```xml
<DataGridTextColumn Width="4*"
                    MinWidth="120"
                    Binding="{Binding Name}"
                    Header="名称"
                    SortMemberPath="Name">
```

其他列同理：

```text
Description      MinWidth="160"
SourceModuleName MinWidth="180"
UpdatedAt        MinWidth="150"
```

### 8.3 不改变现有文本省略

保留：

```xml
TextTrimming="CharacterEllipsis"
```

原因：

```text
极端长文本仍可能超过列宽，省略号是合理兜底。
本需求的核心是允许用户拖宽列。
```

## 9. 保存时序要求

必须避免启动时误保存默认列宽覆盖用户列宽。

推荐顺序：

```text
1. InitializeComponent。
2. LoadWindowLayoutConfig，把配置加载进 RuntimeLayoutState。
3. ContentRendered/ApplicationIdle。
4. 恢复窗口位置。
5. 恢复列宽。
6. 挂载列宽变化监听。
7. 用户拖动列宽后，写入 RuntimeLayoutState 并触发 500ms debounce 保存。
```

禁止：

```text
1. 在恢复列宽之前挂列宽监听。
2. 在恢复列宽过程中触发保存。
3. 启动时把默认 4*/5*/5*/4* 立刻保存覆盖用户配置。
```

## 10. 测试方案

### 10.1 单元测试

建议新增或扩展测试：

```text
C:\Users\shee_\OneDrive\Desktop\VSLoader\VSLoader.Tests\WindowLayoutServiceTests.cs
```

测试内容：

```text
1. WindowLayoutService 可以保存并读取 ShortcutGridColumns。
2. 旧 window-layout.json 没有 ShortcutGridColumns 时可以正常读取。
3. 损坏 layout 文件仍按原逻辑备份并恢复默认。
```

示例：

```csharp
[Fact]
public void Save_and_load_preserves_shortcut_grid_column_widths()
{
    var root = Path.Combine(Path.GetTempPath(), "VSLoader.Tests", Guid.NewGuid().ToString("N"));
    var service = new WindowLayoutService(root);
    var config = new WindowLayoutConfig
    {
        ShortcutGridColumns = new ShortcutGridColumnLayoutConfig
        {
            Name = 240,
            Description = 320,
            SourceModuleName = 420,
            UpdatedAt = 180
        }
    };

    service.Save(config);
    var loaded = service.LoadOrCreateDefault(() => new WindowLayoutConfig(), out var warning);

    Assert.Null(warning);
    Assert.Equal(240, loaded.ShortcutGridColumns?.Name);
    Assert.Equal(320, loaded.ShortcutGridColumns?.Description);
    Assert.Equal(420, loaded.ShortcutGridColumns?.SourceModuleName);
    Assert.Equal(180, loaded.ShortcutGridColumns?.UpdatedAt);
}
```

### 10.2 编译验证

执行：

```powershell
cd C:\Users\shee_\OneDrive\Desktop\VSLoader
dotnet build .\VSLoader.sln -p:UseSharedCompilation=false
```

预期：

```text
Build succeeded.
0 Error(s)
```

### 10.3 全量测试

执行：

```powershell
cd C:\Users\shee_\OneDrive\Desktop\VSLoader
dotnet test .\VSLoader.sln -p:UseSharedCompilation=false
```

预期：

```text
所有测试通过。
```

### 10.4 人工验收

#### 场景一：列宽可拖动

步骤：

```text
1. 启动 VSLoader。
2. 鼠标移动到“名称”和“备注”列头之间。
3. 拖动分隔线。
```

预期：

```text
列宽可以变化。
```

#### 场景二：列宽持久化

步骤：

```text
1. 调宽“原始模块名”列。
2. 等待超过 500ms。
3. 退出程序。
4. 重新启动程序。
```

预期：

```text
原始模块名列保持上次调整后的宽度。
```

#### 场景三：位置和列宽同时持久化

步骤：

```text
1. 调整主窗口大小。
2. 调整列表列宽。
3. 等待超过 500ms。
4. 退出并重启。
```

预期：

```text
主窗口位置/大小和列表列宽都恢复。
```

#### 场景四：旧 layout 兼容

步骤：

```text
1. 使用旧版 window-layout.json，不包含 ShortcutGridColumns。
2. 启动程序。
```

预期：

```text
程序正常启动。
列宽使用 XAML 默认比例。
用户调整后会写入 ShortcutGridColumns。
```

## 11. 风险点

### 11.1 启动时误保存默认列宽

风险：

```text
如果监听列宽变化太早，启动渲染默认列宽时可能触发保存，覆盖用户配置。
```

规避：

```text
先恢复列宽，再挂监听。
恢复列宽期间使用 _isRestoringShortcutGridColumns 屏蔽保存。
```

### 11.2 星号宽度与像素宽度转换

风险：

```text
XAML 默认是 4*/5*/5*/4*，用户拖动后会变成像素宽度。
```

处理：

```text
这是符合需求的。
无保存配置时使用默认星号宽度。
一旦用户手动调整，则保存像素宽度并按像素恢复。
```

### 11.3 列宽过窄

风险：

```text
用户把列拖得太窄，导致显示不可读。
```

规避：

```text
设置每列 MinWidth。
```

### 11.4 配置文件损坏

风险：

```text
window-layout.json 损坏。
```

处理：

```text
沿用 WindowLayoutService 当前逻辑：备份损坏文件并恢复默认。
```

## 12. 验收标准

本需求完成必须满足：

```text
1. 主列表 4 列都可以拖动调整宽度。
2. 列顺序不能拖动改变。
3. 调整后的列宽写入 %AppData%\VSLoader\window-layout.json。
4. 保存机制复用现有 500ms debounce。
5. 重启后恢复上次列宽。
6. 旧 layout 配置没有列宽字段时正常启动。
7. 不影响主窗口/地图窗口位置持久化。
8. 不影响排序、搜索、右键菜单、双击打开。
9. dotnet build 通过。
10. dotnet test 通过。
```

## 13. 建议提交信息

```text
feat: persist shortcut grid column widths
```
