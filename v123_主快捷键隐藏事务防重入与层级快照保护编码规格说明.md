# v123 主快捷键隐藏事务防重入与层级快照保护编码规格说明

## 1. 背景

v121 引入了“主窗口/地图窗口最后活跃窗口”记录。

v122 进一步引入了“主快捷键隐藏/最小化前一刻的窗口层级快照”。

但用户实际验证后发现：

```text
地图窗口在前时，连续按两次主快捷键隐藏/恢复后，主窗口仍然可能跑到地图窗口上面。
```

这说明当前问题已经不是“是否有快照”，而是：

```text
快照在同一次隐藏过程中被后续事件二次覆盖。
```

因此本次需要把主快捷键隐藏流程设计成一个明确的事务，禁止 `StateChanged` 等事件在同一次快捷键隐藏流程中重复捕获快照、重复隐藏地图窗口。

## 2. 问题描述

当前主快捷键隐藏流程类似：

```csharp
private void ToggleWindowFromHotkey()
{
    SaveMainWindowBoundsToSession();
    CaptureForegroundSnapshotBeforeMainWindowHide();
    HideFactoryMapWindow();
    WindowState = WindowState.Minimized;
}
```

其中：

```csharp
WindowState = WindowState.Minimized;
```

会触发：

```csharp
private void MainWindow_StateChanged(object? sender, EventArgs e)
{
    if (WindowState == WindowState.Minimized)
    {
        SaveMainWindowBoundsToSession();
        CaptureForegroundSnapshotBeforeMainWindowHide();
        HideFactoryMapWindow();
        return;
    }
}
```

也就是说，同一次主快捷键隐藏可能发生两次快照捕获：

```text
第一次：ToggleWindowFromHotkey() 中，地图仍在前，快照正确。
第二次：MainWindow_StateChanged() 中，地图已经被 Hide，主窗口可能变成最后活跃窗口，快照被覆盖成错误值。
```

最终恢复时读到的是第二次错误快照，所以主窗口又压到地图窗口上面。

## 3. 根因分析

根因是窗口隐藏流程没有事务边界。

当前有多个入口都会执行“主窗口隐藏/最小化 + 地图同步隐藏”相关逻辑：

- 主快捷键隐藏：`ToggleWindowFromHotkey()`
- 用户点关闭到托盘：`MainWindow_Closing()`
- 主窗口状态变为最小化：`MainWindow_StateChanged()`

这些入口原本都是合理的。

但当主快捷键主动执行：

```csharp
WindowState = WindowState.Minimized;
```

它会触发 `MainWindow_StateChanged()`，导致同一轮隐藏流程被重复处理。

因此 v122 的快照字段虽然存在，但没有防止重复写入。

## 4. 目标

本次目标：

- 主快捷键隐藏流程必须成为单次事务。
- 同一次主快捷键隐藏中，只允许捕获一次层级快照。
- `WindowState = Minimized` 触发的 `MainWindow_StateChanged()` 不允许覆盖主快捷键刚捕获的快照。
- `MainWindow_StateChanged()` 仍然要支持用户手动点击最小化按钮的场景。
- 关闭到托盘仍然要捕获关闭前快照。
- 恢复时继续优先使用隐藏前快照决定主窗口/地图窗口谁在前。
- 不引入配置文件持久化。
- 不改变地图窗口业务功能、布局配置、缩放、节点和连线逻辑。

## 5. 非目标

本次不做以下事项：

- 不重构全局快捷键服务。
- 不引入 Win32 Z-Order API。
- 不改变主窗口和地图窗口的任务栏显示模式。
- 不修改地图快捷键打开/关闭地图的语义。
- 不改动窗口位置、大小、地图视口持久化逻辑。
- 不调整 UI 样式。
- 不把窗口层级写入 JSON 配置文件。

## 6. 推荐方案

### 6.1 新增主快捷键隐藏事务标记

在 `MainWindow.xaml.cs` 中新增字段：

```csharp
private bool _isMainWindowHotkeyHideInProgress;
```

含义：

```text
当前是否处在主快捷键主动触发的隐藏/最小化事务中。
```

该字段只用于阻止事件回调重复执行隐藏逻辑。

不写入配置文件。

### 6.2 主快捷键隐藏使用 try/finally 包裹

修改 `ToggleWindowFromHotkey()` 中的隐藏分支。

推荐结构：

```csharp
private void ToggleWindowFromHotkey()
{
    if (ShouldRestoreFromHotkey(IsVisible, WindowState == WindowState.Minimized, IsVsLoaderActive()))
    {
        RestoreAndActivate();
        return;
    }

    HideMainWindowFromHotkey();
}
```

新增方法：

```csharp
private void HideMainWindowFromHotkey()
{
    if (_isMainWindowHotkeyHideInProgress)
    {
        return;
    }

    _isMainWindowHotkeyHideInProgress = true;
    try
    {
        SaveMainWindowBoundsToSession();
        CaptureForegroundSnapshotBeforeMainWindowHide();
        HideFactoryMapWindow();
        WindowState = WindowState.Minimized;
    }
    finally
    {
        _isMainWindowHotkeyHideInProgress = false;
    }
}
```

关键点：

```text
CaptureForegroundSnapshotBeforeMainWindowHide() 必须在 HideFactoryMapWindow() 前执行。
```

### 6.3 StateChanged 对主快捷键事务防重入

修改 `MainWindow_StateChanged()`：

```csharp
private void MainWindow_StateChanged(object? sender, EventArgs e)
{
    if (WindowState == WindowState.Minimized)
    {
        if (_isMainWindowHotkeyHideInProgress)
        {
            return;
        }

        SaveMainWindowBoundsToSession();
        CaptureForegroundSnapshotBeforeMainWindowHide();
        HideFactoryMapWindow();
        return;
    }

    ShowFactoryMapIfNeeded();
}
```

这样：

- 主快捷键隐藏时，快照只由 `HideMainWindowFromHotkey()` 捕获一次。
- 用户手动点最小化按钮时，`_isMainWindowHotkeyHideInProgress == false`，仍由 `MainWindow_StateChanged()` 捕获快照并隐藏地图。

### 6.4 保留关闭到托盘的快照捕获

`MainWindow_Closing()` 不属于主快捷键隐藏事务。

保持：

```csharp
e.Cancel = true;
SaveMainWindowBoundsToSession();
CaptureForegroundSnapshotBeforeMainWindowHide();
HideFactoryMapWindow();
Hide();
```

如果后续发现关闭到托盘也触发额外状态事件，再单独引入关闭到托盘事务标记。

本次先不扩大范围。

### 6.5 恢复逻辑保持 v122 方案

恢复时继续先解析快照：

```csharp
private void RestoreAndActivate()
{
    Dispatcher.Invoke(() =>
    {
        var preferredForegroundWindow = ResolveForegroundWindowForRestore(
            _foregroundSnapshotBeforeMainWindowHide,
            _lastActiveVsLoaderWindow);

        RestoreMainWindowForHotkey();
        ShowFactoryMapIfNeeded();
        RestorePreferredForegroundWindow(preferredForegroundWindow);
    });
}
```

注意：

```text
preferredForegroundWindow 必须在 RestoreMainWindowForHotkey() 之前读取。
```

因为 `Show()`、`WindowState = Normal`、`Activate()` 都可能触发窗口激活事件。

### 6.6 最终激活仍由单一方法负责

继续保留：

```csharp
private void RestorePreferredForegroundWindow(VsLoaderForegroundWindow preferredForegroundWindow)
```

该方法是恢复流程最后唯一决定谁在前的地方。

禁止在 `ShowFactoryMapIfNeeded()` 末尾额外调用：

```csharp
Activate()
Topmost = true
```

否则会再次打破恢复顺序。

## 7. 边界情况

### 7.1 主快捷键隐藏触发 StateChanged

这是本次核心边界。

流程：

```text
ToggleWindowFromHotkey()
 -> HideMainWindowFromHotkey()
 -> CaptureForegroundSnapshotBeforeMainWindowHide()
 -> HideFactoryMapWindow()
 -> WindowState = Minimized
 -> MainWindow_StateChanged()
```

期望：

```text
MainWindow_StateChanged() 检测到 _isMainWindowHotkeyHideInProgress == true 后直接 return。
```

不得二次捕获快照。

### 7.2 用户手动点最小化

流程：

```text
用户点击主窗口最小化按钮
 -> MainWindow_StateChanged()
```

此时：

```text
_isMainWindowHotkeyHideInProgress == false
```

所以仍然要执行：

```text
保存主窗口位置
捕获层级快照
隐藏地图窗口
```

### 7.3 地图窗口关闭

如果地图窗口关闭：

```text
_factoryMapWindow == null
```

恢复时即使快照是 `FactoryMap`，也必须兜底主窗口。

继续由：

```csharp
ShouldActivateFactoryMapAfterHotkeyRestore(...)
```

判断。

### 7.4 地图窗口最小化

如果地图窗口被用户主动最小化，主快捷键恢复不应强行拉起地图。

快照只表达“隐藏前谁在前”，不能覆盖用户最小化地图的意图。

### 7.5 连续多次主快捷键切换

连续操作：

```text
地图在前
按主快捷键隐藏
按主快捷键恢复
按主快捷键隐藏
按主快捷键恢复
```

期望：

```text
每次恢复后地图仍在前。
```

关键是每次隐藏只捕获一次正确快照。

### 7.6 用户恢复后改变层级

流程：

```text
地图在前 -> 隐藏 -> 恢复
用户点击主窗口，让主窗口在前
再次隐藏 -> 再次恢复
```

期望：

```text
第二次恢复后主窗口在前。
```

因为第二次隐藏前捕获的新快照应覆盖旧快照。

## 8. 影响范围

主要修改：

```text
VSLoader/MainWindow.xaml.cs
```

测试修改：

```text
VSLoader.Tests/MainWindowFactoryMapCloseTests.cs
```

不修改：

```text
VSLoader/Views/FactoryMapWindow.xaml
VSLoader/ViewModels/MainViewModel.cs
VSLoader/Models/Services/*
VSLoader/Styles/*
layout 配置文件
workspace 配置文件
```

## 9. 编码要求

### 9.1 快捷键隐藏必须走单独方法

不要把隐藏分支继续散落在 `ToggleWindowFromHotkey()` 里。

应抽为：

```csharp
private void HideMainWindowFromHotkey()
```

这样事务标记的入口和退出更清晰。

### 9.2 事务标记必须 try/finally 释放

必须使用：

```csharp
_isMainWindowHotkeyHideInProgress = true;
try
{
    ...
}
finally
{
    _isMainWindowHotkeyHideInProgress = false;
}
```

避免中途异常导致状态永久卡住。

### 9.3 StateChanged 只在非快捷键事务中捕获快照

`MainWindow_StateChanged()` 中最小化分支必须先判断：

```csharp
if (_isMainWindowHotkeyHideInProgress)
{
    return;
}
```

不得先捕获快照再判断。

### 9.4 不扩大状态持久化

以下字段都只属于运行时状态：

```text
_lastActiveVsLoaderWindow
_foregroundSnapshotBeforeMainWindowHide
_isMainWindowHotkeyHideInProgress
```

不得写入配置文件。

## 10. 测试要求

### 10.1 新增防重入判断测试

新增静态方法：

```csharp
internal static bool ShouldHandleMainWindowMinimizedStateChanged(bool isMainWindowHotkeyHideInProgress)
{
    return !isMainWindowHotkeyHideInProgress;
}
```

测试：

```csharp
[Theory]
[InlineData(true, false)]
[InlineData(false, true)]
public void ShouldHandleMainWindowMinimizedStateChanged_skips_when_hotkey_hide_transaction_is_running(
    bool isMainWindowHotkeyHideInProgress,
    bool expected)
```

含义：

```text
主快捷键隐藏事务中，StateChanged 不能重复处理最小化。
非快捷键事务中，StateChanged 仍要处理用户手动最小化。
```

### 10.2 保留快照优先级测试

继续保留：

```csharp
ResolveForegroundWindowForRestore_prefers_snapshot_from_before_hotkey_minimize
```

确保：

```text
snapshot = FactoryMap
currentLastActive = Main
恢复选择 FactoryMap
```

### 10.3 保留无快照回退测试

继续保留：

```csharp
ResolveForegroundWindowForRestore_falls_back_to_current_last_active_when_snapshot_is_missing
```

### 10.4 人工验证

必须人工验证：

```text
场景 1：
地图窗口在前 -> 按主快捷键隐藏 -> 按主快捷键恢复
期望：地图在前。

场景 2：
地图窗口在前 -> 连续执行两轮隐藏/恢复
期望：每次恢复后地图都在前。

场景 3：
地图窗口在前 -> 恢复后点击主窗口让主窗口在前 -> 再隐藏/恢复
期望：主窗口在前。

场景 4：
用户点击主窗口最小化按钮
期望：地图随主窗口隐藏，并且恢复后层级符合最小化前状态。

场景 5：
关闭到托盘 -> 托盘恢复
期望：层级符合关闭前状态。
```

### 10.5 构建验证

执行：

```powershell
dotnet test .\VSLoader.Tests\VSLoader.Tests.csproj -c Debug --no-restore
```

执行：

```powershell
dotnet build .\VSLoader.sln -c Debug --no-restore
```

如果 Debug 输出目录被正在运行的 VSLoader 锁定，可以临时使用：

```powershell
dotnet test .\VSLoader.Tests\VSLoader.Tests.csproj -c Debug --no-restore -p:BaseOutputPath=.\artifacts\test-output\
dotnet build .\VSLoader.sln -c Debug --no-restore -p:BaseOutputPath=.\artifacts\test-output\
```

最终交付给用户体验前，需要覆盖正常 Debug 输出目录。

## 11. 验收标准

满足以下条件即完成：

- 主快捷键隐藏流程只捕获一次层级快照。
- `WindowState = Minimized` 触发的 `MainWindow_StateChanged()` 不会覆盖主快捷键隐藏快照。
- 地图在前时，连续多次主快捷键隐藏/恢复，地图都保持在前。
- 用户恢复后主动点击主窗口，下一次隐藏/恢复后主窗口保持在前。
- 手动最小化主窗口仍然能隐藏地图并捕获快照。
- 关闭到托盘仍然能隐藏地图并捕获快照。
- 地图关闭、不可见、最小化时仍兜底主窗口。
- 不新增配置文件字段。
- 相关测试通过。
- Debug 构建通过。

## 12. 风险与注意事项

### 12.1 不要把防重入做成全局禁止 StateChanged

防重入只针对：

```text
主快捷键主动隐藏事务
```

不能影响用户手动最小化。

### 12.2 不要在 StateChanged 中先捕获再判断

错误顺序：

```csharp
CaptureForegroundSnapshotBeforeMainWindowHide();
if (_isMainWindowHotkeyHideInProgress)
{
    return;
}
```

正确顺序：

```csharp
if (_isMainWindowHotkeyHideInProgress)
{
    return;
}
CaptureForegroundSnapshotBeforeMainWindowHide();
```

### 12.3 不要让 ShowFactoryMapIfNeeded 决定最终层级

`ShowFactoryMapIfNeeded()` 只负责显示地图和刷新内容。

最终谁在前必须由：

```csharp
RestorePreferredForegroundWindow(...)
```

统一决定。

### 12.4 如果仍然异常，应加临时调试日志

如果实现后仍有层级错乱，下一步不要继续猜。

应临时记录：

```text
ToggleWindowFromHotkey enter/exit
CaptureForegroundSnapshotBeforeMainWindowHide 捕获的值
MainWindow_StateChanged 是否被跳过
MainWindow_Activated
FactoryMapWindow_Activated
RestorePreferredForegroundWindow 最终选择
```

用日志确认到底是谁覆盖了快照或谁最后激活了主窗口。
