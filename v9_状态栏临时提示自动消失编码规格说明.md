# v9 状态栏临时提示自动消失编码规格说明

## 1. 需求背景

当前 VSLoader 主界面顶部状态栏已经用于显示配置异常提示，以及 AdminUI 打开后的操作结果提示。

现状问题是：  
AdminUI 密码复制成功、未配置 AdminUI 密码等一次性操作提示会一直停留在顶部状态栏，用户容易误以为程序仍然处于某种待处理状态。

本次 v9 需求需要对状态栏提示行为进行分类：

- 长期状态提示：一直显示，直到问题被解决。
- 临时操作提示：显示一段时间后自动消失。

## 2. 本次需求目标

在不改变主界面整体布局的前提下，优化状态栏显示行为：

1. 配置类异常状态继续长期显示。
2. 一次性操作结果提示自动消失。
3. 临时提示消失时不得误清除新的状态提示。
4. 多次快速触发临时提示时，以最后一次提示为准。

## 3. 适用范围

本次修改主要涉及：

- `VSLoader\ViewModels\MainViewModel.cs`
- 必要时可少量调整 `MainWindow.xaml`，但本需求不要求改 UI 样式。

不涉及：

- AdminUI 下载逻辑
- AdminUI `.jnlp` 文件打开逻辑
- 密码加密存储逻辑
- 设置窗口 UI
- VSCode 快捷项管理逻辑

## 4. 状态消息分类

### 4.1 长期状态提示

长期状态提示表示程序当前存在需要用户处理的配置问题，应一直显示。

包括但不限于：

- 配置文件读取失败。
- VSCode 路径未配置或无效。

当前对应逻辑主要在：

- `MainViewModel.UpdateStatusMessage()`

长期提示不应自动消失。

### 4.2 临时操作提示

临时操作提示表示某次操作的结果，只需要短暂告知用户。

包括但不限于：

- `AdminUI 已打开，密码已复制到剪贴板。`
- `AdminUI 已打开，但未配置 AdminUI 密码。`

当前对应逻辑主要在：

- `MainViewModel.OpenAdminUi()`

临时提示应在显示后自动消失。

## 5. 推荐交互规则

### 5.1 临时提示显示时长

默认显示时长建议为：

```text
3 秒
```

如后续体验需要，可将该值改为常量，例如：

```csharp
private static readonly TimeSpan TemporaryStatusDuration = TimeSpan.FromSeconds(3);
```

### 5.2 临时提示自动消失规则

临时提示显示后，等待指定时长。

等待结束后：

- 如果状态栏当前显示的仍然是本次临时提示，则清空状态栏。
- 如果状态栏已经被新的提示覆盖，则不做任何操作。

这样可以避免旧的延迟任务把新的提示误清除。

### 5.3 多次快速触发规则

如果用户短时间内多次点击 `AdminUI`：

- 每次点击成功后都可以刷新临时提示内容。
- 最后一次提示应重新开始计时。
- 旧的自动消失任务不能清除新的提示。

## 6. 推荐技术实现

### 6.1 保留长期提示方法

保留或调整当前的长期提示方法：

```csharp
private void ShowStatusMessage(string message)
{
    StatusMessage = message;
    HasStatusMessage = true;
}
```

该方法用于长期状态提示。

### 6.2 新增临时提示方法

新增异步方法：

```csharp
private async void ShowTemporaryStatusMessage(string message)
```

推荐实现逻辑：

1. 将 `StatusMessage` 设置为传入消息。
2. 将 `HasStatusMessage` 设置为 `true`。
3. 保存本次消息内容或版本号。
4. `await Task.Delay(TemporaryStatusDuration)`。
5. 延迟结束后判断当前提示是否仍是本次提示。
6. 如果仍是，则清空：

```csharp
StatusMessage = string.Empty;
HasStatusMessage = false;
```

### 6.3 推荐使用版本号防止误清除

建议在 `MainViewModel` 中增加字段：

```csharp
private int _statusMessageVersion;
```

每次显示任何状态提示时递增：

```csharp
_statusMessageVersion++;
```

临时提示方法记录当前版本号：

```csharp
var version = ++_statusMessageVersion;
```

延迟结束后仅在版本号未变化时清空：

```csharp
if (_statusMessageVersion == version)
{
    StatusMessage = string.Empty;
    HasStatusMessage = false;
}
```

长期提示也应递增版本号，避免之前的临时提示延迟任务清除长期提示。

## 7. 需要修改的代码点

### 7.1 `MainViewModel` 字段

新增：

```csharp
private static readonly TimeSpan TemporaryStatusDuration = TimeSpan.FromSeconds(3);
private int _statusMessageVersion;
```

### 7.2 `ShowStatusMessage`

修改为：

```csharp
private void ShowStatusMessage(string message)
{
    _statusMessageVersion++;
    StatusMessage = message;
    HasStatusMessage = true;
}
```

### 7.3 新增 `ShowTemporaryStatusMessage`

新增：

```csharp
private async void ShowTemporaryStatusMessage(string message)
{
    var version = ++_statusMessageVersion;
    StatusMessage = message;
    HasStatusMessage = true;

    await Task.Delay(TemporaryStatusDuration);

    if (_statusMessageVersion == version)
    {
        StatusMessage = string.Empty;
        HasStatusMessage = false;
    }
}
```

如实现时希望避免 `async void`，也可以使用返回 `Task` 的方法，但调用点需要合理处理 fire-and-forget。

### 7.4 修改 `OpenAdminUi`

将当前 AdminUI 成功类状态提示从：

```csharp
ShowStatusMessage("AdminUI 已打开，密码已复制到剪贴板。");
ShowStatusMessage("AdminUI 已打开，但未配置 AdminUI 密码。");
```

调整为：

```csharp
ShowTemporaryStatusMessage("AdminUI 已打开，密码已复制到剪贴板。");
ShowTemporaryStatusMessage("AdminUI 已打开，但未配置 AdminUI 密码。");
```

失败提示仍保留弹窗：

```csharp
_dialogService.ShowError(...);
```

## 8. 行为验收标准

### 8.1 AdminUI 密码复制成功

前置条件：

- 已配置 AdminUI 密码。
- 已存在对应 `.jnlp` 文件。

操作：

- 选中快捷项。
- 点击 `AdminUI`。

期望：

- `.jnlp` 被打开。
- 密码写入剪贴板。
- 顶部状态栏显示 `AdminUI 已打开，密码已复制到剪贴板。`
- 约 3 秒后该提示自动消失。

### 8.2 AdminUI 未配置密码

前置条件：

- 未配置 AdminUI 密码。
- 已存在对应 `.jnlp` 文件。

操作：

- 选中快捷项。
- 点击 `AdminUI`。

期望：

- `.jnlp` 被打开。
- 顶部状态栏显示 `AdminUI 已打开，但未配置 AdminUI 密码。`
- 约 3 秒后该提示自动消失。

### 8.3 VSCode 路径无效

前置条件：

- VSCode 路径未配置或路径无效。

期望：

- 顶部状态栏显示 `尚未配置有效的 VSCode 路径，请进入设置。`
- 该提示不自动消失。

### 8.4 配置文件读取失败

前置条件：

- `%AppData%\VSLoader\config.json` 被手动破坏。

期望：

- 启动时提示配置读取失败。
- 顶部状态栏显示配置读取失败信息。
- 该提示不自动消失。

### 8.5 快速连续点击 AdminUI

操作：

- 快速多次点击 `AdminUI`。

期望：

- 状态栏以最后一次点击产生的提示为准。
- 旧的延迟清理任务不会提前清除最后一次提示。

### 8.6 临时提示期间出现长期状态

场景：

- 临时提示显示期间，程序触发长期状态提示。

期望：

- 长期状态提示显示后不被临时提示的延迟任务清除。

## 9. 测试计划

实现后至少执行：

```powershell
dotnet build .\VSLoader.sln
```

期望：

```text
0 warnings
0 errors
```

人工验证：

1. 配置 AdminUI 密码后点击 `AdminUI`，确认顶部临时提示约 3 秒后消失。
2. 清空 AdminUI 密码后点击 `AdminUI`，确认未配置密码提示约 3 秒后消失。
3. 清空或设置无效 VSCode 路径，确认 VSCode 配置提示不会自动消失。
4. 快速连续点击 `AdminUI`，确认最后一次提示不会被旧任务提前清掉。

## 10. 注意事项

- 不要把所有状态栏提示都做成自动消失。
- 不要移除错误弹窗，错误弹窗仍用于失败场景。
- 不要改动密码保存和加密逻辑。
- 不要改动 AdminUI 下载、匹配、打开 `.jnlp` 的核心逻辑。
- 本次只处理状态提示的生命周期行为。
