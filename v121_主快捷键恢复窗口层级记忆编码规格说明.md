# v121 主快捷键恢复窗口层级记忆编码规格说明

## 1. 背景

当前地图窗口已经从早期的主窗口附属窗口，重构为带独立任务栏入口的窗口。

主窗口和地图窗口之间仍需要保留一个重要联动：

- 用户按主窗口全局快捷键时，可以让 VSLoader 主窗口和地图窗口一起隐藏。
- 用户再次按主窗口全局快捷键时，可以让 VSLoader 主窗口和地图窗口一起恢复。

现在的问题不是“能不能恢复”，而是恢复后的前后层级不符合用户上一次的真实使用状态。

典型场景：

```text
1. 用户打开主窗口和地图窗口。
2. 用户把地图窗口放到最大或最前面。
3. 用户切换到其他程序。
4. 用户按主窗口快捷键恢复 VSLoader。
5. 当前实际结果：主窗口在上，地图窗口在下。
6. 用户期望结果：恢复到切换前的人为布局，地图窗口仍然在上。
```

## 2. 问题描述

当前 `RestoreAndActivate()` 的恢复顺序固定为：

```csharp
Show();
RestoreMainWindowBoundsFromSession();
WindowState = WindowState.Normal;
Activate();
Topmost = true;
Topmost = false;
Focus();
ShowFactoryMapIfNeeded();
```

文件位置：

```text
VSLoader/MainWindow.xaml.cs
```

这个方法被多个入口复用：

```text
ToggleWindowFromHotkey()
托盘图标左键点击
托盘图标双击
托盘菜单“显示 VSLoader”
```

由于当前方法每次都先强制激活主窗口，并通过 `Topmost = true; Topmost = false;` 把主窗口顶到前面，所以即使地图窗口在用户切换前位于最上层，再次恢复时也会被主窗口压到下面。

## 3. 根因分析

根因不是地图窗口没有恢复，而是恢复过程缺少“本次 session 内最后活跃窗口”的概念。

当前程序只记录了：

- 主窗口位置和大小。
- 地图窗口位置和大小。
- 地图窗口是否打开。
- 地图窗口是否因为主窗口隐藏而临时隐藏。

但没有记录：

```text
主窗口和地图窗口中，用户最后实际操作的是哪一个窗口
```

因此 `RestoreAndActivate()` 只能按固定顺序恢复，无法判断恢复完成后应该把主窗口还是地图窗口放到最前。

## 4. 目标

本次目标：

- 主快捷键恢复 VSLoader 时，尽量恢复用户切换前的窗口前后关系。
- 如果切换前地图窗口在最前，恢复后地图窗口仍然在最前。
- 如果切换前主窗口在最前，恢复后主窗口仍然在最前。
- 如果地图窗口关闭、未创建、不可见或被用户最小化，则恢复后主窗口在最前。
- 不引入配置文件持久化。
- 不改变主窗口和地图窗口的位置、大小、缩放、地图视口等已有逻辑。
- 不破坏地图快捷键打开/关闭地图的功能。
- 不破坏托盘显示 VSLoader 的功能。

## 5. 非目标

本次不做以下事项：

- 不把窗口前后层级写入 `layout` 配置文件。
- 不记录跨程序重启后的窗口层级。
- 不实现复杂的 Windows Z-Order 持久化。
- 不修改地图窗口业务逻辑。
- 不修改地图节点、连线、右键菜单、滚动、缩放逻辑。
- 不修改主窗口快捷项列表、筛选、批量新增、更新检测等业务逻辑。
- 不改变地图窗口最小化到任务栏的现有行为。

## 6. 推荐方案

### 6.1 使用 session 内存记录最后活跃窗口

在 `MainWindow.xaml.cs` 中新增枚举：

```csharp
private enum VsLoaderForegroundWindow
{
    Main,
    FactoryMap
}
```

新增字段：

```csharp
private VsLoaderForegroundWindow _lastActiveVsLoaderWindow = VsLoaderForegroundWindow.Main;
```

这个字段只在程序本次运行期间有效。

不写入配置文件，原因是窗口前后层级属于临时交互状态：

- 用户这一次把地图放前面，不代表下一次启动也一定希望地图抢前台。
- 跨重启持久化层级容易制造困惑。
- 当前问题只发生在本次运行中隐藏/恢复 VSLoader 的链路上，用 session 内存即可解决。

### 6.2 通过 Activated 事件更新最后活跃窗口

主窗口初始化时订阅自身 `Activated`：

```csharp
Activated += MainWindow_Activated;
```

处理逻辑：

```csharp
private void MainWindow_Activated(object? sender, EventArgs e)
{
    _lastActiveVsLoaderWindow = VsLoaderForegroundWindow.Main;
}
```

地图窗口创建后订阅 `Activated`：

```csharp
_factoryMapWindow.Activated += FactoryMapWindow_Activated;
```

处理逻辑：

```csharp
private void FactoryMapWindow_Activated(object? sender, EventArgs e)
{
    _lastActiveVsLoaderWindow = VsLoaderForegroundWindow.FactoryMap;
}
```

地图窗口关闭时回退：

```csharp
_lastActiveVsLoaderWindow = VsLoaderForegroundWindow.Main;
```

这样可以表达：

```text
谁最后被用户激活过，谁就是下次主快捷键恢复后的优先前台窗口。
```

### 6.3 拆分 RestoreAndActivate 的职责

当前 `RestoreAndActivate()` 同时做了三件事：

```text
1. 恢复主窗口可见和位置。
2. 强制激活主窗口。
3. 恢复地图窗口。
```

建议拆成两个阶段：

```text
第一阶段：恢复主窗口和地图窗口的可见性，不最终决定谁在前。
第二阶段：根据 _lastActiveVsLoaderWindow 决定最终激活哪个窗口。
```

推荐结构：

```csharp
private void RestoreAndActivate()
{
    Dispatcher.Invoke(() =>
    {
        RestoreMainWindowForHotkey();
        ShowFactoryMapIfNeeded();
        RestorePreferredForegroundWindow();
    });
}
```

其中：

```csharp
private void RestoreMainWindowForHotkey()
{
    Show();
    RestoreMainWindowBoundsFromSession();

    if (WindowState == WindowState.Minimized)
    {
        WindowState = WindowState.Normal;
    }
}
```

最终激活逻辑放到：

```csharp
private void RestorePreferredForegroundWindow()
```

### 6.4 最终激活窗口的判断规则

推荐规则：

```text
如果最后活跃窗口是地图，并且地图当前可被激活，则激活地图。
否则激活主窗口。
```

地图可被激活需要同时满足：

```text
_isFactoryMapOpen == true
_factoryMapWindow != null
_factoryMapWindow.IsVisible == true
_factoryMapWindow.WindowState != WindowState.Minimized
```

推荐辅助方法：

```csharp
private bool ShouldActivateFactoryMapAfterHotkeyRestore()
{
    return ShouldActivateFactoryMapAfterHotkeyRestore(
        _lastActiveVsLoaderWindow,
        _isFactoryMapOpen,
        _factoryMapWindow is not null,
        _factoryMapWindow?.IsVisible == true,
        _factoryMapWindow?.WindowState == WindowState.Minimized);
}
```

可测试静态方法：

```csharp
internal static bool ShouldActivateFactoryMapAfterHotkeyRestore(
    VsLoaderForegroundWindow lastActiveWindow,
    bool isFactoryMapOpen,
    bool hasFactoryMapWindow,
    bool isFactoryMapVisible,
    bool isFactoryMapMinimized)
{
    return lastActiveWindow == VsLoaderForegroundWindow.FactoryMap
        && isFactoryMapOpen
        && hasFactoryMapWindow
        && isFactoryMapVisible
        && !isFactoryMapMinimized;
}
```

主窗口激活方法保留当前强制前台能力：

```csharp
private void ActivateMainWindow()
{
    Activate();
    Topmost = true;
    Topmost = false;
    Focus();
}
```

地图窗口激活方法：

```csharp
private void ActivateFactoryMapWindow()
{
    if (_factoryMapWindow is null)
    {
        ActivateMainWindow();
        return;
    }

    _factoryMapWindow.Activate();
    _factoryMapWindow.Topmost = true;
    _factoryMapWindow.Topmost = false;
    _factoryMapWindow.Focus();
}
```

注意：

- 激活地图前不应再次强制激活主窗口。
- 如果为了把应用整体带回前台必须短暂激活主窗口，则最后必须再次激活地图窗口。
- 不能在地图被用户最小化时强行恢复地图窗口，除非原有逻辑已经是因为主窗口隐藏导致的临时隐藏恢复。

## 7. 边界情况

### 7.1 地图窗口关闭

如果地图窗口已经关闭：

```text
_factoryMapWindow == null
```

即使 `_lastActiveVsLoaderWindow` 仍是 `FactoryMap`，也必须回退激活主窗口。

地图 `Closed` 事件中应主动设置：

```csharp
_lastActiveVsLoaderWindow = VsLoaderForegroundWindow.Main;
```

### 7.2 地图窗口被用户最小化

如果用户主动把地图窗口最小化到任务栏，再按主窗口快捷键恢复 VSLoader：

```text
不应强行把地图窗口从任务栏拉出来压到最前。
```

此时激活主窗口即可。

原因：

```text
最小化是用户明确表达“不看这个窗口”。
```

### 7.3 主窗口隐藏时地图被同步隐藏

如果地图窗口是因为主窗口快捷键隐藏而被同步隐藏：

```text
_isFactoryMapHiddenByMainWindow == true
```

恢复主窗口时仍应按现有逻辑恢复地图窗口。

恢复后，如果隐藏前最后活跃窗口是地图，则地图应回到前台。

### 7.4 地图快捷键不受影响

地图快捷键负责快速打开/关闭地图。

本次只调整主快捷键恢复 VSLoader 时的最终前台窗口，不改变：

```text
TryToggleFactoryMapFromHotkey()
ToggleFactoryMapWindow()
RestoreMinimizedFactoryMapWindow()
```

的业务语义。

### 7.5 托盘显示行为

托盘图标左键、双击、托盘菜单“显示 VSLoader”当前也复用 `RestoreAndActivate()`。

本次建议让它们和主快捷键保持一致：

```text
恢复 VSLoader 时尊重本次 session 最后活跃窗口。
```

这样用户从托盘恢复时也不会突然改变主窗口/地图窗口的前后关系。

## 8. 影响范围

主要修改：

```text
VSLoader/MainWindow.xaml.cs
```

可能修改测试：

```text
VSLoader.Tests/MainWindowFactoryMapCloseTests.cs
```

或新增：

```text
VSLoader.Tests/MainWindowForegroundRestoreTests.cs
```

不修改：

```text
VSLoader/Views/FactoryMapWindow.xaml
VSLoader/ViewModels/MainViewModel.cs
VSLoader/Models/Services/*
VSLoader/Styles/*
workspace 配置文件
layout 配置文件
```

## 9. 编码要求

### 9.1 不引入持久化

不要把 `_lastActiveVsLoaderWindow` 写入任何 JSON。

这不是用户偏好配置，而是本次运行时的交互状态。

### 9.2 不用复杂 Z-Order API

优先使用 WPF 自带：

```csharp
Activate()
Focus()
Topmost = true;
Topmost = false;
```

不要引入 Win32 `SetWindowPos`，除非 WPF 方法无法满足现有行为。

### 9.3 保持最小修改

本次只围绕：

```text
RestoreAndActivate()
ShowFactoryMapIfNeeded()
地图窗口创建和关闭事件
主窗口/地图窗口 Activated 事件
```

做改动。

不要顺手重构地图窗口布局、快捷键注册、托盘菜单、窗口持久化配置。

### 9.4 最终激活动作只能发生一次

`RestoreAndActivate()` 的尾部应只有一个最终决策：

```text
激活主窗口
或
激活地图窗口
```

避免先激活主窗口、又激活地图、又被某个后续逻辑重新激活主窗口。

## 10. 测试要求

### 10.1 静态判断测试

新增或补充测试：

```csharp
[Fact]
public void ShouldActivateFactoryMapAfterHotkeyRestore_returns_true_when_map_was_last_active_and_visible()
```

断言：

```text
lastActiveWindow = FactoryMap
isFactoryMapOpen = true
hasFactoryMapWindow = true
isFactoryMapVisible = true
isFactoryMapMinimized = false
结果为 true
```

新增测试：

```csharp
[Fact]
public void ShouldActivateFactoryMapAfterHotkeyRestore_returns_false_when_main_was_last_active()
```

新增测试：

```csharp
[Fact]
public void ShouldActivateFactoryMapAfterHotkeyRestore_returns_false_when_map_is_closed()
```

新增测试：

```csharp
[Fact]
public void ShouldActivateFactoryMapAfterHotkeyRestore_returns_false_when_map_is_minimized()
```

### 10.2 人工验证

需要手动验证以下场景：

```text
场景 1：
打开主窗口和地图窗口 -> 点击地图窗口使其在最前 -> 切到其他程序 -> 按主快捷键
期望：地图窗口在最前，主窗口在后。

场景 2：
打开主窗口和地图窗口 -> 点击主窗口使其在最前 -> 切到其他程序 -> 按主快捷键
期望：主窗口在最前。

场景 3：
打开地图窗口 -> 用户最小化地图窗口 -> 按主快捷键恢复 VSLoader
期望：不强行拉起最小化的地图窗口，主窗口在最前。

场景 4：
打开地图窗口并让地图在最前 -> 按主快捷键隐藏 VSLoader -> 再按主快捷键恢复
期望：主窗口和地图窗口都恢复，地图窗口在最前。

场景 5：
地图窗口关闭 -> 按主快捷键恢复 VSLoader
期望：只恢复主窗口，不报错。
```

### 10.3 构建验证

执行：

```powershell
dotnet test .\VSLoader.Tests\VSLoader.Tests.csproj -c Debug --no-restore
```

执行：

```powershell
dotnet build .\VSLoader.sln -c Debug --no-restore
```

如果 `--no-restore` 因本地依赖状态失败，再执行不带 `--no-restore` 的版本确认是否为 restore 问题。

## 11. 验收标准

满足以下条件即完成：

- 地图窗口在最前时，切走再按主快捷键恢复，地图仍在最前。
- 主窗口在最前时，切走再按主快捷键恢复，主窗口仍在最前。
- 地图窗口被用户最小化时，主快捷键恢复不会强行拉起地图窗口。
- 地图窗口关闭后，主快捷键恢复不会访问空窗口对象导致异常。
- 托盘“显示 VSLoader”行为与主快捷键恢复逻辑一致。
- 地图快捷键打开/关闭地图功能不受影响。
- 不新增配置文件字段。
- 不改变现有窗口位置、大小、缩放比例持久化逻辑。
- 相关测试通过。
- Debug 构建通过。

## 12. 风险与注意事项

### 12.1 Activated 事件可能被程序主动 Activate 触发

程序主动调用 `Activate()` 也会触发 `Activated`。

这是可接受的，因为最终激活哪个窗口，本质上也代表程序当前认为哪个窗口是前台。

但需要避免：

```text
RestorePreferredForegroundWindow() 内部先激活主窗口导致 _lastActiveVsLoaderWindow 被改成 Main，
然后判断逻辑再也无法激活地图。
```

因此判断是否激活地图应在任何最终 `Activate()` 前完成。

### 12.2 不要把地图显示和地图激活混在一起

`ShowFactoryMapIfNeeded()` 负责：

```text
地图窗口是否应该显示
地图窗口位置是否恢复
地图内容是否刷新
```

`RestorePreferredForegroundWindow()` 负责：

```text
最终谁在前台
```

这两个职责要分开，避免后续维护时再次出现固定层级覆盖问题。

### 12.3 保留主窗口兜底

任何异常或不确定状态都应回退到主窗口：

```text
地图窗口为空
地图窗口不可见
地图窗口最小化
地图窗口已关闭但状态字段还没同步
```

主窗口是 VSLoader 的基础入口，兜底激活主窗口最安全。
