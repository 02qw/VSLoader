# v83 地图节点原始机型名视觉层级优化编码规格说明

## 1. 文档目的

本文件用于指导编程 Agent 优化工厂地图节点中的文字视觉层级。

核心目标：

```text
地图节点第一行设备名称保持加粗。
地图节点第二行原始机型名改为不加粗，并使用更轻的颜色。
```

这样用户在查看地图时，可以更容易区分：

```text
设备中文名称
原始机型编号
```

## 2. 背景说明

当前地图节点中会显示：

```text
设备名称
原始机型名
```

示例：

```text
热贴机_003
TSSM003
```

当前源码中，这两行文字被拼接成同一个字符串，并放在同一个 `TextBlock` 中：

```csharp
var displayText = string.IsNullOrWhiteSpace(deviceCode)
    ? device.Name
    : $"{device.Name}{Environment.NewLine}{deviceCode}";
```

然后统一设置：

```csharp
FontSize = 12
FontWeight = FontWeights.SemiBold
```

因此两行文字都是加粗样式，视觉层级不明显，用户看多了容易眼花。

## 3. 当前代码位置

主要修改文件：

```text
VSLoader\Views\FactoryMapWindow.xaml.cs
```

当前节点绘制方法：

```text
DrawDevice(FactoryMapDeviceViewNode device)
```

当前节点尺寸常量：

```csharp
private const double DeviceWidth = 150;
private const double DeviceHeight = 58;
```

当前节点文字样式：

```csharp
Foreground = #0F172A
FontFamily = Microsoft YaHei UI
FontSize = 12
LineHeight = 15
LineStackingStrategy = BlockLineHeight
FontWeight = SemiBold
```

## 4. 需求目标

完成后应满足：

```text
1. 节点第一行设备名称仍然显示。
2. 节点第一行设备名称保持 SemiBold。
3. 节点第二行原始机型名仍然显示。
4. 节点第二行原始机型名改为 Normal，不加粗。
5. 节点第二行原始机型名颜色比第一行更轻。
6. 没有原始机型名时，只显示设备名称。
7. 节点宽高不变。
8. 节点拖拽、连线、选中、右键菜单不受影响。
```

## 5. 非目标范围

本阶段不实现：

```text
1. 不修改节点宽度。
2. 不修改节点高度。
3. 不修改节点背景、边框、圆角。
4. 不修改地图缩放、拖拽、连线逻辑。
5. 不修改地图布局 JSON。
6. 不修改主界面快捷项列表。
7. 不修改批量新增算法。
8. 不修改原始机型名提取算法。
9. 不新增用户可配置字体设置。
10. 不修改节点名称内容。
```

## 6. 推荐实现方案

### 6.1 拆分单个 TextBlock

当前一个 `TextBlock` 同时显示两行文字，无法给两行设置不同字重。

推荐改成：

```text
Border
  -> StackPanel Vertical
       -> TextBlock：设备名称
       -> TextBlock：原始机型名
```

### 6.2 第一行设备名称样式

推荐保持接近当前样式：

```text
FontFamily = Microsoft YaHei UI
FontSize = 12
FontWeight = SemiBold
Foreground = #0F172A
TextAlignment = Center
TextWrapping = Wrap
TextTrimming = None
LineHeight = 15
LineStackingStrategy = BlockLineHeight
```

### 6.3 第二行原始机型名样式

推荐样式：

```text
FontFamily = Microsoft YaHei UI
FontSize = 11
FontWeight = Normal
Foreground = #64748B
TextAlignment = Center
TextWrapping = NoWrap 或 Wrap
TextTrimming = None
LineHeight = 14
LineStackingStrategy = BlockLineHeight
Margin = 0,1,0,0
```

说明：

```text
FontSize 11 + Normal + #64748B 可以明显降低第二行权重。
```

如果担心节点内空间紧张，可保持第二行 `FontSize = 12`，但必须保证：

```text
FontWeight = Normal
```

本规格推荐：

```text
第二行 FontSize = 11
```

### 6.4 没有原始机型名时

如果 `deviceCode` 为空：

```text
只创建或只显示第一行设备名称。
```

不要显示空白第二行。

## 7. 代码示例

目标方法：

```text
FactoryMapWindow.DrawDevice
```

推荐将当前 `TextBlock text = new TextBlock { ... }` 改为：

```csharp
var content = new StackPanel
{
    Orientation = Orientation.Vertical,
    VerticalAlignment = VerticalAlignment.Center,
    HorizontalAlignment = HorizontalAlignment.Center
};

content.Children.Add(new TextBlock
{
    Text = device.Name,
    TextAlignment = TextAlignment.Center,
    TextWrapping = TextWrapping.Wrap,
    TextTrimming = TextTrimming.None,
    Foreground = new SolidColorBrush(WpfColor.FromRgb(15, 23, 42)),
    FontFamily = new WpfFontFamily("Microsoft YaHei UI"),
    FontSize = 12,
    LineHeight = 15,
    LineStackingStrategy = LineStackingStrategy.BlockLineHeight,
    FontWeight = FontWeights.SemiBold
});

if (!string.IsNullOrWhiteSpace(deviceCode))
{
    content.Children.Add(new TextBlock
    {
        Text = deviceCode,
        Margin = new Thickness(0, 1, 0, 0),
        TextAlignment = TextAlignment.Center,
        TextWrapping = TextWrapping.NoWrap,
        TextTrimming = TextTrimming.None,
        Foreground = new SolidColorBrush(WpfColor.FromRgb(100, 116, 139)),
        FontFamily = new WpfFontFamily("Microsoft YaHei UI"),
        FontSize = 11,
        LineHeight = 14,
        LineStackingStrategy = LineStackingStrategy.BlockLineHeight,
        FontWeight = FontWeights.Normal
    });
}
```

然后节点 Border：

```csharp
Child = content
```

## 8. 布局要求

保持现有节点外观：

```text
Width = 150
Height = 58
Background = White
BorderBrush = #CBD5E1
BorderThickness = 1
CornerRadius = 6
Padding = 8,4,8,4
```

注意：

```text
1. StackPanel 必须居中。
2. 设备名称和原始机型名都应水平居中。
3. 原始机型名不应挤压节点导致明显溢出。
4. 没有原始机型名时，设备名称仍垂直居中。
```

## 9. 测试要求

当前地图节点为 WPF 动态 UI，单元测试不适合直接断言视觉树内部字体。

本次要求：

```text
1. dotnet build 通过，确保 code-behind 编译正确。
2. dotnet test 通过，确保现有地图、布局、快捷项逻辑未被破坏。
```

如果项目中已有 FactoryMapWindow 相关 UI 测试，可补充测试：

```text
DrawDevice creates separate text elements for name and device code
```

但本阶段不强制新增 UI 单元测试。

## 10. 手工验收

### 场景一：有原始机型名的节点

操作：

```text
1. 打开主程序。
2. 打开地图窗口。
3. 查看包含原始机型名的节点。
```

预期：

```text
第一行设备名称加粗。
第二行原始机型名不加粗。
第二行颜色更浅。
两行仍然居中。
```

### 场景二：无原始机型名的节点

操作：

```text
查看没有解析出 TCDE003 / TSSM003 等编号的节点。
```

预期：

```text
只显示设备名称。
不显示空白第二行。
设备名称仍垂直居中。
```

### 场景三：地图交互回归

操作：

```text
1. 拖动节点。
2. 框选节点。
3. 右键节点。
4. 连线节点。
5. 缩放和平移地图。
```

预期：

```text
所有交互行为与修改前一致。
```

## 11. 风险点

### 11.1 节点内容垂直不居中

风险：

```text
改成 StackPanel 后，如果 VerticalAlignment 没设置，内容可能靠上。
```

规避：

```text
StackPanel 设置 VerticalAlignment = Center。
```

### 11.2 第二行占用过多高度

风险：

```text
节点高度保持 58，如果两行文字太高可能显得拥挤。
```

规避：

```text
第二行字号推荐 11，LineHeight 推荐 14。
```

### 11.3 误改节点交互

风险：

```text
事件绑定在 Border 上，Child 从 TextBlock 改 StackPanel 不应影响交互。
```

规避：

```text
不要改 Border 的事件绑定、Tag、Canvas 坐标、尺寸。
```

## 12. 验收标准

本需求完成必须满足：

```text
1. 原始机型名不加粗。
2. 原始机型名颜色比设备名称更轻。
3. 设备名称仍保持加粗。
4. 节点尺寸不变。
5. 地图节点交互不变。
6. dotnet build 通过。
7. dotnet test 通过。
```

## 13. 推荐执行命令

实现前如需释放文件占用：

```powershell
Get-Process -Name VSLoader -ErrorAction SilentlyContinue | Stop-Process -Force
```

构建：

```powershell
dotnet build C:\Users\shee_\OneDrive\Desktop\VSLoader\VSLoader.sln -p:UseSharedCompilation=false
```

测试：

```powershell
dotnet test C:\Users\shee_\OneDrive\Desktop\VSLoader\VSLoader.sln -p:UseSharedCompilation=false
```

如果用户正在运行 VSLoader，不希望关闭程序，可使用临时输出目录验证：

```powershell
$out = Join-Path $env:TEMP ('VSLoader-build-' + [guid]::NewGuid().ToString('N'))
dotnet build C:\Users\shee_\OneDrive\Desktop\VSLoader\VSLoader.sln -p:UseSharedCompilation=false -p:OutDir="$out\"
```

## 14. 推荐提交信息

```text
style: improve factory map device code typography
```
