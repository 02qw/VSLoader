# v30 托盘菜单自定义渲染器样式优化编码规格说明

## 1. 需求背景

v28 已实现 VSLoader 最小化到托盘。

v29 尝试通过 WinForms `ContextMenuStrip` 的 `ToolStripProfessionalRenderer` 和 `ProfessionalColorTable` 优化托盘右键菜单样式。

当前问题：

```text
托盘菜单 hover 选中态仍然出现明显边框/框选效果，观感不够干净。
```

用户希望：

```text
不需要蓝色框选，也不需要明显框选。
选中态只需要淡灰色背景即可。
```

现有 v29 方案只修改颜色表，但 WinForms 默认渲染器仍然会参与绘制菜单项选中边框、焦点边框或系统风格边界，因此无法稳定得到干净的 hover 效果。

本次 v30 需求需要改为自定义渲染器，接管托盘菜单绘制。

## 2. 需求目标

本次开发目标：

1. 保持 v28 托盘功能不变。
2. 保持托盘右键菜单功能不变。
3. 托盘菜单仍然使用轻量的 WinForms `ContextMenuStrip`。
4. 不使用 WPF 自绘托盘浮窗。
5. 不再依赖 `ToolStripProfessionalRenderer + ProfessionalColorTable` 的默认选中绘制。
6. 新增自定义 `ToolStripRenderer` 或继承 `ToolStripProfessionalRenderer` 的自定义渲染器。
7. hover 选中态只显示整行淡灰色背景。
8. hover 状态不显示蓝色边框。
9. hover 状态不显示明显框线。
10. 菜单文字比例自然，不截断。
11. 菜单背景保持纯白。
12. 菜单边框保持浅灰色。

## 3. 当前代码问题分析

当前托盘菜单代码位于：

```text
VSLoader\MainWindow.xaml.cs
```

大致结构：

```csharp
var menu = new ContextMenuStrip
{
    BackColor = Color.White,
    ForeColor = Color.FromArgb(17, 24, 39),
    Font = new Font("Microsoft YaHei UI", 9F),
    MinimumSize = new Size(160, 0),
    Padding = new Padding(0),
    ShowImageMargin = false,
    ShowCheckMargin = false,
    Renderer = new ToolStripProfessionalRenderer(new TrayMenuColorTable())
};
```

当前问题：

- `ToolStripProfessionalRenderer` 仍然会使用 WinForms 默认菜单项绘制流程。
- `ProfessionalColorTable` 只能影响部分颜色，不能完全取消默认边框/焦点绘制。
- hover 时会出现类似框选的视觉效果。
- 该框选效果不符合 VSLoader 目前应用内右键菜单的干净风格。

## 4. 技术路线

继续保留：

```csharp
System.Windows.Forms.ContextMenuStrip
System.Windows.Forms.ToolStripMenuItem
```

但替换：

```csharp
ToolStripProfessionalRenderer(new TrayMenuColorTable())
```

为：

```csharp
new TrayMenuRenderer()
```

由 `TrayMenuRenderer` 手动控制：

- 菜单整体背景
- 菜单整体边框
- 菜单项 hover 背景
- 菜单文字颜色
- 菜单文字位置

## 5. 修改范围

主要修改文件：

```text
VSLoader\MainWindow.xaml.cs
```

原则上不需要修改：

- `MainWindow.xaml`
- `App.xaml`
- `App.xaml.cs`
- `GlobalHotkeyService`
- `MainViewModel`
- `VSLoader.csproj`
- 配置文件结构

## 6. 删除或废弃旧颜色表

当前可能存在：

```csharp
private sealed class TrayMenuColorTable : WinForms.ProfessionalColorTable
```

本次建议删除或不再使用。

原因：

- 颜色表无法完全控制 hover 边框。
- 后续维护容易误以为颜色表能解决全部样式问题。

替换为：

```csharp
private sealed class TrayMenuRenderer : WinForms.ToolStripProfessionalRenderer
```

或：

```csharp
private sealed class TrayMenuRenderer : WinForms.ToolStripRenderer
```

推荐继承：

```csharp
WinForms.ToolStripProfessionalRenderer
```

并重写关键绘制方法。

## 7. 菜单整体样式

`CreateTrayMenu()` 中推荐设置：

```csharp
var menu = new WinForms.ContextMenuStrip
{
    BackColor = System.Drawing.Color.White,
    ForeColor = System.Drawing.Color.FromArgb(17, 24, 39),
    Font = new System.Drawing.Font("Microsoft YaHei UI", 9F),
    MinimumSize = new System.Drawing.Size(160, 0),
    Padding = new WinForms.Padding(0),
    ShowImageMargin = false,
    ShowCheckMargin = false,
    Renderer = new TrayMenuRenderer()
};
```

说明：

- `ShowImageMargin = false` 去掉左侧图标预留区。
- `ShowCheckMargin = false` 去掉勾选预留区。
- `Padding = 0` 保持菜单边缘紧凑。
- `MinimumSize = 160` 避免文字截断。

## 8. 菜单项样式

继续使用辅助方法：

```csharp
private static WinForms.ToolStripMenuItem CreateTrayMenuItem(string text, EventHandler onClick)
```

推荐实现：

```csharp
private static WinForms.ToolStripMenuItem CreateTrayMenuItem(string text, EventHandler onClick)
{
    var item = new WinForms.ToolStripMenuItem(text)
    {
        AutoSize = false,
        Width = 160,
        Height = 34,
        Padding = new WinForms.Padding(0),
        Margin = new WinForms.Padding(0)
    };

    item.Click += onClick;
    return item;
}
```

说明：

- 文字内边距不再依赖 `ToolStripMenuItem.Padding`。
- 文字位置交给自定义渲染器统一绘制。
- 避免 WinForms 默认 padding 影响 hover 背景范围。

## 9. 自定义渲染器设计

新增：

```csharp
private sealed class TrayMenuRenderer : WinForms.ToolStripProfessionalRenderer
```

建议定义颜色常量：

```csharp
private static readonly System.Drawing.Color BackgroundColor = System.Drawing.Color.White;
private static readonly System.Drawing.Color BorderColor = System.Drawing.Color.FromArgb(209, 213, 219);
private static readonly System.Drawing.Color HoverColor = System.Drawing.Color.FromArgb(243, 244, 246);
private static readonly System.Drawing.Color TextColor = System.Drawing.Color.FromArgb(17, 24, 39);
```

建议定义文字边距：

```csharp
private const int TextLeftPadding = 14;
private const int TextRightPadding = 14;
```

## 10. 背景绘制

重写：

```csharp
protected override void OnRenderToolStripBackground(WinForms.ToolStripRenderEventArgs e)
```

推荐实现：

```csharp
using var brush = new System.Drawing.SolidBrush(BackgroundColor);
e.Graphics.FillRectangle(brush, new System.Drawing.Rectangle(System.Drawing.Point.Empty, e.ToolStrip.Size));
```

效果：

- 菜单整体背景纯白。
- 不依赖默认系统背景。

## 11. 边框绘制

重写：

```csharp
protected override void OnRenderToolStripBorder(WinForms.ToolStripRenderEventArgs e)
```

推荐实现：

```csharp
using var pen = new System.Drawing.Pen(BorderColor);
var rectangle = new System.Drawing.Rectangle(
    0,
    0,
    e.ToolStrip.Width - 1,
    e.ToolStrip.Height - 1);
e.Graphics.DrawRectangle(pen, rectangle);
```

效果：

- 菜单外边框统一浅灰。
- 不显示系统默认深色边框。

## 12. hover 背景绘制

重写：

```csharp
protected override void OnRenderMenuItemBackground(WinForms.ToolStripItemRenderEventArgs e)
```

推荐实现：

```csharp
if (!e.Item.Selected)
{
    return;
}

using var brush = new System.Drawing.SolidBrush(HoverColor);
var rectangle = new System.Drawing.Rectangle(System.Drawing.Point.Empty, e.Item.Size);
e.Graphics.FillRectangle(brush, rectangle);
```

关键要求：

- 只画淡灰背景。
- 不画边框。
- 不调用 `base.OnRenderMenuItemBackground(e)`。
- 避免 WinForms 默认蓝色边框或焦点框出现。

## 13. 文字绘制

重写：

```csharp
protected override void OnRenderItemText(WinForms.ToolStripItemTextRenderEventArgs e)
```

推荐实现：

```csharp
e.TextColor = TextColor;
e.TextFont = e.Item.Font;
e.TextRectangle = new System.Drawing.Rectangle(
    TextLeftPadding,
    0,
    e.Item.Width - TextLeftPadding - TextRightPadding,
    e.Item.Height);
e.TextFormat = WinForms.TextFormatFlags.Left
    | WinForms.TextFormatFlags.VerticalCenter
    | WinForms.TextFormatFlags.EndEllipsis
    | WinForms.TextFormatFlags.NoPrefix;

base.OnRenderItemText(e);
```

说明：

- 文字左边距固定为 14。
- 垂直居中。
- 太长时末尾省略。
- 不使用蓝色文字。
- 不显示访问键下划线。

## 14. 禁止默认选中边框

本次必须保证：

```text
hover 时不出现蓝色边框
hover 时不出现明显框线
hover 时只出现淡灰背景
```

因此：

```csharp
OnRenderMenuItemBackground
```

中不要调用：

```csharp
base.OnRenderMenuItemBackground(e)
```

如果仍出现边框，需要继续检查是否还有：

```csharp
MenuItemBorder
ToolStripProfessionalRenderer
ProfessionalColorTable
```

残留参与绘制。

## 15. 不允许改变的功能

本次不允许改变：

- 点击 `X` 隐藏到托盘。
- 托盘图标显示。
- 托盘菜单 `显示 VSLoader`。
- 托盘菜单 `退出`。
- 双击托盘图标显示窗口。
- 隐藏到托盘后快捷键恢复窗口。
- 全局快捷键释放逻辑。
- 应用内主列表右键菜单。

## 16. 验收标准

### 16.1 菜单 hover 效果

操作：

1. 启动 VSLoader。
2. 右键托盘图标。
3. 鼠标悬停 `显示 VSLoader`。

期望：

- 当前菜单项显示淡灰色背景。
- 不显示蓝色边框。
- 不显示明显框选。
- 选中背景覆盖完整菜单项宽度。

### 16.2 菜单文字

期望：

- `显示 VSLoader` 完整显示。
- `退出` 完整显示。
- 文字比例自然。
- 文字垂直居中。
- 文字左边距舒适。

### 16.3 菜单背景与边框

期望：

- 菜单背景纯白。
- 外边框为浅灰。
- 不出现突兀深色块。

### 16.4 功能不回归

操作：

1. 点击 `显示 VSLoader`。
2. 双击托盘图标。
3. 点击 `退出`。
4. 点击窗口 `X` 隐藏到托盘。
5. 隐藏到托盘后按全局快捷键。

期望：

- 所有 v28 功能保持正常。

### 16.5 编译验收

执行：

```powershell
dotnet build .\VSLoader.sln
```

期望：

```text
0 warnings
0 errors
```

如果提示 `VSLoader.exe` 被占用，说明程序正在运行，应先通过托盘菜单 `退出` 或结束进程后重新构建。

## 17. 建议实施步骤

1. 打开 `VSLoader\MainWindow.xaml.cs`。
2. 找到 `CreateTrayMenu()`。
3. 将 `Renderer = new ToolStripProfessionalRenderer(new TrayMenuColorTable())` 替换为 `Renderer = new TrayMenuRenderer()`。
4. 修改 `CreateTrayMenuItem()`，去掉依赖菜单项 padding 的文字布局。
5. 删除或废弃 `TrayMenuColorTable`。
6. 新增 `TrayMenuRenderer` 类。
7. 重写背景、边框、菜单项 hover、文字绘制方法。
8. 编译项目。
9. 手动检查托盘菜单 hover 效果。
10. 验证托盘显示、退出、双击、快捷键恢复功能未回归。

## 18. 明确不做的事情

本次不做：

- WPF 自绘托盘菜单。
- 托盘菜单圆角。
- 托盘菜单阴影。
- 托盘菜单动画。
- 托盘菜单图标。
- 托盘菜单分割线。
- 托盘左键单击行为。
- 托盘气泡通知。
