# v10 主列表单行快捷项视觉优化编码规格说明

## 1. 需求背景

当前 VSLoader 主界面快捷项列表已经可以正常展示、搜索、选中、双击打开、右键操作。

但现有列表样式偏默认表格风格，每一行快捷项在字体、间距、选中态、悬停态方面较为死板，不够产品化。

用户明确希望：

- 保留“一行代表一个快捷方式”的结构。
- 不改成双行信息展示。
- 不改成卡片式列表。
- 不影响快速扫描大量快捷项的大局观。
- 在不影响性能的前提下，通过间距、层级、选中态和可读性提升整体质感。

本次 v10 需求只做视觉样式优化，不改变业务逻辑。

## 2. 设计原则

本次优化遵循以下原则：

```text
优雅不靠装饰，而靠间距、层级、选中态和可读性。
```

具体原则：

1. 保持单行一项，维持列表密度和大局观。
2. 保持当前三列结构：名称、备注、更新时间。
3. 不重新展示目标路径列。
4. 不添加复杂图标、阴影、动画、卡片背景。
5. 不改变搜索、排序、选中、双击、右键菜单逻辑。
6. 继续使用 WPF `DataGrid`，保留虚拟化能力。

## 3. 修改范围

主要修改文件：

- `VSLoader\MainWindow.xaml`

原则上不需要修改：

- `VSLoader\ViewModels\MainViewModel.cs`
- `VSLoader\Models\ShortcutItem.cs`
- `VSLoader\Services\ShortcutSortService.cs`

除非实际实现中发现样式绑定必须增加辅助属性，否则不要修改业务代码。

## 4. 当前保留结构

主列表继续使用：

```xml
<DataGrid>
    <DataGrid.Columns>
        <DataGridTextColumn Header="名称" Binding="{Binding Name}" />
        <DataGridTextColumn Header="备注" Binding="{Binding Description}" />
        <DataGridTextColumn Header="更新时间" Binding="{Binding UpdatedAt}" />
    </DataGrid.Columns>
</DataGrid>
```

本次不改为双行模板，不把备注放到名称下面。

## 5. 视觉优化目标

### 5.1 行高

当前行高偏紧，建议调整为：

```text
RowHeight: 44
Row MinHeight: 44
```

不要超过 `48`，避免降低大批量列表的扫描效率。

### 5.2 表头高度

建议：

```text
ColumnHeaderHeight: 38
```

表头保持清晰但弱化默认表格感。

### 5.3 字体

整体字体：

```text
Microsoft YaHei UI
```

主字号：

```text
14
```

不同列建议形成轻微层级：

- 名称列：深色、SemiBold。
- 备注列：普通字重、灰色。
- 更新时间列：较弱灰色、字号略小。

## 6. 推荐颜色规范

### 6.1 背景

```text
列表背景：#FFFFFF
表头背景：#F9FAFB
普通行背景：#FFFFFF
隔行背景：#FCFCFD
```

### 6.2 文本颜色

```text
名称：#111827
备注：#4B5563
更新时间：#6B7280
表头：#6B7280
禁用/弱文本：#9CA3AF
```

### 6.3 分割线

```text
行分割线：#EEF0F3
表头底线：#E5E7EB
外边框：#DDE1E7
```

### 6.4 悬停和选中

```text
Hover 背景：#F6FAFF
Selected 背景：#EEF6FF
Selected 左侧强调线：#2563EB
```

## 7. DataGrid 样式要求

### 7.1 DataGrid 基础属性

建议确保或新增以下属性：

```xml
EnableRowVirtualization="True"
EnableColumnVirtualization="True"
VirtualizingPanel.IsVirtualizing="True"
VirtualizingPanel.VirtualizationMode="Recycling"
ScrollViewer.CanContentScroll="True"
RowHeight="44"
ColumnHeaderHeight="38"
GridLinesVisibility="Horizontal"
HorizontalGridLinesBrush="#EEF0F3"
RowHeaderWidth="0"
```

说明：

- 不启用复杂视觉效果。
- 保持横向弱分割线。
- 不显示纵向网格线。
- 保留虚拟化能力，避免大量快捷项时性能下降。

### 7.2 单元格样式

建议调整 `DataGrid.CellStyle`：

```xml
<Setter Property="Padding" Value="12,0" />
<Setter Property="VerticalContentAlignment" Value="Center" />
<Setter Property="BorderThickness" Value="0" />
<Setter Property="FocusVisualStyle" Value="{x:Null}" />
```

目标：

- 增加左右呼吸感。
- 保持单行垂直居中。
- 减少默认焦点框带来的控件感。

### 7.3 表头样式

建议：

```xml
<Setter Property="Background" Value="#F9FAFB" />
<Setter Property="Foreground" Value="#6B7280" />
<Setter Property="FontSize" Value="13" />
<Setter Property="FontWeight" Value="SemiBold" />
<Setter Property="Padding" Value="12,0" />
```

表头不应比内容更抢眼。

### 7.4 行样式

需要重写 `DataGrid.RowStyle`，实现：

- 普通状态白底。
- 隔行非常浅的背景。
- 鼠标悬停浅蓝灰背景。
- 选中时浅蓝背景。
- 选中时左侧 3px 蓝色强调线。
- 行高不因 hover/selected 变化。

推荐通过 `DataGridRow.Template` 实现左侧强调线。

伪结构：

```xml
<ControlTemplate TargetType="DataGridRow">
    <Border x:Name="RowBorder" Background="{TemplateBinding Background}">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="3" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Border x:Name="SelectedIndicator"
                    Background="Transparent" />
            <DataGridCellsPresenter Grid.Column="1" />
        </Grid>
    </Border>
</ControlTemplate>
```

触发器：

```xml
<Trigger Property="IsMouseOver" Value="True">
    <Setter TargetName="RowBorder" Property="Background" Value="#F6FAFF" />
</Trigger>

<Trigger Property="IsSelected" Value="True">
    <Setter TargetName="RowBorder" Property="Background" Value="#EEF6FF" />
    <Setter TargetName="SelectedIndicator" Property="Background" Value="#2563EB" />
</Trigger>
```

注意：

- `IsSelected` 视觉优先级应高于 `IsMouseOver`。
- 不要使用系统默认深蓝选中背景。

## 8. 列样式要求

当前 `DataGridTextColumn` 默认文本样式较弱，建议为每列设置 `ElementStyle`。

### 8.1 名称列

建议：

```xml
<Style TargetType="TextBlock">
    <Setter Property="Foreground" Value="#111827" />
    <Setter Property="FontWeight" Value="SemiBold" />
    <Setter Property="TextTrimming" Value="CharacterEllipsis" />
    <Setter Property="VerticalAlignment" Value="Center" />
</Style>
```

列宽建议：

```text
名称列：380
```

如果当前窗口宽度较大，也可使用 `400`。

### 8.2 备注列

建议：

```xml
<Style TargetType="TextBlock">
    <Setter Property="Foreground" Value="#4B5563" />
    <Setter Property="TextTrimming" Value="CharacterEllipsis" />
    <Setter Property="VerticalAlignment" Value="Center" />
</Style>
```

列宽：

```text
备注列：*
```

备注为空时保持空白，不显示 `无备注`。

### 8.3 更新时间列

建议：

```xml
<Style TargetType="TextBlock">
    <Setter Property="Foreground" Value="#6B7280" />
    <Setter Property="FontSize" Value="13" />
    <Setter Property="TextAlignment" Value="Right" />
    <Setter Property="VerticalAlignment" Value="Center" />
</Style>
```

列宽建议：

```text
更新时间列：180 或 190
```

更新时间靠右可以降低视觉噪音，并提升列表整齐感。

## 9. 右键菜单和双击行为

本次不修改右键菜单功能。

必须保持：

- 右键某一行时选中该行。
- 右键菜单仍显示 `AdminUI`、`编辑`、`删除`。
- 双击某一行仍然使用 VSCode 打开对应快捷项。
- 主界面按钮状态仍然受 `SelectedShortcut` 控制。

如 RowStyle 重写导致右键选中或双击失效，需要调整模板但不能改业务行为。

## 10. 性能要求

本次样式优化不得明显影响大量快捷项列表性能。

必须避免：

- 每行阴影。
- 每行动画。
- 每行复杂嵌套卡片。
- 每行图片或图标。
- 关闭虚拟化。

必须保持或启用：

- `EnableRowVirtualization="True"`
- `EnableColumnVirtualization="True"`
- `VirtualizingPanel.VirtualizationMode="Recycling"`

## 11. 验收标准

### 11.1 视觉验收

完成后主列表应满足：

- 一行仍然只代表一个快捷方式。
- 行间距比当前更舒展，但不松散。
- 名称、备注、更新时间有清楚视觉层级。
- 选中行清晰但不刺眼。
- 鼠标悬停有轻微反馈。
- 列表整体仍然像高效工具，不像卡片页面。

### 11.2 功能验收

以下功能必须保持不变：

- 搜索过滤。
- 自然排序。
- 单击选中。
- 双击打开 VSCode。
- 点击 `AdminUI` 打开 `.jnlp`。
- 右键菜单操作。
- 编辑、删除。
- 自动获取连接。

### 11.3 编译验收

执行：

```powershell
dotnet build .\VSLoader.sln
```

期望：

```text
0 warnings
0 errors
```

如果构建失败且提示 `VSLoader.exe` 被占用，说明程序正在运行，应先关闭 VSLoader 后重新构建。

## 12. 建议实施步骤

1. 打开 `VSLoader\MainWindow.xaml`。
2. 调整 `DataGrid` 基础属性。
3. 优化 `DataGrid.ColumnHeaderStyle`。
4. 优化 `DataGrid.CellStyle`。
5. 重写或增强 `DataGrid.RowStyle`。
6. 为名称、备注、更新时间列添加 `ElementStyle`。
7. 编译项目。
8. 手动检查列表选中、悬停、右键菜单、双击是否正常。

## 13. 明确不做的事情

本次不做：

- 双行列表。
- 卡片式布局。
- 恢复目标路径列。
- 新增图标列。
- 新增动画。
- 新增排序功能。
- 修改数据模型。
- 修改配置文件结构。
- 修改 AdminUI 或 VSCode 打开逻辑。
