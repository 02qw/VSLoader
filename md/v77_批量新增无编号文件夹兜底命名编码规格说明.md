# v77 批量新增无编号文件夹兜底命名编码规格说明

## 1. 文档目的

本文件用于指导编程 Agent 优化 VSLoader 的批量新增识别逻辑。

本次只解决一个问题：

```text
批量新增识别时，目标文件夹名后半段没有 001/002/003 这类编号 No，导致当前算法无法识别并跳过。
```

用户确认采用的兜底方案：

```text
当无法从文件夹名提取 No 时，使用 DisplayName_Type 作为生成名称。
```

示例：

```text
10892_CommonUI -> DisplayName_CommonUI
```

## 2. 当前问题

当前简化模块名映射模式中，文件：

```text
VSLoader\Models\Services\BatchImportService.cs
```

方法：

```csharp
CreateSimpleModuleMapName(...)
```

现有编号提取规则：

```csharp
@"^(?<Code>\d+)_(?<Type>[A-Za-z]+)(?<No>\d+)$"
```

该规则只能识别：

```text
15346_TCVF001
4124_TPUW001
3076_TAPM017
```

但无法识别：

```text
10892_CommonUI
```

因为 `CommonUI` 后面没有数字编号。

当前结果：

```text
状态：已跳过
说明：文件夹名无法提取编号 No。
```

## 3. 需求目标

完成后应达到：

```text
1. 有编号文件夹继续沿用原算法。
2. 无编号文件夹不再因为无法提取 No 直接跳过。
3. 无编号文件夹使用 DisplayName_Type 生成快捷项名称。
4. 不修改 CSV 规则文件格式。
5. 不影响 AdminUI、WebUI、地图、工作区等其他功能。
```

## 4. 非目标范围

本阶段不实现：

```text
1. 不新增 CSV 列。
2. 不新增 FallbackNameTemplate。
3. 不改复杂 Regex/Contains 模式。
4. 不改 AdminUI 下载逻辑。
5. 不改 WebUI 拼接逻辑。
6. 不改地图节点逻辑。
7. 不改工作区逻辑。
8. 不改主列表显示逻辑。
9. 不改快捷项数据结构。
```

## 5. 适用范围

本需求只作用于：

```text
简化模块名映射 CSV 模式
```

也就是 CSV 表头类似：

```csv
ModuleName,DisplayName
```

或当前已经支持的简单模块映射文件。

不作用于旧复杂规则模式：

```csv
MatchType,Pattern,DisplayName,NameTemplate
```

复杂规则模式仍然由用户自定义 Regex/NameTemplate 自己负责。

## 6. 目标文件

主要修改：

```text
VSLoader\Models\Services\BatchImportService.cs
```

主要测试：

```text
VSLoader.Tests\BatchImportServiceSimpleModuleMapTests.cs
```

如果该测试文件不存在，则在现有批量新增测试文件中添加对应测试。

## 7. 新解析规则

### 7.1 优先解析带 No 的文件夹名

优先匹配：

```regex
^(?<Code>\d+)_(?<Type>[A-Za-z]+)(?<No>\d+)$
```

示例：

```text
15346_TCVF001
```

解析：

```text
Code = 15346
Type = TCVF
No = 001
```

生成名称：

```text
{DisplayName}_{No}
```

示例：

```text
Pink-真空炉_001
```

排序：

```text
SortNo = 1
```

### 7.2 兜底解析无 No 的文件夹名

如果无法匹配带 No 格式，则继续尝试匹配：

```regex
^(?<Code>\d+)_(?<Type>[^\\\/]+)$
```

说明：

```text
Code 必须仍然是开头数字。
下划线后面的 Type 只要求非空，并且不包含路径分隔符。
```

示例：

```text
10892_CommonUI
```

解析：

```text
Code = 10892
Type = CommonUI
No = 空
```

生成名称：

```text
{DisplayName}_{Type}
```

示例：

```text
公共UI_CommonUI
```

排序：

```text
SortNo = null 或 int.MaxValue
```

建议使用：

```csharp
SortNo = null
```

原因：

```text
现有排序中 ThenBy(item => item.SortNo ?? int.MaxValue)，null 会自然排在同规则有编号项后面。
```

### 7.3 完全无法解析时继续跳过

如果既不匹配：

```text
数字_字母类型编号
```

也不匹配：

```text
数字_类型名
```

则继续跳过。

建议提示：

```text
文件夹名无法提取类型信息。
```

不要继续使用：

```text
文件夹名无法提取编号 No。
```

因为现在无编号并不一定是错误。

## 8. 推荐代码结构

### 8.1 提取文件夹标识解析方法

建议在 `BatchImportService` 中新增私有方法：

```csharp
private static FolderIdentity? TryParseFolderIdentity(string folderName)
```

推荐 record：

```csharp
private sealed record FolderIdentity(string Code, string Type, string? No, int? SortNo);
```

推荐实现：

```csharp
private static FolderIdentity? TryParseFolderIdentity(string folderName)
{
    var withNoMatch = Regex.Match(folderName, @"^(?<Code>\d+)_(?<Type>[A-Za-z]+)(?<No>\d+)$", RegexOptions.IgnoreCase);
    if (withNoMatch.Success && withNoMatch.Groups["No"].Success)
    {
        var noText = withNoMatch.Groups["No"].Value;
        var sortNo = int.TryParse(noText, out var parsedNo) ? parsedNo : int.MaxValue;
        return new FolderIdentity(
            withNoMatch.Groups["Code"].Value,
            withNoMatch.Groups["Type"].Value,
            noText,
            sortNo);
    }

    var withoutNoMatch = Regex.Match(folderName, @"^(?<Code>\d+)_(?<Type>[^\\/]+)$", RegexOptions.IgnoreCase);
    if (withoutNoMatch.Success && !string.IsNullOrWhiteSpace(withoutNoMatch.Groups["Type"].Value))
    {
        return new FolderIdentity(
            withoutNoMatch.Groups["Code"].Value,
            withoutNoMatch.Groups["Type"].Value.Trim(),
            null,
            null);
    }

    return null;
}
```

### 8.2 修改 CreateSimpleModuleMapName

当前逻辑：

```csharp
var noMatch = Regex.Match(folderName, @"^(?<Code>\d+)_(?<Type>[A-Za-z]+)(?<No>\d+)$", RegexOptions.IgnoreCase);
if (!noMatch.Success || !noMatch.Groups["No"].Success)
{
    return SimpleModuleMapResult.Fail(StatusSkipped, "文件夹名无法提取编号 No。");
}
```

应改为：

```csharp
var folderIdentity = TryParseFolderIdentity(folderName);
if (folderIdentity is null)
{
    return SimpleModuleMapResult.Fail(StatusSkipped, "文件夹名无法提取类型信息。");
}
```

然后：

```csharp
var sortNo = folderIdentity.SortNo;
```

生成名称：

```csharp
var generatedName = !string.IsNullOrWhiteSpace(folderIdentity.No)
    ? $"{matchedRule.DisplayName.Trim()}_{folderIdentity.No}"
    : $"{matchedRule.DisplayName.Trim()}_{folderIdentity.Type}";
```

返回：

```csharp
return new SimpleModuleMapResult(
    true,
    generatedName,
    moduleName,
    sortNo,
    matchedRule.SortIndex,
    null,
    StatusImportable);
```

## 9. 行为示例

### 9.1 有编号设备

文件夹：

```text
15346_TCVF001
```

CSV：

```csv
ModuleName,DisplayName
eap-Pink-VacFur,Pink-真空炉
```

结果：

```text
生成名称：Pink-真空炉_001
SortNo：1
状态：可新增 / 可更新 / 已跳过，按已有路径逻辑判断
```

### 9.2 无编号模块

文件夹：

```text
10892_CommonUI
```

CSV：

```csv
ModuleName,DisplayName
eap-xxx-common-ui,公共UI
```

结果：

```text
生成名称：公共UI_CommonUI
SortNo：null
状态：可新增 / 可更新 / 已跳过，按已有路径逻辑判断
```

### 9.3 无法解析

文件夹：

```text
CommonUI
```

结果：

```text
状态：已跳过
说明：文件夹名无法提取类型信息。
```

## 10. 测试要求

### 10.1 新增测试：无编号文件夹可以生成兜底名称

测试目标：

```text
10892_CommonUI 能生成 DisplayName_CommonUI。
```

测试建议：

```csharp
[Fact]
public void BuildPreview_simple_module_map_uses_type_suffix_when_folder_has_no_no()
```

断言：

```text
1. Status == 可新增
2. GeneratedName == "公共UI_CommonUI"
3. SortNo == null
4. FolderName == "10892_CommonUI"
```

### 10.2 保持原有有编号行为

新增或确认已有测试：

```csharp
[Fact]
public void BuildPreview_simple_module_map_keeps_no_suffix_when_folder_has_no()
```

断言：

```text
15346_TCVF001 仍然生成 DisplayName_001。
```

### 10.3 无法解析仍跳过

新增测试：

```csharp
[Fact]
public void BuildPreview_simple_module_map_skips_when_folder_identity_cannot_be_parsed()
```

断言：

```text
1. Status == 已跳过
2. Message 包含 文件夹名无法提取类型信息
```

## 11. 风险点

### 11.1 误影响复杂 Regex 模式

风险：

```text
复杂规则模式用户本来可以通过 Regex 自定义 No 或模板。
```

规避：

```text
只修改 IsSimpleModuleMapMode(rules) 为 true 时调用的 CreateSimpleModuleMapName。
不要改 GenerateName、FindMatchingRule、TryGetRegexNo。
```

### 11.2 无编号名称重复

风险：

```text
多个文件夹可能映射出同一个 DisplayName_Type。
```

处理：

```text
沿用现有名称重复检测逻辑。
不在本需求中额外加序号。
```

### 11.3 Type 中包含特殊字符

风险：

```text
文件夹后半段 Type 可能包含短横线、数字、大小写混合。
```

处理：

```text
兜底规则允许 Type 为 [^\\/]+，即只排除路径分隔符。
生成名称直接使用 Type.Trim()。
```

## 12. 验收标准

本需求完成必须满足：

```text
1. 10892_CommonUI 不再因无法提取 No 被跳过。
2. 无编号文件夹生成名称为 DisplayName_Type。
3. 有编号文件夹仍生成 DisplayName_No。
4. 完全不符合 数字_类型 格式的文件夹仍跳过。
5. 不修改 CSV 格式。
6. 不影响复杂 Regex/Contains 模式。
7. dotnet build 通过。
8. dotnet test 通过。
```

## 13. 推荐执行命令

实现前停止运行中的程序：

```powershell
Get-Process -Name VSLoader -ErrorAction SilentlyContinue | Stop-Process -Force
```

构建：

```powershell
dotnet build C:\Users\shee_\OneDrive\Desktop\VSLoader\VSLoader.sln -p:UseSharedCompilation=false
```

测试：

```powershell
dotnet test C:\Users\shee_\OneDrive\Desktop\VSLoader\VSLoader.sln -p:UseSharedCompilation=false
```

## 14. 推荐提交信息

```text
fix: add fallback naming for unnumbered batch folders
```
