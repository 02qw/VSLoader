# v104 地图窗口内快捷键开关编码规格说明

## 1. 背景

当前主界面第二行已有“地图”按钮，点击后可以打开或关闭工厂地图附属窗口。

用户希望新增一个更快的操作方式：通过键盘快捷键快速打开/关闭地图，行为与点击“地图”按钮一致。

默认按键为：

```text
M
```

但该快捷键不能做成 Windows 全局快捷键。因为单键 `M` 如果在全局生效，会导致用户在微信、浏览器、Excel、VSCode 等其他软件中正常输入 `m` 时误触地图开关。

因此本次采用“VSLoader 窗口内快捷键”方案：

```text
只有用户正在操作 VSLoader 主窗口或地图窗口时，地图快捷键才生效。
```

## 2. 目标

实现一个轻量、边界清晰、可配置的地图快捷键：

- 默认 `M` 键打开/关闭地图。
- 功能与点击主界面“地图”按钮完全一致。
- 可以在设置中配置地图快捷键。
- 快捷键只在 VSLoader 主窗口或地图窗口处于激活状态时生效。
- 不注册 Windows 全局热键。
- 不影响用户在其他软件中输入。
- 不影响用户在 VSLoader 输入框中输入。
- 不打断弹窗、忙碌遮罩、地图拖拽、节点编辑等正在进行的操作。

## 3. 非目标

本次不做：

- 不做系统全局地图快捷键。
- 不支持程序隐藏到托盘后用 `M` 唤醒地图。
- 不支持主窗口最小化时用 `M` 打开地图。
- 不改变现有程序全局快捷键逻辑，例如 `Alt + V` 显示/最小化程序窗口。
- 不复用 `RegisterHotKey` 注册单键。
- 不支持鼠标侧键作为地图快捷键。
- 不改变地图窗口打开、关闭、定位、布局持久化逻辑。
- 不改变地图 JSON 格式。

## 4. 当前代码定位

### 4.1 地图按钮入口

主界面地图按钮位于：

```text
VSLoader/MainWindow.xaml
```

点击事件：

```csharp
FactoryMapButton_Click
```

对应逻辑：

```csharp
private void FactoryMapButton_Click(object sender, RoutedEventArgs e)
{
    ToggleFactoryMapWindow();
}
```

因此地图快捷键最终也应调用同一个 `ToggleFactoryMapWindow()`，保证行为一致。

### 4.2 地图窗口打开关闭逻辑

当前主窗口中已有：

```csharp
private void ToggleFactoryMapWindow()
private void ShowFactoryMapIfNeeded()
private void HideFactoryMapWindow()
private void CloseFactoryMapForExit()
```

本次不重写这些逻辑，只新增快捷键触发入口。

### 4.3 现有程序全局快捷键

当前已有：

```text
GlobalHotkeyService
HotkeyConfig
SettingsViewModel.Hotkey
AppConfig.Hotkey
```

该快捷键用于显示/最小化 VSLoader 程序窗口，属于 Windows 全局快捷键，要求至少一个修饰键。

本次地图快捷键不应混入该逻辑，应新增独立配置，避免概念混淆。

## 5. 配置设计

### 5.1 新增配置模型

建议新增：

```csharp
public sealed class MapHotkeyConfig
{
    public bool Enabled { get; set; } = true;

    public string Key { get; set; } = "M";

    public MapHotkeyConfig Clone()
}
```

第一阶段只支持单键，不支持 Ctrl/Alt/Shift 组合。

### 5.2 配置保存位置

建议保存到当前工作区配置：

```text
AppConfig
```

新增字段：

```csharp
public MapHotkeyConfig MapHotkey { get; set; } = new();
```

理由：

- 地图是工作区内数据和操作体验的一部分。
- 不影响其他工作区。
- 不污染程序全局配置。
- 符合当前工作区配置中已保存快捷项、AdminUI、WebUI、更新检测等行为配置的模式。

### 5.3 默认兼容

`ConfigService` 加载旧配置时需要兜底：

```csharp
config.MapHotkey ??= new MapHotkeyConfig();
```

这样旧版本配置文件不会因为缺少字段而异常。

## 6. 设置界面设计

### 6.1 设置页新增区域

在设置窗口中新增一块：

```text
地图快捷键
```

建议放在现有“程序快捷键”附近，但文案上明确区分：

```text
程序快捷键：用于显示/最小化 VSLoader 程序窗口
地图快捷键：仅在 VSLoader 窗口内用于打开/关闭地图
```

### 6.2 控件

建议包含：

- 启用地图快捷键 CheckBox。
- 只读 TextBox 显示当前按键。
- “录制”按钮。
- “清除”按钮。

默认显示：

```text
M
```

### 6.3 录制规则

录制地图快捷键时：

- 只记录主键。
- 忽略 Ctrl/Alt/Shift 修饰状态。
- 不允许纯修饰键。
- 不允许鼠标侧键。
- 不允许危险或易冲突按键。

建议允许：

```text
A-Z
0-9
F1-F12
Space
Escape
```

建议第一阶段不允许：

```text
Enter
Tab
Backspace
Delete
方向键
Ctrl
Alt
Shift
```

理由：

- Enter/Tab/Backspace/Delete 在输入和表格中有明确含义。
- 方向键在地图编辑、表格选择中有明确含义。
- 修饰键单独作为快捷键没有明确触发语义。

### 6.4 保存校验

保存设置时需要校验：

- 如果未启用，允许保存。
- 如果启用，Key 不能为空。
- Key 必须属于支持范围。
- Key 不允许与输入控件常用控制键冲突。

地图快捷键可以和程序全局快捷键的主键相同，因为两者作用域不同。

但如果未来支持组合键，则再增加完整冲突检测。本次不做。

## 7. 触发规则

### 7.1 生效窗口

地图快捷键只在以下窗口激活时生效：

- 主窗口 `MainWindow`
- 地图窗口 `FactoryMapWindow`

不在以下情况下生效：

- VSLoader 被其他应用覆盖且没有焦点。
- 主窗口隐藏到托盘。
- 主窗口最小化。
- 设置窗口激活。
- 批量新增窗口激活。
- 自定义确认弹窗激活。
- 其他模态窗口激活。

### 7.2 主窗口触发

主窗口监听：

```csharp
PreviewKeyDown
```

当按键满足地图快捷键，并且通过统一判断后：

```csharp
ToggleFactoryMapWindow();
```

### 7.3 地图窗口触发

地图窗口也需要监听：

```csharp
PreviewKeyDown
```

但不建议地图窗口直接操作主窗口内部状态。建议构造 `FactoryMapWindow` 时增加一个轻量回调：

```csharp
Action? toggleMapWindowFromHotkey
```

地图窗口检测到可触发时调用该回调。

也可以将按键判断放在主窗口层，通过地图窗口事件回调到主窗口处理。

推荐：

```text
主窗口负责最终 ToggleFactoryMapWindow。
地图窗口只负责把快捷键事件转交给主窗口。
```

## 8. 不响应规则

为了避免误触，任何一个条件命中时都不响应地图快捷键。

### 8.1 输入控件不响应

当前键盘焦点位于以下控件内时，不响应：

- `TextBox`
- `PasswordBox`
- 可编辑 `ComboBox`
- `RichTextBox`
- 其他可编辑文本控件

示例：

```text
用户在搜索框输入 m，只输入文字，不打开/关闭地图。
```

### 8.2 忙碌状态不响应

如果主界面 ViewModel 正在忙碌：

```csharp
IsBusy == true
```

不响应。

包括：

- 自动获取连接。
- 扫描预览。
- 导入导出。
- 更新检查或更新动作中。

### 8.3 模态窗口不响应

如果当前有模态窗口打开，不响应。

建议判断方式：

```csharp
Application.Current.Windows
    .OfType<Window>()
    .Any(window => window.IsActive && window != this && window != _factoryMapWindow)
```

实现时需避免误判隐藏窗口。

### 8.4 地图编辑操作中不响应

地图窗口内如果正在进行以下状态，不响应：

- 正在拖动地图。
- 正在拖动单个节点。
- 正在拖动多选节点。
- 正在框选多选区域。
- 正在连线且已选择起点。
- 右键菜单已打开。
- 地图忙碌遮罩显示中。

建议在 `FactoryMapWindow` 暴露内部判断：

```csharp
internal bool CanToggleMapFromHotkey()
```

或静态可测方法：

```csharp
internal static bool ShouldToggleMapFromHotkey(...)
```

### 8.5 主窗口不可用时不响应

以下情况不响应：

- `IsVisible == false`
- `WindowState == WindowState.Minimized`
- `_isExitRequested == true`
- `_hasCleanedUpForClose == true`

## 9. 推荐实现结构

### 9.1 新增配置类

新增文件：

```text
VSLoader/Models/MapHotkeyConfig.cs
```

### 9.2 AppConfig 增加字段

修改：

```text
VSLoader/Models/AppConfig.cs
```

新增：

```csharp
public MapHotkeyConfig MapHotkey { get; set; } = new();
```

### 9.3 ConfigService 兜底

修改：

```text
VSLoader/Models/Services/ConfigService.cs
```

加载后确保：

```csharp
config.MapHotkey ??= new MapHotkeyConfig();
```

### 9.4 SettingsViewModel 增加地图快捷键

修改：

```text
VSLoader/ViewModels/SettingsViewModel.cs
```

新增：

```csharp
MapHotkeyConfig MapHotkey
string MapHotkeyDisplayText
bool IsRecordingMapHotkey
StartRecordMapHotkeyCommand
ClearMapHotkeyCommand
SetRecordedMapHotkey(...)
ValidateMapHotkeyConfig()
```

### 9.5 SettingsWindow 录制地图快捷键

修改：

```text
VSLoader/Views/SettingsWindow.xaml
VSLoader/Views/SettingsWindow.xaml.cs
```

注意：

- 程序全局快捷键录制仍然保留原逻辑。
- 地图快捷键录制走独立状态 `IsRecordingMapHotkey`。
- 如果两个录制状态同时出现，应优先只允许一个为 true。
- 开始录制地图快捷键时，取消程序快捷键录制状态。
- 开始录制程序快捷键时，取消地图快捷键录制状态。

### 9.6 MainViewModel 保存配置

修改：

```text
VSLoader/ViewModels/MainViewModel.cs
```

创建设置窗口时传入：

```csharp
_config.MapHotkey
```

保存时写回：

```csharp
_config.MapHotkey = viewModel.MapHotkey.Clone();
```

### 9.7 MainWindow 监听快捷键

修改：

```text
VSLoader/MainWindow.xaml.cs
```

建议新增：

```csharp
PreviewKeyDown += MainWindow_PreviewKeyDown;
```

或在 XAML 中绑定。

处理逻辑：

```csharp
private void MainWindow_PreviewKeyDown(object sender, KeyEventArgs e)
{
    if (!ShouldHandleMapHotkey(e))
    {
        return;
    }

    ToggleFactoryMapWindow();
    e.Handled = true;
}
```

### 9.8 FactoryMapWindow 转交快捷键

修改：

```text
VSLoader/Views/FactoryMapWindow.xaml.cs
```

构造函数增加：

```csharp
Action<KeyEventArgs>? mapHotkeyPressed
```

或更简单：

```csharp
Func<KeyEventArgs, bool>? tryToggleMapFromHotkey
```

推荐使用：

```csharp
Func<KeyEventArgs, bool>? tryToggleMapFromHotkey
```

地图窗口中：

```csharp
if (tryToggleMapFromHotkey?.Invoke(e) == true)
{
    e.Handled = true;
}
```

主窗口仍然负责完整判断和 Toggle。

## 10. 按键匹配规则

建议新增轻量服务：

```text
VSLoader/Models/Services/MapHotkeyService.cs
```

职责：

- 校验地图快捷键。
- 格式化显示文本。
- 判断 KeyEventArgs 是否匹配配置。
- 判断按键是否属于可录制范围。

示例接口：

```csharp
public static SaveResult Validate(MapHotkeyConfig config)
public static string Format(MapHotkeyConfig config)
public static bool IsMatch(MapHotkeyConfig config, Key key)
public static bool IsSupportedKey(Key key)
public static string GetDisplayKey(Key key)
```

注意 WPF 中 Alt 组合可能出现 `Key.System`，但本次地图快捷键不支持修饰键，因此可以直接忽略带修饰键的触发：

```csharp
Keyboard.Modifiers == ModifierKeys.None
```

如果用户按下 Ctrl+M，不应触发地图快捷键。

## 11. 用户体验要求

### 11.1 默认体验

首次使用：

- 地图快捷键默认启用。
- 默认键为 `M`。
- 用户在主窗口或地图窗口按 `M` 可开关地图。

### 11.2 状态提示

如果触发成功，可以不额外弹窗。

如需提示，可复用状态栏临时提示：

```text
地图已打开
地图已关闭
```

但第一阶段不是必须。

### 11.3 设置文案

设置页文案必须让用户知道作用域：

```text
仅在 VSLoader 主窗口或地图窗口激活时生效
```

避免用户误以为隐藏到托盘后也能用 `M` 唤醒地图。

## 12. 测试建议

### 12.1 MapHotkeyService 测试

新增：

```text
VSLoader.Tests/MapHotkeyServiceTests.cs
```

覆盖：

1. 默认配置 `M` 有效。
2. `M` 匹配 `Key.M`。
3. `Ctrl + M` 不匹配。
4. 空 Key 且启用时校验失败。
5. 纯修饰键校验失败。
6. Enter/Tab/Backspace/Delete/方向键校验失败。
7. A-Z、0-9、F1-F12、Space、Escape 校验通过。

### 12.2 MainWindow 判断测试

新增或扩展：

```text
VSLoader.Tests/MainWindowMapHotkeyTests.cs
```

覆盖静态判断方法：

- 主窗口可见、未最小化、未忙碌、焦点非输入控件时可触发。
- 主窗口隐藏时不可触发。
- 主窗口最小化时不可触发。
- 忙碌中不可触发。
- 有模态窗口时不可触发。
- 输入控件聚焦时不可触发。

### 12.3 FactoryMapWindow 判断测试

新增或扩展：

```text
VSLoader.Tests/FactoryMapWindowHotkeyTests.cs
```

覆盖：

- 地图正常浏览状态可转交快捷键。
- 拖动地图时不可触发。
- 拖动节点时不可触发。
- 框选多选时不可触发。
- 连线已选择起点时不可触发。
- 忙碌遮罩显示时不可触发。

### 12.4 回归验证

必须运行：

```powershell
dotnet test .\VSLoader.sln -c Debug --no-restore
dotnet build .\VSLoader.sln -c Debug --no-restore
```

## 13. 手动验证清单

1. 启动程序，主窗口激活时按 `M`，地图打开。
2. 地图打开后按 `M`，地图关闭。
3. 地图窗口激活时按 `M`，地图关闭。
4. 在搜索框输入 `m`，只输入文字，地图不打开/关闭。
5. 打开设置窗口后按 `M`，地图不变化。
6. 打开批量新增窗口后按 `M`，地图不变化。
7. 自动获取连接忙碌中按 `M`，地图不变化。
8. 地图编辑模式拖动节点时按 `M`，地图不变化。
9. 切到其他软件按 `M`，地图不变化。
10. 主窗口叉掉隐藏到托盘后按 `M`，地图不变化。
11. 使用现有程序全局快捷键唤醒主窗口后，再按 `M` 可打开地图。
12. 设置中修改地图快捷键后，新快捷键生效。
13. 清除地图快捷键后，按 `M` 不再触发。

## 14. 验收标准

满足以下条件即完成：

- 默认 `M` 可以在主窗口/地图窗口激活时打开或关闭地图。
- 行为与点击“地图”按钮一致。
- 设置中可以启用、清除、录制地图快捷键。
- 地图快捷键配置保存到当前工作区配置，不影响其他工作区。
- 旧配置文件缺少 `MapHotkey` 时可以正常启动，并自动使用默认值。
- 地图快捷键不注册 Windows 全局热键。
- 其他应用中按 `M` 不会影响 VSLoader。
- 输入框中输入 `m` 不会触发地图开关。
- 弹窗、忙碌、地图拖拽/编辑过程中不会误触关闭地图。
- 现有程序全局快捷键功能不受影响。
- `dotnet test .\VSLoader.sln -c Debug --no-restore` 通过。
- `dotnet build .\VSLoader.sln -c Debug --no-restore` 通过。
