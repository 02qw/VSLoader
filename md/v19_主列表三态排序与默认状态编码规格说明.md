# v19 主列表三态排序与默认状态编码规格说明

## 1. 需求背景

v17 已实现主列表列头点击排序。

v18 已将排序反馈从 WPF 默认箭头改成显式文字箭头，例如：

```text
名称 ↑
备注 ↓
更新时间 ↑
```

但当前排序状态只有两种：

```text
升序 -> 降序 -> 升序 -> 降序
```

用户希望排序状态应有三种：

```text
默认排序 -> 升序 -> 降序 -> 默认排序
```

也就是说，同一列连续点击三次时：

```text
第一次：升序，显示 ↑
第二次：降序，显示 ↓
第三次：恢复默认排序，箭头消失
```

本次 v19 需求需要实现主列表三态排序。

## 2. 需求目标

本次开发目标：

1. 主列表排序支持三种状态：
   - 默认排序
   - 升序
   - 降序
2. 初始状态为默认排序，不显示任何箭头。
3. 点击任意列第一次，该列升序，显示 `↑`。
4. 再次点击同一列，该列降序，显示 `↓`。
5. 第三次点击同一列，恢复默认排序，所有箭头消失。
6. 点击不同列时，新列从升序开始。
7. 默认排序仍然使用当前的名称自然升序。
8. 不改变业务功能。

## 3. 默认排序定义

默认排序不是“无排序”。

VSLoader 的默认排序定义为：

```text
名称自然升序
```

也就是继续使用当前 `ShortcutSortService` 的名称自然排序逻辑：

```text
热贴机_001
热贴机_002
热贴机_003
```

默认排序状态下：

- 列表仍然按名称自然升序排列。
- 表头不显示箭头。

示例：

```text
名称
备注
更新时间
```

而不是：

```text
名称 ↑
备注
更新时间
```

## 4. 修改范围

可能涉及文件：

- `VSLoader\ViewModels\MainViewModel.cs`
- `VSLoader\MainWindow.xaml.cs`
- `VSLoader\Services\ShortcutSortService.cs`

原则上不需要修改：

- `VSLoader\MainWindow.xaml`
- 数据模型
- 配置服务
- AdminUI 相关服务

本次只修改排序状态机和表头箭头显示逻辑。

## 5. 当前逻辑问题

当前排序逻辑可能类似：

```csharp
if (CurrentSortField == field)
{
    CurrentSortDirection = CurrentSortDirection == ListSortDirection.Ascending
        ? ListSortDirection.Descending
        : ListSortDirection.Ascending;
}
else
{
    CurrentSortField = field;
    CurrentSortDirection = ListSortDirection.Ascending;
}
```

该逻辑只有两态：

```text
Ascending <-> Descending
```

缺少第三态：

```text
Default / None
```

## 6. 推荐状态设计

### 6.1 使用可空方向表示默认状态

推荐将排序方向改成可空：

```csharp
public ListSortDirection? CurrentSortDirection { get; private set; }
```

含义：

```text
null = 默认排序，无箭头
Ascending = 当前列升序
Descending = 当前列降序
```

排序字段仍保留：

```csharp
public ShortcutSortField CurrentSortField { get; private set; }
```

默认状态：

```csharp
CurrentSortField = ShortcutSortField.Name;
CurrentSortDirection = null;
```

### 6.2 默认排序方向常量

底层默认排序仍然需要一个实际比较器方向：

```csharp
private const ListSortDirection DefaultSortDirection = ListSortDirection.Ascending;
```

或者直接在恢复默认排序时使用：

```csharp
SetCustomSort(ShortcutSortField.Name, ListSortDirection.Ascending);
```

## 7. 点击排序状态机

新增或修改 `ApplySort(ShortcutSortField field)`：

### 7.1 当前为默认状态

条件：

```csharp
CurrentSortDirection is null
```

点击任意列：

```text
CurrentSortField = 点击列
CurrentSortDirection = Ascending
```

### 7.2 当前排序列不是点击列

条件：

```csharp
CurrentSortField != field
```

点击新列：

```text
CurrentSortField = 点击列
CurrentSortDirection = Ascending
```

### 7.3 当前排序列是点击列，且当前为升序

条件：

```csharp
CurrentSortField == field
CurrentSortDirection == Ascending
```

点击同一列：

```text
CurrentSortDirection = Descending
```

### 7.4 当前排序列是点击列，且当前为降序

条件：

```csharp
CurrentSortField == field
CurrentSortDirection == Descending
```

点击同一列：

```text
恢复默认排序
CurrentSortField = Name
CurrentSortDirection = null
```

并应用：

```csharp
SetCustomSort(ShortcutSortField.Name, ListSortDirection.Ascending);
```

## 8. ViewModel 推荐实现

推荐 `ApplySort` 返回当前 UI 排序方向，方便 code-behind 更新表头。

可选方案 A：

```csharp
public ListSortDirection? ApplySort(ShortcutSortField field)
```

返回：

- `Ascending`
- `Descending`
- `null`

可选方案 B：

保持 `void ApplySort(...)`，然后 code-behind 读取：

```csharp
viewModel.CurrentSortDirection
```

本项目当前已有 `CurrentSortDirection`，可以使用方案 B。

## 9. 表头箭头显示逻辑

v18 中已有显式文字箭头方法，例如：

```csharp
UpdateSortHeaders(DataGridColumn sortedColumn, ListSortDirection direction)
```

需要改为支持可空方向：

```csharp
UpdateSortHeaders(DataGridColumn? sortedColumn, ListSortDirection? direction)
```

规则：

1. 每次先恢复所有表头原始标题。
2. 如果 `direction is null`，不显示任何箭头。
3. 如果 `direction` 为升序或降序，则只给当前列追加箭头。

示例：

```csharp
if (direction is null || sortedColumn is null)
{
    return;
}

var arrow = direction == ListSortDirection.Ascending ? " ↑" : " ↓";
sortedColumn.Header = $"{title}{arrow}";
```

## 10. 初始状态要求

程序启动后：

```text
名称
备注
更新时间
```

不要显示：

```text
名称 ↑
```

但实际排序仍然是名称自然升序。

因此 `SetInitialSortDirection()` 应调整为：

```csharp
UpdateSortHeaders(null, null);
```

或者直接调用恢复标题方法。

## 11. 点击行为示例

### 11.1 点击名称

初始：

```text
名称
备注
更新时间
```

第一次点击：

```text
名称 ↑
备注
更新时间
```

第二次点击：

```text
名称 ↓
备注
更新时间
```

第三次点击：

```text
名称
备注
更新时间
```

列表恢复默认排序。

### 11.2 点击备注

初始：

```text
名称
备注
更新时间
```

第一次点击：

```text
名称
备注 ↑
更新时间
```

第二次点击：

```text
名称
备注 ↓
更新时间
```

第三次点击：

```text
名称
备注
更新时间
```

列表恢复默认名称自然升序。

### 11.3 切换列

当前：

```text
备注 ↓
```

点击 `更新时间`：

```text
更新时间 ↑
```

新列从升序开始。

## 12. 不允许改变的内容

本次不允许改变：

- 名称自然排序算法。
- 备注排序比较规则。
- 更新时间排序比较规则。
- 搜索过滤。
- 顶部按钮布局。
- 主列表列宽。
- 主列表行样式。
- 右键菜单。
- 双击打开。
- AdminUI 功能。
- 自动获取连接功能。
- 配置保存逻辑。

## 13. 验收标准

### 13.1 初始状态

启动应用后：

- 表头显示 `名称`、`备注`、`更新时间`。
- 不显示任何箭头。
- 列表按名称自然升序排列。

### 13.2 同列三态切换

点击同一列三次：

```text
第一次：显示 ↑
第二次：显示 ↓
第三次：箭头消失，恢复默认排序
```

适用于：

- 名称
- 备注
- 更新时间

### 13.3 不同列切换

当某列处于升序或降序时，点击另一列：

- 新列显示 `↑`。
- 旧列箭头消失。
- 按新列升序排序。

### 13.4 功能回归

以下功能必须正常：

1. 搜索过滤。
2. 单击选中。
3. 双击打开 VSCode。
4. 右键菜单。
5. AdminUI。
6. 打开。
7. 编辑。
8. 删除。
9. 自动获取连接。

### 13.5 编译验收

执行：

```powershell
dotnet build .\VSLoader.sln
```

期望：

```text
0 warnings
0 errors
```

如果提示 `VSLoader.exe` 被占用，说明程序正在运行，应先关闭 VSLoader 后重新构建。

## 14. 建议实施步骤

1. 打开 `VSLoader\ViewModels\MainViewModel.cs`。
2. 将 `CurrentSortDirection` 改为 `ListSortDirection?`。
3. 默认值改为 `null`。
4. 修改 `ApplySort` 为三态状态机。
5. 恢复默认排序时应用名称自然升序。
6. 打开 `VSLoader\MainWindow.xaml.cs`。
7. 修改 `UpdateSortHeaders` 支持 `null` 方向。
8. 初始状态不显示任何箭头。
9. 点击列头后根据 `CurrentSortDirection` 显示或清除箭头。
10. 编译并人工验证。

## 15. 明确不做的事情

本次不做：

- 保存用户排序偏好。
- 多列组合排序。
- 自定义图标箭头。
- 修改排序算法。
- 修改业务逻辑。
