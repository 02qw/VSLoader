# v88 更新提醒关闭确认基线编码规格说明

## 1. 背景

v84 实现了工作区级自动检测更新提醒能力，能够同时检测：

1. rules 批量配置文件。
2. map 地图配置文件。
3. 软件版本文件。

当前提醒关闭行为采用方案 A：

```text
用户点击更新提醒栏的 ×
  ↓
只临时隐藏当前提示
  ↓
不更新 updateTime.json
  ↓
下次后台检测仍然发现同一次更新
  ↓
继续弹出提醒
```

这个逻辑的优点是不会漏提醒，但实际使用中可能比较打扰用户。

现在需要改为方案 B：

```text
用户点击更新提醒栏的 ×
  ↓
代表“这一次更新我已经知道了”
  ↓
程序把当前检测到的文件时间/软件版本写入 updateTime.json
  ↓
后续不再提示这一次更新
  ↓
只有文件再次变化或软件版本再次升高时才重新提示
```

## 2. 本次目标

### 2.1 用户行为目标

当主界面显示：

```text
检测到更新：批量规则文件、地图配置文件、软件版本
```

用户点击右侧关闭按钮 `×` 后：

1. 当前更新提醒栏关闭。
2. 程序记录“用户已经确认过本次检测到的更新”。
3. 将本次检测到的 rules/map 文件更新时间、软件版本号写入当前工作区的 `updateTime.json`。
4. 后续 10 分钟轮询检测到同一个文件时间/同一个软件版本时，不再重复提醒。
5. 如果后续 rules/map 文件再次被修改，或者软件版本文件中的版本号再次升高，则继续提醒。

### 2.2 代码目标

1. `UpdateCheckResult` 需要携带本次检测到的可确认基线值。
2. `UpdateCheckService` 需要提供“确认本次检测结果”的写入方法。
3. `MainViewModel` 需要在显示更新提醒时保存最近一次检测结果。
4. `CloseUpdateNoticeCommand` 需要从“单纯隐藏”改成“确认并隐藏”。
5. 保持更新检测失败栏 `CloseUpdateFailureCommand` 不变。

## 3. 非目标

本次不做：

1. 不改变 10 分钟检测间隔。
2. 不改变设置窗口中的三个更新检测路径配置。
3. 不改变 rules/map 实际导入成功后的基线更新逻辑。
4. 不改变软件自动更新按钮的执行逻辑。
5. 不改变 `updateTime.json` 文件路径。
6. 不增加新的 UI 按钮。
7. 不让关闭失败提醒栏写入任何基线。
8. 不做“稍后提醒我”这种二级选择。

## 4. 当前代码现状

### 4.1 当前提醒关闭逻辑

当前 `MainViewModel.CloseUpdateNotice()` 逻辑为：

```csharp
private void CloseUpdateNotice()
{
    UpdateNoticeMessage = string.Empty;
    HasUpdateNotice = false;
}
```

该逻辑只修改界面状态，不写入 `updateTime.json`。

### 4.2 当前检测结果对象

当前 `UpdateCheckResult` 只有：

```csharp
public List<string> UpdatedItems { get; } = new();
public List<string> Failures { get; } = new();
```

这个结构只能表达“哪些项目有更新”，不能表达“本次检测到的具体更新时间/版本号是多少”。

因此要实现方案 B，不能只改关闭按钮，还必须扩展检测结果对象。

### 4.3 当前 updateTime.json 语义

当前结构：

```csharp
public sealed class UpdateTimeState
{
    public UpdateFileState Rules { get; set; } = new();
    public UpdateFileState Map { get; set; } = new();
    public UpdateSoftwareState Software { get; set; } = new();
}

public sealed class UpdateFileState
{
    public DateTime? LastUsedWriteTimeUtc { get; set; }
}

public sealed class UpdateSoftwareState
{
    public string LastUsedVersion { get; set; } = string.Empty;
}
```

v88 后，这里的字段含义需要扩展为：

```text
LastUsedWriteTimeUtc:
    已经导入/使用过，或已经被用户确认知晓的文件更新时间。

LastUsedVersion:
    当前已安装版本，或已经被用户确认知晓的远端软件版本。
```

## 5. 行为规则

### 5.1 rules 文件更新提醒

检测逻辑：

```text
当前 rules 文件 LastWriteTimeUtc > updateTime.Rules.LastUsedWriteTimeUtc
  ↓
显示“批量规则文件”更新提醒
  ↓
UpdateCheckResult 记录 DetectedRulesWriteTimeUtc
```

用户点击提醒栏 `×`：

```text
updateTime.Rules.LastUsedWriteTimeUtc = DetectedRulesWriteTimeUtc
```

下次检测：

```text
当前 rules 文件 LastWriteTimeUtc == updateTime.Rules.LastUsedWriteTimeUtc
  ↓
不再提示同一次更新
```

### 5.2 map 文件更新提醒

检测逻辑：

```text
当前 map 文件 LastWriteTimeUtc > updateTime.Map.LastUsedWriteTimeUtc
  ↓
显示“地图配置文件”更新提醒
  ↓
UpdateCheckResult 记录 DetectedMapWriteTimeUtc
```

用户点击提醒栏 `×`：

```text
updateTime.Map.LastUsedWriteTimeUtc = DetectedMapWriteTimeUtc
```

### 5.3 软件版本更新提醒

检测逻辑：

```text
软件版本 txt 中的 latestVersion > 当前程序版本 currentVersion
  ↓
显示“软件版本”更新提醒
  ↓
UpdateCheckResult 记录 DetectedSoftwareVersion
```

用户点击提醒栏 `×`：

```text
updateTime.Software.LastUsedVersion = DetectedSoftwareVersion
```

后续规则：

1. 如果软件版本 txt 仍然是同一个版本，不再重复提醒。
2. 如果软件版本 txt 提升到更高版本，继续提醒。
3. 如果用户实际执行软件更新，原有软件更新逻辑保持不变。

注意：

```text
方案 B 下，LastUsedVersion 不再只表示“当前程序已经安装到该版本”，也表示“用户已经确认过该远端版本提醒”。
```

这是用户选择方案 B 后接受的提醒语义。

### 5.4 更新检测失败栏不变

失败栏示例：

```text
更新检测失败：rules 文件不存在、软件版本号格式无效
```

用户点击失败栏 `×`：

1. 只关闭失败栏。
2. 不写 `updateTime.json`。
3. 下次检测仍然失败时，可以再次显示失败栏。

原因：

```text
失败不是可确认的更新基线。
失败栏代表配置或读取异常，不能通过关闭来写入成功状态。
```

## 6. 数据结构修改

### 6.1 UpdateCheckResult 扩展

建议修改：

```csharp
public sealed class UpdateCheckResult
{
    public List<string> UpdatedItems { get; } = new();

    public List<string> Failures { get; } = new();

    public DateTime? DetectedRulesWriteTimeUtc { get; set; }

    public DateTime? DetectedMapWriteTimeUtc { get; set; }

    public string DetectedSoftwareVersion { get; set; } = string.Empty;
}
```

说明：

1. `DetectedRulesWriteTimeUtc` 只在本次检测到 rules 更新时写入。
2. `DetectedMapWriteTimeUtc` 只在本次检测到 map 更新时写入。
3. `DetectedSoftwareVersion` 只在本次检测到软件版本更新时写入。
4. 没有更新的项目保持空值。

### 6.2 可选更清晰结构

如果实现时希望语义更清晰，也可以新增：

```csharp
public sealed class UpdateCheckDetectedBaseline
{
    public DateTime? RulesWriteTimeUtc { get; set; }
    public DateTime? MapWriteTimeUtc { get; set; }
    public string SoftwareVersion { get; set; } = string.Empty;
}
```

然后在 `UpdateCheckResult` 中使用：

```csharp
public UpdateCheckDetectedBaseline DetectedBaseline { get; } = new();
```

两种方式都可以。为了最小修改，推荐使用 6.1 的直接属性方式。

## 7. UpdateCheckService 修改规格

### 7.1 CheckFile 需要写入检测值

当前 `CheckFile` 只负责加入 `UpdatedItems`。

v88 后，当发现文件更新时间大于基线时，需要同时写入检测值。

推荐将 `CheckFile` 增加一个回调参数：

```csharp
Action<DateTime> setDetectedWriteTimeUtc
```

伪代码：

```csharp
if (writeTimeUtc > state.LastUsedWriteTimeUtc.Value)
{
    result.UpdatedItems.Add(updateLabel);
    setDetectedWriteTimeUtc(writeTimeUtc);
}
```

调用示例：

```csharp
CheckFile(
    config.RulesFilePath,
    state.Rules,
    "批量规则文件",
    "rules 文件不存在",
    result,
    value => result.DetectedRulesWriteTimeUtc = value);
```

map 同理。

### 7.2 CheckSoftware 需要写入检测版本

当 `latestVersion > currentVersion` 时：

```csharp
result.UpdatedItems.Add("软件版本");
result.DetectedSoftwareVersion = FormatVersion(latestVersion);
```

注意：

1. 需要使用统一格式化后的版本号，避免 `2.1`、`2.1.0` 比较后写入格式不一致。
2. 如果版本文件格式无效，不写入检测版本。

### 7.3 新增确认方法

建议新增：

```csharp
public SaveResult AcknowledgeDetectedUpdates(
    string updateTimePath,
    UpdateCheckResult result)
```

职责：

1. 读取 `updateTime.json`。
2. 如果 `result.DetectedRulesWriteTimeUtc` 有值，则写入 `state.Rules.LastUsedWriteTimeUtc`。
3. 如果 `result.DetectedMapWriteTimeUtc` 有值，则写入 `state.Map.LastUsedWriteTimeUtc`。
4. 如果 `result.DetectedSoftwareVersion` 非空，则写入 `state.Software.LastUsedVersion`。
5. 保存 `updateTime.json`。

伪代码：

```csharp
public SaveResult AcknowledgeDetectedUpdates(string updateTimePath, UpdateCheckResult result)
{
    var loadResult = LoadState(updateTimePath);
    if (!loadResult.Success)
    {
        return SaveResult.Fail(loadResult.ErrorMessage ?? "updateTime.json 读取失败");
    }

    if (result.DetectedRulesWriteTimeUtc is not null)
    {
        loadResult.State.Rules.LastUsedWriteTimeUtc = result.DetectedRulesWriteTimeUtc;
    }

    if (result.DetectedMapWriteTimeUtc is not null)
    {
        loadResult.State.Map.LastUsedWriteTimeUtc = result.DetectedMapWriteTimeUtc;
    }

    if (!string.IsNullOrWhiteSpace(result.DetectedSoftwareVersion))
    {
        loadResult.State.Software.LastUsedVersion = result.DetectedSoftwareVersion.Trim();
    }

    return SaveState(updateTimePath, loadResult.State);
}
```

### 7.4 空结果处理

如果 `result` 中没有任何可确认值：

```text
DetectedRulesWriteTimeUtc == null
DetectedMapWriteTimeUtc == null
DetectedSoftwareVersion 为空
```

则方法可以直接返回成功，不必写文件。

推荐行为：

```csharp
if (!HasDetectedBaseline(result))
{
    return SaveResult.Ok();
}
```

## 8. MainViewModel 修改规格

### 8.1 新增字段保存最近一次更新检测结果

新增字段：

```csharp
private UpdateCheckResult? _lastUpdateCheckResult;
```

用途：

```text
主界面显示更新提醒时，关闭按钮需要知道本次提醒对应的具体文件时间/版本。
```

### 8.2 ApplyUpdateCheckResult 保存结果

当前逻辑：

```csharp
if (result.UpdatedItems.Count > 0)
{
    UpdateNoticeMessage = $"检测到更新：{string.Join("、", result.UpdatedItems)}";
    HasUpdateNotice = true;
}
else
{
    UpdateNoticeMessage = string.Empty;
    HasUpdateNotice = false;
}
```

v88 后：

```csharp
if (result.UpdatedItems.Count > 0)
{
    _lastUpdateCheckResult = result;
    UpdateNoticeMessage = $"检测到更新：{string.Join("、", result.UpdatedItems)}";
    HasUpdateNotice = true;
}
else
{
    _lastUpdateCheckResult = null;
    UpdateNoticeMessage = string.Empty;
    HasUpdateNotice = false;
}
```

注意：

1. 只有有更新提醒时保存结果。
2. 没有更新时清空，避免关闭按钮误确认旧结果。

### 8.3 CloseUpdateNotice 改为确认并关闭

推荐逻辑：

```csharp
private void CloseUpdateNotice()
{
    if (_lastUpdateCheckResult is not null)
    {
        var saveResult = _updateCheckService.AcknowledgeDetectedUpdates(
            _updateTimePath,
            _lastUpdateCheckResult);

        if (!saveResult.Success)
        {
            UpdateFailureMessage = $"更新提醒确认失败：{saveResult.ErrorMessage}";
            HasUpdateFailure = true;
            return;
        }
    }

    _lastUpdateCheckResult = null;
    UpdateNoticeMessage = string.Empty;
    HasUpdateNotice = false;
}
```

### 8.4 保存失败处理

如果点击关闭提醒时写入 `updateTime.json` 失败：

1. 不关闭更新提醒栏。
2. 显示更新检测失败栏：

```text
更新提醒确认失败：updateTime.json 保存失败：xxx
```

原因：

```text
如果保存失败但仍然隐藏，用户会误以为已经确认成功。
保留提醒可以让用户知道这次关闭没有真正生效。
```

### 8.5 线程要求

`CloseUpdateNotice` 是 UI 命令，当前在 UI 线程执行。

`updateTime.json` 文件很小，可以同步写入。

本次不引入异步命令，避免改动过大。

## 9. 与现有功能关系

### 9.1 与 rules 实际导入成功的关系

现有逻辑：

```text
批量新增识别窗口成功确认导入
  ↓
MarkRulesUsed(...)
  ↓
更新 rules 基线
```

v88 保持该逻辑。

新增逻辑：

```text
用户关闭 rules 更新提醒
  ↓
AcknowledgeDetectedUpdates(...)
  ↓
也会更新 rules 基线
```

两者都允许更新 rules 基线，但语义不同：

1. `MarkRulesUsed` 表示实际使用。
2. `AcknowledgeDetectedUpdates` 表示用户确认知晓。

### 9.2 与 map 实际导入成功的关系

现有逻辑：

```text
地图窗口成功导入图文件并应用
  ↓
MarkMapUsed(...)
```

v88 保持该逻辑。

关闭提醒也可以更新 map 基线，表示用户确认知晓该版本地图文件。

### 9.3 与软件自动更新按钮的关系

v88 不改变“更新软件”按钮。

如果用户关闭了“软件版本”提醒：

```text
updateTime.Software.LastUsedVersion = 远端最新版本
```

即使用户没有实际更新到该版本，也不会再提醒同一个版本。

如果后续远端版本从 `2.1.0` 升到 `2.1.1`：

```text
latestVersion > acknowledgedVersion
```

仍然应该重新提醒。

## 10. 软件版本检测的注意点

当前 `CheckSoftware` 主要比较：

```text
latestVersion > currentVersion
```

方案 B 还需要避免同一个远端版本被重复提醒。

推荐检测逻辑改为：

```text
如果 latestVersion <= currentVersion:
    如果 latestVersion == currentVersion 且 LastUsedVersion != currentVersion:
        更新 LastUsedVersion 为 currentVersion
    不提醒

如果 latestVersion > currentVersion:
    如果 LastUsedVersion == latestVersion:
        不提醒
    否则:
        提醒软件版本更新
        result.DetectedSoftwareVersion = latestVersion
```

这样才能实现：

```text
用户关闭 2.1.0 提醒后，不再提醒 2.1.0。
远端升到 2.1.1 后，再次提醒。
```

否则如果仍然只比较 `latestVersion > currentVersion`，即使写入 `LastUsedVersion = 2.1.0`，下次仍然会因为当前程序版本低于 2.1.0 而重复提醒。

这是本次实现的关键点。

## 11. 测试要求

### 11.1 UpdateCheckServiceTests

新增或调整以下测试。

#### 测试 1：rules 更新结果携带检测时间

场景：

1. `updateTime.json` 中 rules 基线为旧时间。
2. rules 文件更新时间为新时间。

期望：

1. `UpdatedItems` 包含“批量规则文件”。
2. `DetectedRulesWriteTimeUtc` 等于 rules 文件更新时间。

#### 测试 2：确认 rules 更新后不再重复提醒

场景：

1. 第一次检测发现 rules 更新。
2. 调用 `AcknowledgeDetectedUpdates`。
3. 第二次检测同一个 rules 文件。

期望：

1. 第二次检测不再包含“批量规则文件”。

#### 测试 3：map 更新结果携带检测时间

与 rules 同理。

#### 测试 4：确认 map 更新后不再重复提醒

与 rules 同理。

#### 测试 5：软件版本更新携带远端版本

场景：

1. 当前程序版本为 `2.0.0`。
2. 软件版本 txt 为 `2.1.0`。
3. `updateTime.json` 中软件版本为空或低于 `2.1.0`。

期望：

1. `UpdatedItems` 包含“软件版本”。
2. `DetectedSoftwareVersion` 为 `2.1.0`。

#### 测试 6：确认软件版本后不再重复提醒同一版本

场景：

1. 当前程序版本为 `2.0.0`。
2. 远端版本为 `2.1.0`。
3. 第一次检测发现软件更新。
4. 调用 `AcknowledgeDetectedUpdates`。
5. 第二次检测远端仍然为 `2.1.0`。

期望：

1. 第二次检测不再提示“软件版本”。

#### 测试 7：远端版本再次升高后重新提醒

场景：

1. 当前程序版本为 `2.0.0`。
2. 已确认版本为 `2.1.0`。
3. 远端版本改为 `2.1.1`。

期望：

1. 检测结果重新包含“软件版本”。
2. `DetectedSoftwareVersion` 为 `2.1.1`。

### 11.2 MainViewModelTests

新增或调整以下测试。

#### 测试 1：ApplyUpdateCheckResult 保存最近检测结果

场景：

1. 传入包含更新项和检测基线值的 `UpdateCheckResult`。

期望：

1. `HasUpdateNotice = true`。
2. 关闭提醒时可以调用确认写入逻辑。

#### 测试 2：关闭提醒成功后隐藏公告

场景：

1. 当前有更新提醒。
2. `AcknowledgeDetectedUpdates` 返回成功。

期望：

1. `HasUpdateNotice = false`。
2. `UpdateNoticeMessage` 为空。

#### 测试 3：关闭提醒保存失败时保留公告并显示失败栏

场景：

1. 当前有更新提醒。
2. `AcknowledgeDetectedUpdates` 返回失败。

期望：

1. `HasUpdateNotice = true`。
2. `HasUpdateFailure = true`。
3. `UpdateFailureMessage` 包含“更新提醒确认失败”。

## 12. 手工验证

### 12.1 rules 提醒关闭验证

1. 设置 rules 更新检测路径。
2. 修改 rules 文件。
3. 等待或触发更新检测。
4. 主界面出现“检测到更新：批量规则文件”。
5. 点击 `×`。
6. 检查当前工作区 `updateTime.json` 中 rules 时间已更新。
7. 等待下一轮检测。
8. 同一个 rules 文件不再重复提醒。

### 12.2 map 提醒关闭验证

同 rules。

### 12.3 软件版本提醒关闭验证

1. 当前程序版本假设为 `2.0.0`。
2. 软件版本 txt 写入 `2.1.0`。
3. 等待检测。
4. 主界面出现“检测到更新：软件版本”。
5. 点击 `×`。
6. 检查 `updateTime.json` 中：

```json
{
  "Software": {
    "LastUsedVersion": "2.1.0"
  }
}
```

7. 下一轮检测不再提示 `2.1.0`。
8. 将软件版本 txt 改为 `2.1.1`。
9. 下一轮检测重新提示“软件版本”。

### 12.4 保存失败验证

1. 人为让 `updateTime.json` 所在目录不可写，或模拟保存失败。
2. 点击更新提醒关闭按钮。
3. 更新提醒栏不消失。
4. 显示“更新提醒确认失败”。

## 13. 风险与注意事项

### 13.1 用户可能误关闭提醒

方案 B 的天然风险：

```text
用户没有实际导入 rules/map，也没有实际更新软件，只是点掉提醒。
后续同一版本不再提醒。
```

这是本次需求明确接受的取舍。

### 13.2 软件版本基线语义变化

`Software.LastUsedVersion` 不再严格等价于“已安装版本”，也可能是“已确认知晓版本”。

如果后续代码需要判断真实安装版本，不能只依赖 `updateTime.json`，应继续使用程序集版本：

```csharp
Assembly.GetExecutingAssembly().GetName().Version
```

### 13.3 不要影响失败提醒栏

失败提醒栏不能写入基线。

例如：

```text
rules 文件不存在
软件版本文件格式无效
```

这些都不是可确认更新，关闭只代表临时隐藏失败消息。

### 13.4 不要误确认旧结果

`_lastUpdateCheckResult` 必须在没有更新时清空。

否则可能出现：

```text
上一轮有更新
下一轮无更新
用户再点关闭
误把上一轮结果写入 updateTime.json
```

### 13.5 软件版本比较必须考虑已确认版本

这是最容易遗漏的点。

如果仍然只判断：

```text
latestVersion > currentVersion
```

则用户关闭提醒后仍然会重复提示。

必须增加：

```text
latestVersion == state.Software.LastUsedVersion 时，不提醒。
```

## 14. 验收标准

满足以下条件视为完成：

1. 点击更新提醒栏 `×` 后，会写入当前工作区 `updateTime.json`。
2. rules 同一文件时间被确认后，不再重复提醒。
3. map 同一文件时间被确认后，不再重复提醒。
4. 软件同一远端版本被确认后，不再重复提醒。
5. 软件远端版本再次升高后，会重新提醒。
6. 更新检测失败栏关闭行为不变，不写入基线。
7. `updateTime.json` 保存失败时，不隐藏更新提醒，并显示失败消息。
8. 不影响 rules/map 实际导入后的基线更新逻辑。
9. 不影响软件自动更新按钮逻辑。
10. `dotnet test .\VSLoader.sln` 通过。
11. `.\build-release.ps1` 通过。

