# v5 自动获取连接网络预检查与进度显示编码规格说明

## 1. 文档目的

本文档用于指导编程 agent 或开发人员优化 VSLoader 的“自动获取连接”功能。

当前“自动获取连接”已经具备忙碌遮罩和操作屏蔽，但仍存在两个问题：

- 下载前没有先测试 AdminUI URL 是否可访问。
- 加载遮罩使用不确定进度条，用户无法知道当前下载进度。

本次需求目标是：

- 在正式下载前先测试网络连接。
- 网络连接失败时立即提示并停止下载。
- 网络连接成功后显示真实下载进度。
- 下载过程中实时显示当前处理进度和当前处理的快捷项。

## 2. 目标效果

用户点击：

```text
自动获取连接
```

后，程序流程应为：

```text
点击自动获取连接
-> 进入忙碌状态
-> 显示加载遮罩
-> 测试 AdminUI BaseUrl 网络连接
-> 连接失败：提示网络连接失败，停止下载，恢复界面
-> 连接成功：开始逐个下载 JNLP
-> 实时更新进度条和进度文字
-> 下载完成后弹出结果汇总
-> 用户关闭结果弹窗
-> 恢复主界面操作
```

## 3. 网络连接预检查

### 3.1 测试目标

测试目标使用当前配置中的：

```text
AdminUiConfig.BaseUrl
```

示例：

```text
https://192.168.15.69:8181/oistarter/
```

### 3.2 测试方式

推荐使用 `HttpClient` 发送请求。

优先使用：

```text
GET BaseUrl
```

不强制要求返回 200，只要服务器有 HTTP 响应即可认为网络可达。

建议成功条件：

```text
HTTP 状态码 < 500
```

说明：

- 200、301、302、403、404 都说明服务器有响应，可以认为网络连通。
- 5xx 或请求异常视为连接失败。
- DNS 失败、超时、连接拒绝、证书失败视为连接失败。

### 3.3 证书策略

网络测试必须复用 AdminUI 配置：

```csharp
IgnoreCertificateErrors
```

如果：

```csharp
IgnoreCertificateErrors = true
```

测试连接时也应忽略 HTTPS 证书错误。

### 3.4 失败提示

如果连接测试失败，弹窗提示：

```text
网络连接失败，请检查 AdminUI BaseUrl、网络环境或 VPN。
```

可以附加异常原因：

```text
网络连接失败，请检查 AdminUI BaseUrl、网络环境或 VPN。

错误原因：{Exception.Message}
```

连接失败时：

- 不进入正式下载流程。
- 不创建或覆盖任何 `.jnlp` 文件。
- 关闭提示后解除忙碌状态。

## 4. 进度显示需求

当前加载遮罩使用不确定进度条：

```xml
<ProgressBar IsIndeterminate="True" />
```

需要改为确定进度条：

```xml
<ProgressBar Minimum="0"
             Maximum="{Binding BusyProgressMaximum}"
             Value="{Binding BusyProgressValue}" />
```

### 4.1 进度文字

遮罩中显示三类信息：

主提示：

```text
正在自动获取 AdminUI 连接，请稍候...
```

进度统计：

```text
当前进度：8 / 35，成功：7，失败：0
```

当前项目：

```text
正在处理：热贴机_009
```

当正在进行网络预检查时，显示：

```text
正在测试 AdminUI 网络连接...
```

## 5. ViewModel 修改规格

修改：

```text
ViewModels\MainViewModel.cs
```

在现有忙碌状态基础上新增属性：

```csharp
[ObservableProperty]
private int busyProgressValue;

[ObservableProperty]
private int busyProgressMaximum;

[ObservableProperty]
private string busyProgressText = string.Empty;

[ObservableProperty]
private string busyCurrentItemText = string.Empty;
```

### 5.1 初始状态

点击“自动获取连接”后：

```csharp
IsBusy = true;
BusyMessage = "正在自动获取 AdminUI 连接，请稍候...";
BusyProgressValue = 0;
BusyProgressMaximum = Shortcuts.Count;
BusyProgressText = "正在测试 AdminUI 网络连接...";
BusyCurrentItemText = string.Empty;
```

### 5.2 网络测试失败

如果网络测试失败：

```csharp
BusyProgressText = "网络连接失败。";
BusyCurrentItemText = string.Empty;
```

然后弹窗提示失败原因，并在 `finally` 中恢复状态。

### 5.3 下载过程中

下载过程中根据服务层进度回调更新：

```csharp
BusyProgressValue = progress.CompletedCount;
BusyProgressMaximum = progress.TotalCount;
BusyProgressText = $"当前进度：{progress.CompletedCount} / {progress.TotalCount}，成功：{progress.SuccessCount}，失败：{progress.FailedCount}";
BusyCurrentItemText = string.IsNullOrWhiteSpace(progress.CurrentShortcutName)
    ? string.Empty
    : $"正在处理：{progress.CurrentShortcutName}";
```

### 5.4 finally 恢复

无论成功、失败、异常，都必须在 `finally` 中恢复：

```csharp
IsBusy = false;
BusyMessage = string.Empty;
BusyProgressValue = 0;
BusyProgressMaximum = 0;
BusyProgressText = string.Empty;
BusyCurrentItemText = string.Empty;
```

## 6. 新增进度模型

新增文件：

```text
Services\AdminUiDownloadProgress.cs
```

内容：

```csharp
public sealed class AdminUiDownloadProgress
{
    public int TotalCount { get; init; }

    public int CompletedCount { get; init; }

    public int SuccessCount { get; init; }

    public int FailedCount { get; init; }

    public string CurrentShortcutName { get; init; } = string.Empty;

    public string Message { get; init; } = string.Empty;
}
```

## 7. AdminUiService 修改规格

修改：

```text
Services\AdminUiService.cs
```

### 7.1 新增网络测试方法

新增：

```csharp
public async Task<LaunchResult> TestConnectionAsync(
    AdminUiConfig config,
    CancellationToken cancellationToken = default)
```

行为：

- 使用 `config.BaseUrl` 发起 GET 请求。
- 使用 `config.IgnoreCertificateErrors` 控制是否忽略证书错误。
- 响应状态码 `< 500` 视为成功。
- 请求异常返回失败结果。

失败结果示例：

```text
网络连接失败，请检查 AdminUI BaseUrl、网络环境或 VPN。
```

### 7.2 修改 DownloadAllAsync 签名

当前：

```csharp
public async Task<AdminUiDownloadResult> DownloadAllAsync(
    IEnumerable<ShortcutItem> shortcuts,
    AdminUiConfig config,
    CancellationToken cancellationToken = default)
```

修改为：

```csharp
public async Task<AdminUiDownloadResult> DownloadAllAsync(
    IEnumerable<ShortcutItem> shortcuts,
    AdminUiConfig config,
    IProgress<AdminUiDownloadProgress>? progress = null,
    CancellationToken cancellationToken = default)
```

### 7.3 进度回调时机

下载开始前，上报：

```csharp
progress?.Report(new AdminUiDownloadProgress
{
    TotalCount = total,
    CompletedCount = 0,
    SuccessCount = 0,
    FailedCount = 0,
    CurrentShortcutName = string.Empty,
    Message = "准备下载。"
});
```

处理每个快捷项前，上报：

```csharp
CurrentShortcutName = shortcut.Name;
Message = "正在处理。"
```

处理每个快捷项后，上报：

```csharp
CompletedCount = completed;
SuccessCount = successCount;
FailedCount = failedCount;
CurrentShortcutName = shortcut.Name;
Message = "处理完成。"
```

说明：

- 进度以“快捷项数量”为单位。
- 不要求显示单个文件的字节级下载进度。
- 每处理完一个快捷项，进度条前进一步。

## 8. MainViewModel 下载流程修改

修改：

```text
DownloadAdminUiLinksAsync()
```

目标流程：

```csharp
try
{
    IsBusy = true;
    BusyMessage = "正在自动获取 AdminUI 连接，请稍候...";
    BusyProgressValue = 0;
    BusyProgressMaximum = Shortcuts.Count;
    BusyProgressText = "正在测试 AdminUI 网络连接...";
    BusyCurrentItemText = string.Empty;

    var testResult = await _adminUiService.TestConnectionAsync(_config.AdminUi);
    if (!testResult.Success)
    {
        _dialogService.ShowError(testResult.ErrorMessage ?? "网络连接失败，请检查 AdminUI BaseUrl、网络环境或 VPN。");
        return;
    }

    var progress = new Progress<AdminUiDownloadProgress>(p =>
    {
        BusyProgressValue = p.CompletedCount;
        BusyProgressMaximum = p.TotalCount;
        BusyProgressText = $"当前进度：{p.CompletedCount} / {p.TotalCount}，成功：{p.SuccessCount}，失败：{p.FailedCount}";
        BusyCurrentItemText = string.IsNullOrWhiteSpace(p.CurrentShortcutName)
            ? string.Empty
            : $"正在处理：{p.CurrentShortcutName}";
    });

    var result = await _adminUiService.DownloadAllAsync(Shortcuts, _config.AdminUi, progress);

    // 原有结果弹窗
}
finally
{
    IsBusy = false;
    BusyMessage = string.Empty;
    BusyProgressValue = 0;
    BusyProgressMaximum = 0;
    BusyProgressText = string.Empty;
    BusyCurrentItemText = string.Empty;
}
```

注意：

- 网络测试失败时，`return` 之前仍会进入 `finally`。
- 下载完成结果弹窗仍在 `IsBusy = true` 期间显示。
- 用户关闭结果弹窗后才解除遮罩。

## 9. MainWindow.xaml 修改规格

修改：

```text
MainWindow.xaml
```

### 9.1 替换进度条

将：

```xml
<ProgressBar Height="8"
             IsIndeterminate="True" />
```

改为：

```xml
<ProgressBar Height="10"
             Minimum="0"
             Maximum="{Binding BusyProgressMaximum}"
             Value="{Binding BusyProgressValue}" />
```

### 9.2 增加进度文字

在进度条下增加：

```xml
<TextBlock Margin="0,12,0,0"
           Text="{Binding BusyProgressText}"
           TextAlignment="Center"
           TextWrapping="Wrap" />

<TextBlock Margin="0,6,0,0"
           Text="{Binding BusyCurrentItemText}"
           TextAlignment="Center"
           TextWrapping="Wrap" />
```

### 9.3 遮罩大小

遮罩内白色面板建议加宽：

```xml
Width="460"
```

避免中文进度文字换行过多。

## 10. 错误处理

| 场景 | 处理方式 |
| --- | --- |
| BaseUrl 为空 | 设置保存阶段应已阻止；如果运行时为空，网络测试失败 |
| BaseUrl 无法访问 | 弹窗提示网络连接失败 |
| HTTPS 证书错误 | 如果 IgnoreCertificateErrors=true，应忽略；否则测试失败 |
| 网络预检查失败 | 不执行下载 |
| 下载过程中单个快捷项失败 | 记录失败，进度继续 |
| 下载过程中整体异常 | 弹窗提示，恢复界面 |
| 下载过程中用户操作界面 | 主界面仍保持禁用 |

## 11. 验收标准

- 点击“自动获取连接”后显示加载遮罩。
- 遮罩显示“正在测试 AdminUI 网络连接...”。
- 如果 BaseUrl 不可访问，弹窗提示网络连接失败。
- 网络连接失败时不下载任何 `.jnlp`。
- 网络连接失败后主界面恢复可操作。
- 网络连接成功后开始下载。
- 进度条为确定进度条，不再是不确定动画。
- 进度条最大值等于当前快捷项总数。
- 每处理完一个快捷项，进度条前进一步。
- 进度文字显示当前进度、成功数、失败数。
- 当前项目文字显示正在处理的快捷项名称。
- 下载完成后弹出结果汇总。
- 用户关闭结果汇总后遮罩消失。
- 异常情况下遮罩也必须消失。
- `dotnet build .\VSLoader.sln` 必须通过。

