# v12 顶部第二行按钮紧凑分组编码规格说明

## 1. 需求背景

v11 已经将 VSLoader 顶部工具区从一行拆成两行：

```text
第一行：[搜索框] [设置]
第二行：[新增] [批量新增识别] [自动获取连接]        [AdminUI] [打开] [编辑] [删除]
```

拆分后解决了顶部按钮过多导致窗口过宽的问题，但第二行按钮目前采用左右两端分布，中间存在较大的空白区域。

这个大留白会造成视觉割裂：

- 左侧创建/批量操作按钮像一组。
- 右侧选中项操作按钮像另一组孤立按钮。
- 中间空白过大，整体不够自然。

本次 v12 需求是在保持 v11 两行布局的前提下，优化第二行按钮排布，让它更像一个紧凑、自然的工具栏。

## 2. 需求目标

本次优化目标：

1. 保留顶部两行布局。
2. 第一行保持 `搜索框 + 设置` 不变。
3. 第二行所有按钮整体左对齐。
4. 第二行仍保留两个逻辑分组：
   - 创建/批量操作组
   - 当前选中项操作组
5. 两个分组之间使用固定小间距，而不是中间 `*` 大留白。
6. 不增加第三行。
7. 不隐藏按钮。
8. 不遮挡下方快捷项列表。
9. 不改变任何业务逻辑和命令绑定。

## 3. 修改范围

主要修改文件：

- `VSLoader\MainWindow.xaml`

原则上不需要修改：

- `VSLoader\ViewModels\MainViewModel.cs`
- `VSLoader\MainWindow.xaml.cs`
- 任何服务层、模型层代码

本次仅调整顶部第二行按钮布局。

## 4. 当前布局问题

当前第二行使用类似结构：

```xml
<Grid Grid.Row="1">
    <Grid.ColumnDefinitions>
        <ColumnDefinition Width="Auto" />
        <ColumnDefinition Width="*" />
        <ColumnDefinition Width="Auto" />
    </Grid.ColumnDefinitions>

    <StackPanel Grid.Column="0">...</StackPanel>
    <StackPanel Grid.Column="2">...</StackPanel>
</Grid>
```

中间 `*` 列会吃掉所有剩余空间，将两组按钮推到左右两端。

在窗口宽度较大时，视觉上会出现明显断层。

## 5. 新布局设计

### 5.1 第一行保持不变

第一行仍然是：

```text
[搜索框........................................] [设置]
```

不调整搜索框和设置按钮的位置。

### 5.2 第二行改为整体左对齐

第二行改成：

```text
[新增] [批量新增识别] [自动获取连接]    [AdminUI] [打开] [编辑] [删除]
```

特点：

- 所有按钮整体靠左排列。
- 两个逻辑组之间保留固定间距。
- 不再使用中间 `*` 将按钮推到两端。

### 5.3 分组间距

建议两个按钮组之间使用：

```text
24px
```

可接受范围：

```text
20px ~ 28px
```

优先使用 `Margin="24,0,0,0"`，不建议添加明显分割线。

## 6. 推荐实现结构

推荐将第二行改为一个外层水平 `StackPanel`，内部包含两个按钮组：

```xml
<StackPanel Grid.Row="1"
            Margin="0,10,0,0"
            Orientation="Horizontal">
    <StackPanel Orientation="Horizontal">
        <!-- 新增 / 批量新增识别 / 自动获取连接 -->
    </StackPanel>

    <StackPanel Margin="24,0,0,0"
                Orientation="Horizontal">
        <!-- AdminUI / 打开 / 编辑 / 删除 -->
    </StackPanel>
</StackPanel>
```

也可以使用单个 `StackPanel` 加一个固定宽度空白元素：

```xml
<Border Width="24" />
```

但推荐两个内部 `StackPanel`，因为逻辑分组更清晰。

## 7. 按钮顺序要求

### 7.1 左侧分组

从左到右：

```text
新增
批量新增识别
自动获取连接
```

### 7.2 右侧分组

从左到右：

```text
AdminUI
打开
编辑
删除
```

## 8. 按钮尺寸要求

沿用 v11 的按钮尺寸：

```text
按钮高度：36
按钮间距：8
```

推荐继续保持：

```xml
Height="36"
Margin="0,0,8,0"
```

每组最后一个按钮可以不设置右侧 `8` 间距，避免组尾多余空白。

## 9. 不允许改变的内容

本次不允许改变：

- 窗口默认宽度和最小宽度。
- 第一行搜索框和设置按钮布局。
- 下方快捷项列表布局。
- 快捷项列表列宽。
- 搜索逻辑。
- 按钮命令绑定。
- 右键菜单。
- 双击行为。
- AdminUI、VSCode、批量识别等业务逻辑。

## 10. 功能绑定要求

所有按钮必须保持原命令绑定：

| 按钮 | 命令 |
|---|---|
| 新增 | `AddShortcutCommand` |
| 批量新增识别 | `OpenBatchImportCommand` |
| 自动获取连接 | `DownloadAdminUiLinksCommand` |
| AdminUI | `OpenAdminUiCommand` |
| 打开 | `OpenShortcutCommand` |
| 编辑 | `EditShortcutCommand` |
| 删除 | `DeleteShortcutCommand` |

第一行设置按钮仍绑定：

```xml
Command="{Binding OpenSettingsCommand}"
```

## 11. 验收标准

### 11.1 视觉验收

启动应用后：

- 第一行仍然是搜索框 + 设置。
- 第二行按钮整体左对齐。
- 第二行两个按钮组之间有适度间距。
- 中间不再出现明显大留白。
- 顶部区域仍然只占两行。
- 下方快捷项列表没有被遮挡。
- 界面整体比左右分散布局更自然。

### 11.2 功能验收

以下功能必须保持正常：

1. 搜索框输入时列表实时过滤。
2. 点击 `新增` 可打开新增窗口。
3. 点击 `批量新增识别` 可打开批量识别窗口。
4. 点击 `自动获取连接` 可执行 AdminUI 下载逻辑。
5. 选中快捷项后点击 `AdminUI` 可打开对应 `.jnlp`。
6. 选中快捷项后点击 `打开` 可用 VSCode 打开路径。
7. 选中快捷项后点击 `编辑` 可编辑。
8. 选中快捷项后点击 `删除` 可删除。
9. 点击 `设置` 可打开设置窗口。
10. 右键菜单和双击功能不受影响。

### 11.3 编译验收

执行：

```powershell
dotnet build .\VSLoader.sln
```

期望：

```text
0 warnings
0 errors
```

如果提示 `VSLoader.exe` 被占用，说明程序正在运行，应先关闭 VSLoader 后重新构建。

## 12. 建议实施步骤

1. 打开 `VSLoader\MainWindow.xaml`。
2. 找到顶部工具区 `Grid.Row="1"` 的第二行按钮布局。
3. 移除第二行内部的三列 `Grid` 布局。
4. 改为外层水平 `StackPanel`。
5. 内部保留两个水平 `StackPanel` 作为按钮组。
6. 第二个按钮组设置 `Margin="24,0,0,0"`。
7. 保持所有按钮命令绑定不变。
8. 编译项目。
9. 手动确认按钮排列和功能。

## 13. 明确不做的事情

本次不做：

- 添加更多菜单。
- 隐藏低频按钮。
- 改成图标按钮。
- 添加竖向分割线。
- 改变窗口尺寸。
- 改变列表样式。
- 改变任何业务逻辑。
