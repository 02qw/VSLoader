# v15 禁用主列表列宽拖拽调整编码规格说明

## 1. 需求背景

当前 VSLoader 主列表已经在 v14 中禁用了列重排：

```xml
CanUserReorderColumns="False"
```

但用户仍然发现，在 `备注` 和 `更新时间` 之间存在一个几乎看不见的可拖动区域。  
拖动该区域后，备注列或更新时间列宽会被改变，导致：

- 备注内容被遮挡或过度省略。
- 更新时间内容位置发生变化。
- 列表版面被用户误操作破坏。
- 用户误以为存在一个隐藏控件或异常遮挡层。

从 WPF 技术角度看，这个区域是 `DataGrid` 默认启用的列宽调整拖拽柄，不是额外控件，也不是数据错位。

本次 v15 需求需要禁用主列表列宽拖拽调整，让列表布局保持稳定。

## 2. 需求目标

本次优化目标：

1. 禁止用户拖动主列表列宽。
2. 去掉 `备注` 和 `更新时间` 之间“看不见但可拖动”的列宽调整行为。
3. 保持主列表列宽由程序样式统一控制。
4. 避免备注和更新时间内容被误拖拽遮挡。
5. 保持 v13/v14 的列宽和对齐策略。
6. 不改变任何业务逻辑。

## 3. 修改范围

主要修改文件：

- `VSLoader\MainWindow.xaml`

原则上不需要修改：

- `VSLoader\ViewModels\MainViewModel.cs`
- `VSLoader\MainWindow.xaml.cs`
- 服务层代码
- 模型层代码

本次只修改主列表 `DataGrid` 的交互属性。

## 4. 当前问题来源

WPF `DataGrid` 默认允许用户调整列宽。

即使已经设置：

```xml
CanUserReorderColumns="False"
```

也只能禁止列顺序拖拽，不能禁止列宽拖拽。

因此在列头之间，例如：

```text
备注 | 更新时间
```

中间仍然存在一个列宽调整拖拽柄。

该拖拽柄默认不明显，但鼠标靠近列边界时可以左右拖动。

## 5. 推荐实现方案

在主列表 `DataGrid` 上增加：

```xml
CanUserResizeColumns="False"
```

最终主列表应同时具备：

```xml
CanUserReorderColumns="False"
CanUserResizeColumns="False"
```

含义：

- `CanUserReorderColumns="False"`：禁止拖动列头改变列顺序。
- `CanUserResizeColumns="False"`：禁止拖动列边界改变列宽。

## 6. 保持现有列宽策略

不修改 v13 之后的列宽策略：

```text
名称：2*
备注：3*
更新时间：180
```

不修改 v14 之后的对齐策略：

- 更新时间列头右对齐。
- 更新时间内容右对齐。

## 7. 不允许改变的内容

本次不允许改变：

- 顶部工具区布局。
- 第二行按钮均匀分布布局。
- 主列表列宽数值。
- 主列表列数量。
- 是否显示目标路径列。
- 主列表行高。
- 主列表字体样式。
- 搜索逻辑。
- 排序逻辑。
- 双击打开逻辑。
- 右键菜单逻辑。
- AdminUI 相关逻辑。
- 配置保存逻辑。

## 8. 验收标准

### 8.1 交互验收

启动应用后：

- 鼠标移动到 `名称` 与 `备注` 列之间，不应再出现可拖动调整列宽的行为。
- 鼠标移动到 `备注` 与 `更新时间` 列之间，不应再出现可拖动调整列宽的行为。
- 用户无法通过拖动列边界遮挡备注或更新时间内容。
- 用户仍然无法拖动列头改变列顺序。

### 8.2 视觉验收

- 列表列宽保持稳定。
- 备注内容按照既定列宽显示，过长时正常省略。
- 更新时间仍然完整显示。
- 更新时间表头与时间内容仍保持右对齐。

### 8.3 功能验收

以下功能必须保持正常：

1. 搜索过滤正常。
2. 单击选中正常。
3. 双击打开 VSCode 正常。
4. 右键菜单正常。
5. 顶部按钮功能正常。
6. AdminUI 功能正常。
7. 自动获取连接功能正常。

### 8.4 编译验收

执行：

```powershell
dotnet build .\VSLoader.sln
```

期望：

```text
0 warnings
0 errors
```

如果提示 `VSLoader.exe` 被占用，说明程序正在运行，应先关闭 VSLoader 后重新构建。

## 9. 建议实施步骤

1. 打开 `VSLoader\MainWindow.xaml`。
2. 找到主列表 `DataGrid x:Name="ShortcutsGrid"`。
3. 在已有属性附近增加：

```xml
CanUserResizeColumns="False"
```

4. 确认 `CanUserReorderColumns="False"` 仍然存在。
5. 编译项目。
6. 手动尝试拖动列边界，确认无法调整列宽。

## 10. 明确不做的事情

本次不做：

- 重新设计列宽。
- 修改列内容。
- 增加列宽恢复功能。
- 允许用户保存自定义列宽。
- 修改 DataGrid 行样式。
- 修改业务逻辑。
