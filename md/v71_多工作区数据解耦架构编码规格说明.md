# v71 多工作区数据解耦架构编码规格说明

## 1. 需求背景

当前 VSLoader 是单数据源模式。

也就是说，程序默认只使用一套用户数据，所有用户后来配置进去的内容都集中在：

```text
%AppData%\VSLoader
```

典型文件和目录包括：

```text
%AppData%\VSLoader\config.json
%AppData%\VSLoader\window-layout.json
%AppData%\VSLoader\factory-map.layout.json
%AppData%\VSLoader\UIdownload
```

这些数据包括：

```text
1. 快捷项列表
2. 批量新增识别历史路径
3. AdminUI 配置
4. AdminUI 密码
5. WebUI 配置
6. 主窗口位置、地图窗口位置
7. 主列表列宽
8. 地图节点位置和连线
9. 下载的 jnlp 文件
```

用户现在希望把 VSLoader 程序本体和这些用户配置数据解耦。

目标是把这些“后来配置出来的数据”做成一个个独立的工作区。  
VSLoader 程序本体只负责管理和打开工作区，不再固定绑定唯一一套数据。

可以理解为：

```text
VSLoader 程序本体 = 程序壳
工作区管理器 = 大箱子
每个工作区 = 小箱子
小箱子里保存快捷项、地图、布局、AdminUI/WebUI 配置、下载文件等
```

## 2. 核心概念

### 2.1 程序级配置

程序级配置属于 VSLoader 本体，不跟随某个工作区变化。

本次确认采用方案 C：

```text
VSCode 路径是程序级配置。
```

原因：

```text
VSCode.exe 路径通常是当前电脑环境的一部分。
同一台电脑上，不同工作区一般共用同一个 VSCode。
```

程序级配置还包括：

```text
1. 工作区列表
2. 上次打开的工作区
3. 是否自动打开上次工作区
4. VSCode.exe 路径
```

### 2.2 工作区级配置

工作区级配置属于某一个小箱子。

每个工作区独立保存：

```text
1. 快捷项列表
2. 批量新增识别上次使用的目标父级文件夹
3. 批量新增识别上次使用的 CSV 文件路径
4. AdminUI 配置
5. AdminUI 密码
6. WebUI 配置
7. 主窗口位置、大小
8. 主列表列宽
9. 地图窗口位置、大小
10. 地图缩放/偏移状态
11. 地图节点位置和连线
12. UIdownload 下载目录
```

说明：

```text
不同工作区之间的数据互相隔离。
切换工作区后，主界面的快捷项、地图、布局、下载文件都应该跟着切换。
```

## 3. 目标架构

改造后目录结构建议为：

```text
%AppData%\VSLoader
  app-settings.json
  Workspaces
    Default
      workspace.json
      config.json
      window-layout.json
      factory-map.layout.json
      UIdownload
    LineA
      workspace.json
      config.json
      window-layout.json
      factory-map.layout.json
      UIdownload
    Test
      workspace.json
      config.json
      window-layout.json
      factory-map.layout.json
      UIdownload
```

其中：

```text
app-settings.json
```

是程序级配置。

每个工作区目录中的：

```text
workspace.json
config.json
window-layout.json
factory-map.layout.json
UIdownload
```

是工作区级数据。

## 4. 需求目标

完成后：

```text
1. VSLoader 支持多工作区。
2. 程序启动时可以打开上次使用的工作区。
3. 第一次启动或没有可用工作区时，可以创建默认工作区。
4. 旧版单数据源自动迁移为 Default 工作区。
5. VSCode 路径迁移为程序级配置。
6. 快捷项、地图、布局、批量导入历史、AdminUI/WebUI 配置、密码、UIdownload 跟随工作区。
7. 主界面标题显示当前工作区名称。
8. 设置窗口提供当前工作区信息和切换工作区入口，或者主界面提供切换入口。
9. 保持现有快捷项、地图、AdminUI、WebUI、批量新增、窗口布局功能可用。
```

## 5. 非目标范围

本阶段不实现：

```text
1. 不实现工作区 zip 导入导出。
2. 不实现云同步。
3. 不实现多人协作。
4. 不实现工作区权限控制。
5. 不实现工作区模板市场。
6. 不实现多个工作区同时打开。
7. 不重构 UI 视觉风格。
8. 不改变快捷项数据结构的业务含义。
```

说明：

```text
本阶段重点是把单数据源改成多工作区数据源，并保证旧数据安全迁移。
```

## 6. 数据归属规则

### 6.1 程序级

程序级配置保存在：

```text
%AppData%\VSLoader\app-settings.json
```

包括：

```text
1. VSCodePath
2. LastWorkspaceId
3. OpenLastWorkspaceOnStartup
4. Workspaces
5. MigrationCompleted
```

### 6.2 工作区级

工作区目录保存：

```text
config.json
window-layout.json
factory-map.layout.json
UIdownload
workspace.json
```

其中：

```text
config.json
```

继续保存：

```text
1. Shortcuts
2. AdminUi
3. Hotkey
4. BatchImport
5. WebUi
```

但需要从 `config.json` 中移除或不再使用：

```text
VSCodePath
```

兼容旧文件时：

```text
如果工作区 config.json 里仍有 VSCodePath，可以忽略。
真正使用 app-settings.json 中的 VSCodePath。
```

注意：

```text
Hotkey 是否工作区级需要特别说明。
本规格建议 Hotkey 暂时仍保留在工作区 config.json 中，维持当前最小迁移成本。
如果后续希望所有工作区共用同一个全局快捷键，可以再独立迁移为程序级。
```

## 7. 新增模型

### 7.1 AppSettings

新增文件：

```text
C:\Users\shee_\OneDrive\Desktop\VSLoader\VSLoader\Models\AppSettings.cs
```

建议结构：

```csharp
namespace VSLoader.Models;

public sealed class AppSettings
{
    public string VSCodePath { get; set; } = string.Empty;

    public string LastWorkspaceId { get; set; } = string.Empty;

    public bool OpenLastWorkspaceOnStartup { get; set; } = true;

    public bool MigrationCompleted { get; set; }

    public List<WorkspaceInfo> Workspaces { get; set; } = new();
}
```

### 7.2 WorkspaceInfo

新增文件：

```text
C:\Users\shee_\OneDrive\Desktop\VSLoader\VSLoader\Models\WorkspaceInfo.cs
```

建议结构：

```csharp
namespace VSLoader.Models;

public sealed class WorkspaceInfo
{
    public string Id { get; set; } = string.Empty;

    public string Name { get; set; } = string.Empty;

    public string Path { get; set; } = string.Empty;

    public DateTime CreatedAt { get; set; }

    public DateTime UpdatedAt { get; set; }
}
```

### 7.3 WorkspaceMetadata

每个工作区自己的 `workspace.json`：

```text
Workspaces\Default\workspace.json
```

模型：

```csharp
namespace VSLoader.Models;

public sealed class WorkspaceMetadata
{
    public string Id { get; set; } = string.Empty;

    public string Name { get; set; } = string.Empty;

    public DateTime CreatedAt { get; set; }

    public DateTime UpdatedAt { get; set; }
}
```

### 7.4 WorkspaceContext

新增文件：

```text
C:\Users\shee_\OneDrive\Desktop\VSLoader\VSLoader\Models\WorkspaceContext.cs
```

建议结构：

```csharp
namespace VSLoader.Models;

public sealed class WorkspaceContext
{
    public string Id { get; init; } = string.Empty;

    public string Name { get; init; } = string.Empty;

    public string RootPath { get; init; } = string.Empty;

    public string ConfigPath => System.IO.Path.Combine(RootPath, "config.json");

    public string WindowLayoutPath => System.IO.Path.Combine(RootPath, "window-layout.json");

    public string FactoryMapLayoutPath => System.IO.Path.Combine(RootPath, "factory-map.layout.json");

    public string UiDownloadDirectory => System.IO.Path.Combine(RootPath, "UIdownload");

    public string MetadataPath => System.IO.Path.Combine(RootPath, "workspace.json");
}
```

## 8. 新增服务

### 8.1 AppSettingsService

新增文件：

```text
C:\Users\shee_\OneDrive\Desktop\VSLoader\VSLoader\Models\Services\AppSettingsService.cs
```

职责：

```text
1. 读取 %AppData%\VSLoader\app-settings.json。
2. 文件不存在时创建默认 app-settings.json。
3. 文件损坏时备份并创建默认配置。
4. 保存 VSCodePath。
5. 保存工作区列表。
6. 保存 LastWorkspaceId。
```

路径：

```text
%AppData%\VSLoader\app-settings.json
```

### 8.2 WorkspaceService

新增文件：

```text
C:\Users\shee_\OneDrive\Desktop\VSLoader\VSLoader\Models\Services\WorkspaceService.cs
```

职责：

```text
1. 创建工作区目录。
2. 创建 workspace.json。
3. 创建默认 config.json。
4. 创建默认 window-layout.json。
5. 创建默认 factory-map.layout.json 或允许后续自动创建。
6. 创建 UIdownload 目录。
7. 根据 WorkspaceInfo 创建 WorkspaceContext。
8. 验证工作区路径是否可用。
9. 管理默认工作区。
```

### 8.3 LegacyWorkspaceMigrationService

新增文件：

```text
C:\Users\shee_\OneDrive\Desktop\VSLoader\VSLoader\Models\Services\LegacyWorkspaceMigrationService.cs
```

职责：

```text
1. 检测旧版单数据源是否存在。
2. 在第一次升级后创建 Default 工作区。
3. 把旧数据复制到 Default 工作区。
4. 把旧 config.json 中的 VSCodePath 迁移到 app-settings.json。
5. 设置 MigrationCompleted = true。
6. 保留旧文件，不删除。
```

旧数据来源：

```text
%AppData%\VSLoader\config.json
%AppData%\VSLoader\window-layout.json
%AppData%\VSLoader\factory-map.layout.json
%AppData%\VSLoader\UIdownload
```

迁移目标：

```text
%AppData%\VSLoader\Workspaces\Default\config.json
%AppData%\VSLoader\Workspaces\Default\window-layout.json
%AppData%\VSLoader\Workspaces\Default\factory-map.layout.json
%AppData%\VSLoader\Workspaces\Default\UIdownload
```

重要要求：

```text
迁移使用复制，不使用移动。
旧文件保留，避免迁移失败导致数据丢失。
```

## 9. 现有服务改造

### 9.1 ConfigService

当前：

```text
默认读写 %AppData%\VSLoader\config.json
```

改造目标：

```text
支持传入明确的 config.json 路径或工作区目录。
```

推荐构造：

```csharp
public ConfigService()
{
}

public ConfigService(string configDirectory)
{
    this.configDirectory = configDirectory;
}
```

当前已有类似构造。

改造要求：

```text
1. MainViewModel 不再使用默认 ConfigService()。
2. MainViewModel 使用当前 WorkspaceContext.RootPath 创建 ConfigService。
3. ConfigService 保存的 config.json 属于当前工作区。
4. Load 时仍兼容旧字段 VSCodePath，但不作为真正 VSCode 路径来源。
```

### 9.2 WindowLayoutService

当前：

```text
默认读写 %AppData%\VSLoader\window-layout.json
```

当前已有：

```csharp
public WindowLayoutService(string configDirectory)
```

改造要求：

```text
1. MainWindow 根据当前 WorkspaceContext.RootPath 创建 WindowLayoutService。
2. window-layout.json 跟随工作区。
3. 主窗口位置、地图窗口位置、主列表列宽都跟随工作区。
```

### 9.3 FactoryMapLayoutService

当前 `MainWindow.ResolveFactoryMapLayoutPath()` 写死：

```csharp
Path.Combine(appData, "VSLoader", "factory-map.layout.json")
```

改造要求：

```text
使用当前 WorkspaceContext.FactoryMapLayoutPath。
```

### 9.4 AdminUiService

当前 `AdminUiService` 默认下载目录：

```text
%AppData%\VSLoader\UIdownload
```

改造要求：

```text
下载目录改为当前 WorkspaceContext.UiDownloadDirectory。
```

实现方式建议：

```csharp
public AdminUiService()
{
}

public AdminUiService(string downloadDirectory)
{
    ...
}
```

或者：

```csharp
AdminUiConfig 增加 DownloadDirectory 不推荐。
```

原因：

```text
下载目录是工作区上下文，不是业务配置。
```

### 9.5 WebUiService

WebUI 本身不保存文件，主要读取快捷项目标目录中的 `INSTANCE.properties`。

改造要求：

```text
无需直接改造 WebUiService。
WebUiConfig 继续跟随工作区 config.json。
```

### 9.6 PasswordProtectionService

密码保护服务可保持不变。

原因：

```text
AdminUI 密码字段存储在工作区 config.json 的 AdminUi.ProtectedPassword 中。
同一 Windows 用户下加密解密即可。
```

## 10. MainWindow / MainViewModel 改造

### 10.1 MainWindow 构造方式

当前：

```xml
<Window.DataContext>
    <vm:MainViewModel />
</Window.DataContext>
```

这会导致 XAML 自动创建 `MainViewModel`，并且使用默认服务。

多工作区后建议改为：

```text
MainWindow 不再在 XAML 中直接创建 MainViewModel。
程序启动时先确定 WorkspaceContext，再由代码创建 MainViewModel。
```

改造方向：

```text
1. 移除 MainWindow.xaml 中的 Window.DataContext。
2. MainWindow 构造函数接收 AppSettings + WorkspaceContext 或 AppRuntimeContext。
3. 在 MainWindow 构造函数中创建 DataContext。
```

示例：

```csharp
public MainWindow(AppSettings appSettings, WorkspaceContext workspaceContext)
{
    InitializeComponent();
    _appSettings = appSettings;
    _workspaceContext = workspaceContext;
    DataContext = CreateMainViewModel(appSettings, workspaceContext);
    ...
}
```

### 10.2 MainViewModel 使用程序级 VSCodePath

当前 `AppConfig` 中有：

```csharp
VSCodePath
```

改造后：

```text
VSCodePath 来源于 AppSettings.VSCodePath。
```

MainViewModel 可以持有：

```csharp
private readonly AppSettings _appSettings;
private readonly AppSettingsService _appSettingsService;
```

打开 VSCode 时使用：

```csharp
_launcherService.Launch(_appSettings.VSCodePath, SelectedShortcut.TargetPath);
```

设置窗口保存 VSCodePath 时：

```text
保存到 app-settings.json。
```

其他工作区级配置仍保存到当前工作区 `config.json`。

### 10.3 设置窗口拆分保存

当前设置窗口可能同时编辑：

```text
1. VSCodePath
2. AdminUI
3. Hotkey
```

改造后：

```text
VSCodePath 保存到 app-settings.json。
AdminUI / Hotkey / WebUI 保存到当前工作区 config.json。
```

如果设置窗口里新增“当前工作区”信息：

```text
显示当前工作区名称。
提供“打开工作区文件夹”按钮。
提供“切换工作区”按钮。
```

本阶段可以先只显示当前工作区名称，切换工作区通过主菜单或后续版本实现。

## 11. 启动流程

### 11.1 App.xaml.cs 改造

当前 App 可能默认启动 MainWindow。

多工作区后推荐流程：

```text
1. App 启动。
2. AppSettingsService.LoadOrCreate。
3. LegacyWorkspaceMigrationService.TryMigrate。
4. WorkspaceService.ResolveStartupWorkspace。
5. 如果可以自动打开上次工作区，则创建 MainWindow 并进入。
6. 如果没有工作区或上次工作区不可用，则显示 WorkspaceSelectorWindow。
```

### 11.2 第一次启动

如果：

```text
app-settings.json 不存在
且旧版 config.json 不存在
```

则：

```text
创建 app-settings.json。
创建 Default 工作区。
打开 Default 工作区。
```

### 11.3 旧版升级启动

如果：

```text
app-settings.json 不存在或 MigrationCompleted = false
且旧版 %AppData%\VSLoader\config.json 存在
```

则：

```text
1. 创建 Default 工作区。
2. 复制旧 config/window-layout/factory-map/UIdownload。
3. 从旧 config.json 提取 VSCodePath 写入 app-settings.json。
4. app-settings.json 注册 Default 工作区。
5. LastWorkspaceId = Default。
6. MigrationCompleted = true。
7. 打开 Default 工作区。
```

### 11.4 上次工作区不可用

如果：

```text
LastWorkspaceId 找不到
或工作区目录不存在
或工作区 metadata 损坏
```

则：

```text
显示工作区选择窗口。
```

如果本阶段不实现完整选择窗口，则：

```text
回退创建/打开 Default 工作区，并提示用户。
```

## 12. 工作区选择窗口

本阶段可以选择两种实现范围。

### 12.1 最小可用版本

启动时：

```text
默认打开 LastWorkspace。
没有 LastWorkspace 时创建 Default。
```

主界面设置中显示：

```text
当前工作区名称。
```

暂不提供完整选择窗口。

优点：

```text
迁移风险低。
可以先完成底层多工作区解耦。
```

缺点：

```text
用户还不能真正手动切换多个工作区。
```

### 12.2 推荐版本

新增：

```text
WorkspaceSelectorWindow
```

功能：

```text
1. 列出工作区。
2. 打开工作区。
3. 新建工作区。
4. 打开工作区文件夹。
```

暂不做：

```text
导入、导出、删除、重命名。
```

推荐采用 12.2。

原因：

```text
用户提出的核心需求就是“先进入大箱子，再选择小箱子”。
```

## 13. WorkspaceSelectorWindow UI 设计

窗口标题：

```text
选择工作区
```

内容：

```text
VSLoader 工作区

请选择要打开的工作区

[默认工作区]    路径：...\Workspaces\Default     [打开]
[产线A]        路径：...\Workspaces\LineA       [打开]

[新建工作区] [打开工作区文件夹] [取消]
```

交互：

```text
1. 双击工作区项打开。
2. 选中工作区后点击“打开”。
3. 点击“新建工作区”输入名称。
4. 新建后自动注册到 app-settings.json。
5. 打开后更新 LastWorkspaceId。
```

如果用户取消：

```text
程序退出。
```

## 14. 迁移策略细节

### 14.1 复制规则

旧文件复制到 Default 工作区：

```text
config.json              -> Workspaces\Default\config.json
window-layout.json       -> Workspaces\Default\window-layout.json
factory-map.layout.json  -> Workspaces\Default\factory-map.layout.json
UIdownload               -> Workspaces\Default\UIdownload
```

如果目标文件已存在：

```text
不覆盖。
```

如果复制失败：

```text
迁移失败，保留旧文件，显示错误。
```

### 14.2 旧 VSCodePath 处理

从旧：

```text
%AppData%\VSLoader\config.json
```

读取：

```json
"VSCodePath": "..."
```

写入：

```text
app-settings.json -> VSCodePath
```

工作区内的 `config.json` 可以保留旧 `VSCodePath` 字段，但运行时不再使用。

### 14.3 迁移完成标记

`app-settings.json` 写入：

```json
"MigrationCompleted": true
```

防止每次启动重复迁移。

## 15. 文件路径汇总

新增文件建议：

```text
VSLoader\Models\AppSettings.cs
VSLoader\Models\WorkspaceInfo.cs
VSLoader\Models\WorkspaceMetadata.cs
VSLoader\Models\WorkspaceContext.cs
VSLoader\Models\Services\AppSettingsService.cs
VSLoader\Models\Services\WorkspaceService.cs
VSLoader\Models\Services\LegacyWorkspaceMigrationService.cs
VSLoader\Views\WorkspaceSelectorWindow.xaml
VSLoader\Views\WorkspaceSelectorWindow.xaml.cs
VSLoader\ViewModels\WorkspaceSelectorViewModel.cs
```

需要改造：

```text
VSLoader\App.xaml
VSLoader\App.xaml.cs
VSLoader\MainWindow.xaml
VSLoader\MainWindow.xaml.cs
VSLoader\ViewModels\MainViewModel.cs
VSLoader\ViewModels\SettingsViewModel.cs
VSLoader\Views\SettingsWindow.xaml
VSLoader\Models\AppConfig.cs
VSLoader\Models\Services\ConfigService.cs
VSLoader\Models\Services\WindowLayoutService.cs
VSLoader\Models\Services\AdminUiService.cs
```

测试建议新增：

```text
VSLoader.Tests\AppSettingsServiceTests.cs
VSLoader.Tests\WorkspaceServiceTests.cs
VSLoader.Tests\LegacyWorkspaceMigrationServiceTests.cs
VSLoader.Tests\WorkspaceContextTests.cs
```

## 16. 推荐分阶段实现

### 阶段一：底层工作区基础

目标：

```text
引入 AppSettings、WorkspaceContext、WorkspaceService。
创建 Default 工作区。
让 ConfigService / WindowLayoutService / FactoryMapLayout / UIdownload 使用 WorkspaceContext。
```

验收：

```text
程序仍然能正常启动。
现有功能不变。
数据已经从工作区目录读取。
```

### 阶段二：旧数据迁移

目标：

```text
检测旧 %AppData%\VSLoader 下的数据。
复制到 Workspaces\Default。
迁移 VSCodePath 到 app-settings.json。
```

验收：

```text
老用户升级后数据不丢。
快捷项、地图、布局、下载文件都还在。
```

### 阶段三：工作区选择窗口

目标：

```text
启动时可选择工作区。
可新建工作区。
可打开工作区。
```

验收：

```text
用户可以创建多个小箱子并切换打开。
```

### 阶段四：设置窗口展示当前工作区

目标：

```text
设置中显示当前工作区名称和路径。
提供打开工作区文件夹按钮。
```

验收：

```text
用户知道当前操作的是哪个工作区。
```

## 17. 测试方案

### 17.1 AppSettingsServiceTests

覆盖：

```text
1. app-settings.json 不存在时创建默认。
2. 保存并读取 VSCodePath。
3. 保存并读取 Workspaces。
4. 损坏 app-settings.json 时备份并恢复默认。
```

### 17.2 WorkspaceServiceTests

覆盖：

```text
1. 创建 Default 工作区。
2. 创建自定义名称工作区。
3. 工作区目录包含 workspace.json。
4. 工作区目录包含 UIdownload。
5. WorkspaceContext 路径正确。
```

### 17.3 LegacyWorkspaceMigrationServiceTests

覆盖：

```text
1. 旧 config.json 存在时迁移到 Workspaces\Default。
2. 旧 window-layout.json 复制到 Default。
3. 旧 factory-map.layout.json 复制到 Default。
4. 旧 UIdownload 复制到 Default。
5. 旧 VSCodePath 进入 app-settings.json。
6. 旧文件保留。
7. MigrationCompleted 后不重复迁移。
```

### 17.4 集成手工测试

场景一：老用户升级

```text
1. 准备旧版 %AppData%\VSLoader 数据。
2. 启动新版。
3. 检查 Default 工作区生成。
4. 检查快捷项、地图、布局都可用。
```

场景二：新用户首次启动

```text
1. 清空 %AppData%\VSLoader。
2. 启动程序。
3. 创建 Default 工作区。
4. 主界面正常打开。
```

场景三：多个工作区切换

```text
1. 创建工作区 A。
2. 创建工作区 B。
3. A 中添加快捷项。
4. B 中保持空。
5. 切换 A/B，确认数据隔离。
```

场景四：VSCodePath 全局共享

```text
1. 在工作区 A 设置 VSCode 路径。
2. 切换工作区 B。
3. 确认 VSCode 路径仍然存在。
```

场景五：AdminUI/WebUI 工作区隔离

```text
1. 工作区 A 配置 AdminUI。
2. 工作区 B 配置不同 AdminUI。
3. 切换后确认配置不同。
```

## 18. 风险点

### 18.1 旧数据丢失

风险：

```text
迁移过程中移动或覆盖旧文件会导致数据损坏。
```

规避：

```text
只复制，不移动。
目标已存在时不覆盖。
迁移失败时保留旧文件。
```

### 18.2 服务仍使用旧固定路径

风险：

```text
某些服务继续写 %AppData%\VSLoader，导致数据混乱。
```

规避：

```text
检查 ConfigService、WindowLayoutService、FactoryMapLayoutService、AdminUiService 所有路径来源。
所有工作区级数据必须从 WorkspaceContext 派生。
```

### 18.3 VSCodePath 双来源

风险：

```text
旧 config.json 和新 app-settings.json 都有 VSCodePath。
```

规避：

```text
运行时只使用 app-settings.json 的 VSCodePath。
旧 config.json 的 VSCodePath 仅用于迁移。
```

### 18.4 启动流程复杂化

风险：

```text
启动时需要加载 app-settings、迁移旧数据、解析工作区、打开窗口，任一环节失败都可能导致程序打不开。
```

规避：

```text
每一步失败都要有 fallback：
1. app-settings 损坏 -> 备份并默认。
2. 工作区缺失 -> 打开选择窗口或创建 Default。
3. 迁移失败 -> 显示错误但不删除旧数据。
```

## 19. 验收标准

本需求完成必须满足：

```text
1. 程序支持以 WorkspaceContext 作为当前数据源。
2. 工作区目录下拥有独立 config.json。
3. 工作区目录下拥有独立 window-layout.json。
4. 工作区目录下拥有独立 factory-map.layout.json。
5. 工作区目录下拥有独立 UIdownload。
6. VSCodePath 保存在 app-settings.json。
7. AdminUI/WebUI/密码保存在工作区 config.json。
8. 旧版数据可迁移到 Default 工作区。
9. 旧版数据迁移时不删除旧文件。
10. 主界面能显示当前工作区名称。
11. 多个工作区之间快捷项和地图互相隔离。
12. dotnet build 通过。
13. dotnet test 通过。
```

## 20. 建议提交信息

```text
feat: introduce workspace-based data storage
```
