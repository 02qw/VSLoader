# v118 主界面表格触控板纵向滚动降速编码规格说明

## 1. 背景

v117 增加了触控板双指横纵滚动增强能力，核心实现是：

```text
VSLoader/Behaviors/SmoothTouchpadScrollBehavior.cs
```

该行为会对细粒度触控板滚动进行处理，同时保留普通鼠标滚轮 `±120` 的默认行为。

实际使用时发现：

主界面快捷项表格的纵向触控滚动速度明显过快，而设置页面的纵向触控滚动速度相对正常。

## 2. 根因分析

主界面快捷项列表是 `DataGrid`，并且保留了虚拟化和逻辑滚动配置：

```xml
ScrollViewer.CanContentScroll="True"
VirtualizingPanel.VirtualizationMode="Recycling"
```

在这种模式下，`ScrollViewer.VerticalOffset` 的单位更接近“行/项”，而不是像素。

设置页面则是普通 `ScrollViewer`：

```xml
<ScrollViewer x:Name="SettingsScrollViewer">
```

它的 `VerticalOffset` 单位更接近像素。

因此，同样的触控板 delta：

```text
delta = 30
```

在设置页大约表现为滚动几十像素；但在主界面 DataGrid 中可能表现为滚动很多行，所以主界面会显得“飞快”。

## 3. 目标

本次目标：

- 降低主界面快捷项 `DataGrid` 的触控板纵向滚动灵敏度。
- 参考设置页的纵向滚动手感，让主界面滚动更缓和。
- 不改变普通鼠标滚轮行为。
- 不改变滚动条外观。
- 不关闭 DataGrid 虚拟化。
- 不影响横向触控滚动能力。

## 4. 非目标

本次不做以下事项：

- 不修改设置页滚动速度。
- 不修改工作区列表滚动速度。
- 不修改批量新增识别表格滚动速度，除非后续实测也过快。
- 不改变滚动条宽度、高度、颜色。
- 不引入动画惯性。
- 不将 DataGrid 改为像素滚动。
- 不将 `ScrollViewer.CanContentScroll` 改为 `False`。

## 5. 推荐方案

### 5.1 使用现有灵敏度参数

v117 的 `SmoothTouchpadScrollBehavior` 已经提供：

```csharp
VerticalSensitivityProperty
```

XAML 中可通过：

```xml
behaviors:SmoothTouchpadScrollBehavior.VerticalSensitivity="0.04"
```

降低某个控件的纵向触控板滚动灵敏度。

### 5.2 只对主界面 DataGrid 配置低灵敏度

主界面位置：

```text
VSLoader/MainWindow.xaml
```

在 `ShortcutsGrid` 上保留：

```xml
behaviors:SmoothTouchpadScrollBehavior.IsEnabled="True"
behaviors:SmoothTouchpadScrollBehavior.EnableHorizontal="True"
behaviors:SmoothTouchpadScrollBehavior.EnableVertical="True"
```

并新增：

```xml
behaviors:SmoothTouchpadScrollBehavior.VerticalSensitivity="0.04"
```

这样：

- 横向触控板滚动仍然使用默认灵敏度。
- 纵向触控板滚动会明显降速。
- 普通鼠标滚轮仍然不被该行为处理。

### 5.3 为什么不改设置页

设置页是普通像素滚动，当前手感更接近用户预期。

因此设置页保持默认：

```xml
behaviors:SmoothTouchpadScrollBehavior.EnableVertical="True"
```

不额外设置低灵敏度。

### 5.4 为什么不关闭 DataGrid 虚拟化

关闭虚拟化或改为像素滚动可能让主界面大量快捷项时性能下降。

必须保留：

```xml
EnableRowVirtualization="True"
EnableColumnVirtualization="True"
VirtualizingPanel.IsVirtualizing="True"
VirtualizingPanel.VirtualizationMode="Recycling"
ScrollViewer.CanContentScroll="True"
```

本次只通过灵敏度参数修正触控板纵向速度。

## 6. 影响范围

主要修改：

```text
VSLoader/MainWindow.xaml
```

可能调整测试：

```text
VSLoader.Tests/SmoothTouchpadScrollBehaviorTests.cs
VSLoader.Tests/TouchFriendlyScrollingXamlTests.cs
```

不修改：

```text
VSLoader/Views/SettingsWindow.xaml
VSLoader/Views/BatchImportWindow.xaml
VSLoader/Views/WorkspaceSelectorWindow.xaml
VSLoader/Styles/ModernTheme.xaml
```

除非测试需要补充覆盖。

## 7. 编码要求

### 7.1 主界面 DataGrid 配置

`MainWindow.xaml` 中 `ShortcutsGrid` 必须包含：

```xml
behaviors:SmoothTouchpadScrollBehavior.VerticalSensitivity="0.04"
```

建议初始值：

```text
0.04
```

后续可根据实测微调：

- 仍然太快：调低到 `0.03` 或 `0.02`
- 过慢：调高到 `0.05`

### 7.2 普通鼠标滚轮不受影响

`SmoothTouchpadScrollBehavior.IsFineGrainedVerticalDelta` 仍应保持：

```csharp
Math.Abs(delta) < 120
```

普通鼠标滚轮 `±120` 不应被增强行为处理。

### 7.3 增加灵敏度测试

应补充测试证明低灵敏度有效：

```csharp
CalculateTargetOffset(50, 30, 0.04, 0, 100) == 48.8
```

说明：

- 默认灵敏度 `1.0` 时，`delta=30` 会移动 30 个 offset 单位。
- 灵敏度 `0.04` 时，`delta=30` 只移动 1.2 个 offset 单位。

### 7.4 XAML 结构测试

应补充或调整测试，确保主界面 DataGrid 包含：

```xml
behaviors:SmoothTouchpadScrollBehavior.VerticalSensitivity="0.04"
```

同时确保虚拟化配置仍存在。

## 8. 验收标准

满足以下条件即完成：

- 主界面快捷项表格双指上下滚动明显变慢。
- 主界面快捷项表格滚动手感更接近设置页。
- 普通鼠标滚轮滚动不变。
- 主界面横向触控板滚动仍可用。
- 滚动条外观不变。
- DataGrid 虚拟化不被关闭。
- 设置页滚动不受影响。
- 地图窗口缩放、拖拽不受影响。
- `dotnet test .\VSLoader.Tests\VSLoader.Tests.csproj -c Debug --no-restore` 通过。
- `dotnet build .\VSLoader.sln -c Debug --no-restore` 通过。

## 9. 风险与边界

### 9.1 灵敏度需要实测微调

`0.04` 是基于 DataGrid 逻辑滚动单位的保守初始值。

不同触控板驱动输出 delta 可能不同，因此最终手感可能需要用户实测微调。

### 9.2 批量新增 DataGrid 可能也有类似问题

批量新增识别窗口也是 DataGrid。

本次先只修主界面，因为用户明确反馈的是主界面。

如果后续批量新增也过快，可以复用同一配置。

### 9.3 不应改为像素滚动

把主界面 DataGrid 改为像素滚动可能改善手感，但会影响虚拟化性能和大量数据列表表现。

本次不采用该方案。

