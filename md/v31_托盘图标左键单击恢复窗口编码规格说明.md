# v31 托盘图标左键单击恢复窗口编码规格说明

## 1. 需求背景

v28 已实现 VSLoader 最小化到托盘。

当前托盘图标行为：

```text
左键双击托盘图标 -> 恢复/打开 VSLoader 主窗口
右键托盘图标 -> 显示托盘菜单
左键单击托盘图标 -> 无反应
```

用户希望：

```text
左键单击托盘图标即可恢复/打开 VSLoader 主窗口
```

也就是说，不需要双击才能打开。

## 2. 需求目标

本次开发目标：

1. 左键单击托盘图标时恢复并激活 VSLoader 主窗口。
2. 右键单击托盘图标仍然显示托盘菜单。
3. 双击托盘图标仍然可以恢复窗口，不造成回归。
4. 不改变点击 `X` 隐藏到托盘的逻辑。
5. 不改变托盘菜单 `显示 VSLoader` 和 `退出` 功能。
6. 不改变全局快捷键行为。
7. 不改变托盘菜单样式。

## 3. 当前代码原因

当前托盘图标初始化逻辑位于：

```text
VSLoader\MainWindow.xaml.cs
```

当前只绑定了：

```csharp
_notifyIcon.DoubleClick += (_, _) => RestoreAndActivate();
```

没有绑定：

```csharp
MouseClick
```

因此左键单击托盘图标不会触发恢复窗口逻辑。

## 4. 修改范围

主要修改文件：

```text
VSLoader\MainWindow.xaml.cs
```

原则上不需要修改：

- `MainWindow.xaml`
- `App.xaml`
- `App.xaml.cs`
- `MainViewModel`
- `GlobalHotkeyService`
- 托盘菜单渲染器
- 配置文件结构

## 5. 推荐实现

在 `InitializeTrayIcon()` 中，保留现有双击逻辑：

```csharp
_notifyIcon.DoubleClick += (_, _) => RestoreAndActivate();
```

新增左键单击逻辑：

```csharp
_notifyIcon.MouseClick += (_, e) =>
{
    if (e.Button == WinForms.MouseButtons.Left)
    {
        RestoreAndActivate();
    }
};
```

说明：

- 只响应左键单击。
- 右键单击不手动处理，继续交给 `ContextMenuStrip` 显示菜单。
- 双击逻辑保留，避免已有习惯失效。

## 6. 推荐最终代码结构

`InitializeTrayIcon()` 中托盘图标事件绑定应类似：

```csharp
_notifyIcon.MouseClick += (_, e) =>
{
    if (e.Button == WinForms.MouseButtons.Left)
    {
        RestoreAndActivate();
    }
};

_notifyIcon.DoubleClick += (_, _) => RestoreAndActivate();
```

## 7. 行为说明

### 7.1 左键单击

```text
左键单击托盘图标 -> RestoreAndActivate()
```

效果：

- 如果窗口隐藏到托盘，则显示窗口。
- 如果窗口最小化，则恢复窗口。
- 如果窗口在后台，则置前并激活。

### 7.2 右键单击

```text
右键单击托盘图标 -> 显示托盘菜单
```

不需要在 `MouseClick` 中处理右键。

### 7.3 左键双击

```text
左键双击托盘图标 -> RestoreAndActivate()
```

双击时系统可能同时触发单击和双击事件。

由于两者都是执行：

```csharp
RestoreAndActivate()
```

所以重复调用不会造成业务问题。

## 8. 不允许改变的功能

本次不允许改变：

- 点击窗口 `X` 隐藏到托盘。
- 托盘菜单 `显示 VSLoader`。
- 托盘菜单 `退出`。
- 托盘菜单样式。
- 自定义托盘菜单渲染器。
- 全局快捷键唤起窗口。
- 全局快捷键切换窗口状态。
- 程序真正退出时释放热键和托盘图标。

## 9. 验收标准

### 9.1 左键单击恢复窗口

操作：

1. 启动 VSLoader。
2. 点击窗口右上角 `X` 隐藏到托盘。
3. 左键单击托盘图标。

期望：

- VSLoader 主窗口恢复显示。
- 主窗口置于前台。
- 主窗口获得焦点。

### 9.2 右键菜单不受影响

操作：

1. 右键单击托盘图标。

期望：

- 仍然显示托盘菜单。
- 菜单包含 `显示 VSLoader` 和 `退出`。
- 菜单样式不变。

### 9.3 双击不回归

操作：

1. 点击窗口右上角 `X` 隐藏到托盘。
2. 左键双击托盘图标。

期望：

- VSLoader 主窗口恢复显示。
- 不报错。
- 不打开重复窗口。

### 9.4 快捷键不回归

操作：

1. 点击窗口右上角 `X` 隐藏到托盘。
2. 按已配置全局快捷键。

期望：

- VSLoader 主窗口恢复显示。

### 9.5 托盘退出不回归

操作：

1. 右键托盘图标。
2. 点击 `退出`。

期望：

- 程序真正退出。
- 托盘图标消失。

### 9.6 编译验收

执行：

```powershell
dotnet build .\VSLoader.sln
```

期望：

```text
0 warnings
0 errors
```

如果提示 `VSLoader.exe` 被占用，说明程序正在运行，应先通过托盘菜单 `退出` 或结束进程后重新构建。

## 10. 建议实施步骤

1. 打开 `VSLoader\MainWindow.xaml.cs`。
2. 找到 `InitializeTrayIcon()`。
3. 在 `_notifyIcon` 创建完成后新增 `MouseClick` 事件绑定。
4. 仅在 `e.Button == WinForms.MouseButtons.Left` 时调用 `RestoreAndActivate()`。
5. 保留现有 `DoubleClick` 事件绑定。
6. 编译项目。
7. 手动验证左键单击、右键菜单、双击、快捷键、退出功能。

## 11. 明确不做的事情

本次不做：

- 托盘左键单击弹菜单。
- 托盘左键单击切换隐藏/显示。
- 修改托盘菜单样式。
- 修改托盘菜单内容。
- 修改快捷键逻辑。
- 修改窗口关闭逻辑。
