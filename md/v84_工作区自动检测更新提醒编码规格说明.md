# v84 工作区自动检测更新提醒编码规格说明

## 1. 文档目的

本文件用于指导编程 Agent 为 VSLoader 增加工作区级“自动检测更新提醒”功能。

本功能用于后台检测以下三类外部资源是否有新版本：

```text
1. rules 批量规则文件
2. map 地图配置文件
3. 软件版本 txt 文件
```

检测到更新后，只在主界面显示提醒，不自动导入、不自动覆盖、不自动更新基线。

## 2. 核心原则

本需求最重要的规则：

```text
1. 检测只负责提醒。
2. 关闭提醒只是临时隐藏，不更新基线。
3. rules 只有用户成功扫描预览并确认导入后，才更新基线。
4. map 只有用户成功导入图文件并应用到当前地图后，才更新基线。
5. 软件版本只有当前程序版本与版本 txt 中的版本一致时，才更新基线。
6. 更新提醒和检测失败提醒是两个独立提醒栏。
```

## 3. 配置归属

本功能所有配置均属于：

```text
工作区级配置
```

因为不同工作区可能对应不同产线、不同 rules 文件、不同地图文件、不同软件发布目录。

配置必须保存到当前工作区：

```text
%AppData%\VSLoader\Workspaces\<当前工作区>\config.json
```

不能保存到程序级配置：

```text
%AppData%\VSLoader\app-settings.json
```

## 4. 新增配置模型

建议新增模型：

```text
VSLoader\Models\UpdateCheckConfig.cs
```

推荐结构：

```csharp
namespace VSLoader.Models;

public sealed class UpdateCheckConfig
{
    public string RulesFilePath { get; set; } = string.Empty;

    public string MapFilePath { get; set; } = string.Empty;

    public string SoftwareVersionFilePath { get; set; } = string.Empty;

    public UpdateCheckConfig Clone()
    {
        return new UpdateCheckConfig
        {
            RulesFilePath = RulesFilePath,
            MapFilePath = MapFilePath,
            SoftwareVersionFilePath = SoftwareVersionFilePath
        };
    }
}
```

在：

```text
VSLoader\Models\AppConfig.cs
```

新增：

```csharp
public UpdateCheckConfig UpdateCheck { get; set; } = new();
```

`ConfigService.NormalizeConfig` 中必须补：

```csharp
config.UpdateCheck ??= new UpdateCheckConfig();
```

## 5. 新增 updateTime.json

每个工作区新增一个独立基线文件：

```text
%AppData%\VSLoader\Workspaces\<当前工作区>\updateTime.json
```

建议在：

```text
VSLoader\Models\WorkspaceContext.cs
```

新增：

```csharp
public string UpdateTimePath => Path.Combine(RootPath, "updateTime.json");
```

## 6. updateTime.json 数据结构

建议新增模型：

```text
VSLoader\Models\UpdateTimeState.cs
```

推荐结构：

```csharp
namespace VSLoader.Models;

public sealed class UpdateTimeState
{
    public UpdateFileState Rules { get; set; } = new();

    public UpdateFileState Map { get; set; } = new();

    public UpdateSoftwareState Software { get; set; } = new();
}

public sealed class UpdateFileState
{
    public DateTime? LastUsedWriteTimeUtc { get; set; }
}

public sealed class UpdateSoftwareState
{
    public string LastUsedVersion { get; set; } = string.Empty;
}
```

说明：

```text
Rules.LastUsedWriteTimeUtc 代表上一次实际导入使用的 rules 文件修改时间。
Map.LastUsedWriteTimeUtc 代表上一次实际导入使用的 map 文件修改时间。
Software.LastUsedVersion 代表上一次确认当前程序已达到的版本。
```

## 7. 第一次初始化规则

如果进入工作区后：

```text
updateTime.json 不存在
```

或某一项尚未建立基线，则应进行初始化。

初始化规则：

```text
1. 如果 rules 路径已配置且文件存在：
   写入当前 rules 文件 LastWriteTimeUtc 为 Rules.LastUsedWriteTimeUtc。

2. 如果 map 路径已配置且文件存在：
   写入当前 map 文件 LastWriteTimeUtc 为 Map.LastUsedWriteTimeUtc。

3. 如果软件版本 txt 路径已配置且文件存在：
   如果 txt 中版本号与当前程序版本一致：
       写入 Software.LastUsedVersion = 当前程序版本。
   如果 txt 中版本号高于当前程序版本：
       不写入为已使用版本，应提示软件更新。
```

初始化期间：

```text
rules/map 第一次建立基线不提示更新。
软件版本如果 txt 高于当前程序版本，仍应提示软件更新。
```

原因：

```text
rules/map 第一次无法判断是否刚更新，只能建立初始基线。
软件版本可以明确比较当前程序版本和最新版本，若更高就是真正更新。
```

## 8. 检测逻辑

建议新增服务：

```text
VSLoader\Models\Services\UpdateCheckService.cs
```

职责：

```text
1. 读取 updateTime.json。
2. 第一次缺失时初始化基线。
3. 检测 rules/map/software 是否有更新。
4. 检测配置路径是否不存在或读取失败。
5. 保存 updateTime.json。
6. 提供显式方法更新 rules/map/software 基线。
```

### 8.1 rules 检测

输入：

```text
UpdateCheck.RulesFilePath
updateTime.Rules.LastUsedWriteTimeUtc
```

逻辑：

```text
1. 路径为空：不检测，不提示失败。
2. 路径非空但文件不存在：加入检测失败提醒。
3. 文件存在但 LastUsedWriteTimeUtc 为空：初始化基线，不提示更新。
4. 文件存在且 LastWriteTimeUtc > LastUsedWriteTimeUtc：提示 rules 有更新。
5. 否则不提示。
```

### 8.2 map 检测

输入：

```text
UpdateCheck.MapFilePath
updateTime.Map.LastUsedWriteTimeUtc
```

逻辑同 rules。

### 8.3 软件版本检测

输入：

```text
UpdateCheck.SoftwareVersionFilePath
当前程序版本
updateTime.Software.LastUsedVersion
```

软件版本 txt 格式：

```text
第一行就是版本号
```

示例：

```text
1.7.3
```

逻辑：

```text
1. 路径为空：不检测，不提示失败。
2. 路径非空但文件不存在：加入检测失败提醒。
3. 文件存在但第一行为空：加入检测失败提醒。
4. 版本号格式无法解析：加入检测失败提醒。
5. txt 版本 > 当前程序版本：提示软件有更新。
6. txt 版本 <= 当前程序版本：不提示更新。
7. txt 版本 == 当前程序版本：允许更新 Software.LastUsedVersion 为当前程序版本。
```

说明：

```text
软件更新判断以版本号为准，不以 txt 文件修改时间为准。
```

## 9. 版本号比较规则

建议使用：

```csharp
Version.TryParse(...)
```

规则：

```text
1. "1.7.3" 可解析。
2. "1.7.3.0" 可解析。
3. 空字符串不可解析。
4. 不能解析时显示检测失败提醒。
```

当前程序版本来源建议：

```csharp
typeof(App).Assembly.GetName().Version
```

或项目已有版本显示逻辑中使用的同一来源。

要求：

```text
必须和主界面标题显示的软件版本来源保持一致。
```

## 10. 后台检测频率

进入具体工作区并打开主界面后：

```text
启动后台异步检测循环。
```

建议时机：

```text
启动后延迟 5 秒执行第一次检测。
之后每 10 分钟检测一次。
```

要求：

```text
1. 检测不能阻塞 UI。
2. 检测不能导致程序崩溃。
3. 检测异常必须转换为检测失败提醒。
4. 主窗口关闭或工作区切换时应取消检测循环。
```

推荐在 `MainViewModel` 中维护：

```csharp
private CancellationTokenSource? updateCheckCancellationTokenSource;
```

如果当前架构更适合由 `MainWindow` 生命周期触发，也可以放在 code-behind，但业务判断应在服务层。

## 11. 主界面提醒栏

本需求需要两个独立提醒栏：

```text
1. 更新公告栏
2. 更新检测失败栏
```

两者都不自动消失。

### 11.1 更新公告栏

用于显示：

```text
rules 有更新
map 有更新
软件版本有更新
```

示例：

```text
检测到更新：批量规则文件、地图配置文件
```

或：

```text
检测到更新：软件版本
```

按需显示：

```text
有几个显示几个。
没有更新则隐藏。
```

关闭按钮：

```text
用户点击 × 后，本轮隐藏。
不更新 updateTime.json。
下一次检测如果仍然有更新，可以再次显示。
```

### 11.2 更新检测失败栏

用于显示：

```text
配置路径不存在
文件读取失败
软件版本格式错误
```

示例：

```text
更新检测失败：rules 文件不存在、软件版本文件不存在
```

关闭按钮：

```text
用户点击 × 后，本轮隐藏。
不修改配置。
下一次检测如果仍然失败，可以再次显示。
```

## 12. MainViewModel 建议新增属性

建议在：

```text
VSLoader\ViewModels\MainViewModel.cs
```

新增：

```csharp
[ObservableProperty]
private string updateNoticeMessage = string.Empty;

[ObservableProperty]
private bool hasUpdateNotice;

[ObservableProperty]
private string updateFailureMessage = string.Empty;

[ObservableProperty]
private bool hasUpdateFailure;
```

新增命令：

```csharp
[RelayCommand]
private void CloseUpdateNotice()
{
    UpdateNoticeMessage = string.Empty;
    HasUpdateNotice = false;
}

[RelayCommand]
private void CloseUpdateFailure()
{
    UpdateFailureMessage = string.Empty;
    HasUpdateFailure = false;
}
```

注意：

```text
关闭提醒不能更新 updateTime.json。
```

## 13. MainWindow UI 建议

目标文件：

```text
VSLoader\MainWindow.xaml
```

在当前状态栏提示区域附近新增两个提醒栏。

建议顺序：

```text
1. 现有状态栏提示
2. 更新公告栏
3. 更新检测失败栏
```

也可以根据现有布局把更新公告栏放在最上方。

样式建议：

更新公告栏：

```text
浅黄色背景
深黄色/棕色文字
右侧 × 按钮
```

检测失败栏：

```text
浅红色背景
深红色文字
右侧 × 按钮
```

要求：

```text
1. 不自动消失。
2. 关闭按钮只隐藏当前提醒。
3. 不遮挡主列表。
4. 多个更新项在同一条公告中按中文顿号拼接。
```

## 14. 设置窗口改造

目标文件：

```text
VSLoader\ViewModels\SettingsViewModel.cs
VSLoader\Views\SettingsWindow.xaml
```

设置窗口新增分组：

```text
更新检测
```

字段：

```text
rules 批量配置文件
map 地图配置文件
软件版本 txt
```

每项建议：

```text
TextBox + 浏览按钮
```

浏览按钮可以使用现有 DialogService 的选择文件能力，或新增通用选择文件方法。

保存规则：

```text
1. Trim 三个路径。
2. 允许为空，表示不检测该项。
3. 不在保存时强制要求文件存在。
```

原因：

```text
路径可能是网络路径，保存时强制校验会影响配置体验。
不存在由后台检测失败栏提示。
```

## 15. rules 基线更新时机

当前 rules 实际使用入口在：

```text
VSLoader\ViewModels\MainViewModel.cs
OpenBatchImport()
```

已确认规则：

```text
用户在批量新增识别窗口里成功扫描预览，并点击确认导入后，才更新 rules 基线。
只扫描不导入不算。
```

因此应在：

```csharp
if (dialogResult == true)
{
    ...
}
```

成功应用导入结果并保存配置后，调用：

```csharp
UpdateCheckService.MarkRulesUsed(...)
```

该方法读取当前 `UpdateCheck.RulesFilePath` 的 `LastWriteTimeUtc`，写入：

```text
updateTime.json -> Rules.LastUsedWriteTimeUtc
```

如果 rules 文件不存在：

```text
不更新基线。
可以忽略或记录失败，不应阻断导入结果保存。
```

## 16. map 基线更新时机

当前地图实际导入入口在：

```text
VSLoader\Views\FactoryMapWindow.xaml.cs
ImportMapButton_Click
```

已确认规则：

```text
用户在地图窗口里“导入图文件”成功，并且应用到当前地图后，才更新 map 基线。
只是检测到文件存在不算。
```

当前成功流程大致为：

```text
1. 用户选择图文件。
2. layoutService.LoadFromFile(dialog.FileName, ...)
3. RenderMap(loadResult.Map, resetView: false)
4. layoutService.SaveToFile(getLayoutPath(), loadResult.Map)
5. 显示导入成功状态
```

推荐在第 4 步保存成功后调用：

```csharp
UpdateCheckService.MarkMapUsed(...)
```

写入：

```text
updateTime.json -> Map.LastUsedWriteTimeUtc
```

注意：

```text
MarkMapUsed 应记录设置中配置的 MapFilePath 的 LastWriteTimeUtc。
如果用户导入的 dialog.FileName 与配置的 MapFilePath 不同，应优先记录实际导入的 dialog.FileName，还是配置路径？
```

本规格推荐：

```text
记录实际导入的 dialog.FileName。
```

同时，如果实际导入文件与配置路径不同，可以考虑不自动改配置路径。  
本阶段不强制自动改配置路径，避免隐式修改用户设置。

## 17. 软件基线更新时机

软件基线不需要用户手动操作。

检测时如果：

```text
软件版本 txt 存在
txt 版本号可解析
txt 版本号 == 当前程序版本
```

则更新：

```text
updateTime.json -> Software.LastUsedVersion = 当前程序版本
```

如果：

```text
txt 版本号 > 当前程序版本
```

则只提示软件更新，不更新基线。

## 18. 检测结果模型

建议新增：

```text
VSLoader\Models\Services\UpdateCheckResult.cs
```

结构示例：

```csharp
public sealed class UpdateCheckResult
{
    public bool RulesUpdated { get; init; }

    public bool MapUpdated { get; init; }

    public bool SoftwareUpdated { get; init; }

    public List<string> Failures { get; init; } = new();
}
```

也可以用列表：

```csharp
public List<string> UpdatedItems { get; init; } = new();
public List<string> Failures { get; init; } = new();
```

推荐使用列表，便于 UI 直接拼接：

```text
UpdatedItems = ["批量规则文件", "地图配置文件"]
Failures = ["rules 文件不存在", "软件版本文件不存在"]
```

## 19. 检测服务方法建议

建议：

```csharp
public sealed class UpdateCheckService
{
    public UpdateCheckResult Check(
        UpdateCheckConfig config,
        string updateTimePath,
        Version currentVersion);

    public SaveResult MarkRulesUsed(UpdateCheckConfig config, string updateTimePath);

    public SaveResult MarkMapUsed(string mapFilePath, string updateTimePath);

    public SaveResult MarkSoftwareCurrent(UpdateCheckConfig config, string updateTimePath, Version currentVersion);
}
```

也可拆分异步版本：

```csharp
Task<UpdateCheckResult> CheckAsync(...)
```

如果检测网络路径，推荐异步调用，避免 UI 卡顿。

## 20. 检测失败处理

检测失败包括：

```text
1. 配置路径非空但文件不存在。
2. 读取文件异常。
3. 软件版本 txt 第一行为空。
4. 软件版本号解析失败。
5. updateTime.json 损坏或不可写。
```

策略：

```text
1. 不抛出到 UI 导致程序崩溃。
2. 转换为 Failures 列表。
3. 在更新检测失败栏显示。
4. 不影响 VSLoader 其他功能。
```

如果 `updateTime.json` 损坏：

```text
不直接覆盖原文件。
建议返回失败提醒：updateTime.json 读取失败。
```

## 21. 手工验收

### 场景一：首次建立基线

操作：

```text
1. 当前工作区没有 updateTime.json。
2. 设置 rules/map/software 路径。
3. 启动主界面。
```

预期：

```text
1. 生成 updateTime.json。
2. rules/map 当前修改时间写入基线。
3. 如果软件 txt 版本等于当前程序版本，写入软件版本基线。
4. rules/map 不提示更新。
```

### 场景二：rules 文件更新

操作：

```text
1. 建立基线。
2. 修改 rules CSV 文件。
3. 等待下一次检测。
```

预期：

```text
主界面显示更新公告：批量规则文件。
关闭公告后不更新 updateTime.json。
下次检测仍可再次提示。
```

### 场景三：rules 实际导入后更新基线

操作：

```text
1. rules 文件有更新提醒。
2. 打开批量新增识别。
3. 成功扫描预览。
4. 点击确认导入。
```

预期：

```text
Rules.LastUsedWriteTimeUtc 更新为当前 rules 文件 LastWriteTimeUtc。
后续检测不再提示 rules 更新。
```

### 场景四：map 文件更新

操作：

```text
1. 建立 map 基线。
2. 修改 map 配置文件。
3. 等待下一次检测。
```

预期：

```text
主界面显示更新公告：地图配置文件。
```

### 场景五：map 实际导入后更新基线

操作：

```text
1. 地图配置文件有更新提醒。
2. 打开地图窗口。
3. 点击导入图文件。
4. 成功导入并应用。
```

预期：

```text
Map.LastUsedWriteTimeUtc 更新为实际导入文件 LastWriteTimeUtc。
后续检测不再提示 map 更新。
```

### 场景六：软件版本更新

操作：

```text
1. 当前程序版本为 1.7.2。
2. 软件版本 txt 内容为 1.7.3。
```

预期：

```text
主界面显示更新公告：软件版本。
关闭公告不更新基线。
```

### 场景七：软件版本一致

操作：

```text
1. 当前程序版本为 1.7.3。
2. 软件版本 txt 内容为 1.7.3。
```

预期：

```text
不显示软件更新。
Software.LastUsedVersion 更新为 1.7.3。
```

### 场景八：路径不存在

操作：

```text
1. 设置 rules 路径为不存在文件。
2. 等待检测。
```

预期：

```text
不显示更新公告。
显示更新检测失败栏：rules 文件不存在。
```

## 22. 测试要求

### 22.1 UpdateCheckService 单元测试

新增测试文件：

```text
VSLoader.Tests\UpdateCheckServiceTests.cs
```

至少覆盖：

```text
1. Missing_updateTime_initializes_rules_and_map_without_update_notice
2. Rules_file_newer_than_baseline_returns_rules_update
3. Closing_notice_does_not_update_baseline
4. MarkRulesUsed_updates_rules_baseline
5. Map_file_newer_than_baseline_returns_map_update
6. MarkMapUsed_updates_map_baseline
7. Software_version_greater_than_current_returns_software_update
8. Software_version_equal_current_updates_software_baseline
9. Missing_configured_file_returns_failure
10. Invalid_software_version_returns_failure
```

### 22.2 ConfigService 兼容测试

确认旧 config.json 没有 UpdateCheck 字段时：

```text
config.UpdateCheck 不为 null。
```

### 22.3 SettingsViewModel 测试

确认：

```text
1. 设置保存时 Trim 三个路径。
2. 允许路径为空。
3. 保存后 MainViewModel 写回当前工作区 config.json。
```

### 22.4 MainViewModel 测试

建议覆盖：

```text
1. 检测结果有 UpdatedItems 时设置 HasUpdateNotice。
2. 检测结果有 Failures 时设置 HasUpdateFailure。
3. CloseUpdateNotice 不写 updateTime.json。
4. CloseUpdateFailure 不写 updateTime.json。
```

## 23. 风险点

### 23.1 检测后自动更新基线

风险：

```text
用户还没实际导入/使用，提醒就消失。
```

规避：

```text
Check 方法绝不能因为发现更新而更新 rules/map 基线。
```

### 23.2 关闭提醒后不再提醒

风险：

```text
关闭按钮误写 updateTime.json，导致用户漏更新。
```

规避：

```text
CloseUpdateNotice / CloseUpdateFailure 只清 UI 状态。
```

### 23.3 网络路径卡 UI

风险：

```text
检测网络文件状态时阻塞主线程。
```

规避：

```text
后台 Task 执行检测，UI 只接收结果。
```

### 23.4 检测异常导致程序崩溃

风险：

```text
网络断开、权限不足、文件损坏时抛异常。
```

规避：

```text
服务层捕获异常并返回失败信息。
```

## 24. 验收标准

本需求完成必须满足：

```text
1. 设置窗口可配置 rules/map/software 三个检测路径。
2. 配置保存到当前工作区 config.json。
3. 工作区生成并维护 updateTime.json。
4. 程序进入主界面后后台异步检测。
5. 检测间隔为 10 分钟。
6. rules/map 更新时显示更新公告。
7. 软件版本高于当前程序版本时显示更新公告。
8. 配置路径不存在时显示检测失败栏。
9. 更新公告和检测失败栏互相独立。
10. 关闭提醒不更新基线。
11. rules 确认导入后更新 rules 基线。
12. map 成功导入图文件后更新 map 基线。
13. 软件版本一致时更新软件基线。
14. dotnet build 通过。
15. dotnet test 通过。
```

## 25. 推荐执行命令

实现前如需释放文件占用：

```powershell
Get-Process -Name VSLoader -ErrorAction SilentlyContinue | Stop-Process -Force
```

构建：

```powershell
dotnet build C:\Users\shee_\OneDrive\Desktop\VSLoader\VSLoader.sln -p:UseSharedCompilation=false
```

测试：

```powershell
dotnet test C:\Users\shee_\OneDrive\Desktop\VSLoader\VSLoader.sln -p:UseSharedCompilation=false
```

## 26. 推荐提交信息

```text
feat: add workspace update check reminders
```
