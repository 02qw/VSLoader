# v112 现代窗口外边界层级与标题栏控制按钮统一编码规格说明

## 1. 背景

v111 已经将 VSLoader 的主窗口、工作区窗口、设置窗口、批量新增窗口、地图窗口和部分弹窗改为现代化自绘窗口外壳。

当前用户反馈两个视觉问题：

1. 页面 / 弹窗 / 标题栏的窗口外边界太淡，几乎看不见。
2. 标题栏右上角最小化、最大化、关闭按钮图标大小不一致，风格不统一。

该问题不是业务逻辑问题，而是 v111 自绘窗口外壳后视觉层级没有补完整。

## 2. 当前代码现状

### 2.1 自绘窗口外壳

以下窗口已经使用自绘标题栏：

```text
VSLoader/MainWindow.xaml
VSLoader/Views/WorkspaceSelectorWindow.xaml
VSLoader/Views/SettingsWindow.xaml
VSLoader/Views/BatchImportWindow.xaml
VSLoader/Views/ShortcutEditWindow.xaml
VSLoader/Views/WorkspaceNameDialog.xaml
VSLoader/Views/FactoryMapWindow.xaml
VSLoader/Views/MessageDialogWindow.xaml
```

典型写法：

```xml
WindowStyle="None"
```

以及：

```xml
<shell:WindowChrome.WindowChrome>
    <shell:WindowChrome CaptionHeight="44"
                        CornerRadius="0"
                        GlassFrameThickness="0"
                        ResizeBorderThickness="6"
                        UseAeroCaptionButtons="False" />
</shell:WindowChrome.WindowChrome>
```

这意味着 Windows 原生窗口边框已经被移除，窗口边界需要由 VSLoader 自己绘制。

### 2.2 当前标题栏边界

当前标题栏位于：

```text
VSLoader/Views/Controls/ModernTitleBar.xaml
```

当前只绘制了标题栏底部边线：

```xml
<Border Background="{StaticResource ModernTitleBarBackgroundBrush}"
        BorderBrush="{StaticResource ModernTitleBarBorderBrush}"
        BorderThickness="0,0,0,1">
```

资源位于：

```text
VSLoader/Styles/ModernWindowChrome.xaml
```

当前颜色：

```xml
<SolidColorBrush x:Key="ModernTitleBarBackgroundBrush" Color="#F8FAFC" />
<SolidColorBrush x:Key="ModernTitleBarBorderBrush" Color="#E3EAF3" />
```

问题：

- 只有标题栏底线，没有完整窗口外边框。
- 标题栏背景、窗口内容背景、页面背景都偏浅。
- 窗口叠在窗口上时边界不明显。
- 工作区窗口、设置窗口等弹出时看起来像内容浮在主窗口上，缺少清晰边界。

### 2.3 当前窗口控制按钮

当前标题栏按钮位于：

```text
VSLoader/Views/Controls/ModernTitleBar.xaml
```

当前使用文字字符作为图标：

```xml
<Button Content="—" />
<Button Content="□" />
<Button Content="×" />
```

样式位于：

```text
VSLoader/Styles/ModernWindowChrome.xaml
```

当前普通按钮字号：

```xml
<Setter Property="FontSize" Value="15" />
```

关闭按钮字号：

```xml
<Setter Property="FontSize" Value="18" />
```

问题：

- `—`、`□`、`×` 属于不同字形，天然视觉比例不同。
- 字体渲染会受字体、DPI、系统缩放影响。
- 关闭按钮字号和其他按钮不同，视觉重心更不一致。
- 最大化方框看起来偏小，最小化横线偏长，关闭叉号又偏重。

## 3. 目标

本次 v112 目标：

- 给现代化窗口补上清晰但克制的外边界层级。
- 让窗口之间、弹窗和主窗口之间有明确边界。
- 标题栏右上角控制按钮统一成矢量图标。
- 保留 v111 的现代化浅色风格，不回退成老式灰色系统窗口。
- 不改变窗口业务行为。
- 不改变窗口默认位置、布局持久化、地图窗口跟随逻辑。

## 4. 非目标

本次不做：

- 不重新设计主窗口内容布局。
- 不重新设计设置页、批量新增页、地图页内容。
- 不改变任何按钮的业务功能。
- 不改变主窗口关闭到托盘逻辑。
- 不改变地图窗口最小化等同关闭逻辑。
- 不改变工作区选择、重命名、删除逻辑。
- 不引入第三方 UI 框架。
- 不做深色模式。
- 不做复杂动画。

## 5. 视觉方案

### 5.1 设计方向

参考 Apple / Google 类浅色生产力工具的处理方式：

- 不使用重黑边。
- 不使用高对比硬阴影。
- 使用轻微边框 + 轻微层级差。
- 弹窗可以比主窗口多一点边界存在感。
- 标题栏和内容区保持统一，但边界必须可辨识。

### 5.2 推荐颜色 Token

在：

```text
VSLoader/Styles/ModernWindowChrome.xaml
```

新增或调整：

```xml
<SolidColorBrush x:Key="ModernWindowOuterBorderBrush" Color="#CBD5E1" />
<SolidColorBrush x:Key="ModernWindowOuterBorderActiveBrush" Color="#B8C7DA" />
<SolidColorBrush x:Key="ModernWindowSubtleShadowBrush" Color="#220F172A" />
<SolidColorBrush x:Key="ModernTitleBarBackgroundBrush" Color="#F8FAFC" />
<SolidColorBrush x:Key="ModernTitleBarBorderBrush" Color="#D6E0EC" />
<SolidColorBrush x:Key="ModernTitleBarIconBrush" Color="#64748B" />
<SolidColorBrush x:Key="ModernTitleBarIconHoverBrush" Color="#334155" />
<SolidColorBrush x:Key="ModernTitleBarCloseIconHoverBrush" Color="#DC2626" />
```

说明：

- `#CBD5E1` 比当前 `#E3EAF3` 更可见，但不会刺眼。
- 标题栏底线可略深于原来，增强顶层分隔。
- 图标默认使用灰蓝色，不使用纯黑。
- 关闭按钮 hover 使用红色语义，但背景保持浅红。

### 5.3 窗口外边界

推荐为现代化窗口根布局增加统一外层 Border：

```xml
<Border BorderBrush="{StaticResource ModernWindowOuterBorderBrush}"
        BorderThickness="1"
        Background="{StaticResource ModernAppBackgroundBrush}">
    <!-- 原窗口根 Grid 内容 -->
</Border>
```

对于 `MessageDialogWindow` 这类透明弹窗，可保持：

```xml
Background="Transparent"
AllowsTransparency="True"
```

但内部内容外壳应有：

```xml
BorderBrush="{StaticResource ModernWindowOuterBorderBrush}"
BorderThickness="1"
```

并保留轻微阴影。

## 6. 标题栏控制按钮方案

### 6.1 不再使用文字字符

必须移除：

```xml
Content="—"
Content="□"
Content="×"
```

改为矢量图形。

原因：

- 字符图标无法保证大小一致。
- 字体指标不同会导致视觉重心不一致。
- 矢量图形可统一线宽、尺寸、位置。

### 6.2 按钮点击区域

保持现有点击区域：

```text
宽：44
高：44
```

内部图标建议：

```text
图标画布：12 x 12 或 14 x 14
线宽：1.5 - 1.7
默认颜色：#64748B
hover 颜色：#334155
close hover 颜色：#DC2626
```

### 6.3 图标语义

最小化：

```xml
<Line X1="1" Y1="7" X2="13" Y2="7" />
```

最大化：

```xml
<Rectangle Width="11" Height="11" />
```

还原：

```xml
两个错位 Rectangle
```

关闭：

```xml
<Line X1="2" Y1="2" X2="12" Y2="12" />
<Line X1="12" Y1="2" X2="2" Y2="12" />
```

### 6.4 最大化 / 还原状态

当前 `ModernTitleBar.xaml.cs` 中按钮没有根据窗口状态切换图标。

本次建议至少做到：

- 最大化按钮默认显示最大化图标。
- 窗口最大化后显示还原图标。
- 窗口还原后重新显示最大化图标。

可在：

```text
VSLoader/Views/Controls/ModernTitleBar.xaml.cs
```

监听：

```csharp
window.StateChanged += ...
```

并更新图标可见性。

如果为了最小改动，也可以第一阶段只统一最大化图标，不做状态切换。但推荐本次一起处理，因为“最大化/还原”本来就是一个按钮的完整语义。

## 7. 建议修改文件

### 7.1 必改文件

```text
VSLoader/Styles/ModernWindowChrome.xaml
VSLoader/Views/Controls/ModernTitleBar.xaml
VSLoader/Views/Controls/ModernTitleBar.xaml.cs
```

职责：

- 定义窗口边界颜色资源。
- 定义标题栏按钮图标颜色资源。
- 移除字符图标。
- 改为矢量图标。
- 处理最大化 / 还原图标状态。

### 7.2 窗口根布局文件

需要检查并按最小改动增加外层边界：

```text
VSLoader/MainWindow.xaml
VSLoader/Views/WorkspaceSelectorWindow.xaml
VSLoader/Views/SettingsWindow.xaml
VSLoader/Views/BatchImportWindow.xaml
VSLoader/Views/ShortcutEditWindow.xaml
VSLoader/Views/WorkspaceNameDialog.xaml
VSLoader/Views/FactoryMapWindow.xaml
VSLoader/Views/MessageDialogWindow.xaml
```

原则：

- 不改变内部 Grid 行列结构。
- 不改变任何控件名称。
- 不改变任何事件绑定。
- 只在最外层补边界容器或调整已有最外层 Border。

## 8. 实现细节

### 8.1 ModernTitleBar.xaml

将当前按钮从：

```xml
<Button x:Name="MinimizeButton"
        Style="{StaticResource ModernTitleBarButtonStyle}"
        Click="MinimizeButton_Click"
        Content="—"
        ToolTip="最小化" />
```

改为：

```xml
<Button x:Name="MinimizeButton"
        Style="{StaticResource ModernTitleBarButtonStyle}"
        Click="MinimizeButton_Click"
        ToolTip="最小化">
    <Grid Width="14" Height="14">
        <Line X1="1" Y1="7" X2="13" Y2="7"
              Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}"
              StrokeThickness="1.6"
              StrokeStartLineCap="Round"
              StrokeEndLineCap="Round" />
    </Grid>
</Button>
```

最大化按钮建议内部包含两个图标容器：

```xml
<Grid Width="14" Height="14">
    <Rectangle x:Name="MaximizeIcon"
               Width="11"
               Height="11"
               Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}"
               StrokeThickness="1.5" />
    <Grid x:Name="RestoreIcon" Visibility="Collapsed">
        <!-- 两个错位矩形 -->
    </Grid>
</Grid>
```

关闭按钮：

```xml
<Button Style="{StaticResource ModernTitleBarCloseButtonStyle}"
        Click="CloseButton_Click"
        ToolTip="关闭">
    <Grid Width="14" Height="14">
        <Line X1="3" Y1="3" X2="11" Y2="11"
              Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}"
              StrokeThickness="1.7"
              StrokeStartLineCap="Round"
              StrokeEndLineCap="Round" />
        <Line X1="11" Y1="3" X2="3" Y2="11"
              Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}"
              StrokeThickness="1.7"
              StrokeStartLineCap="Round"
              StrokeEndLineCap="Round" />
    </Grid>
</Button>
```

### 8.2 ModernWindowChrome.xaml

保留按钮点击区域不变：

```xml
<Setter Property="Width" Value="44" />
<Setter Property="Height" Value="44" />
```

移除对图标不再有意义的字体设置，或让其不影响图标：

```xml
<Setter Property="Foreground" Value="{StaticResource ModernTitleBarIconBrush}" />
```

hover：

```xml
<Setter Property="Foreground" Value="{StaticResource ModernTitleBarIconHoverBrush}" />
```

close hover：

```xml
<Setter Property="Foreground" Value="{StaticResource ModernTitleBarCloseIconHoverBrush}" />
```

### 8.3 ModernTitleBar.xaml.cs

在 Loaded 后绑定窗口状态：

```csharp
Loaded += (_, _) =>
{
    UpdateMaximizeButtonVisibility();
    UpdateMaximizeRestoreIcon();

    if (OwnerWindow is { } window)
    {
        window.StateChanged -= OwnerWindow_StateChanged;
        window.StateChanged += OwnerWindow_StateChanged;
    }
};
```

新增：

```csharp
private void OwnerWindow_StateChanged(object? sender, EventArgs e)
{
    UpdateMaximizeRestoreIcon();
}
```

图标切换逻辑：

```csharp
private void UpdateMaximizeRestoreIcon()
{
    if (OwnerWindow is not { } window)
    {
        return;
    }

    var isMaximized = window.WindowState == WindowState.Maximized;
    MaximizeIcon.Visibility = isMaximized ? Visibility.Collapsed : Visibility.Visible;
    RestoreIcon.Visibility = isMaximized ? Visibility.Visible : Visibility.Collapsed;
}
```

注意：

- 如果 `ResizeMode == NoResize`，最大化按钮仍然隐藏。
- 不要改变 `CloseRequested` 语义。
- 不要改变 `CloseButton_Click` 原逻辑。

### 8.4 外边界容器

对普通 `WindowChrome` 窗口，推荐结构：

```xml
<Border BorderBrush="{StaticResource ModernWindowOuterBorderBrush}"
        BorderThickness="1"
        Background="{StaticResource ModernAppBackgroundBrush}"
        SnapsToDevicePixels="True">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="44" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <chromeControls:ModernTitleBar Grid.Row="0" />
        <!-- 原内容 -->
    </Grid>
</Border>
```

如果原根节点已经是：

```xml
<Grid Background="{StaticResource ModernAppBackgroundBrush}">
```

则外层包 Border 后，内部 Grid 可以保留原 Background，也可以移到 Border 上。为了最小风险，优先保留内部背景，只增加外层 Border。

## 9. 特殊窗口注意事项

### 9.1 MainWindow

必须保留：

- 关闭按钮调用 `Close()` 后仍由现有 Closing 逻辑接管。
- 最小化到托盘逻辑不回归。
- 窗口布局持久化不回归。

不得改动：

- 快捷项列表。
- 托盘退出。
- 自动获取连接。
- 地图按钮。
- 更新检测按钮。

### 9.2 FactoryMapWindow

必须保留：

- 地图窗口最小化等同关闭。
- 地图窗口跟随主窗口位置关系。
- 地图快捷键打开/关闭逻辑。
- 地图内部缩放、拖拽、编辑、多选、连线功能。

注意：

- 如果标题栏最小化按钮现有逻辑会调用 `WindowState = Minimized`，需要确认地图窗口 code-behind 是否已有拦截。
- 本次不要借机调整地图业务逻辑。

### 9.3 MessageDialogWindow

必须保留：

- 确认弹窗关闭按钮语义。
- `CloseRequested` 逻辑。
- Owner 居中。
- 弹窗返回结果。

注意：

- `MessageDialogWindow` 使用 `AllowsTransparency=True`，外壳实现方式可能和普通窗口不同。
- 可只调整内部 Border，不强行改成 `WindowChrome`。

## 10. 测试建议

### 10.1 扩展 ModernWindowChromeResourceTests

文件：

```text
VSLoader.Tests/ModernWindowChromeResourceTests.cs
```

新增断言：

```csharp
Assert.Contains("ModernWindowOuterBorderBrush", chromeXaml);
Assert.Contains("ModernTitleBarIconBrush", chromeXaml);
Assert.Contains("ModernTitleBarIconHoverBrush", chromeXaml);
Assert.Contains("ModernTitleBarCloseIconHoverBrush", chromeXaml);
```

### 10.2 扩展 ModernWindowChromeUsageTests

文件：

```text
VSLoader.Tests/ModernWindowChromeUsageTests.cs
```

新增断言：

```csharp
Assert.DoesNotContain("Content=\"—\"", titleBarXaml);
Assert.DoesNotContain("Content=\"□\"", titleBarXaml);
Assert.DoesNotContain("Content=\"×\"", titleBarXaml);
Assert.Contains("<Line", titleBarXaml);
Assert.Contains("<Rectangle", titleBarXaml);
```

并检查核心窗口包含：

```text
ModernWindowOuterBorderBrush
```

或其他统一外边界标记。

### 10.3 必跑验证命令

```powershell
dotnet test .\VSLoader.sln -c Debug --no-restore
dotnet build .\VSLoader.sln -c Debug --no-restore
```

## 11. 手动验证清单

### 11.1 外边界

1. 启动主窗口。
2. 检查主窗口外边界能看见，但不突兀。
3. 打开设置窗口。
4. 设置窗口叠在主窗口上时，边界清楚。
5. 打开工作区选择窗口。
6. 工作区窗口外壳不再像漂浮在背景中。
7. 打开批量新增识别窗口。
8. 窗口外边界清晰。
9. 打开提示弹窗。
10. 弹窗边界和阴影自然，不出现黑线或脏边。

### 11.2 标题栏图标

1. 检查最小化、最大化、关闭图标大小一致。
2. 三个图标线宽一致。
3. 三个图标垂直居中。
4. hover 状态清晰。
5. 关闭按钮 hover 为浅红背景和红色图标。
6. 不可调整大小的窗口不显示最大化按钮。
7. 可调整大小的窗口最大化后，最大化图标切换为还原图标。
8. 还原后图标切回最大化图标。

### 11.3 行为回归

1. 主窗口标题栏可拖动。
2. 主窗口可缩放。
3. 主窗口最大化 / 还原正常。
4. 主窗口关闭仍最小化到托盘。
5. 托盘右键退出仍真正退出。
6. 设置窗口保存 / 取消正常。
7. 工作区打开 / 新建 / 重命名 / 删除正常。
8. 地图窗口打开 / 关闭 / 快捷键开关正常。
9. 地图窗口最小化行为不回归。
10. 提示弹窗关闭、确认、取消结果正常。

## 12. 风险与规避

### 12.1 外层 Border 影响布局

风险：

- 给根布局包 Border 可能导致窗口内容整体多出 1px 边距。

规避：

- BorderThickness 仅为 1。
- 不改变窗口 Width / Height。
- 不改变内部 Grid 行列定义。
- 通过手动验证确认内容没有被裁切。

### 12.2 WindowChrome 缩放边缘风险

风险：

- 外层 Border 可能影响边缘缩放手感。

规避：

- 保持 `ResizeBorderThickness="6"` 不变。
- 不给外层 Border 设置会拦截鼠标的复杂事件。
- 保持 `WindowChrome.IsHitTestVisibleInChrome` 只用于按钮区域。

### 12.3 图标绑定风险

风险：

- Path / Line 的 Stroke 绑定 Button Foreground 失败，导致图标不可见。

规避：

- 使用：

```xml
Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}"
```

- 构建后手动打开窗口验证。

### 12.4 最大化图标状态风险

风险：

- `StateChanged` 多次绑定导致重复事件。

规避：

- 绑定前先解除：

```csharp
window.StateChanged -= OwnerWindow_StateChanged;
window.StateChanged += OwnerWindow_StateChanged;
```

### 12.5 弹窗关闭语义风险

风险：

- 修改 `ModernTitleBar` 时破坏 `CloseRequested`，导致确认弹窗关闭语义异常。

规避：

- 不修改 `CloseButton_Click` 的现有判断顺序。
- `CloseRequested` 仍优先。

## 13. 验收标准

满足以下条件即完成：

- 所有现代化窗口外边界比 v111 更清楚。
- 外边界不突兀、不厚重、不回退为旧式灰框。
- 标题栏底部分隔线更清晰。
- 最小化、最大化、关闭按钮不再使用文字字符图标。
- 三个窗口控制按钮图标大小、线宽、视觉重心统一。
- 最大化 / 还原图标语义正确。
- 主窗口关闭到托盘逻辑不回归。
- 地图窗口特殊最小化 / 关闭逻辑不回归。
- 工作区、设置、批量新增、快捷项编辑、提示弹窗功能不回归。
- `dotnet test .\VSLoader.sln -c Debug --no-restore` 通过。
- `dotnet build .\VSLoader.sln -c Debug --no-restore` 通过。

