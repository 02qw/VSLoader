# v50 工厂地图轻量拖拽编辑器编码规格说明

## 1. 需求背景

当前 VSLoader 已经实现：

```text
地图按钮
右侧工厂地图窗口
地图 JSON 配置
地图默认全局适应窗口
鼠标滚轮缩放地图
鼠标左键拖动空白区域平移地图
点击地图机器节点联动左侧快捷项
```

当前地图模型仍偏向：

```text
设备类型节点
一个节点内展示多台设备
```

用户现在希望进一步升级为轻量拖拽编辑器：

```text
地图上的最小单元是一台具体设备。
每台设备是一个独立方框。
用户可以进入编辑模式拖动设备方框。
拖动完成后保存地图配置。
```

该功能的目标不是做完整流程图编辑器，而是在不显著拖累软件性能的前提下，让用户可以用拖拽方式维护设备地图布局。

## 2. 需求目标

实现一个轻量地图编辑能力：

1. 地图上每个方框代表一个具体快捷项，也就是一台具体设备。
2. 地图窗口顶部新增一个锁按钮，用于切换浏览模式和编辑模式。
3. 红色锁表示不可编辑状态，即浏览模式。
4. 绿色锁表示可编辑状态，即编辑模式。
5. 浏览模式下：
   - 鼠标滚轮缩放地图。
   - 鼠标左键按住空白区域拖动画布。
   - 点击设备方框选中左侧快捷项。
6. 编辑模式下：
   - 鼠标滚轮仍然缩放地图。
   - 鼠标左键按住空白区域仍然拖动画布。
   - 鼠标左键按住设备方框并移动时，拖动该设备方框。
   - 设备方框拖动完成后保存位置配置。
7. 连线第一版不做拖拽编辑，只根据配置中的 `from/to` 关系自动绘制。

## 3. 设计原则

本次实现必须遵守：

1. 轻量化优先。
2. 尽量沿用当前 WPF `Canvas` 绘制方案。
3. 不引入大型流程图编辑器或重型第三方控件。
4. 地图浏览操作和地图编辑操作必须明确分离。
5. 拖拽设备位置只影响地图布局配置，不影响快捷项本身。
6. 快捷项仍然由主程序配置维护，地图配置只维护布局和连线关系。

## 4. 最小单元设计

本次明确：

```text
地图最小节点 = 单个快捷项 = 单台设备
```

示例：

```text
热贴机_001
热贴机_002
热贴机_003
```

应该显示为 3 个独立方框，而不是显示为一个“热贴机”父节点。

原因：

1. 单台设备更方便查找。
2. 点击地图节点时能精确对应左侧某一个快捷项。
3. 后续如果做设备状态、WebUI、AdminUI 快捷入口，单设备节点扩展更自然。

## 5. 地图配置保存策略

地图布局保存到本地 JSON 配置。

推荐保存位置：

```text
%AppData%\VSLoader\factory-map.layout.json
```

说明：

1. `VSLoader/Config/factory-map.example.json` 继续作为安装包内置示例配置。
2. 用户拖拽编辑后的真实布局保存到 `%AppData%`。
3. 程序优先读取 `%AppData%\VSLoader\factory-map.layout.json`。
4. 如果用户布局配置不存在，再读取安装目录或程序目录下的示例配置。

这样可以避免：

```text
程序升级覆盖用户编辑好的地图布局。
```

## 6. 推荐 JSON 数据结构

推荐新增或升级地图配置结构为：

```json
{
  "version": 2,
  "canvas": {
    "width": 1600,
    "height": 900
  },
  "devices": [
    {
      "key": "\\\\192.168.15.69\\instances\\5534_TATP012",
      "name": "自动点锡机_012",
      "x": 120,
      "y": 160
    }
  ],
  "edges": [
    {
      "from": "\\\\192.168.15.69\\instances\\5534_TATP012",
      "to": "\\\\192.168.15.69\\instances\\5535_TSSM013"
    }
  ]
}
```

字段说明：

```text
version: 地图配置版本。
canvas: 地图逻辑画布尺寸。
devices: 单设备节点布局。
devices.key: 设备唯一匹配键，第一版推荐使用 ShortcutItem.TargetPath。
devices.name: 保存时的显示名快照，用于人工查看 JSON，不作为强匹配依据。
devices.x/y: 设备节点左上角坐标。
edges: 设备之间的流程关系。
edges.from/to: 使用 devices.key 关联设备。
```

## 7. 设备唯一键设计

第一版推荐使用：

```text
ShortcutItem.TargetPath
```

作为设备节点唯一键。

原因：

1. 当前快捷项没有稳定 `Id` 字段。
2. 目标路径天然对应一台设备实例。
3. 批量新增修复逻辑已经逐步向“按路径去重”靠拢。

匹配规则：

```text
TargetPath 去除首尾空白。
Windows 路径比较忽略大小写。
```

如果后续 `ShortcutItem` 增加稳定 `Id`，地图配置可以再升级为使用 `Id`。

## 8. 兼容旧地图配置

当前旧版地图配置是：

```text
nodes: 设备类型节点
edges: 类型节点之间的连线
```

v50 第一版允许不做完整旧配置自动迁移，但必须安全处理：

1. 旧配置仍可读取时，不应导致程序崩溃。
2. 如果检测到旧配置没有 `devices`，可以按当前快捷项自动生成默认单设备布局。
3. 自动生成布局后，用户进入编辑模式并拖动保存时，写出新版 `version: 2` 配置。

推荐默认生成规则：

```text
按当前快捷项排序。
每行固定数量，例如 5 或 6 个设备。
从左到右、从上到下生成坐标。
```

## 9. 地图窗口顶部工具区

地图窗口新增顶部工具区。

建议结构：

```text
[锁按钮] [保存状态文本]
地图内容区域
底部状态栏
```

锁按钮状态：

```text
红色 / 锁定 / 浏览
绿色 / 解锁 / 编辑
```

按钮文案可以使用：

```text
锁定
编辑
```

也可以使用图标加 tooltip。

第一版以清楚可用为优先，不要求复杂视觉效果。

状态文本示例：

```text
浏览模式：滚轮缩放，拖动空白区域平移地图
编辑模式：拖动设备调整位置，松开后自动保存
地图布局已保存
地图布局保存失败：xxx
```

## 10. 浏览模式交互

红色锁状态为浏览模式。

规则：

1. 鼠标滚轮缩放地图。
2. 鼠标左键按住空白区域拖动画布。
3. 鼠标点击设备方框，选中左侧快捷项。
4. 鼠标拖动设备方框不改变设备位置。

该模式应保持 v49 的交互体验。

## 11. 编辑模式交互

绿色锁状态为编辑模式。

规则：

1. 鼠标滚轮缩放地图。
2. 鼠标左键按住空白区域拖动画布。
3. 鼠标左键按住设备方框并移动，拖动该设备位置。
4. 拖动设备时不触发画布拖拽。
5. 拖动设备时不触发快捷项选中，或只在没有形成拖拽时保留点击选中。
6. 鼠标松开后，将设备新坐标保存到地图布局 JSON。

建议增加拖拽阈值：

```text
鼠标移动超过 3 到 5 像素后才认定为拖拽。
```

目的：

```text
避免用户点击设备时因为手抖产生误拖动。
```

## 12. 画布拖拽设计

画布拖拽不修改 JSON。

它只修改当前视口平移量：

```text
MapTranslateTransform.X
MapTranslateTransform.Y
```

规则：

1. 鼠标按在空白区域时进入画布拖拽。
2. 鼠标按在设备方框时不进入画布拖拽。
3. 浏览模式和编辑模式都允许空白区域拖动画布。
4. 画布拖拽过程中可以使用鼠标捕获。
5. 鼠标松开后结束画布拖拽。

## 13. 设备拖拽设计

设备拖拽只在编辑模式生效。

设备拖拽修改的是设备逻辑坐标：

```text
device.x
device.y
```

注意：

```text
鼠标在屏幕上移动的是缩放后的距离。
保存到 JSON 的是地图逻辑坐标。
```

因此计算设备移动距离时需要除以当前地图缩放比例：

```text
device.X += delta.X / currentTotalScale
device.Y += delta.Y / currentTotalScale
```

拖动过程中：

1. 实时更新设备方框在 Canvas 上的位置。
2. 连线根据设备最新位置实时重绘或移动后重绘。
3. 鼠标松开后保存 JSON。

第一版可选择：

```text
拖动过程中只移动节点。
松开后重绘连线。
```

这样实现更轻量。

## 14. 连线设计

第一版连线不做拖拽编辑。

连线只表示设备之间的流程关系。

配置保存：

```json
{
  "from": "设备A的 TargetPath",
  "to": "设备B的 TargetPath"
}
```

显示时：

1. 根据 `from` 找到起点设备。
2. 根据 `to` 找到终点设备。
3. 根据两个设备方框的位置自动绘制连线。

连线形态：

第一版推荐自动折线：

```text
起点设备右侧中心
横向到中间点
纵向到终点高度
横向到终点设备左侧中心
```

如果实现成本更低，也可以第一版使用直线：

```text
起点设备中心 -> 终点设备中心
```

但推荐保留接口，方便后续升级为折线。

不推荐第一版保存每条线的折点，除非当前已有配置中明确需要保留 `points`。

## 15. 保存时机

推荐第一版使用：

```text
设备拖拽结束后自动保存
```

也可以保留一个显式保存入口，但不作为第一版必要项。

保存成功：

```text
顶部状态文本显示：地图布局已保存
```

保存失败：

```text
顶部状态文本显示：地图布局保存失败：xxx
必要时弹出轻量错误提示
```

不要因为保存失败导致程序崩溃。

## 16. 服务层设计

建议新增或扩展：

```text
FactoryMapLayoutService
```

职责：

1. 获取用户地图布局路径。
2. 加载用户布局 JSON。
3. 用户布局不存在时加载示例配置。
4. 根据当前快捷项生成默认设备节点。
5. 保存设备坐标和连线配置。
6. 处理损坏 JSON，不覆盖原文件。

不要把文件读写逻辑写死在 `FactoryMapWindow.xaml.cs` 中。

## 17. 模型设计

建议新增模型：

```text
FactoryMapLayoutConfig
FactoryMapDeviceNode
FactoryMapDeviceEdge
FactoryMapDeviceViewData
```

其中 `FactoryMapDeviceNode` 至少包含：

```csharp
public string Key { get; set; }
public string Name { get; set; }
public double X { get; set; }
public double Y { get; set; }
```

视图数据中需要包含：

```text
ShortcutItem
```

用于点击地图设备后选中主窗口快捷项。

## 18. 与现有 FactoryMapService 的关系

当前 `FactoryMapService` 基于设备类型节点构建地图。

v50 可以有两种实现方式：

### 18.1 推荐方案

新增 `FactoryMapLayoutService`，保留旧 `FactoryMapService`。

优点：

1. 不强行破坏 v47/v48 的旧逻辑。
2. 新的单设备地图模型边界更清楚。
3. 后续确认稳定后，再删除旧类型节点逻辑。

### 18.2 备选方案

直接改造 `FactoryMapService`。

缺点：

1. 改动范围更大。
2. 测试需要整体重写。
3. 旧配置兼容更容易混乱。

第一版推荐使用方案 18.1。

## 19. 性能要求

地图节点数量可能较多，但第一版应保持轻量。

要求：

1. 不使用每帧定时器。
2. 不在鼠标移动时频繁读写 JSON。
3. 拖动过程中只更新当前节点位置。
4. JSON 保存只在拖动结束时触发。
5. 如果连线重绘成本较高，可以只在拖动结束后重绘连线。
6. 不引入大型图形库。

## 20. 非目标范围

本次不实现：

1. 不做连线拖拽编辑。
2. 不做添加/删除设备节点。
3. 不做节点吸附网格。
4. 不做撤销/重做。
5. 不做多选拖拽。
6. 不做框选。
7. 不做小地图预览。
8. 不做复杂节点样式编辑。
9. 不做流程线折点手动编辑。
10. 不做地图布局导入导出。

## 21. 预期改动文件

预计新增：

```text
VSLoader/Models/FactoryMapLayoutConfig.cs
VSLoader/Models/FactoryMapDeviceNode.cs
VSLoader/Models/FactoryMapDeviceEdge.cs
VSLoader/Models/FactoryMapDeviceViewData.cs
VSLoader/Models/Services/FactoryMapLayoutService.cs
VSLoader.Tests/FactoryMapLayoutServiceTests.cs
```

预计修改：

```text
VSLoader/Views/FactoryMapWindow.xaml
VSLoader/Views/FactoryMapWindow.xaml.cs
VSLoader/MainWindow.xaml.cs
VSLoader/VSLoader.csproj
```

可能修改：

```text
VSLoader/Config/factory-map.example.json
```

## 22. 测试建议

### 22.1 单元测试

重点测试服务层：

1. 用户布局不存在时，能根据快捷项生成默认设备节点。
2. 用户布局存在时，能按 `TargetPath` 匹配快捷项。
3. 快捷项新增后，布局配置中不存在的设备能自动补默认位置。
4. 快捷项删除后，孤立布局节点不会导致程序崩溃。
5. 保存布局后 JSON 内容包含设备坐标。
6. 损坏 JSON 时不覆盖原文件，并返回错误。
7. 连线 `from/to` 找不到设备时计入无效连线，不崩溃。

### 22.2 自动化命令

执行：

```powershell
cd C:\Users\shee_\OneDrive\Desktop\VSLoader
dotnet test .\VSLoader.sln -p:UseSharedCompilation=false
dotnet build .\VSLoader.sln -p:UseSharedCompilation=false
```

如果 `VSLoader.exe` 被占用：

```powershell
Get-Process -Name VSLoader -ErrorAction SilentlyContinue | Stop-Process -Force
```

## 23. 手工验收

1. 启动 VSLoader。
2. 点击 `地图`。
3. 地图窗口打开，默认显示单设备方框。
4. 红锁状态下：
   - 滚轮可以缩放地图。
   - 拖动空白区域可以平移地图。
   - 点击设备可以选中左侧快捷项。
   - 拖动设备不会改变设备位置。
5. 点击锁按钮切换到绿锁编辑状态。
6. 编辑状态下：
   - 拖动设备方框，设备位置变化。
   - 拖动空白区域，整张地图平移。
   - 滚轮仍然缩放地图。
7. 松开设备后，顶部显示保存成功。
8. 关闭地图窗口再打开，设备保持上次拖动后的位置。
9. 重启程序后打开地图，设备仍保持上次保存位置。
10. 损坏地图 JSON 后，程序不崩溃，并给出读取失败提示。

## 24. 验收标准

最终效果：

```text
地图节点以单台设备为最小单位。
用户可以通过锁按钮切换浏览/编辑模式。
浏览模式不修改布局。
编辑模式可拖动设备节点并保存到 JSON。
画布拖拽和平移不影响 JSON。
连线根据配置关系自动绘制，不做第一版拖线编辑。
```

构建要求：

```text
dotnet test 通过。
dotnet build 0 个错误。
不引入新的无关警告。
```
