# v59 首次默认窗口布局与地图初始适配比例编码规格说明

## 1. 需求背景

当前 VSLoader 已经实现运行期内存 Session 状态保持：

```text
1. 用户在本次运行期间调整主窗口位置和大小后，可以在隐藏/恢复时保持。
2. 用户在本次运行期间调整地图窗口位置和大小后，可以在关闭/打开时保持。
3. 用户在地图中滚轮缩放或拖拽平移后，可以在隐藏/恢复时保持地图视角。
4. 程序真正退出后，不保留这些运行期 Session 状态。
```

现在需要进一步明确并优化：

```text
本次软件启动后，第一次默认打开时的主窗口位置、地图窗口位置、地图默认缩放比例。
```

用户期望第一次默认效果接近截图：

```text
1. 主窗口位于屏幕左侧。
2. 地图窗口第一次点击打开时贴在主窗口右侧。
3. 两个窗口整体按屏幕比例布局，不写死像素。
4. 地图内容第一次打开时自动适配到一屏可见，比例类似截图中的全局俯视效果。
```

## 2. 核心目标

本次目标是调整“首次默认布局基准”，不是改动用户操作后的 Session 记忆逻辑。

完成后：

```text
1. 程序首次启动时，主窗口默认靠屏幕左侧显示。
2. 本次 Session 第一次点击“地图”时，地图窗口默认贴在主窗口右侧。
3. 地图窗口默认宽度使用右侧剩余工作区，高度与主窗口观感一致。
4. 地图内容首次显示时自动缩放到整体可见。
5. 用户后续移动、缩放、平移后，仍由 v58 的内存 Session 状态接管。
```

## 3. 非目标范围

本次不实现：

```text
1. 跨程序重启持久化窗口布局。
2. 新增设置页面配置默认窗口比例。
3. 新增重置布局按钮。
4. 修改地图节点坐标、连线 JSON 结构。
5. 修改地图拖拽、连线、多选、导入导出逻辑。
6. 修改快捷项列表布局。
7. 修改快捷键隐藏/恢复逻辑。
```

本次只允许最小范围修改：

```text
1. 主窗口首次默认比例参数。
2. 地图窗口首次默认比例参数。
3. 地图首次 Fit 参数或 Fit 调用时机。
```

## 4. 关键原则

### 4.1 默认状态和 Session 状态必须分离

必须区分两类状态：

```text
默认状态：
    程序本次启动后，尚未有任何用户运行期状态时使用。

Session 状态：
    用户本次运行期间移动窗口、缩放地图、平移地图后保存的状态。
```

默认状态只在没有 Session 状态时生效。

一旦存在 Session 状态：

```text
1. 主窗口恢复使用 RuntimeLayoutState 中的主窗口 Bounds。
2. 地图窗口恢复使用 RuntimeLayoutState 中的地图窗口 Bounds。
3. 地图视角恢复使用 FactoryMapViewState。
```

### 4.2 不能把首次默认 Fit 误存为用户视图状态

地图第一次打开时的自动适配不等于用户操作。

因此：

```text
1. FitMapToView() 不应自动写入 FactoryMapViewState。
2. 地图窗口 Show / SizeChanged 不应自动写入地图视图状态。
3. 只有用户滚轮缩放或拖拽空白区域平移后，才可以保存地图视图状态。
```

### 4.3 默认布局必须按屏幕工作区比例计算

不能写死：

```text
Left = 20
Width = 1000
Height = 720
```

应继续使用：

```csharp
SystemParameters.WorkArea
```

按比例计算，保证不同分辨率下观感一致。

## 5. 预计修改文件

预计修改：

```text
VSLoader/MainWindow.xaml.cs
VSLoader/Views/FactoryMapWindow.xaml.cs
```

可选修改：

```text
VSLoader/Models/FactoryMapViewportFitCalculator.cs
VSLoader.Tests/FactoryMapViewportFitCalculatorTests.cs
```

不预计修改：

```text
VSLoader/Models/RuntimeLayoutState.cs
VSLoader/Models/FactoryMapViewState.cs
VSLoader/Models/Services/FactoryMapLayoutService.cs
VSLoader/Config/factory-map.layout.json
```

## 6. 当前相关代码位置

### 6.1 主窗口默认布局

位置：

```text
VSLoader/MainWindow.xaml.cs
```

当前相关常量：

```csharp
private const double DefaultLayoutLeftRatio = 0.01;
private const double DefaultLayoutTopRatio = 0.055;
private const double DefaultLayoutMainWidthRatio = 0.50;
private const double DefaultLayoutMainHeightRatio = 0.70;
private const double DefaultLayoutRightMarginRatio = 0.01;
private const double DefaultLayoutGap = 0;
```

当前相关方法：

```csharp
ApplyDefaultWindowLayoutOnce()
ApplyDefaultMainWindowLayout()
PositionFactoryMapWindow(bool useSessionBounds)
```

### 6.2 地图首次自动适配

位置：

```text
VSLoader/Views/FactoryMapWindow.xaml.cs
```

当前相关常量：

```csharp
private const double ViewPadding = 36;
```

当前相关方法：

```csharp
RequestFitMapToView()
FitMapToView()
RenderMap(FactoryMapDeviceViewData map, bool resetView)
```

缩放计算位置：

```text
VSLoader/Models/FactoryMapViewportFitCalculator.cs
```

## 7. 设计方案

### 7.1 主窗口默认布局

继续使用 `ApplyDefaultMainWindowLayout()`。

推荐只调整比例常量，不改整体算法。

建议目标：

```text
1. 主窗口左边距非常小，保持贴近屏幕左侧。
2. 主窗口宽度约占工作区 50%。
3. 主窗口高度约占工作区 70% 到 75%。
4. 主窗口顶部距离按工作区高度比例计算，避免贴到浏览器标题栏或屏幕边缘。
```

可保留当前值作为基础：

```csharp
DefaultLayoutLeftRatio = 0.01;
DefaultLayoutTopRatio = 0.055;
DefaultLayoutMainWidthRatio = 0.50;
DefaultLayoutMainHeightRatio = 0.70;
```

如果实际效果与截图不一致，只允许微调这些比例值。

### 7.2 地图窗口默认布局

继续使用 `PositionFactoryMapWindow(useSessionBounds: true)`。

规则：

```text
1. 如果 RuntimeLayoutState.HasFactoryMapBounds == true，则恢复 Session Bounds。
2. 如果没有地图 Session Bounds，则按主窗口右侧剩余区域计算默认地图窗口。
3. 默认地图窗口 Left = 主窗口 Left + 主窗口 Width + DefaultLayoutGap。
4. DefaultLayoutGap 保持 0，实现贴边。
5. 地图窗口右边距使用 DefaultLayoutRightMarginRatio。
6. 地图窗口高度默认跟主窗口高度一致。
```

如果截图目标需要地图更宽：

```text
优先通过主窗口宽度比例和右侧剩余宽度计算实现。
不要给地图窗口写固定宽度。
```

### 7.3 地图内容首次缩放比例

地图第一次打开时，仍走：

```text
RenderMap(resetView: true)
RequestFitMapToView()
FitMapToView()
FactoryMapViewportFitCalculator.Calculate(...)
```

调整方向：

```text
1. 保证全部节点在地图窗口内可见。
2. 允许四周留出舒适空白。
3. 不需要强行放大到填满窗口。
4. 不要让节点贴边。
```

如果需要让截图效果更接近当前视觉，可优先微调：

```csharp
private const double ViewPadding = 36;
```

例如：

```text
ViewPadding 变大：地图整体更小，四周留白更多。
ViewPadding 变小：地图整体更大，更贴近窗口边缘。
```

除非必要，不修改 `FactoryMapViewportFitCalculator.Calculate(...)` 的核心算法。

## 8. 与 v58 Session 逻辑的关系

必须保持：

```text
1. 首次默认布局只在没有 Session Bounds 时生效。
2. 首次默认 Fit 只在没有 FactoryMapViewState 时生效。
3. 用户缩放/平移后，后续隐藏/恢复不再触发默认 Fit。
4. 用户关闭地图再打开，如果本次 Session 有视图状态，应恢复用户视图状态。
5. 程序真正退出后，下次启动重新使用默认布局。
```

不得出现：

```text
1. 第一次打开地图时就误用一个空的 FactoryMapViewState。
2. 地图窗口 SizeChanged 把默认 Fit 视图保存为用户视图。
3. 快捷键隐藏/恢复时重新计算默认地图比例。
4. 主窗口恢复时重新套默认 WorkArea 比例。
```

## 9. 最小实现步骤

### 9.1 检查当前默认比例

检查：

```text
DefaultLayoutLeftRatio
DefaultLayoutTopRatio
DefaultLayoutMainWidthRatio
DefaultLayoutMainHeightRatio
DefaultLayoutRightMarginRatio
DefaultLayoutGap
ViewPadding
```

确认是否需要微调。

### 9.2 保持主窗口默认靠左

如需调整，只改比例常量。

不要新增复杂布局服务。

### 9.3 保持地图首次打开贴右侧

检查 `PositionFactoryMapWindow(useSessionBounds: true)`：

```text
没有 HasFactoryMapBounds 时，必须进入默认计算分支。
有 HasFactoryMapBounds 时，必须进入 Session 恢复分支。
```

### 9.4 保持地图首次 Fit

检查：

```text
_runtimeLayoutState.FactoryMapView is null
```

时必须：

```csharp
RenderMap(loadResult.Map, resetView: true)
```

有视图状态时才：

```csharp
RenderMap(loadResult.Map, resetView: false)
RestoreViewState(...)
```

### 9.5 防止默认 Fit 被误存

确认 `SaveFactoryMapStateToSession`：

```text
只有 includeViewState == true 且 FactoryMapWindow.HasUserViewState == true 时，才写入 FactoryMapView。
```

## 10. 手工验收标准

### 10.1 首次启动主窗口

步骤：

```text
1. 完全退出 VSLoader。
2. 重新启动 VSLoader。
3. 不进行任何窗口拖拽。
```

期望：

```text
1. 主窗口位于屏幕左侧。
2. 主窗口位置和大小按屏幕比例适配。
3. 不同分辨率下观感保持一致。
```

### 10.2 本次 Session 第一次打开地图

步骤：

```text
1. 启动 VSLoader。
2. 第一次点击“地图”。
```

期望：

```text
1. 地图窗口贴在主窗口右侧。
2. 地图窗口宽度使用右侧剩余区域。
3. 地图窗口高度与主窗口接近。
4. 地图内容整体可见。
5. 地图默认比例接近截图中的俯视效果。
```

### 10.3 用户操作后 Session 接管

步骤：

```text
1. 打开地图。
2. 滚轮缩放地图。
3. 拖拽空白区域平移地图。
4. 按快捷键隐藏程序。
5. 再按快捷键恢复程序。
```

期望：

```text
1. 地图不重新执行默认 Fit。
2. 地图保持用户缩放和平移后的视角。
3. 主窗口和地图窗口保持本次 Session 中的位置和大小。
```

### 10.4 程序退出后恢复默认

步骤：

```text
1. 调整主窗口和地图窗口。
2. 托盘右键退出程序。
3. 重新启动程序。
```

期望：

```text
1. 不恢复上一次程序运行时的窗口位置。
2. 重新使用首次默认布局。
```

## 11. 自动化验证

执行：

```powershell
cd C:\Users\shee_\OneDrive\Desktop\VSLoader
dotnet test .\VSLoader.sln -p:UseSharedCompilation=false
dotnet build .\VSLoader.sln -p:UseSharedCompilation=false
```

如果 VSLoader 正在运行导致文件占用：

```powershell
Get-Process -Name VSLoader -ErrorAction SilentlyContinue | Stop-Process -Force
```

## 12. 验收标准

最终必须满足：

```text
1. 首次启动主窗口默认靠左，且按 WorkArea 比例计算。
2. 首次打开地图窗口默认贴在主窗口右侧。
3. 首次地图内容自动 Fit 到全局可见。
4. 默认布局不写死像素。
5. 用户缩放/平移后，v58 Session 状态仍然正常保持。
6. 快捷键隐藏/恢复不重置地图视角。
7. 地图布局 JSON 不新增字段。
8. dotnet test 通过。
9. dotnet build 0 错误。
```
