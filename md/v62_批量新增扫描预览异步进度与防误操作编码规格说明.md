# v62 批量新增扫描预览异步进度与防误操作编码规格说明

## 1. 需求背景

当前 VSLoader 的“批量新增识别”窗口支持：

```text
1. 输入目标父级文件夹路径。
2. 输入或选择 CSV 规则文件。
3. 点击“扫描预览”生成待新增/可更新/已跳过/规则错误等预览结果。
4. 勾选结果后确认新增。
```

当前存在两个问题：

```text
1. 点击“扫描预览”后没有加载遮罩和进度提示，用户不知道程序正在处理什么。
2. 扫描过程可能很慢，用户仍然可以继续点击按钮或修改输入，容易造成状态混乱。
```

性能方面，当前扫描慢的主要可能原因：

```text
1. ScanPreview 在 UI 线程同步执行，导致窗口卡顿。
2. 目标父级路径可能是网络共享路径，Directory.Exists / Directory.EnumerateDirectories / File.Read 可能很慢。
3. 简化模块映射模式下，每个子目录都可能读取 META-INF\ZAM-DEPLOY.xml。
4. 规则多、目录多时，正则匹配也会增加耗时，但优先级低于网络 IO。
```

本次目标是先解决：

```text
扫描过程可见、可控、不阻塞 UI、不允许用户误操作。
```

不在本次大规模重写批量新增算法。

## 2. 需求目标

完成后：

```text
1. 点击“扫描预览”后，批量新增窗口显示覆盖面板。
2. 覆盖面板显示扫描进度条和当前处理信息。
3. 扫描期间禁用窗口内其它操作，防止用户重复点击或修改输入。
4. 扫描逻辑在后台线程执行，窗口不假死。
5. 扫描完成后关闭覆盖面板，并展示预览结果。
6. 如果网络路径或文件读取很慢，用户能从进度信息看出当前处理到哪里。
```

## 3. 非目标范围

本次不实现：

```text
1. 取消扫描按钮。
2. 多线程并发扫描网络目录。
3. 大规模重写规则匹配算法。
4. 修改批量新增的最终导入逻辑。
5. 修改快捷项数据结构。
6. 修改配置文件结构。
7. 修改 AdminUI / WebUI / 地图功能。
8. 修改主窗口自动获取链接逻辑。
```

本次允许做的优化：

```text
1. ScanPreview 异步化。
2. 增加批量新增窗口自己的 Busy 状态。
3. 增加扫描进度模型。
4. BatchImportService.BuildPreview 增加可选 progress 回调。
5. 扫描时先枚举子目录列表，用于获得总数量和稳定进度。
```

## 4. 当前代码分析

### 4.1 批量新增 ViewModel

位置：

```text
VSLoader/ViewModels/BatchImportViewModel.cs
```

当前扫描方法：

```csharp
[RelayCommand]
private void ScanPreview()
```

当前问题：

```text
该方法同步执行。
LoadRules、BuildPreview、PreviewItems 更新都在 UI 线程内。
如果 parentFolderPath 是网络路径，窗口会明显卡顿。
```

### 4.2 批量新增服务

位置：

```text
VSLoader/Models/Services/BatchImportService.cs
```

当前预览方法：

```csharp
public IReadOnlyList<BatchImportPreviewItem> BuildPreview(
    string parentFolderPath,
    IReadOnlyList<BatchImportRule> rules,
    IEnumerable<ShortcutItem> existingShortcuts,
    IEnumerable<string>? ruleErrors = null)
```

当前慢点：

```text
1. Directory.EnumerateDirectories(parentFolderPath)
2. 每个目录规则匹配
3. 简化模块映射模式下读取 ZAM-DEPLOY.xml
```

### 4.3 批量新增窗口

位置：

```text
VSLoader/Views/BatchImportWindow.xaml
```

当前界面没有独立 Busy 覆盖层。

可参考主窗口：

```text
VSLoader/MainWindow.xaml
```

主窗口已有：

```text
IsBusy
BusyMessage
BusyProgressValue
BusyProgressMaximum
BusyProgressText
BusyCurrentItemText
覆盖面板
```

本次不复用 MainViewModel 的 Busy 状态，而是在 BatchImportViewModel 中新增同类状态。

## 5. 设计方案

### 5.1 新增扫描进度模型

新增文件：

```text
VSLoader/Models/Services/BatchImportScanProgress.cs
```

建议结构：

```csharp
namespace VSLoader.Services;

public sealed class BatchImportScanProgress
{
    public int CompletedCount { get; init; }

    public int TotalCount { get; init; }

    public string CurrentFolderName { get; init; } = string.Empty;

    public string Stage { get; init; } = string.Empty;
}
```

字段含义：

```text
CompletedCount：已处理目录数。
TotalCount：总目录数。
CurrentFolderName：当前正在处理的目录名。
Stage：当前阶段，例如“正在枚举子文件夹”“正在匹配规则”“正在读取模块信息”。
```

### 5.2 BatchImportService 增加 progress 参数

修改方法签名为：

```csharp
public IReadOnlyList<BatchImportPreviewItem> BuildPreview(
    string parentFolderPath,
    IReadOnlyList<BatchImportRule> rules,
    IEnumerable<ShortcutItem> existingShortcuts,
    IEnumerable<string>? ruleErrors = null,
    IProgress<BatchImportScanProgress>? progress = null)
```

兼容性要求：

```text
progress 默认为 null，不影响现有测试和调用。
```

### 5.3 目录枚举改为先 ToList

当前：

```csharp
foreach (var directory in Directory.EnumerateDirectories(parentFolderPath))
```

建议改为：

```csharp
progress?.Report(new BatchImportScanProgress
{
    CompletedCount = 0,
    TotalCount = 0,
    Stage = "正在枚举子文件夹..."
});

var directories = Directory.EnumerateDirectories(parentFolderPath).ToList();
var totalCount = directories.Count;
```

然后：

```csharp
for (var index = 0; index < directories.Count; index++)
{
    var directory = directories[index];
    var folderName = Path.GetFileName(directory);

    progress?.Report(new BatchImportScanProgress
    {
        CompletedCount = index,
        TotalCount = totalCount,
        CurrentFolderName = folderName,
        Stage = "正在匹配规则..."
    });

    // 原有处理逻辑

    progress?.Report(new BatchImportScanProgress
    {
        CompletedCount = index + 1,
        TotalCount = totalCount,
        CurrentFolderName = folderName,
        Stage = "已处理"
    });
}
```

说明：

```text
先 ToList 的好处是获得总数，方便进度条显示。
代价是枚举阶段本身仍可能慢，所以枚举前要显示“正在枚举子文件夹”。
```

### 5.4 扫描方法异步化

修改：

```csharp
[RelayCommand]
private void ScanPreview()
```

为：

```csharp
[RelayCommand(CanExecute = nameof(CanRunScan))]
private async Task ScanPreviewAsync()
```

或使用 CommunityToolkit 自动生成：

```csharp
[RelayCommand(CanExecute = nameof(CanRunScan))]
private async Task ScanPreview()
```

推荐保持命令名仍为：

```text
ScanPreviewCommand
```

因此方法名可继续叫 `ScanPreview`，但返回 `Task`。

### 5.5 BatchImportViewModel 新增 Busy 属性

新增：

```csharp
[ObservableProperty]
[NotifyCanExecuteChangedFor(nameof(ScanPreviewCommand))]
[NotifyCanExecuteChangedFor(nameof(BrowseParentFolderCommand))]
[NotifyCanExecuteChangedFor(nameof(BrowseCsvCommand))]
[NotifyCanExecuteChangedFor(nameof(ConfirmImportCommand))]
[NotifyCanExecuteChangedFor(nameof(CancelCommand))]
private bool isBusy;
```

新增：

```csharp
[ObservableProperty]
private string busyMessage = string.Empty;

[ObservableProperty]
private int busyProgressValue;

[ObservableProperty]
private int busyProgressMaximum;

[ObservableProperty]
private string busyProgressText = string.Empty;

[ObservableProperty]
private string busyCurrentItemText = string.Empty;

public bool IsNotBusy => !IsBusy;
```

并实现：

```csharp
partial void OnIsBusyChanged(bool value)
{
    OnPropertyChanged(nameof(IsNotBusy));
}
```

### 5.6 命令 CanExecute

扫描、浏览、确认、取消在 Busy 时禁用。

建议：

```csharp
private bool CanRunScan()
{
    return !IsBusy;
}
```

已有：

```csharp
private bool CanConfirmImport()
{
    return SelectedImportCount > 0;
}
```

需要改为：

```csharp
private bool CanConfirmImport()
{
    return !IsBusy && SelectedImportCount > 0;
}
```

Browse / Cancel 可加：

```csharp
[RelayCommand(CanExecute = nameof(CanRunWindowCommand))]
```

```csharp
private bool CanRunWindowCommand()
{
    return !IsBusy;
}
```

### 5.7 ScanPreviewAsync 流程

目标流程：

```text
1. 校验输入。
2. 设置 IsBusy = true。
3. 初始化 BusyMessage / BusyProgress。
4. 在后台线程中执行 LoadRules 和 BuildPreview。
5. 通过 Progress 更新 UI 进度。
6. 回到 UI 线程后更新 PreviewItems。
7. 刷新统计。
8. HasSuccessfulScan = true。
9. finally 中清理 Busy 状态。
```

伪代码：

```csharp
private async Task ScanPreview()
{
    var parentPath = ParentFolderPath.Trim();
    var csvPath = CsvPath.Trim();

    if (!ValidateInput(parentPath, csvPath))
    {
        return;
    }

    IsBusy = true;
    BusyMessage = "正在扫描预览，请稍候...";
    BusyProgressValue = 0;
    BusyProgressMaximum = 1;
    BusyProgressText = "准备扫描...";
    BusyCurrentItemText = string.Empty;

    try
    {
        var progress = new Progress<BatchImportScanProgress>(scanProgress =>
        {
            BusyProgressValue = scanProgress.CompletedCount;
            BusyProgressMaximum = Math.Max(1, scanProgress.TotalCount);
            BusyProgressText = scanProgress.TotalCount > 0
                ? $"当前进度：{scanProgress.CompletedCount} / {scanProgress.TotalCount}"
                : scanProgress.Stage;
            BusyCurrentItemText = string.IsNullOrWhiteSpace(scanProgress.CurrentFolderName)
                ? scanProgress.Stage
                : $"{scanProgress.Stage}：{scanProgress.CurrentFolderName}";
        });

        var result = await Task.Run(() =>
        {
            var rules = _batchImportService.LoadRules(csvPath, out var ruleErrors);
            if (HasFatalRuleErrors(ruleErrors))
            {
                return BatchImportScanResult.Fatal(ruleErrors);
            }

            var previewItems = _batchImportService.BuildPreview(parentPath, rules, _existingShortcuts, ruleErrors, progress);
            return BatchImportScanResult.Success(previewItems);
        });

        if (!result.Success)
        {
            _dialogService.ShowError(...);
            return;
        }

        ReplacePreviewItems(result.PreviewItems);
        RefreshStatistics();
        HasSuccessfulScan = true;
    }
    catch (Exception ex)
    {
        _dialogService.ShowError($"扫描预览失败：{ex.Message}");
    }
    finally
    {
        ClearBusyState();
    }
}
```

可以不新增 `BatchImportScanResult` 类型，使用局部变量或 tuple。

本次以代码简洁为优先。

### 5.8 批量新增窗口覆盖层

修改：

```text
VSLoader/Views/BatchImportWindow.xaml
```

当前根元素是：

```xml
<DockPanel Background="#F6F7F9" Margin="16">
```

建议改为：

```xml
<Grid>
    <DockPanel IsEnabled="{Binding IsNotBusy}" ...>
        原有内容
    </DockPanel>

    <Grid Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}">
        半透明背景
        居中的进度面板
    </Grid>
</Grid>
```

覆盖层内容参考主窗口：

```text
1. BusyMessage
2. ProgressBar
3. BusyProgressText
4. BusyCurrentItemText
```

注意：

```text
覆盖层必须覆盖整个批量新增窗口，防止用户继续操作。
```

### 5.9 无子文件夹提示

当前扫描后再次调用：

```csharp
if (!Directory.EnumerateDirectories(parentPath).Any())
{
    _dialogService.ShowInfo("未扫描到子文件夹。");
}
```

这会再次枚举目录，网络路径下会造成额外慢。

本次应改为：

```text
根据 previewItems 是否为空或扫描结果中的目录数量判断。
```

推荐：

```text
如果 previewItems.Count == 0，则提示未扫描到子文件夹。
```

如果存在 ruleErrors，previewItems 可能只有规则错误项。

更稳妥做法：

```text
BuildPreview 进度或返回值提供目录总数。
```

为保持最小改动，可以先用：

```csharp
if (!previewItems.Any(item => !string.IsNullOrWhiteSpace(item.TargetPath)))
{
    _dialogService.ShowInfo("未扫描到子文件夹。");
}
```

避免第二次 Directory 枚举。

## 6. 性能优化边界

### 6.1 本次必须做

```text
1. UI 线程不再同步执行扫描。
2. 扫描过程显示进度。
3. 扫描过程禁用操作。
4. 不重复枚举目录判断空目录。
```

### 6.2 本次可选做

```text
1. 对目录列表先 ToList 获取总数。
2. 进度显示当前文件夹名。
```

### 6.3 本次不做

```text
1. 并行读取多个网络目录。
2. 为 ZAM-DEPLOY.xml 增加全局缓存。
3. 正则预编译缓存。
```

这些可以等进度显示后，通过实际卡顿位置再判断是否需要。

## 7. 错误处理

### 7.1 CSV 表头错误或读取失败

保持现有行为：

```text
弹出错误提示，不更新预览列表。
```

### 7.2 目标父级路径不存在

保持现有行为：

```text
弹出错误提示，不进入 Busy 状态。
```

### 7.3 扫描过程中异常

新增兜底：

```text
弹出“扫描预览失败：xxx”
finally 清理 Busy 状态。
```

### 7.4 网络路径卡顿

没有取消按钮的情况下：

```text
覆盖层持续显示当前阶段。
窗口保持响应，但用户不能操作批量新增内容。
```

## 8. 手工验收

### 8.1 正常扫描

步骤：

```text
1. 打开批量新增识别窗口。
2. 输入有效父级路径和 CSV。
3. 点击扫描预览。
```

期望：

```text
1. 立即显示覆盖层。
2. 显示正在扫描预览。
3. 进度条更新。
4. 扫描完成后覆盖层消失。
5. 预览列表正常展示。
```

### 8.2 扫描期间防误操作

步骤：

```text
1. 点击扫描预览。
2. 扫描未完成前尝试点击扫描预览、选择 CSV、确认新增、取消。
```

期望：

```text
1. 原窗口操作被禁用。
2. 不会重复触发扫描。
3. 不会关闭窗口或提交导入。
```

### 8.3 网络路径扫描

步骤：

```text
1. 使用网络共享父级路径。
2. 点击扫描预览。
```

期望：

```text
1. 窗口不假死。
2. 覆盖层显示正在枚举或当前处理文件夹。
3. 如果某个文件夹很慢，用户能看到当前处理信息。
```

### 8.4 CSV 错误

步骤：

```text
1. 选择错误表头 CSV。
2. 点击扫描预览。
```

期望：

```text
1. 弹出原有错误信息。
2. Busy 状态最终清理。
3. 窗口可继续操作。
```

### 8.5 空目录

步骤：

```text
1. 选择一个没有子文件夹的父级目录。
2. 点击扫描预览。
```

期望：

```text
1. 不进行第二次网络目录枚举。
2. 提示未扫描到子文件夹。
3. Busy 状态清理。
```

## 9. 自动化验证

执行：

```powershell
cd C:\Users\shee_\OneDrive\Desktop\VSLoader
dotnet test .\VSLoader.sln -p:UseSharedCompilation=false
dotnet build .\VSLoader.sln -p:UseSharedCompilation=false
```

如果 VSLoader 正在运行导致文件占用：

```powershell
Get-Process -Name VSLoader -ErrorAction SilentlyContinue | Stop-Process -Force
```

## 10. 验收标准

最终必须满足：

```text
1. 点击扫描预览后显示覆盖层。
2. 扫描期间批量新增窗口内容不可操作。
3. 扫描进度至少能显示阶段、已处理数量、总数量、当前文件夹。
4. 扫描不再阻塞 UI 线程。
5. 扫描完成后预览结果与原逻辑一致。
6. 不重复枚举目录来判断空目录。
7. CSV 错误、路径错误、扫描异常都有提示。
8. dotnet test 通过。
9. dotnet build 0 错误。
```
