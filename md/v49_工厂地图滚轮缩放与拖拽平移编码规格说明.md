# v49 工厂地图滚轮缩放与拖拽平移编码规格说明

## 1. 需求背景

v47 已经实现工厂地图附属窗口：

```text
地图按钮
右侧地图窗口
JSON 节点坐标
Canvas 绘制节点和连线
点击地图机器节点联动主列表快捷项
```

v48 已经实现地图默认自动适应窗口：

```text
地图打开后默认能够一眼看到完整流程图。
不再依赖滚动条查看全局。
```

当前新增需求：

```text
当鼠标位于地图窗口内部时，用户可以用鼠标滚轮缩放地图。
用户可以按住鼠标左键拖动地图。
当鼠标移出地图窗口后，不再处理地图缩放和平移交互。
```

该需求的目标是让地图既能默认全局展示，又能在用户需要查看局部细节时进行放大、缩小和平移。

## 2. 需求目标

在工厂地图窗口中增加交互能力：

1. 鼠标滚轮向上：放大地图。
2. 鼠标滚轮向下：缩小地图。
3. 鼠标左键点击地图空白区域并长按移动：拖拽平移地图。
4. 鼠标点击机器按钮：保持原有功能，选中主窗口对应快捷项。
5. 鼠标离开地图交互区域后：停止响应滚轮缩放和非拖拽移动事件。
6. 地图底部状态栏不参与缩放，也不参与拖拽。

## 3. 监听范围设计

本次监听范围定义为：

```text
地图内容显示区域
```

不包含：

1. Windows 标题栏。
2. 地图窗口边框。
3. 底部状态栏。
4. 主窗口快捷项列表区域。

推荐命名：

```xml
MapViewport
```

`MapViewport` 是地图交互区域，负责：

1. 接收鼠标滚轮事件。
2. 接收鼠标左键按下、移动、松开事件。
3. 限制地图绘制内容不要溢出显示区域。

底部状态栏继续放在 `DockPanel.Dock="Bottom"`，不包裹进地图缩放容器。

## 4. 当前代码现状

地图窗口文件：

```text
VSLoader/Views/FactoryMapWindow.xaml
VSLoader/Views/FactoryMapWindow.xaml.cs
```

当前 XAML 结构类似：

```xml
<DockPanel>
    <Border DockPanel.Dock="Bottom">状态栏</Border>

    <Grid ClipToBounds="True">
        <Viewbox Stretch="Uniform"
                 StretchDirection="DownOnly">
            <Canvas x:Name="MapCanvas" />
        </Viewbox>
    </Grid>
</DockPanel>
```

当前问题：

```text
Viewbox 适合做默认自适应缩放。
但是后续如果要支持用户手动缩放和平移，只使用 Viewbox 不够灵活。
```

## 5. 推荐实现方案

将地图显示结构从单纯 `Viewbox + Canvas` 调整为：

```xml
<Grid x:Name="MapViewport"
      ClipToBounds="True">
    <Viewbox x:Name="InitialFitViewbox"
             Stretch="Uniform"
             StretchDirection="DownOnly">
        <Grid x:Name="MapTransformHost">
            <Grid.RenderTransform>
                <TransformGroup>
                    <ScaleTransform x:Name="UserScaleTransform" />
                    <TranslateTransform x:Name="UserTranslateTransform" />
                </TransformGroup>
            </Grid.RenderTransform>

            <Canvas x:Name="MapCanvas" />
        </Grid>
    </Viewbox>
</Grid>
```

说明：

1. `InitialFitViewbox` 继续负责 v48 的默认适应窗口。
2. `UserScaleTransform` 负责用户滚轮缩放。
3. `UserTranslateTransform` 负责用户拖拽平移。
4. `MapViewport` 负责裁剪溢出内容并限定鼠标监听范围。

如果实际测试发现 `Viewbox + RenderTransform` 坐标换算复杂，也允许改为：

```text
去掉 Viewbox。
在代码中手动计算初始适应比例。
所有缩放和平移统一用 ScaleTransform + TranslateTransform 管理。
```

但第一版推荐尽量保持 v48 已有结构，减少改动范围。

## 6. 鼠标滚轮缩放规则

触发条件：

```text
鼠标位于 MapViewport 内部。
```

行为：

1. 滚轮向上放大。
2. 滚轮向下缩小。
3. 缩放中心尽量以鼠标当前位置为中心。
4. 缩放时阻止事件继续向外冒泡，避免影响其他控件。

推荐参数：

```text
每次滚轮缩放倍率：1.1
最小用户缩放倍率：0.5
最大用户缩放倍率：4.0
```

注意：

```text
这里的最小/最大倍率是用户额外缩放倍率。
地图默认适应窗口的基础比例由 Viewbox 或初始适配逻辑控制。
```

## 7. 鼠标拖拽平移规则

触发条件：

```text
鼠标左键按下的位置是地图空白区域。
```

不应触发拖拽的情况：

1. 鼠标按下位置是机器按钮。
2. 鼠标按下位置是节点内可点击按钮。
3. 鼠标按下位置是底部状态栏。

行为：

1. 鼠标左键按下空白区域后，记录起点。
2. 鼠标移动时，根据移动差值更新 `TranslateTransform`。
3. 拖拽过程中调用 `Mouse.Capture(MapViewport)`。
4. 鼠标左键松开后释放捕获。
5. 拖拽过程中鼠标临时移出地图区域，仍允许本次拖拽继续，直到松开左键。
6. 非拖拽状态下鼠标移出地图区域，不响应地图操作。

拖拽光标建议：

```text
未拖拽：Arrow
拖拽中：Hand 或 SizeAll
```

## 8. 鼠标离开处理

`MapViewport.MouseLeave` 处理规则：

1. 如果当前不在拖拽状态，停止地图交互状态。
2. 如果当前正在拖拽，不立即中断拖拽。
3. 拖拽状态只由鼠标左键松开或窗口失去捕获结束。

窗口失去焦点或鼠标捕获丢失时：

```text
强制结束拖拽状态。
恢复默认光标。
释放 Mouse.Capture。
```

## 9. 和机器按钮点击的关系

已有机器按钮点击逻辑：

```text
点击地图中的机器按钮 -> selectShortcut(machine.Shortcut)
```

本次必须保持不变。

实现时应避免在按钮点击时启动地图拖拽。

推荐判断方式：

1. 在 `MapViewport.PreviewMouseLeftButtonDown` 中判断事件源。
2. 如果事件源来自 `Button` 或其子元素，则不进入拖拽。
3. 如果事件源是 `Canvas`、背景区域、连线等非按钮元素，则允许拖拽。

## 10. 重置策略

第一版不新增可见按钮。

但可以支持一个轻量快捷操作：

```text
双击地图空白区域：重置用户缩放和平移。
```

如果实现双击会干扰按钮点击，则第一版可以暂不实现。

最低要求：

```text
重新打开地图窗口后，地图回到默认适应窗口状态。
重新渲染地图时，地图回到默认适应窗口状态。
```

## 11. 非目标范围

本次不实现：

1. 不做地图节点拖拽编辑。
2. 不保存用户缩放比例。
3. 不保存用户平移位置。
4. 不做缩放滑块。
5. 不做小地图预览。
6. 不做键盘快捷缩放。
7. 不改变 JSON 坐标格式。
8. 不改变节点和连线绘制算法。
9. 不改变主窗口地图按钮的打开/关闭逻辑。

## 12. 预期改动文件

预计改动：

```text
VSLoader/Views/FactoryMapWindow.xaml
VSLoader/Views/FactoryMapWindow.xaml.cs
```

不预计改动：

```text
VSLoader/Models/Services/FactoryMapService.cs
VSLoader/Config/factory-map.example.json
VSLoader/MainWindow.xaml
VSLoader/MainWindow.xaml.cs
```

除非实现过程中发现地图窗口生命周期需要配合处理，否则不要扩大改动范围。

## 13. 关键实现细节

### 13.1 状态字段

建议在 `FactoryMapWindow.xaml.cs` 中增加：

```csharp
private const double ZoomFactor = 1.1;
private const double MinUserScale = 0.5;
private const double MaxUserScale = 4.0;

private bool isDraggingMap;
private Point lastDragPoint;
private double currentUserScale = 1.0;
```

### 13.2 缩放计算

滚轮时：

```text
newScale = currentUserScale * ZoomFactor 或 / ZoomFactor
newScale 限制在 MinUserScale 和 MaxUserScale 之间
更新 ScaleTransform.ScaleX / ScaleY
```

如果实现鼠标位置为中心缩放，需要同时修正平移量。

如果第一版复杂度较高，可以先以地图中心缩放，但需要保证交互稳定。

推荐优先级：

```text
稳定缩放 > 鼠标点精准缩放
```

### 13.3 平移计算

拖拽时：

```text
delta = currentPoint - lastDragPoint
TranslateTransform.X += delta.X
TranslateTransform.Y += delta.Y
lastDragPoint = currentPoint
```

拖拽坐标应基于 `MapViewport`。

## 14. 异常与边界处理

需要处理：

1. 地图尚未加载时，不应抛异常。
2. `MapCanvas.Width` 或 `MapCanvas.Height` 为 0 时，不应抛异常。
3. 鼠标捕获丢失时，应恢复拖拽状态。
4. 窗口关闭时，不需要保存缩放和平移状态。
5. 重新 `RenderMap` 时，应重置 `currentUserScale`、`TranslateTransform`。

## 15. 测试与验证

### 15.1 自动化验证

执行：

```powershell
cd C:\Users\shee_\OneDrive\Desktop\VSLoader
dotnet test .\VSLoader.sln -p:UseSharedCompilation=false
dotnet build .\VSLoader.sln -p:UseSharedCompilation=false
```

如果 `VSLoader.exe` 被占用：

```powershell
Get-Process -Name VSLoader -ErrorAction SilentlyContinue | Stop-Process -Force
```

### 15.2 手工验收

1. 启动 VSLoader。
2. 点击 `地图`。
3. 地图默认仍然完整显示。
4. 鼠标移动到地图内容区域。
5. 滚轮向上，地图放大。
6. 滚轮向下，地图缩小。
7. 左键按住地图空白区域拖动，地图可以平移。
8. 点击机器按钮，主窗口快捷项仍然被选中。
9. 鼠标移到底部状态栏，滚轮不应缩放地图。
10. 鼠标移出地图窗口后，滚轮不应继续控制地图。
11. 拖拽过程中鼠标短暂移出地图区域，松开左键后拖拽状态正常结束。
12. 重新打开地图窗口，地图恢复默认全局适应状态。

## 16. 验收标准

最终效果：

```text
地图打开时默认全局可见。
鼠标在地图区域内可以滚轮缩放。
鼠标按住空白区域可以拖拽平移。
机器按钮点击联动不受影响。
底部状态栏不缩放、不拖拽、不参与地图交互。
```

构建要求：

```text
dotnet test 通过。
dotnet build 0 个错误。
不引入新的无关警告。
```
