# v3 批量识别结果与主列表排序编码规格说明

## 1. 文档目的

本文档用于指导编程 agent 或开发人员在当前 VSLoader 项目中实现排序优化。

目标是让同一种机器类型的不同个体在批量识别预览窗口和主界面快捷项列表中尽量聚合展示，避免同类型机器分散在列表各处。

## 2. 用户确认的实现选择

| 项目 | 选择 |
| --- | --- |
| 排序影响范围 | 批量新增识别预览窗口和主界面快捷项列表 |
| 同一规则下排序 | 优先按正则命名捕获组 `No` 的数字大小排序 |
| 不可新增项排序 | 命中规则的重复项跟随规则排序，未匹配/规则错误放最后 |

## 3. 当前问题

当前批量识别结果基本按文件夹扫描顺序显示。

例如：

```text
银烧结_001
银烧结NG抓取装置_005
烧结载具切换机_002
3D_008
银烧结_002
3D_012
3D_009
```

用户期望同一种机器聚在一起，例如：

```text
银烧结_001
银烧结_002
银烧结NG抓取装置_005
烧结载具切换机_002
3D_008
3D_009
3D_012
```

## 4. 排序核心原则

排序优先级：

1. 按 CSV 规则行顺序排序。
2. 同一规则下，优先按正则命名捕获组 `No` 的数字大小排序。
3. 如果没有可用的 `No`，按生成名称排序。
4. 未匹配任何规则的已跳过项放最后。
5. 规则错误项放最后。

说明：

- CSV 中规则越靠前，对应机器类型越靠前。
- 用户可以通过调整 CSV 行顺序控制机器类型展示顺序。
- 同一类机器下，`001`、`002`、`009`、`012` 按数字升序排列。

## 5. 示例

CSV：

```csv
MatchType,Pattern,DisplayName,NameTemplate
Regex,^(?<Code>\d+)_(?<Type>TSSP)(?<No>\d+)$,银烧结,{DisplayName}_{No}
Regex,^(?<Code>\d+)_(?<Type>TRSM)(?<No>\d+)$,银烧结NG抓取装置,{DisplayName}_{No}
Regex,^(?<Code>\d+)_(?<Type>TAOI)(?<No>\d+)$,3D,{DisplayName}_{No}
Regex,^(?<Code>\d+)_(?<Type>TSSM)(?<No>\d+)$,热贴机,{DisplayName}_{No}
```

扫描到：

```text
8812_TSSM001
8864_TSSM002
5921_TAOI008
6048_TAOI012
6098_TAOI009
3134_TSSP001
5924_TSSP002
3365_TRSM005
9294_TRSM006
```

预期排序：

```text
银烧结_001
银烧结_002
银烧结NG抓取装置_005
银烧结NG抓取装置_006
3D_008
3D_009
3D_012
热贴机_001
热贴机_002
```

## 6. 数据模型修改

需要修改：

```text
Models\BatchImportPreviewItem.cs
```

新增排序辅助字段：

```csharp
public int SortRuleIndex { get; set; } = int.MaxValue;

public int? SortNo { get; set; }

public string SortName { get; set; } = string.Empty;
```

字段说明：

| 字段 | 说明 |
| --- | --- |
| `SortRuleIndex` | 命中的 CSV 规则行序号，从 0 开始；未匹配或规则错误使用 `int.MaxValue` |
| `SortNo` | 正则命名捕获组 `No` 解析出的数字；没有则为 `null` |
| `SortName` | 备用排序名称，通常为生成名称或文件夹名 |

这些字段只用于排序，不需要显示在 UI 中。

## 7. BatchImportRule 修改

需要修改：

```text
Models\BatchImportRule.cs
```

新增：

```csharp
public int SortIndex { get; set; }
```

规则：

- `LoadRules` 读取 CSV 时，为每条有效规则设置 `SortIndex`。
- 第一条有效规则为 `0`。
- 第二条有效规则为 `1`。
- 以此类推。

注意：

- 如果 CSV 中有无效规则，该规则不参与排序编号。
- 也就是说，排序编号按有效规则顺序计算。

## 8. BatchImportService 修改规格

主要修改：

```text
Services\BatchImportService.cs
```

### 8.1 LoadRules

读取有效规则时设置：

```csharp
rule.SortIndex = validRules.Count;
```

### 8.2 BuildPreview

创建每个 `BatchImportPreviewItem` 时填充排序字段。

#### 可新增项

```csharp
SortRuleIndex = matchedRule.SortIndex;
SortNo = TryGetRegexNo(matchResult.RegexMatch);
SortName = generatedName;
```

#### 名称重复项

名称重复项仍然命中了规则，所以：

```csharp
SortRuleIndex = matchedRule.SortIndex;
SortNo = TryGetRegexNo(matchResult.RegexMatch);
SortName = generatedName;
```

#### 已跳过项

未匹配任何规则：

```csharp
SortRuleIndex = int.MaxValue - 1;
SortNo = null;
SortName = folderName;
```

#### 规则错误项

CSV 规则错误或模板错误：

```csharp
SortRuleIndex = int.MaxValue;
SortNo = null;
SortName = generatedName 或 folderName
```

### 8.3 排序输出

`BuildPreview` 返回前必须排序：

```csharp
return items
    .OrderBy(item => item.SortRuleIndex)
    .ThenBy(item => item.SortNo ?? int.MaxValue)
    .ThenBy(item => item.SortName, StringComparer.CurrentCultureIgnoreCase)
    .ToList();
```

### 8.4 TryGetRegexNo

新增辅助方法：

```csharp
private static int? TryGetRegexNo(Match? regexMatch)
```

规则：

- 如果 `regexMatch` 为 `null`，返回 `null`。
- 如果存在命名捕获组 `No` 且可解析为整数，返回该整数。
- 否则返回 `null`。

示例：

```text
No = 001 -> 1
No = 009 -> 9
No = 012 -> 12
```

## 9. 主界面排序规格

用户确认排序也影响主界面快捷项列表。

当前主界面使用：

```text
ShortcutsView
```

需要实现主界面列表排序。

### 9.1 推荐实现方式

在 `MainViewModel` 中对 `ShortcutsView` 添加排序。

由于 `ShortcutItem` 当前没有 `SortRuleIndex` 或 `SortNo` 字段，主界面不能直接知道原 CSV 规则行顺序。

因此主界面排序使用生成名称进行自然排序：

1. 按名称中的机器类型部分排序。
2. 同类型下按结尾编号数字排序。
3. 无法解析编号时按完整名称排序。

示例：

```text
3D_008
3D_009
3D_012
热贴机_001
热贴机_002
银烧结_001
银烧结_002
```

### 9.2 ShortcutItem 排序辅助

建议新增服务或静态辅助类：

```text
Services\ShortcutSortService.cs
```

职责：

- 从快捷项名称中解析排序 key。
- 支持形如 `名称_001`、`名称_009`、`名称_012` 的编号。
- 未匹配时使用完整名称排序。

建议方法：

```csharp
public static int Compare(ShortcutItem? x, ShortcutItem? y)
```

解析规则：

- 使用正则：

```regex
^(?<Name>.+)_(?<No>\d+)$
```

- `Name` 作为机器类型名称。
- `No` 解析为整数。

排序规则：

```text
Name 升序
No 升序
完整名称升序
```

### 9.3 MainViewModel 接入

推荐方式：

- 使用 `ListCollectionView.CustomSort`。
- 或在保存/刷新后重建排序后的集合。

优先推荐 `CustomSort`，避免破坏现有过滤逻辑。

## 10. 批量导入后主列表排序

批量导入确认后：

1. 将导入项加入 `Shortcuts`。
2. 保存配置。
3. 刷新 `ShortcutsView`。
4. 主界面列表应按新排序展示。

手动新增或编辑快捷项后，也应适用同一主界面排序规则。

## 11. 错误处理

| 场景 | 处理方式 |
| --- | --- |
| Regex 没有 `No` 命名捕获组 | 同一规则下按生成名称排序 |
| `No` 无法解析成数字 | 同一规则下按生成名称排序 |
| 未匹配规则 | 放在预览列表最后的已跳过区域 |
| CSV 规则错误 | 放在预览列表最后 |
| 主界面名称不符合 `名称_编号` | 按完整名称排序 |

## 12. 验收标准

- 批量新增识别预览窗口中，同类型机器聚合展示。
- 预览排序优先遵守 CSV 规则行顺序。
- 同一规则下，`001`、`002`、`009`、`012` 按数字升序。
- 名称重复项如果命中了规则，应跟随该规则分组展示。
- 未匹配任何规则的已跳过项放在后面。
- 规则错误项放在最后。
- 确认导入后，主界面快捷项列表也聚合展示同类型机器。
- 主界面中 `3D_008`、`3D_009`、`3D_012` 应连续展示，并按编号升序。
- 手动新增、编辑、批量导入后，主界面排序仍然生效。
- `dotnet build .\VSLoader.sln` 必须通过。

