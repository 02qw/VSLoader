# v93 设置页输入控件滚轮转交外层滚动编码规格说明

## 1. 背景

当前设置窗口内容较长，外层使用 `ScrollViewer` 承载整页滚动。

用户发现：

当鼠标位于“AdminUI 密码”的登录密码输入框上方时，滚动鼠标滚轮，设置页面不会继续上下滚动。

经源码检查：

文件：

```text
VSLoader\Views\SettingsWindow.xaml
VSLoader\Views\SettingsWindow.xaml.cs
```

当前结构：

```xml
<ScrollViewer VerticalScrollBarVisibility="Auto">
    ...
    <PasswordBox x:Name="AdminUiPasswordBox" />
    ...
</ScrollViewer>
```

`SettingsWindow.xaml.cs` 当前只处理：

```csharp
PreviewKeyDown
PreviewMouseDown
```

没有处理：

```csharp
PreviewMouseWheel
MouseWheel
```

因此该问题不是快捷键录制逻辑主动拦截，而是 WPF 输入控件自身可能处理滚轮事件，导致外层 `ScrollViewer` 收不到滚轮，页面无法滚动。

## 2. 需求目标

### 2.1 用户目标

用户在设置页面中，无论鼠标停留在：

1. 普通输入框 `TextBox`
2. 密码输入框 `PasswordBox`
3. 只读快捷键输入框 `TextBox`

上方时，滚动鼠标滚轮都应该优先滚动整个设置页面。

用户不需要刻意把鼠标移到空白区域或滚动条上。

### 2.2 技术目标

1. 给设置页输入控件统一增加滚轮转交处理。
2. 外层 `ScrollViewer` 负责最终滚动。
3. 不影响输入、选择文本、密码输入。
4. 不影响快捷键录制逻辑。
5. 不影响其他窗口。
6. 保持实现轻量，不引入新控件库。

## 3. 修改范围

### 3.1 只修改设置窗口

本次只修改：

```text
VSLoader\Views\SettingsWindow.xaml
VSLoader\Views\SettingsWindow.xaml.cs
```

不修改：

1. 主窗口列表滚动。
2. 地图窗口滚轮缩放。
3. 工作区选择窗口滚动。
4. 自定义弹窗滚动。

原因：

地图窗口滚轮有自己的缩放语义，不能套用设置页滚动逻辑。

## 4. 当前问题代码

### 4.1 外层 ScrollViewer 未命名

当前：

```xml
<ScrollViewer VerticalScrollBarVisibility="Auto">
```

问题：

code-behind 中无法直接定位外层滚动容器。

### 4.2 子输入控件没有 PreviewMouseWheel 兜底

当前：

```xml
<PasswordBox x:Name="AdminUiPasswordBox"
             Grid.Column="1"
             Height="30"
             VerticalContentAlignment="Center"
             Padding="8,4"
             PasswordChanged="AdminUiPasswordBox_PasswordChanged" />
```

问题：

鼠标在 `PasswordBox` 上滚轮时，外层页面不滚动。

## 5. 实现方案

### 5.1 给外层 ScrollViewer 命名

修改：

```xml
<ScrollViewer x:Name="SettingsScrollViewer"
              VerticalScrollBarVisibility="Auto">
```

### 5.2 输入控件统一绑定滚轮处理

对设置页内的单行输入控件统一增加：

```xml
PreviewMouseWheel="SettingsInput_PreviewMouseWheel"
```

适用控件：

1. 所有 `TextBox`
2. `PasswordBox`

包括：

1. VSCode 路径输入框。
2. AdminUI 各配置输入框。
3. AdminUI 密码框。
4. WebUI 各配置输入框。
5. 软件更新 manifest 路径输入框。
6. 更新检测 rules/map 路径输入框。
7. 程序快捷键只读输入框。

不需要绑定：

1. `CheckBox`
2. `Button`
3. `TextBlock`
4. `GroupBox`

### 5.3 code-behind 滚轮转交方法

新增方法：

```csharp
private void SettingsInput_PreviewMouseWheel(object sender, MouseWheelEventArgs e)
{
    if (e.Delta == 0)
    {
        return;
    }

    SettingsScrollViewer.ScrollToVerticalOffset(SettingsScrollViewer.VerticalOffset - e.Delta);
    e.Handled = true;
}
```

说明：

1. `e.Delta > 0` 表示向上滚动。
2. `ScrollToVerticalOffset(VerticalOffset - e.Delta)` 符合 WPF 常见滚动方向。
3. 标记 `e.Handled = true`，避免子控件继续吞或重复处理。

### 5.4 边界保护

可以增加边界钳制，避免 offset 超出范围：

```csharp
var targetOffset = SettingsScrollViewer.VerticalOffset - e.Delta;
targetOffset = Math.Max(0, Math.Min(targetOffset, SettingsScrollViewer.ScrollableHeight));
SettingsScrollViewer.ScrollToVerticalOffset(targetOffset);
```

推荐采用边界钳制，更严谨。

## 6. 交互边界

### 6.1 不影响输入

该逻辑只响应鼠标滚轮，不处理键盘输入，不影响：

1. 输入文字。
2. 输入密码。
3. 删除文字。
4. 复制粘贴。
5. 光标定位。

### 6.2 不影响快捷键录制

设置窗口已有：

```csharp
SettingsWindow_PreviewKeyDown
SettingsWindow_PreviewMouseDown
```

快捷键录制支持：

1. 键盘组合。
2. 鼠标侧键。

本次新增的是：

```csharp
SettingsInput_PreviewMouseWheel
```

不应在该方法中调用：

```csharp
SetRecordedHotkey(...)
```

也不应把滚轮当作快捷键输入。

### 6.3 不影响地图窗口滚轮缩放

地图窗口有自己的：

```csharp
MapViewport_MouseWheel
```

该逻辑用于地图缩放，不能复用设置页滚动转交逻辑。

本次只处理 `SettingsWindow`。

## 7. 测试与验证

### 7.1 编译验证

必须通过：

```powershell
dotnet build .\VSLoader.sln -c Debug
```

### 7.2 自动测试

必须通过：

```powershell
dotnet test .\VSLoader.sln -c Debug
```

### 7.3 手工验证

打开设置窗口后验证：

1. 鼠标停在 `AdminUI 密码 -> 登录密码` 输入框上。
2. 滚动鼠标滚轮。
3. 设置页面应上下滚动。

继续验证：

1. 鼠标停在 `AdminUI Host` 输入框上。
2. 滚动鼠标滚轮。
3. 设置页面应上下滚动。

继续验证：

1. 鼠标停在 `WebUI BaseUrl` 输入框上。
2. 滚动鼠标滚轮。
3. 设置页面应上下滚动。

继续验证：

1. 鼠标停在 `程序快捷键` 只读输入框上。
2. 滚动鼠标滚轮。
3. 设置页面应上下滚动。

### 7.4 回归验证

确认以下能力不受影响：

1. 密码仍能输入和保存。
2. VSCode 路径仍能编辑。
3. AdminUI/WebUI 配置仍能编辑。
4. 快捷键录制仍能使用键盘组合。
5. 快捷键录制仍能使用鼠标侧键。
6. 设置窗口保存/取消仍正常。

## 8. 验收标准

满足以下条件视为完成：

1. 设置页外层 `ScrollViewer` 命名为 `SettingsScrollViewer`。
2. 设置页所有 `TextBox` 和 `PasswordBox` 统一绑定滚轮转交处理。
3. 鼠标停在密码框上滚轮时页面可以滚动。
4. 鼠标停在普通输入框上滚轮时页面可以滚动。
5. 鼠标停在只读快捷键输入框上滚轮时页面可以滚动。
6. 输入、密码保存、快捷键录制不受影响。
7. 不修改地图窗口滚轮缩放逻辑。
8. `dotnet build .\VSLoader.sln -c Debug` 通过。
9. `dotnet test .\VSLoader.sln -c Debug` 通过。

