# v101 托盘退出温和清理与三秒兜底编码规格说明

## 1. 背景

当前 VSLoader 支持最小化到托盘：

- 点击窗口右上角关闭按钮时，不真正退出，而是隐藏到托盘。
- 用户需要在托盘区右键菜单点击“退出”，程序才应真正关闭。

实际使用中，用户反馈：

```text
程序已经在托盘区域右键退出了，但仍然有残留进程或者 dll 文件被占用，导致删除程序本体时提示文件被占用。
```

结合代码排查后，问题本质大概率不是 dll 自己残留，而是 `VSLoader.exe` 进程仍然存活。只要主进程没有真正退出，`VSLoader.exe`、`VSLoader.dll` 以及相关程序集就会被 Windows 锁定，无法删除或覆盖。

## 2. 当前代码现状

### 2.1 普通关闭按钮

`MainWindow_Closing` 中，如果不是显式退出，会拦截关闭并隐藏窗口：

```csharp
if (_isExitRequested)
{
    return;
}

e.Cancel = true;
SaveMainWindowBoundsToSession();
HideFactoryMapWindow();
Hide();
```

这是正确的托盘最小化行为。

### 2.2 托盘右键退出

当前托盘右键“退出”调用：

```csharp
private void ExitApplication()
{
    Dispatcher.Invoke(() =>
    {
        _isExitRequested = true;
        Close();
    });
}
```

然后依赖 `Closed` 事件触发：

```csharp
private void MainWindow_Closed(object? sender, EventArgs e)
{
    CleanupForClose();
}
```

`CleanupForClose()` 中会执行：

- 保存主窗口和地图窗口状态
- 关闭地图窗口
- 立即保存窗口布局
- 停止更新检测循环
- 释放全局快捷键
- 释放托盘图标
- 解绑 ViewModel 事件

### 2.3 现有风险点

当前退出链路存在几个隐患：

1. 托盘退出只调用 `Close()`，依赖 WPF 关闭事件完成清理，没有显式调用应用级 `Application.Current.Shutdown()`。
2. `StopUpdateCheckLoop()` 只 `Cancel + Dispose`，没有等待后台检测循环真正结束。
3. 布局保存存在 500ms debounce 后台任务，虽然退出时会取消并立即保存，但退出流程没有统一的“正在退出”防重复保护。
4. 软件更新路径中 `RequestApplicationExit` 直接调用 `Application.Current.Shutdown()`，未统一使用主窗口真实退出链路。

因此需要将“真实退出”改为统一、可控、温和但有兜底的退出流程。

## 3. 目标

实现一个统一的真实退出流程：

```text
温和清理 + 最多等待 3 秒 + 应用级 Shutdown
```

目标：

- 托盘右键“退出”后，尽可能确保 VSLoader 主进程真正结束。
- 不使用 `Process.Kill()` 强杀自身进程。
- 优先保存布局和地图状态，避免损坏配置文件。
- 释放全局快捷键、鼠标 hook、托盘图标等系统资源。
- 停止后台更新检测任务，并最多等待 3 秒。
- 防止用户重复点击退出导致清理流程重入。

## 4. 设计原则

### 4.1 不强杀进程

本次不使用：

```csharp
Process.GetCurrentProcess().Kill();
Environment.FailFast(...);
```

原因：

- 强杀可能中断文件写入。
- 强杀可能造成窗口布局、updateTime.json、config.json 等配置文件损坏。
- 强杀会跳过 WPF 和 WinForms 资源释放流程。

### 4.2 退出要有上限

后台任务不能无限等待。

本次采用用户确认的兜底时间：

```text
3 秒
```

如果后台任务 3 秒内结束，则正常退出。

如果超过 3 秒，记录或忽略超时，继续应用级 Shutdown。

### 4.3 所有真实退出入口统一

以下入口应尽量走同一套真实退出逻辑：

- 托盘右键“退出”
- 软件更新前退出主程序
- 未来其他需要真实退出的入口

普通窗口右上角关闭仍然保持最小化到托盘，不进入真实退出。

## 5. 影响范围

主要修改：

- `VSLoader/MainWindow.xaml.cs`
- `VSLoader/ViewModels/MainViewModel.cs`

可能新增或调整测试：

- 主窗口退出状态判断相关测试
- 更新检测循环停止等待相关测试

不修改：

- 托盘菜单 UI 样式
- 最小化到托盘行为
- 地图窗口业务逻辑
- 自动更新下载/解压逻辑
- 配置文件格式

## 6. 推荐实现方案

### 6.1 MainWindow 增加真实退出状态

新增字段：

```csharp
private bool _isShutdownInProgress;
```

用途：

- 防止托盘菜单重复点击“退出”。
- 防止退出流程重入。
- 防止清理过程被普通 Closing 逻辑错误拦截。

建议保留现有 `_isExitRequested`，并在真实退出开始时同时设置：

```csharp
_isExitRequested = true;
_isShutdownInProgress = true;
```

### 6.2 新增真实退出方法

建议新增：

```csharp
private async void ExitApplication()
```

内部通过 Dispatcher 进入 UI 线程后调用：

```csharp
await ExitApplicationAsync();
```

核心方法：

```csharp
private async Task ExitApplicationAsync()
```

流程建议：

1. 如果 `_isShutdownInProgress` 为 `true`，直接返回。
2. 设置 `_isShutdownInProgress = true`。
3. 设置 `_isExitRequested = true`。
4. 调用 `CleanupForClose()` 执行同步清理。
5. 等待 ViewModel 后台任务停止，最多 3 秒。
6. 调用 `System.Windows.Application.Current.Shutdown()`。

注意：

- 不再单纯依赖 `Close()`。
- 可选择在 Shutdown 前调用 `Close()`，但需要确保不会因 Closing 逻辑再次拦截。
- `CleanupForClose()` 本身已有幂等保护 `_hasCleanedUpForClose`，可以安全重复调用。

### 6.3 CleanupForClose 保持幂等

当前已有：

```csharp
if (_hasCleanedUpForClose)
{
    return;
}

_hasCleanedUpForClose = true;
```

应继续保留。

建议将清理顺序调整为：

1. 保存主窗口状态。
2. 保存/关闭地图窗口。
3. 取消布局保存 debounce，并立即保存布局。
4. 停止 ViewModel 后台任务。
5. 释放快捷键和鼠标 hook。
6. 释放托盘图标。
7. 解绑事件。

现有顺序基本可用，不需要大规模重构。

### 6.4 MainViewModel 支持等待更新检测循环停止

当前：

```csharp
public void StopUpdateCheckLoop()
{
    if (_updateCheckCancellationTokenSource is null)
    {
        return;
    }

    _updateCheckCancellationTokenSource.Cancel();
    _updateCheckCancellationTokenSource.Dispose();
    _updateCheckCancellationTokenSource = null;
}
```

问题：

- 没有保存 `_ = RunUpdateCheckLoopAsync(...)` 返回的 Task。
- 无法等待后台循环确认退出。

建议新增字段：

```csharp
private Task? _updateCheckLoopTask;
```

启动时：

```csharp
_updateCheckLoopTask = RunUpdateCheckLoopAsync(_updateCheckCancellationTokenSource.Token);
```

新增异步停止方法：

```csharp
public async Task StopUpdateCheckLoopAsync(TimeSpan timeout)
```

行为：

1. 如果没有 CTS 或 Task，直接返回。
2. Cancel。
3. 等待 `_updateCheckLoopTask` 完成。
4. 最多等待传入的 timeout。
5. 无论是否超时，都 Dispose CTS 并置空字段。

保留原同步方法：

```csharp
public void StopUpdateCheckLoop()
```

同步方法可以继续用于旧调用，但内部应尽量复用取消逻辑。

### 6.5 三秒等待实现建议

兼容 .NET 8，可使用：

```csharp
await _updateCheckLoopTask.WaitAsync(timeout);
```

注意捕获：

- `OperationCanceledException`
- `TimeoutException`
- 其他异常

超时不弹窗，不阻塞用户退出。

如果需要后续诊断，可写入已有错误日志目录，但本次不强制。

### 6.6 软件更新前退出也走统一链路

当前 `MainViewModel.RequestApplicationExit` 默认是：

```csharp
System.Windows.Application.Current.Shutdown()
```

在 `MainWindow` 创建 ViewModel 后，应覆盖为统一退出入口，例如：

```csharp
viewModel.RequestApplicationExit = RequestRealApplicationExit;
```

`RequestRealApplicationExit` 内部调用主窗口的真实退出流程。

注意：

- 软件更新流程启动 updater 后才请求退出。
- 如果 updater 启动失败，不应退出。
- 该逻辑保持现有语义，只替换退出执行方式。

## 7. 行为要求

### 7.1 点击窗口右上角关闭

保持原行为：

- 不退出。
- 保存位置状态。
- 隐藏地图窗口。
- 主窗口隐藏到托盘。
- 快捷键继续监听。

### 7.2 托盘右键退出

新行为：

- 设置真实退出状态。
- 不再只是隐藏窗口。
- 保存窗口和地图状态。
- 停止后台检测。
- 释放快捷键和托盘图标。
- 最多等待 3 秒。
- 调用 `Application.Current.Shutdown()`。

### 7.3 重复点击退出

如果用户连续点击托盘“退出”：

- 第二次及之后点击直接忽略。
- 不重复保存。
- 不重复 Dispose。
- 不抛异常。

### 7.4 后台检测卡住

如果更新检测后台任务没有在 3 秒内结束：

- 不继续等待。
- 继续执行 Shutdown。
- 不强杀进程。
- 不弹出阻塞用户的提示。

### 7.5 软件更新前退出

点击“更新软件”并确认后：

- updater 成功启动。
- 主程序通过统一真实退出流程退出。
- 不再绕过主窗口清理链路。

## 8. 风险与边界

### 8.1 不能保证外部程序不占用文件

如果用户通过 VSLoader 打开了：

- VSCode
- 浏览器 WebUI
- Java WebStart/JNLP 客户端

这些外部程序可能占用它们自己的文件或目标文件夹。

本次只保证 VSLoader 自身尽量释放自己的进程和程序集。

### 8.2 不能强杀 updater

如果 `VSLoader.Updater.exe` 正在运行，它是独立更新器进程。

本次不处理 updater 的生命周期。

### 8.3 用户仍需区分隐藏到托盘和真正退出

右上角关闭仍然是隐藏。

只有托盘右键“退出”才是真正退出。

## 9. 测试建议

建议新增或调整测试：

1. `MainViewModel.StopUpdateCheckLoopAsync`：
   - 未启动循环时调用不报错。
   - 启动后取消能在短时间内返回。

2. 真实退出防重入逻辑：
   - 第一次进入返回允许执行。
   - 第二次进入被忽略。

3. 构建与回归：

```powershell
dotnet test .\VSLoader.sln -c Debug --no-restore
dotnet build .\VSLoader.sln -c Debug --no-restore
```

手动验证：

1. 启动程序。
2. 点击右上角关闭，确认程序仍在托盘。
3. 托盘右键点击“退出”。
4. 观察任务管理器中 `VSLoader.exe` 消失。
5. 尝试删除或覆盖 Debug 目录中的 `VSLoader.exe` / `VSLoader.dll`，不应再提示被 VSLoader 自身占用。

## 10. 验收标准

满足以下条件即完成：

- 普通关闭仍然最小化到托盘。
- 托盘右键“退出”后，VSLoader 主进程能可靠结束。
- 退出前保存窗口位置、地图状态、列宽配置。
- 全局快捷键和鼠标 hook 被释放。
- 托盘图标被释放。
- 更新检测后台循环被取消，并最多等待 3 秒。
- 不使用强杀进程。
- 软件更新前退出也走统一真实退出链路。
- `dotnet test .\VSLoader.sln -c Debug --no-restore` 通过。
- `dotnet build .\VSLoader.sln -c Debug --no-restore` 通过。

