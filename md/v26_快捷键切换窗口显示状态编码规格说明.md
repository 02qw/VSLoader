# v26 快捷键切换窗口显示状态编码规格说明

## 1. 需求背景

v24 已实现键盘全局快捷键唤起 VSLoader。

v25 已扩展支持鼠标侧键组合，例如：

```text
Ctrl + Mouse4
Alt + Mouse5
```

当前快捷键行为是：

```text
按快捷键 -> 永远唤起/激活 VSLoader 窗口
```

用户现在希望快捷键具备“切换”能力：

```text
如果窗口已经显示且正在使用，则按快捷键最小化。
如果窗口最小化，则按快捷键恢复显示。
如果窗口被遮挡或在后台，则按快捷键切到前台。
```

本次 v26 需求需要调整快捷键触发后的窗口行为。

## 2. 需求目标

本次开发目标：

1. 快捷键不再只是单向唤起窗口。
2. 快捷键根据当前窗口状态切换显示状态。
3. 键盘快捷键和鼠标侧键快捷键都使用同一套切换逻辑。
4. 最小化时按快捷键恢复窗口并置前。
5. 当前窗口处于前台激活状态时按快捷键最小化。
6. 当前窗口可见但在后台时按快捷键置前。
7. 不改变快捷键注册逻辑。
8. 不改变快捷键配置 UI。

## 3. 修改范围

主要修改文件：

- `VSLoader\MainWindow.xaml.cs`

原则上不需要修改：

- `GlobalHotkeyService`
- `SettingsViewModel`
- `SettingsWindow`
- `HotkeyConfig`
- 配置文件结构

本次只修改快捷键触发后的窗口响应逻辑。

## 4. 当前逻辑

当前主窗口中可能有类似方法：

```csharp
private void ActivateFromHotkey()
{
    if (WindowState == WindowState.Minimized)
    {
        WindowState = WindowState.Normal;
    }

    Show();
    Activate();
    Topmost = true;
    Topmost = false;
    Focus();
}
```

该逻辑无论窗口当前是什么状态，都会尝试显示并激活窗口。

本次需要改成状态切换逻辑。

## 5. 推荐状态判断规则

不要简单使用：

```text
如果可见就最小化，否则显示
```

因为窗口可能处于：

```text
可见，但被其他窗口遮挡
```

这种情况下用户按快捷键通常是想把 VSLoader 叫到前台，而不是最小化。

推荐规则：

```text
1. 如果 WindowState == Minimized：
   恢复窗口 + 显示 + 激活

2. 否则，如果 IsActive == true：
   最小化窗口

3. 否则：
   显示 + 激活到前台
```

解释：

- 最小化时，快捷键表示“打开”。
- 当前正在使用 VSLoader 时，快捷键表示“收起”。
- VSLoader 在后台但没最小化时，快捷键表示“切回前台”。

## 6. 推荐实现

将原方法：

```csharp
ActivateFromHotkey()
```

改名或改造为：

```csharp
ToggleWindowFromHotkey()
```

推荐实现：

```csharp
private void ToggleWindowFromHotkey()
{
    if (WindowState == WindowState.Minimized)
    {
        RestoreAndActivate();
        return;
    }

    if (IsActive)
    {
        WindowState = WindowState.Minimized;
        return;
    }

    RestoreAndActivate();
}
```

新增辅助方法：

```csharp
private void RestoreAndActivate()
{
    if (WindowState == WindowState.Minimized)
    {
        WindowState = WindowState.Normal;
    }

    Show();
    Activate();
    Topmost = true;
    Topmost = false;
    Focus();
}
```

注册热键回调时：

```csharp
_hotkeyService.Initialize(this, ToggleWindowFromHotkey);
```

替换原来的：

```csharp
_hotkeyService.Initialize(this, ActivateFromHotkey);
```

## 7. 键盘和鼠标快捷键一致性

本次不区分快捷键来源。

无论触发来源是：

```text
Ctrl + Alt + V
Ctrl + Mouse4
Alt + Mouse5
```

都执行同一个窗口切换方法：

```text
ToggleWindowFromHotkey
```

## 8. 不允许改变的内容

本次不允许改变：

- 快捷键录制逻辑。
- 快捷键配置 UI。
- 快捷键合法性校验。
- 键盘热键注册逻辑。
- 鼠标侧键钩子逻辑。
- 配置文件结构。
- 主窗口布局。
- 业务功能。

## 9. 验收标准

### 9.1 最小化状态

操作：

1. 将 VSLoader 最小化。
2. 按已配置快捷键。

期望：

- VSLoader 恢复显示。
- VSLoader 置前并激活。

### 9.2 前台激活状态

操作：

1. VSLoader 当前位于前台且是激活窗口。
2. 按已配置快捷键。

期望：

- VSLoader 最小化。

### 9.3 后台被遮挡状态

操作：

1. VSLoader 正常显示但被其他窗口遮挡。
2. 按已配置快捷键。

期望：

- VSLoader 回到前台。
- 不最小化。

### 9.4 键盘快捷键

使用：

```text
Ctrl + Alt + V
```

期望：

- 行为符合上述三种状态切换规则。

### 9.5 鼠标侧键快捷键

使用：

```text
Ctrl + Mouse4
```

期望：

- 行为符合上述三种状态切换规则。
- 仍然拦截匹配的 Mouse4/Mouse5 事件。

### 9.6 编译验收

执行：

```powershell
dotnet build .\VSLoader.sln
```

期望：

```text
0 warnings
0 errors
```

如果提示 `VSLoader.exe` 被占用，说明程序正在运行，应先关闭 VSLoader 后重新构建。

## 10. 建议实施步骤

1. 打开 `VSLoader\MainWindow.xaml.cs`。
2. 找到当前快捷键回调方法，例如 `ActivateFromHotkey`。
3. 将其改为三态切换逻辑。
4. 新增 `RestoreAndActivate` 辅助方法。
5. 修改 `_hotkeyService.Initialize(...)` 的回调指向。
6. 编译项目。
7. 手动验证最小化、前台、后台三种状态。

## 11. 明确不做的事情

本次不做：

- 托盘隐藏。
- 退出后热键启动程序。
- 修改热键配置。
- 修改鼠标钩子。
- 修改键盘热键注册。
