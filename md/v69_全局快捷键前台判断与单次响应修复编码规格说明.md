# v69 全局快捷键前台判断与单次响应修复编码规格说明

## 1. 需求背景

当前 VSLoader 支持全局快捷键，例如：

```text
Alt + V
```

用户反馈：

```text
有时候按一次快捷键没有明显反应，需要按两次才会显示程序窗口。
```

经代码排查，当前热键切换逻辑主要依据：

```csharp
IsVisible
WindowState == WindowState.Minimized
```

当前逻辑近似为：

```text
1. 如果窗口不可见或最小化，则恢复并激活。
2. 否则，最小化窗口。
```

问题是：

```text
窗口可能 IsVisible = true 且 WindowState != Minimized，
但它实际上被其他程序窗口遮挡，或者不在前台。
```

此时用户按下快捷键，本意是把 VSLoader 拉到前台。  
但程序会误判为“窗口已经显示”，于是执行最小化。  
用户视觉上就会觉得“第一次没反应”，第二次才恢复显示。

## 2. 需求目标

完成后：

```text
1. 按下全局快捷键时，如果 VSLoader 当前隐藏或最小化，则恢复并激活。
2. 如果 VSLoader 当前可见但不在前台，则恢复并激活。
3. 只有当 VSLoader 主窗口或地图窗口已经处于前台时，再按快捷键才执行最小化/隐藏。
4. 地图窗口是 VSLoader 的附属窗口，如果地图窗口处于前台，也视为 VSLoader 已经处于前台。
5. 修复 Alt + V 有时候需要按两次才显示的问题。
6. 不修改快捷键注册逻辑，不修改快捷键配置页面。
```

## 3. 非目标范围

本次不实现：

```text
1. 不修改 GlobalHotkeyService 的 RegisterHotKey 注册方式。
2. 不修改快捷键校验规则。
3. 不修改设置页面快捷键 UI。
4. 不新增快捷键类型。
5. 不修改托盘菜单功能。
6. 不修改地图窗口布局和地图业务逻辑。
7. 不改变“前台时再次按快捷键最小化”的既有需求。
```

## 4. 当前问题代码位置

文件：

```text
C:\Users\shee_\OneDrive\Desktop\VSLoader\VSLoader\MainWindow.xaml.cs
```

当前方法：

```csharp
private void ToggleWindowFromHotkey()
{
    if (!IsVisible || WindowState == WindowState.Minimized)
    {
        RestoreAndActivate();
        return;
    }

    SaveMainWindowBoundsToSession();
    HideFactoryMapWindow();
    WindowState = WindowState.Minimized;
}
```

问题点：

```text
只判断窗口是否可见和是否最小化，没有判断窗口是否处于前台。
```

## 5. 新行为定义

### 5.1 状态一：程序隐藏或最小化

条件：

```text
!IsVisible
或
WindowState == WindowState.Minimized
```

行为：

```text
调用 RestoreAndActivate()
```

### 5.2 状态二：程序可见但不在前台

条件：

```text
IsVisible == true
WindowState != Minimized
主窗口 IsActive == false
地图窗口不存在或地图窗口 IsActive == false
```

行为：

```text
调用 RestoreAndActivate()
```

说明：

```text
这种状态就是原 bug 的核心场景。
窗口虽然存在，但被其他程序盖住。
用户按快捷键时应该把 VSLoader 拉到前台，而不是最小化。
```

### 5.3 状态三：主窗口当前在前台

条件：

```text
IsVisible == true
WindowState != Minimized
IsActive == true
```

行为：

```text
保存主窗口位置。
隐藏地图窗口。
最小化主窗口。
```

### 5.4 状态四：地图窗口当前在前台

条件：

```text
_factoryMapWindow is { IsVisible: true, IsActive: true }
```

行为：

```text
视为 VSLoader 已经在前台。
再次按快捷键应执行最小化/隐藏。
```

原因：

```text
地图窗口是 VSLoader 的附属窗口。
用户正在操作地图时，再按快捷键应符合“当前程序已显示，所以切换为隐藏/最小化”的直觉。
```

## 6. 推荐实现方案

### 6.1 增加前台判断方法

在 `MainWindow.xaml.cs` 中新增私有方法：

```csharp
private bool IsVsLoaderActive()
{
    return IsActive
        || _factoryMapWindow is { IsVisible: true, IsActive: true };
}
```

说明：

```text
1. 主窗口激活，返回 true。
2. 地图窗口激活，返回 true。
3. 两者都不激活，返回 false。
```

### 6.2 修改 ToggleWindowFromHotkey

修改为：

```csharp
private void ToggleWindowFromHotkey()
{
    if (!IsVisible || WindowState == WindowState.Minimized || !IsVsLoaderActive())
    {
        RestoreAndActivate();
        return;
    }

    SaveMainWindowBoundsToSession();
    HideFactoryMapWindow();
    WindowState = WindowState.Minimized;
}
```

行为解释：

```text
只有当窗口可见、未最小化，并且 VSLoader 或地图窗口已经在前台时，才执行最小化。
其他所有情况，都恢复并激活窗口。
```

## 7. RestoreAndActivate 要求

当前 `RestoreAndActivate()` 已有类似逻辑：

```csharp
Show();
RestoreMainWindowBoundsFromSession();

if (WindowState == WindowState.Minimized)
{
    WindowState = WindowState.Normal;
}

Activate();
Topmost = true;
Topmost = false;
Focus();
ShowFactoryMapIfNeeded();
```

本次尽量不修改该方法。

说明：

```text
RestoreAndActivate 已经承担恢复窗口、激活窗口、恢复地图窗口的职责。
本次主要修正进入该方法的判断条件。
```

如实现后仍存在偶发抢不到焦点的问题，再另行设计更强的 Win32 前台窗口激活方案。  
本次不引入 `GetForegroundWindow` 或 `SetForegroundWindow`，保持最小修改。

## 8. 测试方案

### 8.1 单元测试可行性

`ToggleWindowFromHotkey()` 当前依赖 WPF 窗口状态和 `IsActive`，直接单元测试成本较高。

本次建议：

```text
1. 对纯逻辑部分保持最小改动。
2. 不强行重构 MainWindow 以适配单元测试。
3. 使用人工验收覆盖窗口前台/后台行为。
4. 保留现有自动化测试，确保无编译和回归问题。
```

如果希望提高可测试性，可以后续将窗口切换判断抽成纯函数，例如：

```csharp
internal static bool ShouldRestoreFromHotkey(bool isVisible, bool isMinimized, bool isVsLoaderActive)
```

但本次为了最小修改，不强制要求。

### 8.2 编译验证

执行：

```powershell
cd C:\Users\shee_\OneDrive\Desktop\VSLoader
dotnet build .\VSLoader.sln -p:UseSharedCompilation=false
```

预期：

```text
Build succeeded.
0 Error(s)
```

### 8.3 全量测试

执行：

```powershell
cd C:\Users\shee_\OneDrive\Desktop\VSLoader
dotnet test .\VSLoader.sln -p:UseSharedCompilation=false
```

预期：

```text
所有测试通过。
```

### 8.4 人工验收场景

#### 场景一：程序最小化

步骤：

```text
1. 启动 VSLoader。
2. 最小化 VSLoader。
3. 按 Alt + V。
```

预期：

```text
VSLoader 一次按键恢复并激活到前台。
```

#### 场景二：程序隐藏到托盘

步骤：

```text
1. 启动 VSLoader。
2. 点击右上角关闭按钮，让程序隐藏到托盘。
3. 按 Alt + V。
```

预期：

```text
VSLoader 一次按键恢复并激活到前台。
```

#### 场景三：程序被其他窗口遮挡

步骤：

```text
1. 启动 VSLoader。
2. 保持 VSLoader 没有最小化。
3. 打开其他程序窗口并覆盖 VSLoader。
4. 按 Alt + V。
```

预期：

```text
VSLoader 一次按键恢复到前台。
不会第一次把 VSLoader 最小化。
```

#### 场景四：主窗口已在前台

步骤：

```text
1. VSLoader 主窗口处于前台。
2. 按 Alt + V。
```

预期：

```text
VSLoader 最小化或隐藏。
```

#### 场景五：地图窗口已在前台

步骤：

```text
1. 打开 VSLoader。
2. 打开地图窗口。
3. 点击地图窗口，使地图窗口处于前台。
4. 按 Alt + V。
```

预期：

```text
视为 VSLoader 已处于前台。
按键后执行最小化/隐藏逻辑。
地图窗口也随主窗口隐藏。
```

#### 场景六：地图窗口曾经打开但主程序被遮挡

步骤：

```text
1. 打开 VSLoader 和地图窗口。
2. 切换到其他程序，使 VSLoader 和地图都不在前台。
3. 按 Alt + V。
```

预期：

```text
VSLoader 和地图窗口一次按键恢复到可见状态。
不会先最小化。
```

## 9. 风险点

### 9.1 IsActive 的边界

风险：

```text
WPF IsActive 只表示当前窗口是否激活。
如果焦点在子窗口或附属窗口，需要额外判断地图窗口。
```

规避：

```text
IsVsLoaderActive 同时判断主窗口和地图窗口。
```

### 9.2 前台抢焦点限制

风险：

```text
Windows 对跨进程抢前台窗口有限制。
极端情况下 Activate() 可能不能完全抢到焦点。
```

处理：

```text
当前 RestoreAndActivate 已使用 Topmost true/false 辅助置前。
本次不额外引入 Win32 SetForegroundWindow。
若后续仍复现，再单独设计加强方案。
```

### 9.3 地图窗口隐藏状态

风险：

```text
地图窗口曾经打开，但主窗口最小化时被 Hide。
```

处理：

```text
RestoreAndActivate 末尾已有 ShowFactoryMapIfNeeded()。
保持原逻辑不变。
```

## 10. 验收标准

本需求完成必须满足：

```text
1. Alt + V 在程序隐藏或最小化时，一次按键恢复窗口。
2. Alt + V 在程序被其他窗口遮挡时，一次按键把 VSLoader 拉到前台。
3. Alt + V 在主窗口已经处于前台时，执行最小化/隐藏。
4. Alt + V 在地图窗口处于前台时，执行最小化/隐藏。
5. 不修改快捷键注册和配置逻辑。
6. dotnet build 通过。
7. dotnet test 通过。
```

## 11. 建议提交信息

```text
fix: restore window when hotkey pressed while app is not active
```
