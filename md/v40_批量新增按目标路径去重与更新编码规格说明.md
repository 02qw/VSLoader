# v40 批量新增按目标路径去重与更新编码规格说明

## 1. 需求背景

当前 VSLoader 的“批量新增识别”主要通过生成后的快捷项名称判断重复。

当用户修改 CSV 规则后，同一个源文件夹可能生成不同名称。例如：

旧规则生成：

```text
银烧结_001 -> 3134_TSSP001
银烧结_002 -> 5924_TSSP002
```

新规则生成：

```text
amx银烧结_001 -> 3134_TSSP001
amx银烧结_002 -> 5924_TSSP002
```

虽然名称不同，但它们对应的目标文件夹路径是同一个。

当前程序会把新名称当作新快捷项再次添加，导致主列表中出现多个指向同一源文件夹的快捷项。

本次需求要求：无论 CSV 规则如何变化，只要源文件夹路径相同，程序中就只保留一份快捷项，并以最新规则生成的结果更新原快捷项。

## 2. 需求目标

批量新增识别需要从“按名称判断新增”升级为：

```text
按 TargetPath 判断唯一性。
```

最终规则：

1. 如果扫描出的 `TargetPath` 在主列表中不存在，则新增快捷项。
2. 如果扫描出的 `TargetPath` 在主列表中已经存在，则更新已有快捷项。
3. 同一个 `TargetPath` 在主列表中最终只能保留一条快捷项。
4. CSV 规则变化导致生成名称变化时，更新已有项名称，而不是新增第二条。
5. 用户确认导入后，列表与 `config.json` 都保存最新结果。

## 3. 当前代码现状

批量预览生成逻辑：

`VSLoader/Models/Services/BatchImportService.cs`

当前 `BuildPreview` 逻辑中使用：

```csharp
var existingNames = new HashSet<string>(
    existingShortcuts.Select(shortcut => shortcut.Name.Trim()),
    StringComparer.OrdinalIgnoreCase);
var previewNames = new HashSet<string>(StringComparer.OrdinalIgnoreCase);
```

并通过：

```csharp
if (existingNames.Contains(generatedName) || !previewNames.Add(generatedName))
```

判断名称重复。

确认导入逻辑：

`VSLoader/ViewModels/MainViewModel.cs`

当前导入确认后直接：

```csharp
foreach (var shortcut in importedShortcuts)
{
    Shortcuts.Add(shortcut);
}
```

这导致路径已存在但名称变化时仍然会新增一条。

## 4. 设计原则

### 4.1 TargetPath 是批量识别的唯一源标识

批量新增识别产生的快捷项，本质来源是一个被扫描到的子文件夹。

因此应使用：

```text
TargetPath
```

判断是否是同一个源。

### 4.2 名称是可变映射结果

名称来自 CSV 规则：

```text
DisplayName + NameTemplate
```

CSV 改变后，名称可以变化。

因此名称不应作为判断同一个源文件夹的唯一依据。

### 4.3 CreatedAt 和 UpdatedAt 语义

新增时：

```text
CreatedAt = 当前时间
UpdatedAt = 当前时间
```

更新时：

```text
CreatedAt = 保留旧值
UpdatedAt = 当前时间
```

更新时应修改：

```text
Name
TargetPath
Description
UpdatedAt
```

## 5. 路径归一化规则

比较 `TargetPath` 前必须先归一化。

建议新增方法：

```csharp
private static string NormalizePathKey(string path)
```

归一化规则：

1. `Trim()` 去掉前后空格。
2. `/` 替换为 `\`。
3. 移除末尾多余的 `\`。
4. Windows 下使用不区分大小写比较。

示例应视为相同：

```text
\\192.168.15.69\instances\3134_TSSP001
\\192.168.15.69\instances\3134_TSSP001\
\\192.168.15.69/instances/3134_TSSP001
```

注意：

- 不要求对网络路径做实际访问。
- 不要求调用 `Path.GetFullPath` 强制解析网络路径，避免不可访问时抛异常。
- 只做字符串层面的稳定比较。

## 6. 预览状态设计

新增一个状态：

```csharp
public const string StatusUpdate = "可更新";
```

预览结果状态含义：

| 状态 | 含义 | CanImport | 默认选中 |
| --- | --- | --- | --- |
| `可新增` | TargetPath 不存在，名称也可用 | true | true |
| `可更新` | TargetPath 已存在，将更新已有快捷项 | true | true |
| `名称重复` | TargetPath 不同，但生成名称与其他项冲突 | false | false |
| `已跳过` | 未匹配规则 | false | false |
| `规则错误` | CSV 或模板错误 | false | false |

排序优先级建议：

```text
可新增
可更新
已跳过
规则错误
名称重复
```

原因：

- 可操作项放前面。
- 名称重复通常较多，仍然放最后。

## 7. 名称重复规则调整

需要区分两种情况：

### 7.1 路径相同

如果生成名称与已有快捷项不同，但 `TargetPath` 相同：

```text
可更新
```

此时不算名称重复。

确认后更新同一路径对应的已有快捷项。

### 7.2 路径不同但名称相同

如果生成名称与已有快捷项相同，但 `TargetPath` 不同：

```text
名称重复
```

此时不能导入，避免主列表出现同名但不同路径的快捷项。

### 7.3 本次预览内部路径重复

正常情况下 `Directory.EnumerateDirectories(parentFolderPath)` 不应产生重复路径。

但为了健壮性，仍建议维护：

```csharp
previewPathKeys
```

如果本次预览中出现归一化后相同的路径，只保留第一条，后续标记为 `名称重复` 或 `规则错误` 均可。

推荐标记为：

```text
名称重复
```

Message：

```text
本次预览中已存在相同目标路径。
```

## 8. BatchImportPreviewItem 改造

当前模型：

`VSLoader/Models/BatchImportPreviewItem.cs`

建议新增字段：

```csharp
public string ExistingTargetPath { get; set; } = string.Empty;
public string ExistingName { get; set; } = string.Empty;
public bool IsUpdate { get; set; }
```

用途：

- `IsUpdate`：确认导入时判断新增还是更新。
- `ExistingTargetPath`：记录匹配到的已有项路径。
- `ExistingName`：预览提示可使用。

如果希望更清晰，也可以使用枚举或字符串动作类型：

```csharp
public string ActionType { get; set; } = "Add";
```

但为保持最小改动，推荐先使用 `IsUpdate`。

## 9. BatchImportService 改造

### 9.1 BuildPreview

需要新增：

```csharp
var existingByPath = existingShortcuts
    .GroupBy(shortcut => NormalizePathKey(shortcut.TargetPath))
    .ToDictionary(group => group.Key, group => group.First(), StringComparer.OrdinalIgnoreCase);
```

扫描目录时：

1. 生成 `pathKey = NormalizePathKey(directory)`。
2. 如果 `existingByPath` 中存在该路径：
   - 状态为 `可更新`。
   - `CanImport = true`。
   - `IsSelected = true`。
   - `IsUpdate = true`。
   - `ExistingName = existingShortcut.Name`。
   - Message 示例：

```text
目标路径已存在，将更新：银烧结_001 -> amx银烧结_001
```

3. 如果路径不存在：
   - 继续按原逻辑判断名称是否重复。
   - 名称不重复则 `可新增`。

### 9.2 名称集合处理

原有：

```csharp
existingNames
previewNames
```

需要调整为：

1. 对路径已存在的更新项，不应因为它自己的旧名称而被判定 `名称重复`。
2. 如果更新后的新名称与另一个不同路径的快捷项名称重复，应判定 `名称重复`。

建议方法：

```csharp
private static bool HasNameConflict(
    string generatedName,
    string currentPathKey,
    IEnumerable<ShortcutItem> existingShortcuts)
```

判断逻辑：

```text
如果已有快捷项名称等于 generatedName 且路径不同，则冲突。
如果路径相同，则不冲突，因为这是更新同一个源。
```

本次预览内部也需要防止：

```text
不同路径生成相同名称
```

因此 `previewNames` 仍需要保留。

但如果同一路径被视为更新，不应和自己冲突。

## 10. CreateShortcuts 或导入结果改造

当前 `CreateShortcuts` 只返回新增快捷项：

```csharp
public IReadOnlyList<ShortcutItem> CreateShortcuts(IEnumerable<BatchImportPreviewItem> previewItems)
```

本次需要支持新增和更新两种动作。

推荐新增结果模型：

```csharp
public sealed class BatchImportApplyItem
{
    public bool IsUpdate { get; set; }
    public string ExistingTargetPath { get; set; } = string.Empty;
    public ShortcutItem Shortcut { get; set; } = new();
}
```

或者更简单：

```csharp
public IReadOnlyList<BatchImportPreviewItem> SelectedItems
```

由 `MainViewModel` 直接根据预览项执行新增或更新。

为了保持职责清晰，推荐新增服务方法：

```csharp
public IReadOnlyList<BatchImportApplyItem> CreateApplyItems(IEnumerable<BatchImportPreviewItem> previewItems)
```

并保留或替换原 `CreateShortcuts`。

## 11. MainViewModel 确认导入改造

当前逻辑：

```csharp
foreach (var shortcut in importedShortcuts)
{
    Shortcuts.Add(shortcut);
}
```

需要改为：

```text
foreach applyItem:
  if IsUpdate:
    找到 Shortcuts 中 TargetPath 相同的旧项
    更新 Name/TargetPath/Description/UpdatedAt
    保留 CreatedAt
  else:
    Shortcuts.Add(newShortcut)
```

提示文案建议：

```text
已新增 X 个快捷项，已更新 Y 个快捷项。
```

如果只有新增：

```text
已新增 X 个快捷项。
```

如果只有更新：

```text
已更新 Y 个快捷项。
```

## 12. 数据持久化

导入确认后仍调用：

```csharp
SaveCurrentConfig();
ShortcutsView.Refresh();
```

要求：

1. 新增项写入 `config.json`。
2. 更新项也写入 `config.json`。
3. 应用关闭后再打开，不出现同路径重复项。

## 13. UI 要求

本次不强制新增列。

批量新增识别预览页面中现有状态列应显示：

```text
可更新
```

Message 应说明将更新已有项。

主列表仍然不显示目标路径列。

## 14. 非目标范围

本次不实现：

1. 不清理历史中已经存在的同路径重复数据。
2. 不新增“自动去重清理”按钮。
3. 不重新展示目标路径列。
4. 不改变手动新增/编辑页面的名称重复规则。
5. 不实现复杂路径真实解析或网络路径连通性校验。

说明：

如果用户当前已经存在历史重复项，本次逻辑只能防止后续继续重复，并在再次批量导入同路径时更新匹配到的已有项。历史重复数据清理可以作为后续独立需求。

## 15. 测试要求

当前已有测试项目：

`VSLoader.Tests`

建议新增：

`VSLoader.Tests/BatchImportServicePathUpdateTests.cs`

至少覆盖：

### 15.1 路径已存在时标记为可更新

已有快捷项：

```text
Name = 银烧结_001
TargetPath = C:\Root\3134_TSSP001
```

新规则生成：

```text
Name = amx银烧结_001
TargetPath = C:\Root\3134_TSSP001
```

预期：

```text
Status = 可更新
CanImport = true
IsSelected = true
IsUpdate = true
```

### 15.2 路径不同但名称相同仍为名称重复

已有快捷项：

```text
Name = 银烧结_001
TargetPath = C:\Root\A
```

扫描结果：

```text
Name = 银烧结_001
TargetPath = C:\Root\B
```

预期：

```text
Status = 名称重复
CanImport = false
```

### 15.3 路径归一化

以下路径应视为相同：

```text
C:\Root\3134_TSSP001
C:\Root\3134_TSSP001\
C:/Root/3134_TSSP001
```

### 15.4 确认导入后更新而不是新增

主列表中已有：

```text
银烧结_001 -> C:\Root\3134_TSSP001
```

确认导入后应变为：

```text
amx银烧结_001 -> C:\Root\3134_TSSP001
```

且主列表数量不增加。

## 16. 验收场景

手工验收：

1. 使用旧 CSV 批量新增 `3134_TSSP001`，生成 `银烧结_001`。
2. 修改 CSV，使同一文件夹生成 `amx银烧结_001`。
3. 再次打开批量新增识别并扫描预览。
4. 该项应显示 `可更新`，不是 `可新增`。
5. 点击确认后，主列表中只保留一条 `3134_TSSP001` 对应快捷项。
6. 名称更新为 `amx银烧结_001`。
7. `UpdatedAt` 更新为当前时间。
8. 关闭并重启程序后，仍然只有一条。

## 17. 构建与测试验证

实现完成后执行：

```powershell
cd C:\Users\shee_\OneDrive\Desktop\VSLoader
dotnet test .\VSLoader.sln -p:UseSharedCompilation=false
dotnet build .\VSLoader.sln -p:UseSharedCompilation=false
```

如果 `VSLoader.exe` 被占用，应先关闭正在运行的程序：

```powershell
Get-Process -Name VSLoader -ErrorAction SilentlyContinue | Stop-Process -Force
```

最终要求：

- 测试全部通过。
- 构建 `0 个错误`。
- 不引入新的无关警告。

## 18. 预期改动文件

预计改动：

- `VSLoader/Models/BatchImportPreviewItem.cs`
- `VSLoader/Models/Services/BatchImportService.cs`
- `VSLoader/ViewModels/BatchImportViewModel.cs`
- `VSLoader/ViewModels/MainViewModel.cs`

预计新增：

- `VSLoader/Models/BatchImportApplyItem.cs`
- `VSLoader.Tests/BatchImportServicePathUpdateTests.cs`

## 19. 最终效果

无论 CSV 规则如何调整，只要扫描到的是同一个源文件夹路径，VSLoader 主列表中都只保留一条快捷项。

CSV 规则生成的新名称会同步更新到已有快捷项上。

这能避免用户因为调整规则文件而反复产生同一路径的重复快捷项。

