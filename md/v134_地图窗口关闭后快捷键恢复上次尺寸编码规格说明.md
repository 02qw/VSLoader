# v134 地图窗口关闭后快捷键恢复上次尺寸编码规格说明

## 1. 背景

当前 VSLoader 已经将主窗口和工厂地图窗口改造成两个独立窗口。

预期行为是：

```text
地图窗口关闭后，再按地图快捷键 Alt + X 打开地图，应恢复为上一次关闭前的位置、大小、缩放视图等状态。
```

但是当前用户反馈：

```text
地图关闭后，再按 Alt + X 打开，每次都是一个很小的窗口。
没有恢复上一次关闭时的地图窗口尺寸。
```

这说明地图窗口独立后，窗口恢复逻辑仍然残留了旧的“地图依赖主窗口状态”的约束。

本次需求不是重做地图窗口系统，而是对现有窗口状态保存与恢复链路做最小且完整的修复。

## 2. 当前代码现状

### 2.1 地图快捷键打开入口

文件：

```text
VSLoader/MainWindow.xaml.cs
```

入口方法：

```csharp
private void ToggleFactoryMapFromGlobalHotkey()
```

当前打开分支：

```csharp
case FactoryMapHotkeyAction.Open:
    _isFactoryMapOpen = true;
    _runtimeLayoutState.WasFactoryMapOpen = true;
    ShowFactoryMapIfNeeded();
    ActivateFactoryMapWindow();
    break;
```

`ShowFactoryMapIfNeeded()` 中会创建地图窗口，并调用：

```csharp
PositionFactoryMapWindow(useSessionBounds: true);
_factoryMapWindow.Show();
```

理论上它应该使用 `_runtimeLayoutState.FactoryMapLeft/Top/Width/Height` 恢复上次窗口尺寸。

### 2.2 当前地图窗口定位方法

文件：

```text
VSLoader/MainWindow.xaml.cs
```

当前逻辑：

```csharp
private void PositionFactoryMapWindow(bool useSessionBounds)
{
    if (_factoryMapWindow is null || WindowState == WindowState.Minimized)
    {
        return;
    }

    if (useSessionBounds && _runtimeLayoutState.HasFactoryMapBounds)
    {
        ...
        ApplyWindowBounds(_factoryMapWindow, ...);
        return;
    }

    ...
}
```

问题点：

```text
如果主窗口处于最小化状态，方法会直接 return。
这会导致地图窗口无法应用已保存的 FactoryMapWindow bounds。
新建地图窗口就会使用 XAML 默认尺寸。
```

地图窗口默认尺寸来自：

文件：

```text
VSLoader/Views/FactoryMapWindow.xaml
```

```xml
Width="620"
Height="720"
MinWidth="460"
MinHeight="360"
```

因此用户看到的“小窗口”很可能就是默认尺寸。

### 2.3 当前地图窗口保存状态

文件：

```text
VSLoader/MainWindow.xaml.cs
```

方法：

```csharp
private void SaveFactoryMapStateToSession(bool includeViewState = true)
```

它会保存：

```text
FactoryMapLeft
FactoryMapTop
FactoryMapWidth
FactoryMapHeight
FactoryMapView
WasFactoryMapOpen
```

并通过：

```csharp
ScheduleWindowLayoutSave()
```

延迟 500ms 写入：

```text
window-layout.json
```

### 2.4 当前地图关闭路径不统一

地图窗口使用通用标题栏：

文件：

```text
VSLoader/Views/Controls/ModernTitleBar.xaml.cs
```

关闭按钮默认逻辑：

```csharp
if (CloseRequested is not null)
{
    CloseRequested.Invoke(this, EventArgs.Empty);
    return;
}

OwnerWindow?.Close();
```

当前 `FactoryMapWindow.xaml` 中的 `ModernTitleBar` 没有命名，也没有订阅 `CloseRequested`。

因此用户点击地图右上角关闭时，会直接走：

```text
OwnerWindow.Close()
```

而不是统一走主窗口中的：

```csharp
CloseFactoryMapByUserAction()
```

这会让“主界面地图按钮关闭”和“地图窗口右上角关闭”的状态保存路径不完全一致。

## 3. 总体目标

### 3.1 用户目标

用户关闭地图窗口后：

```text
再次按 Alt + X 打开地图，应恢复上次关闭时的位置和尺寸。
```

如果用户拖动或缩放了地图窗口：

```text
关闭前应保存最新位置尺寸。
```

如果主窗口此时最小化：

```text
Alt + X 仍然只恢复地图窗口，不应因为主窗口最小化而放弃恢复地图 bounds。
```

### 3.2 技术目标

1. 地图窗口恢复 bounds 不再受主窗口 `WindowState.Minimized` 阻断。
2. 有历史地图 bounds 时，优先恢复历史 bounds。
3. 没有历史地图 bounds 时，才使用默认贴边或安全默认布局。
4. 地图标题栏关闭按钮统一走主窗口的地图关闭入口。
5. 地图关闭前同步保存一次 `window-layout.json`，避免 500ms debounce 来不及落盘。
6. 防止新建窗口默认尺寸 `620x720` 在恢复前误写入布局文件。
7. 不新增最大化状态持久化，不扩大数据结构。

## 4. 设计原则

### 4.1 地图窗口独立优先

地图窗口已经是独立窗口，因此：

```text
地图窗口的位置、大小、显示状态，应优先由地图自己的 runtime/session/layout 状态决定。
```

主窗口状态只允许参与：

```text
没有地图历史 bounds 时，计算默认贴边位置。
```

主窗口状态不允许阻止：

```text
恢复已有地图 bounds。
```

### 4.2 保存路径统一

以下关闭方式都应走同一套保存逻辑：

```text
1. 主窗口点击“地图”按钮关闭地图
2. 地图窗口右上角关闭
3. 程序退出时关闭地图
```

保存逻辑应保证：

```text
关闭前捕获地图当前 bounds。
关闭前捕获地图 view state。
关闭前同步写入 window-layout.json。
```

### 4.3 不持久化最大化状态

本次不新增：

```text
FactoryMapWindowState
```

原因：

```text
用户当前痛点是关闭后恢复尺寸变成小窗口。
最大化状态持久化会引入数据结构升级、RestoreBounds、WindowState 恢复顺序等额外复杂度。
本次先保证普通 bounds 恢复稳定。
```

## 5. 详细实现方案

### 5.1 调整 PositionFactoryMapWindow 早退逻辑

文件：

```text
VSLoader/MainWindow.xaml.cs
```

当前逻辑：

```csharp
if (_factoryMapWindow is null || WindowState == WindowState.Minimized)
{
    return;
}

if (useSessionBounds && _runtimeLayoutState.HasFactoryMapBounds)
{
    ...
}
```

修改为：

```csharp
if (_factoryMapWindow is null)
{
    return;
}

if (useSessionBounds && _runtimeLayoutState.HasFactoryMapBounds)
{
    var sessionBounds = new Rect(
        _runtimeLayoutState.FactoryMapLeft,
        _runtimeLayoutState.FactoryMapTop,
        _runtimeLayoutState.FactoryMapWidth,
        _runtimeLayoutState.FactoryMapHeight);

    ApplyWindowBounds(_factoryMapWindow, ClampBoundsToWorkArea(
        sessionBounds,
        _factoryMapWindow.MinWidth,
        _factoryMapWindow.MinHeight));
    return;
}

if (WindowState == WindowState.Minimized)
{
    ApplyWindowBounds(_factoryMapWindow, CalculateDefaultFactoryMapBoundsWithoutMainWindow());
    return;
}
```

说明：

```text
有历史地图 bounds 时，无论主窗口是否最小化，都必须恢复历史 bounds。
只有没有历史地图 bounds 时，才需要 fallback。
```

### 5.2 增加无主窗口依赖的地图默认 bounds

文件：

```text
VSLoader/MainWindow.xaml.cs
```

新增私有方法：

```csharp
private Rect CalculateDefaultFactoryMapBoundsWithoutMainWindow()
```

建议行为：

```text
使用当前屏幕工作区。
宽度取工作区宽度的 50% 到 70% 之间。
高度取工作区高度的 80% 到 90% 之间。
位置居中或靠右。
经过 MinWidth/MinHeight 和工作区 clamp。
```

推荐公式：

```csharp
var workArea = GetWorkArea();
var width = Clamp(workArea.Width * 0.55, _factoryMapWindow.MinWidth, workArea.Width);
var height = Clamp(workArea.Height * DefaultLayoutMapHeightRatio, _factoryMapWindow.MinHeight, workArea.Height * 0.90);
var left = workArea.Left + (workArea.Width - width) / 2;
var top = workArea.Top + (workArea.Height - height) / 2;
return ClampBoundsToWorkArea(new Rect(left, top, width, height), _factoryMapWindow.MinWidth, _factoryMapWindow.MinHeight);
```

这个 fallback 只在：

```text
没有历史地图 bounds 且主窗口最小化
```

时使用。

### 5.3 地图标题栏关闭统一走主窗口关闭入口

文件：

```text
VSLoader/Views/FactoryMapWindow.xaml
```

将：

```xml
<chromeControls:ModernTitleBar Grid.Row="0" />
```

改为：

```xml
<chromeControls:ModernTitleBar x:Name="MapTitleBar"
                               Grid.Row="0" />
```

文件：

```text
VSLoader/MainWindow.xaml.cs
```

在创建地图窗口后订阅：

```csharp
_factoryMapWindow.MapCloseRequested += ...
```

或者如果直接暴露 `MapTitleBar` 不合适，则在 `FactoryMapWindow.xaml.cs` 中新增事件：

```csharp
public event EventHandler? CloseRequested;
```

构造函数中：

```csharp
MapTitleBar.CloseRequested += (_, _) => CloseRequested?.Invoke(this, EventArgs.Empty);
```

主窗口创建地图窗口后：

```csharp
_factoryMapWindow.CloseRequested += (_, _) => CloseFactoryMapByUserAction();
```

推荐使用 `FactoryMapWindow.CloseRequested` 事件，不建议让 `MainWindow` 直接访问 `MapTitleBar` 控件。

### 5.4 防止递归关闭

因为 `CloseFactoryMapByUserAction()` 最终会调用：

```csharp
window.Close();
```

而地图标题栏关闭也会触发关闭请求，所以需要避免重复关闭。

推荐新增字段：

```csharp
private bool _isClosingFactoryMapByOwner;
```

在 `CloseFactoryMapByUserAction()` 中：

```csharp
if (_isClosingFactoryMapByOwner)
{
    return;
}

_isClosingFactoryMapByOwner = true;
try
{
    ...
    window.Close();
}
finally
{
    _isClosingFactoryMapByOwner = false;
}
```

如果实现时发现不会递归，也仍建议保留防护字段，防止后续修改标题栏关闭逻辑时产生重复保存或空引用。

### 5.5 关闭前同步保存布局

文件：

```text
VSLoader/MainWindow.xaml.cs
```

在：

```csharp
CloseFactoryMapByUserAction()
```

中，保存地图状态后增加：

```csharp
SaveWindowLayoutImmediately();
```

推荐顺序：

```csharp
SaveFactoryMapStateToSession();
_isFactoryMapOpen = false;
_runtimeLayoutState.WasFactoryMapOpen = false;
SaveWindowLayoutImmediately();
window.Close();
```

注意：

```text
如果希望 layout 文件记录 WasFactoryMapOpen = false，则 SaveWindowLayoutImmediately() 必须在设置 WasFactoryMapOpen=false 之后调用。
如果希望保留“上次关闭前是否打开”，则另议。
```

本次建议：

```text
关闭地图后 WasFactoryMapOpen = false。
但 FactoryMapWindow bounds 必须保留。
```

### 5.6 Closed 事件不要覆盖有效状态

当前创建地图窗口时订阅：

```csharp
_factoryMapWindow.Closed += (_, _) =>
{
    SaveFactoryMapStateToSession();
    _isFactoryMapOpen = false;
    _runtimeLayoutState.WasFactoryMapOpen = false;
    _factoryMapWindow = null;
};
```

这个逻辑存在风险：

```text
如果 CloseFactoryMapByUserAction 已经保存并设置 _factoryMapWindow = null，
Closed 事件里的 SaveFactoryMapStateToSession 可能只更新 WasFactoryMapOpen，不再保存 bounds。
```

建议改为：

```csharp
_factoryMapWindow.Closed += (_, _) =>
{
    _isFactoryMapOpen = false;
    _runtimeLayoutState.WasFactoryMapOpen = false;
    _factoryMapWindow = null;
};
```

也就是说：

```text
主动关闭路径负责保存。
Closed 事件只负责清理引用和状态。
```

对于系统级关闭或异常关闭：

```text
窗口拖动/缩放过程中已经有 500ms debounce 保存。
程序退出路径 CloseFactoryMapForExit() 会主动保存。
```

因此 `Closed` 事件不需要再二次保存。

### 5.7 程序退出路径保留同步保存

文件：

```text
VSLoader/MainWindow.xaml.cs
```

方法：

```csharp
CloseFactoryMapForExit()
```

当前已有：

```csharp
SaveFactoryMapStateToSession();
...
_factoryMapWindow.Close();
```

建议补充：

```csharp
SaveWindowLayoutImmediately();
```

原因：

```text
程序退出时不应该依赖 500ms debounce。
```

注意：

```text
CleanupForClose() 里后面也会调用 SaveWindowLayoutImmediately()。
如果已有立即保存覆盖，则不必重复。
实现时要检查调用顺序，避免无意义重复，但重复一次也不应影响正确性。
```

## 6. 测试设计

### 6.1 纯逻辑测试：有历史 bounds 时不受主窗口最小化影响

当前 `PositionFactoryMapWindow` 是私有实例方法，直接单测困难。

建议新增一个内部纯函数，降低测试成本：

文件：

```text
VSLoader/MainWindow.xaml.cs
```

新增：

```csharp
internal static bool ShouldRestoreFactoryMapBounds(
    bool useSessionBounds,
    bool hasFactoryMapBounds)
{
    return useSessionBounds && hasFactoryMapBounds;
}
```

测试：

文件：

```text
VSLoader.Tests/MainWindowFactoryMapRestoreTests.cs
```

```csharp
[Theory]
[InlineData(true, true, true)]
[InlineData(true, false, false)]
[InlineData(false, true, false)]
public void ShouldRestoreFactoryMapBounds_depends_on_session_bounds_not_main_window_state(
    bool useSessionBounds,
    bool hasFactoryMapBounds,
    bool expected)
{
    Assert.Equal(expected, MainWindow.ShouldRestoreFactoryMapBounds(useSessionBounds, hasFactoryMapBounds));
}
```

注意：

```text
测试名称必须明确：恢复地图 bounds 不依赖主窗口是否最小化。
```

### 6.2 静态测试：PositionFactoryMapWindow 不再用主窗口最小化阻断 session bounds

文件：

```text
VSLoader.Tests/MainWindowFactoryMapRestoreTests.cs
```

读取：

```text
VSLoader/MainWindow.xaml.cs
```

断言不再出现：

```csharp
_factoryMapWindow is null || WindowState == WindowState.Minimized
```

示例：

```csharp
[Fact]
public void PositionFactoryMapWindow_does_not_skip_session_bounds_when_main_window_is_minimized()
{
    var code = File.ReadAllText(TestProjectPaths.GetProjectFilePath("VSLoader", "MainWindow.xaml.cs"));

    Assert.DoesNotContain("_factoryMapWindow is null || WindowState == WindowState.Minimized", code);
    Assert.Contains("ShouldRestoreFactoryMapBounds", code);
}
```

### 6.3 静态测试：地图标题栏关闭被接管

文件：

```text
VSLoader.Tests/FactoryMapWindowTitleBarTests.cs
```

断言：

```text
FactoryMapWindow.xaml 中 ModernTitleBar 有 x:Name="MapTitleBar"。
FactoryMapWindow.xaml.cs 中订阅 MapTitleBar.CloseRequested。
FactoryMapWindow.xaml.cs 中暴露 CloseRequested 事件。
MainWindow.xaml.cs 中订阅 _factoryMapWindow.CloseRequested。
```

### 6.4 静态测试：用户关闭地图会同步保存

文件：

```text
VSLoader.Tests/MainWindowFactoryMapRestoreTests.cs
```

断言：

```text
CloseFactoryMapByUserAction 方法中包含 SaveWindowLayoutImmediately。
Closed 事件不再调用 SaveFactoryMapStateToSession。
```

如果静态测试过脆，可以只测关键字符串：

```csharp
Assert.Contains("SaveWindowLayoutImmediately();", closeFactoryMapByUserActionBody);
```

## 7. 验证命令

### 7.1 定向测试

```powershell
dotnet test .\VSLoader.Tests\VSLoader.Tests.csproj -c Debug --no-restore --filter "FullyQualifiedName~MainWindowFactoryMapRestoreTests|FullyQualifiedName~FactoryMapWindowTitleBarTests"
```

### 7.2 地图相关已有测试

```powershell
dotnet test .\VSLoader.Tests\VSLoader.Tests.csproj -c Debug --no-restore --filter "FullyQualifiedName~MainWindowFactoryMapCloseTests|FullyQualifiedName~MapHotkeyTriggerTests|FullyQualifiedName~FactoryMapWindowTaskbarTests"
```

### 7.3 Debug 构建

```powershell
dotnet build .\VSLoader.sln -c Debug --no-restore
```

如果 Debug 输出目录被正在运行的 VSLoader 占用，先关闭程序后重试。

## 8. 手动验证流程

实现后手动验证：

```text
1. 打开主程序。
2. 按 Alt + X 打开地图。
3. 将地图窗口拖到明显位置，并调整为较大尺寸。
4. 点击地图右上角关闭。
5. 再按 Alt + X。
6. 地图应恢复到刚才关闭前的位置和大小。
```

继续验证：

```text
1. 打开地图并调整尺寸。
2. 最小化主窗口。
3. 按 Alt + X。
4. 地图应按上次尺寸出现，不应变成 620x720 小窗口。
```

继续验证：

```text
1. 打开地图并调整尺寸。
2. 最小化地图。
3. 按 Alt + X。
4. 地图应从最小化恢复到原尺寸。
```

继续验证：

```text
1. 打开地图并调整尺寸。
2. 关闭整个程序。
3. 重新启动程序。
4. 按 Alt + X。
5. 地图应读取 window-layout.json 中的上次尺寸。
```

## 9. 边界情况清单

实现完成后必须逐条确认：

```text
1. 有历史地图 bounds 时，Alt + X 打开地图一定恢复历史 bounds。
2. 主窗口最小化时，有历史地图 bounds 也必须恢复地图 bounds。
3. 没有历史地图 bounds 时，地图使用安全默认尺寸，不出现屏幕外。
4. 地图右上角关闭和主窗口地图按钮关闭都能保存 bounds。
5. 关闭地图后 window-layout.json 中 FactoryMapWindow 不被清空。
6. 关闭地图后 window-layout.json 中 WasFactoryMapOpen 为 false。
7. 地图最小化后 Alt + X 恢复，不重建成默认小窗口。
8. 地图在后台被遮挡时 Alt + X 激活，不重置 bounds。
9. 程序退出时地图 bounds 仍能保存。
10. 现有主窗口快捷键逻辑不受影响。
11. 现有地图布局文件 factory-map.layout.json 不受影响。
12. 地图内部缩放视图 FactoryMapView 不被打开/关闭流程误清空。
```

## 10. 不做的事情

本次不做：

```text
1. 不新增地图最大化状态持久化。
2. 不修改 WindowLayoutConfig 数据结构。
3. 不改变地图节点、连线、缩放算法。
4. 不改变地图快捷键默认值和设置页面。
5. 不改变主窗口快捷键行为。
6. 不引入新的窗口管理服务。
```

## 11. 预期效果

完成后：

```text
地图关闭后，Alt + X 再打开，会恢复上次关闭前的位置和大小。
主窗口是否最小化，不再影响地图窗口恢复历史尺寸。
地图标题栏关闭、主界面地图按钮关闭、程序退出关闭，保存行为一致。
```

最终体验：

```text
地图像一个真正独立窗口一样记住自己的位置和尺寸。
Alt + X 只是快速打开/恢复/激活地图，不再导致窗口变成默认小尺寸。
```
