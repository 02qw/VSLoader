# v117 触控板双指横纵平滑滚动增强编码规格说明

## 1. 背景

v116 的实现方向偏向“触控友好滚动条”，引入了公共滚动条样式和滚动条尺寸调整。

但真实需求并不是改变滚动条外观或尺寸，而是：

- 原来的滚动条保持原样。
- 原来的鼠标滚轮体验保持原样。
- 在不影响普通鼠标用户的前提下，增强触控板双指滚动体验。
- 触控板双指左右滑动时，横向滚动条能够跟手移动。
- 触控板双指上下滑动时，纵向滚动更丝滑。

因此需要重新设计为“触控板手势增强”，而不是“滚动条视觉改造”。

## 2. 目标

本次目标：

- 回退或移除 v116 中改变滚动条外观/尺寸的全局样式影响。
- 保持原生滚动条视觉和尺寸。
- 保持普通鼠标滚轮行为不变。
- 增强触控板双指横向滚动。
- 增强触控板双指纵向滚动的跟手感，但不抢普通鼠标滚轮。
- 重点覆盖主界面和批量新增识别这类横向内容溢出的表格。

## 3. 非目标

本次不做以下事项：

- 不改变滚动条外观。
- 不改变滚动条宽度或高度。
- 不全局重写 `ScrollBar` 样式。
- 不关闭 DataGrid 虚拟化。
- 不改变普通鼠标滚轮一次滚动的距离和节奏。
- 不修改地图画布缩放、拖拽、节点移动逻辑。
- 不引入第三方输入库。
- 不做复杂惯性动画引擎。

## 4. 当前需要修正的 v116 内容

v116 中以下方向需要撤回或不继续扩大：

```xml
ModernScrollBarStyle
<Style TargetType="ScrollBar" BasedOn="{StaticResource ModernScrollBarStyle}" />
```

原因：

- 它改变了滚动条视觉和尺寸。
- 用户明确要求原滚轮和滚动条保持不变。

可以保留或重新评估的内容：

```xml
ScrollViewer.PanningMode
ScrollViewer.PanningRatio
ScrollViewer.PanningDeceleration
```

但要注意：

- `PanningMode` 主要影响触摸屏/触控平移。
- 对 Windows 精密触控板横向滚动支持不一定充分。
- 因此它可以作为辅助配置，但不能作为核心方案。

## 5. 核心设计

### 5.1 新增触控板滚动增强行为

新增一个可复用附加行为：

```text
VSLoader/Behaviors/SmoothTouchpadScrollBehavior.cs
```

建议命名：

```csharp
SmoothTouchpadScrollBehavior
```

它提供附加属性：

```csharp
public static readonly DependencyProperty IsEnabledProperty;
public static readonly DependencyProperty EnableHorizontalProperty;
public static readonly DependencyProperty EnableVerticalProperty;
public static readonly DependencyProperty HorizontalSensitivityProperty;
public static readonly DependencyProperty VerticalSensitivityProperty;
```

XAML 使用方式：

```xml
behaviors:SmoothTouchpadScrollBehavior.IsEnabled="True"
behaviors:SmoothTouchpadScrollBehavior.EnableHorizontal="True"
behaviors:SmoothTouchpadScrollBehavior.EnableVertical="True"
```

命名空间示例：

```xml
xmlns:behaviors="clr-namespace:VSLoader.Behaviors"
```

### 5.2 横向滚动处理

Windows 横向滚轮消息通常来自：

```text
WM_MOUSEHWHEEL
```

常量：

```csharp
private const int WM_MOUSEHWHEEL = 0x020E;
```

行为启用后，在目标控件加载时获取宿主窗口 `HwndSource`，通过 Hook 监听该消息。

处理逻辑：

1. 判断鼠标当前是否位于启用行为的控件内部。
2. 找到目标控件内部的 `ScrollViewer`。
3. 如果 `EnableHorizontal = true` 且 `ScrollableWidth > 0`：
   - 读取横向 delta。
   - 转换为 `HorizontalOffset` 偏移。
   - 调用：

```csharp
scrollViewer.ScrollToHorizontalOffset(targetOffset);
```

4. 只有实际处理成功时才标记 handled。

注意：

- 不要全局吞掉所有横向滚动。
- 鼠标不在目标控件上时不处理。
- 目标控件没有横向可滚区域时不处理。

### 5.3 纵向滚动处理

纵向滚动更敏感，因为普通鼠标滚轮和触控板双指上下经常都表现为 `MouseWheel`。

因此要求：

- 普通鼠标滚轮不抢。
- 只对疑似触控板细粒度滚动做增强。

建议策略：

1. 监听目标控件的 `PreviewMouseWheel`。
2. 如果 `EnableVertical = false`，不处理。
3. 如果 `Math.Abs(e.Delta) >= 120`，认为是普通鼠标滚轮，交给 WPF 默认处理。
4. 如果 `Math.Abs(e.Delta) < 120`，认为更可能来自触控板细粒度滚动：
   - 找到内部 `ScrollViewer`。
   - 计算像素级偏移。
   - 调用：

```csharp
scrollViewer.ScrollToVerticalOffset(targetOffset);
```

5. 只在实际发生偏移时 `e.Handled = true`。

说明：

- 这是保守策略。
- 如果某些触控板驱动仍然输出 `±120`，本策略不会改变它，避免误伤普通鼠标。
- 后续如果实测需要，可以再增加配置开关。

### 5.4 ScrollViewer 查找

行为需要支持作用在：

- `DataGrid`
- `ListBox`
- `ScrollViewer`

如果目标本身就是 `ScrollViewer`，直接使用。

否则在视觉树中递归查找第一个子级 `ScrollViewer`。

建议实现：

```csharp
private static ScrollViewer? FindScrollViewer(DependencyObject source)
```

为避免频繁查找，可在 `Loaded` 后缓存，但需要注意控件模板重建时缓存失效。

最小实现可每次事件中查找，当前窗口规模可接受。

## 6. 应用范围

### 6.1 主界面快捷项 DataGrid

位置：

```text
VSLoader/MainWindow.xaml
```

目标：

- 双指上下：纵向滚动更自然。
- 双指左右：横向滚动条跟手移动。

配置：

```xml
behaviors:SmoothTouchpadScrollBehavior.IsEnabled="True"
behaviors:SmoothTouchpadScrollBehavior.EnableHorizontal="True"
behaviors:SmoothTouchpadScrollBehavior.EnableVertical="True"
```

必须保留：

```xml
ScrollViewer.CanContentScroll="True"
EnableColumnVirtualization="True"
EnableRowVirtualization="True"
VirtualizingPanel.IsVirtualizing="True"
VirtualizingPanel.VirtualizationMode="Recycling"
```

### 6.2 批量新增识别 DataGrid

位置：

```text
VSLoader/Views/BatchImportWindow.xaml
```

目标：

- 结果表格列多，重点增强横向双指滚动。

配置：

```xml
behaviors:SmoothTouchpadScrollBehavior.IsEnabled="True"
behaviors:SmoothTouchpadScrollBehavior.EnableHorizontal="True"
behaviors:SmoothTouchpadScrollBehavior.EnableVertical="True"
```

### 6.3 设置窗口 ScrollViewer

位置：

```text
VSLoader/Views/SettingsWindow.xaml
```

目标：

- 设置页主要是纵向滚动。
- 保留已有 `SettingsInput_PreviewMouseWheel` 鼠标滚轮转交逻辑。
- 可启用纵向触控板增强。

配置：

```xml
behaviors:SmoothTouchpadScrollBehavior.IsEnabled="True"
behaviors:SmoothTouchpadScrollBehavior.EnableHorizontal="False"
behaviors:SmoothTouchpadScrollBehavior.EnableVertical="True"
```

### 6.4 工作区列表

位置：

```text
VSLoader/Views/WorkspaceSelectorWindow.xaml
```

目标：

- 工作区列表只需要纵向触控滚动增强。

配置：

```xml
behaviors:SmoothTouchpadScrollBehavior.IsEnabled="True"
behaviors:SmoothTouchpadScrollBehavior.EnableHorizontal="False"
behaviors:SmoothTouchpadScrollBehavior.EnableVertical="True"
```

### 6.5 快捷项编辑备注框

位置：

```text
VSLoader/Views/ShortcutEditWindow.xaml
```

备注框是可编辑 TextBox，内部涉及文本选择和光标定位。

本次不强制启用增强行为，避免影响文本编辑。

如果实测备注框仍需要优化，再单独处理。

### 6.6 地图窗口

位置：

```text
VSLoader/Views/FactoryMapWindow.xaml
```

地图不纳入本次行为。

原因：

- 地图不是普通滚动容器。
- 地图已经使用鼠标滚轮缩放、左键拖拽平移。
- 普通滚动增强可能和地图交互冲突。

## 7. 编码要求

### 7.1 回退滚动条视觉改动

如果当前代码存在：

```xml
ModernScrollBarStyle
<Style TargetType="ScrollBar" BasedOn="{StaticResource ModernScrollBarStyle}" />
```

应删除或禁用。

不得保留任何会改变全局滚动条尺寸或视觉的样式。

### 7.2 保留 PanningMode 但不依赖它

可以保留：

```xml
ScrollViewer.PanningMode
PanningMode
```

作为触摸屏辅助。

但核心跟手横向滚动必须由 `SmoothTouchpadScrollBehavior` 处理。

### 7.3 不误伤普通鼠标滚轮

纵向增强逻辑必须区分普通滚轮与细粒度触控板输入。

保守规则：

```csharp
if (Math.Abs(e.Delta) >= 120)
{
    return;
}
```

这样普通鼠标滚轮保持 WPF 默认处理。

### 7.4 横向消息处理必须限定范围

`WM_MOUSEHWHEEL` 是窗口级消息。

处理时必须判断鼠标位置是否在启用行为的目标控件范围内。

不要在窗口级别全局滚动当前任意控件。

## 8. 测试建议

建议新增测试：

```text
VSLoader.Tests/SmoothTouchpadScrollBehaviorTests.cs
VSLoader.Tests/TouchpadScrollXamlTests.cs
```

测试点：

1. 滚动偏移计算：

```csharp
CalculateTargetOffset(current, delta, sensitivity, min, max)
```

应正确 clamp 到范围内。

2. 普通鼠标滚轮判断：

```csharp
IsFineGrainedVerticalDelta(120) == false
IsFineGrainedVerticalDelta(-120) == false
IsFineGrainedVerticalDelta(30) == true
```

3. XAML 应包含行为命名空间：

```xml
xmlns:behaviors="clr-namespace:VSLoader.Behaviors"
```

4. 主界面 DataGrid 启用横纵增强。

5. 批量新增 DataGrid 启用横纵增强。

6. 设置页 ScrollViewer 只启用纵向增强。

7. 工作区 ListBox 只启用纵向增强。

8. `ModernTheme.xaml` 不应包含全局 `ScrollBar` 样式覆盖。

## 9. 验收标准

满足以下条件即完成：

- 滚动条外观与原来一致，不出现变窄、变粗、颜色突变等视觉变化。
- 普通鼠标滚轮上下滚动体验不变。
- 主界面快捷项表格支持触控板双指左右横向滚动。
- 主界面快捷项表格支持触控板双指上下纵向滚动，且更跟手。
- 批量新增识别表格支持触控板双指左右横向滚动。
- 设置页双指上下滚动自然，原有输入框滚轮转交逻辑不失效。
- 工作区列表双指上下滚动自然。
- 地图窗口缩放、拖拽、节点移动不受影响。
- DataGrid 虚拟化未被关闭。
- `dotnet test .\VSLoader.Tests\VSLoader.Tests.csproj -c Debug --no-restore` 通过。
- `dotnet build .\VSLoader.sln -c Debug --no-restore` 通过。

## 10. 风险与边界

### 10.1 不同触控板驱动差异

不同 Windows 触控板驱动对双指滚动输出不同：

- 有的会输出 `WM_MOUSEHWHEEL`。
- 有的可能只输出传统 `MouseWheel`。
- 有的纵向 delta 仍然是 `±120`。

本次采用保守增强策略，优先不误伤普通鼠标。

如果某些设备仍不够丝滑，后续可以增加可配置灵敏度或强制增强开关。

### 10.2 HwndSource Hook 生命周期

行为需要在控件 Loaded 后注册 Hook，并在 Unloaded 时移除。

必须避免：

- 重复添加 Hook。
- 窗口关闭后 Hook 未释放。
- 多个控件启用行为时互相抢事件。

### 10.3 DataGrid 内部 ScrollViewer 查找

DataGrid 的 ScrollViewer 由模板生成。

需要在控件 Loaded 后查找，未加载前可能找不到。

如果找不到 ScrollViewer，应直接不处理，不抛异常。

### 10.4 设置页输入框滚轮逻辑

设置页已有：

```csharp
SettingsInput_PreviewMouseWheel
```

本次新增行为不能破坏该逻辑。

如果行为和该事件同时处理同一普通鼠标滚轮，应以“不处理普通滚轮”为原则，避免重复滚动。

