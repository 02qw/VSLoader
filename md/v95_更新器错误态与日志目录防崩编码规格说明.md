# v95 更新器错误态与日志目录防崩编码规格说明

## 1. 背景

在 `VSLoader.Updater` 的更新流程中，当前已经完成了 `releaseNotes` 延迟展示的优化。但进一步盘查后发现，更新器还存在两个值得补强的点：

1. 错误态下可能保留旧的 `releaseNotes` 展示状态，导致用户在失败界面里看到不应出现的更新内容。
2. “打开日志目录”按钮的执行逻辑缺少异常保护，一旦目录创建或系统打开失败，可能导致更新器再次抛异常。

这两个问题不会影响更新主流程的核心逻辑，但都会影响错误场景下的稳定性和可理解性。

## 2. 目标

本次修复只做以下两件事：

- 当更新器进入错误态时，自动清理更新内容展示状态，避免错误界面沿用旧的 `releaseNotes`。
- 为“打开日志目录”命令增加完整异常保护，确保按钮点击不会把 updater 再次带崩。

要求：

- 不改动更新下载、manifest 读取、版本判断、SHA 校验、解压、替换、回滚等主流程。
- 不改动 releaseNotes 的“延迟展示”策略。
- 不改动确认弹窗的主交互。

## 3. 影响范围

主要修改文件：

- `VSLoader.Updater/MainWindow.xaml.cs`

如需补测试，可新增或调整：

- `VSLoader.Tests/UpdaterReleaseNotesDisplayStateTests.cs`
- `VSLoader.Tests` 中 updater 错误态相关测试

## 4. 现状问题分析

### 4.1 错误态保留旧更新内容

当前 `ShowError(string message)` 只做了：

- `isUpdating = false`
- `HasError = true`
- `StatusText = message`

但没有同步清理：

- `ShowReleaseNotes`
- `ReleaseNotesText`

因此可能出现：

- 前面已经读取过 `manifest.json`
- `ShowReleaseNotes = true`
- 后面在替换、解压、校验等环节失败
- 错误界面仍然保留旧的更新内容展示

这不会直接崩溃，但会让错误界面和错误原因混在一起，不够干净。

### 4.2 打开日志目录命令缺少保护

当前 `OpenErrorLogDirectory()` 的逻辑是：

```csharp
var directory = Path.Combine(Environment.GetFolderPath(Environment.SpecialFolder.LocalApplicationData), "VSLoader", "errorLog");
Directory.CreateDirectory(directory);
Process.Start(new ProcessStartInfo
{
    FileName = directory,
    UseShellExecute = true
});
```

问题点：

- `Directory.CreateDirectory` 可能因权限、路径异常、磁盘问题失败
- `Process.Start` 也可能因为系统 shell、文件关联或环境异常失败
- 该方法目前没有 `try/catch`

如果 updater 已经处于错误状态，再次点击“打开日志目录”就可能发生二次异常。

## 5. 设计方案

### 5.1 错误态统一清理 releaseNotes

在 `ShowError(string message)` 中增加清理动作：

- `ShowReleaseNotes = false`
- `ReleaseNotesText = string.Empty`

这样错误界面只显示错误信息和日志按钮，不再残留此前的 releaseNotes 区域。

建议行为：

- 只要进入错误态，更新内容区就完全隐藏。
- 错误态不展示 `本次更新内容`，避免用户误以为错误消息和 release notes 有直接关系。

### 5.2 打开日志目录增加异常保护

为 `OpenErrorLogDirectory()` 添加完整 `try/catch`：

- 创建目录失败时，在界面上显示明确错误原因
- 打开目录失败时，在界面上显示明确错误原因

建议错误文案：

```text
打开日志目录失败：{ex.Message}
```

如果需要更稳一点，可以保留原错误态，同时将状态文本改成“打开日志目录失败：...”，但不要让异常继续冒泡。

### 5.3 命令仍保持无参数

不需要给现有按钮改成复杂的命令参数模式。

保持：

- `OpenErrorLogDirectoryCommand` 仍然是无参数命令
- `CloseCommand` 保持原样

只在内部补异常保护即可。

### 5.4 releaseNotes 展示逻辑不回退

本次修复不改变以下规则：

- `manifest.json` 未加载完成前，不展示更新内容区域
- 读取完成后再展示
- 为空时显示“本次更新未填写更新说明。”

本次只是保证：

- 错误态不再残留 releaseNotes
- 打开日志目录不会额外崩掉

## 6. 测试建议

建议增加以下验证：

1. 错误态清理
   - 先模拟已经显示 releaseNotes
   - 再调用 `ShowError`
   - 断言错误态下 `ShowReleaseNotes` 为 `false`
   - 断言 `ReleaseNotesText` 为空

2. 打开日志目录异常保护
   - 模拟 `Directory.CreateDirectory` 或 `Process.Start` 异常
   - 断言异常不会向外抛出
   - 断言界面状态会给出明确错误信息

3. 原有 releaseNotes 延迟展示行为不回退
   - manifest 未读到前不展示
   - manifest 读到后展示
   - 错误态隐藏

## 7. 验收标准

满足以下条件即算完成：

- updater 错误态不会再显示旧的 releaseNotes 内容
- 点击“打开日志目录”不会因异常导致 updater 崩溃
- 原有更新主流程不受影响
- `dotnet test .\\VSLoader.sln -c Debug` 通过
- `dotnet build .\\VSLoader.sln -c Debug` 通过

