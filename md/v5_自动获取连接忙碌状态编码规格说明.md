# v5 自动获取连接忙碌状态编码规格说明

## 1. 文档目的

本文档用于指导编程 agent 或开发人员修复“自动获取连接”执行期间缺少加载状态和操作屏蔽的问题。

目标是在 AdminUI JNLP 自动下载过程中，明确提示用户程序正在处理，并禁止用户继续操作主界面，避免重复点击或并发操作导致程序状态混乱。

## 2. 当前问题

当前用户点击主界面的：

```text
自动获取连接
```

后，程序会后台遍历所有快捷项、读取 `INSTANCE.properties`、拼接 JNLP URL 并下载 `.jnlp` 文件。

该过程可能耗时较长，但当前界面没有明显加载状态，用户仍然可以继续点击：

- 自动获取连接
- AdminUI
- 新增
- 批量新增识别
- 编辑
- 删除
- 打开
- 设置
- 快捷项列表
- 搜索框

这可能导致：

- 重复触发下载任务。
- 下载期间打开未完成的 `.jnlp`。
- 下载期间修改快捷项列表。
- 下载期间修改 AdminUI 配置。
- 程序表现为卡顿或状态混乱。

## 3. 目标效果

用户点击“自动获取连接”后：

1. 程序进入忙碌状态。
2. 主界面显示加载提示。
3. 主界面主要操作全部禁用。
4. 鼠标光标变为等待状态。
5. 后台继续执行下载。
6. 下载完成后弹出结果汇总。
7. 用户关闭结果弹窗后，解除忙碌状态。
8. 主界面恢复可操作。

流程：

```text
点击自动获取连接
-> IsBusy = true
-> 显示加载遮罩
-> 禁用主界面操作
-> 下载 AdminUI JNLP
-> 弹出完成结果
-> IsBusy = false
-> 恢复主界面操作
```

## 4. 需要修改的文件

主要修改：

```text
ViewModels\MainViewModel.cs
MainWindow.xaml
```

不需要修改：

```text
AdminUiService.cs
ConfigService.cs
ShortcutItem.cs
AppConfig.cs
```

## 5. ViewModel 修改规格

修改：

```text
ViewModels\MainViewModel.cs
```

新增属性：

```csharp
[ObservableProperty]
[NotifyCanExecuteChangedFor(nameof(AddShortcutCommand))]
[NotifyCanExecuteChangedFor(nameof(OpenBatchImportCommand))]
[NotifyCanExecuteChangedFor(nameof(DownloadAdminUiLinksCommand))]
[NotifyCanExecuteChangedFor(nameof(OpenAdminUiCommand))]
[NotifyCanExecuteChangedFor(nameof(EditShortcutCommand))]
[NotifyCanExecuteChangedFor(nameof(DeleteShortcutCommand))]
[NotifyCanExecuteChangedFor(nameof(OpenShortcutCommand))]
[NotifyCanExecuteChangedFor(nameof(OpenSettingsCommand))]
private bool isBusy;

[ObservableProperty]
private string busyMessage = string.Empty;
```

新增辅助属性：

```csharp
public bool IsNotBusy => !IsBusy;
```

当 `IsBusy` 变化时，需要通知：

```csharp
OnPropertyChanged(nameof(IsNotBusy));
```

## 6. 命令 CanExecute 修改规格

所有主界面操作命令都应在忙碌时不可执行。

规则：

| 命令 | CanExecute |
| --- | --- |
| `AddShortcutCommand` | `!IsBusy` |
| `OpenBatchImportCommand` | `!IsBusy` |
| `DownloadAdminUiLinksCommand` | `!IsBusy` |
| `OpenAdminUiCommand` | `!IsBusy && SelectedShortcut != null` |
| `EditShortcutCommand` | `!IsBusy && SelectedShortcut != null` |
| `DeleteShortcutCommand` | `!IsBusy && SelectedShortcut != null` |
| `OpenShortcutCommand` | `!IsBusy && SelectedShortcut != null` |
| `OpenSettingsCommand` | `!IsBusy` |

建议新增方法：

```csharp
private bool CanRunGlobalCommand()
{
    return !IsBusy;
}

private bool HasSelectedShortcutAndNotBusy()
{
    return !IsBusy && SelectedShortcut is not null;
}
```

现有 `HasSelectedShortcut()` 可以替换或调整为：

```csharp
private bool HasSelectedShortcut()
{
    return !IsBusy && SelectedShortcut is not null;
}
```

## 7. 自动获取连接命令修改规格

修改：

```csharp
DownloadAdminUiLinksAsync()
```

执行逻辑：

```csharp
if (IsBusy)
{
    return;
}

try
{
    IsBusy = true;
    BusyMessage = "正在自动获取 AdminUI 连接，请稍候...";

    // 原有下载逻辑

    // 下载完成后弹窗显示结果
}
finally
{
    IsBusy = false;
    BusyMessage = string.Empty;
}
```

注意：

- 必须使用 `finally` 确保异常时也能恢复界面。
- 结果弹窗应在 `IsBusy = true` 期间弹出，用户关闭弹窗后再执行 `finally` 恢复界面。
- 如果当前没有快捷项，也可以不进入忙碌状态，直接提示。

## 8. XAML 修改规格

修改：

```text
MainWindow.xaml
```

### 8.1 主内容禁用

将主窗口原有内容包裹到一个根 `Grid` 中。

主内容区域绑定：

```xml
IsEnabled="{Binding IsNotBusy}"
```

这样忙碌时禁用：

- 搜索框
- 按钮
- 快捷项列表

### 8.2 加载遮罩

在根 `Grid` 中新增一个覆盖层。

显示条件：

```xml
Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}"
```

遮罩建议：

```xml
<Border Background="#80000000">
    <Border Background="White"
            Width="360"
            Padding="24"
            CornerRadius="6"
            HorizontalAlignment="Center"
            VerticalAlignment="Center">
        <StackPanel>
            <TextBlock Text="{Binding BusyMessage}"
                       TextAlignment="Center"
                       FontSize="16"
                       Margin="0,0,0,16" />
            <ProgressBar IsIndeterminate="True"
                         Height="8" />
        </StackPanel>
    </Border>
</Border>
```

### 8.3 鼠标等待光标

主窗口根元素建议绑定：

```xml
Cursor="{Binding IsBusy, Converter={StaticResource BusyCursorConverter}}"
```

需要新增转换器：

```text
Converters\BusyCursorConverter.cs
```

实现：

```csharp
public sealed class BusyCursorConverter : IValueConverter
{
    public object Convert(object value, Type targetType, object parameter, CultureInfo culture)
    {
        return value is true ? Cursors.Wait : Cursors.Arrow;
    }

    public object ConvertBack(object value, Type targetType, object parameter, CultureInfo culture)
    {
        throw new NotSupportedException();
    }
}
```

并在 `App.xaml` 注册：

```xml
<converters:BusyCursorConverter x:Key="BusyCursorConverter" />
```

## 9. 用户体验要求

加载文案固定为：

```text
正在自动获取 AdminUI 连接，请稍候...
```

下载完成后仍然使用现有结果弹窗：

```text
自动获取连接完成。
成功：X
失败：Y
```

用户关闭结果弹窗后，主界面恢复可操作。

## 10. 错误处理

| 场景 | 处理方式 |
| --- | --- |
| 下载过程中异常 | 弹窗提示错误，随后解除忙碌状态 |
| 下载过程中用户尝试点击按钮 | 按钮不可用 |
| 下载过程中用户尝试操作列表 | 列表不可用 |
| 下载过程中用户尝试搜索 | 搜索框不可用 |
| 下载过程中结果弹窗显示 | 允许用户关闭弹窗 |
| 结果弹窗关闭后 | 恢复主界面操作 |

## 11. 验收标准

- 点击“自动获取连接”后出现加载遮罩。
- 加载遮罩显示文案：

```text
正在自动获取 AdminUI 连接，请稍候...
```

- 加载期间搜索框不可编辑。
- 加载期间快捷项列表不可操作。
- 加载期间所有主界面按钮不可点击。
- 加载期间鼠标显示等待光标。
- 不能重复触发“自动获取连接”。
- 下载完成后弹出结果汇总。
- 用户关闭结果汇总弹窗后，加载遮罩消失。
- 用户关闭结果汇总弹窗后，主界面恢复可操作。
- 下载失败或异常时，界面也必须恢复可操作。
- `dotnet build .\VSLoader.sln` 必须通过。

