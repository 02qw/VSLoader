# v105 地图窗口最小化等同关闭与状态统一编码规格说明

## 1. 背景

当前工厂地图是主窗口右侧的附属窗口：

```text
FactoryMapWindow
```

窗口配置中：

```xml
ShowInTaskbar="False"
```

因此地图窗口不会显示在任务栏里。

但地图窗口仍然保留系统标题栏上的最小化按钮。用户点击最小化后，Windows 会把这个不在任务栏中的附属窗口变成屏幕底部的小型最小化窗口，视觉上像一个“缩到屏幕最下角的小窗口”，体验很奇怪，也不符合 VSLoader 当前“地图是主窗口附属面板”的产品语义。

用户希望优化该行为。

本次采用方案：

```text
地图窗口点击最小化 = 等同于关闭地图窗口
```

也就是地图窗口不进入 Windows 原生最小化状态，而是走现有“地图按钮关闭地图”的语义。

## 2. 目标

实现地图窗口用户主动收起行为统一：

- 点击地图窗口右上角最小化按钮时，不出现屏幕底部小窗口。
- 地图窗口最小化等同于关闭地图。
- 点击地图窗口右上角关闭按钮时，也同步为“地图已关闭”状态。
- 点击主界面“地图”按钮关闭、按地图快捷键关闭、地图窗口最小化、地图窗口关闭按钮，最终都保持一致状态。
- 保留当前地图窗口的位置、大小、缩放等运行期/持久化布局保存逻辑。
- 不影响主窗口最小化到托盘逻辑。
- 不影响程序退出时关闭地图逻辑。

## 3. 非目标

本次不做：

- 不隐藏地图窗口标题栏。
- 不自定义地图窗口标题栏按钮。
- 不移除最小化按钮样式。
- 不把地图窗口最小化到任务栏。
- 不改变主窗口最小化到托盘逻辑。
- 不改变地图 JSON 格式。
- 不改变地图窗口默认打开位置、大小、缩放比例。
- 不改变地图快捷键 `M` 的触发规则。

## 4. 当前代码定位

### 4.1 地图窗口 XAML

文件：

```text
VSLoader/Views/FactoryMapWindow.xaml
```

当前窗口声明：

```xml
<Window x:Class="VSLoader.Views.FactoryMapWindow"
        ...
        ShowInTaskbar="False">
```

由于 `ShowInTaskbar="False"`，窗口最小化后不会进入任务栏，而是在桌面底部形成很小的最小化外观。

### 4.2 主窗口地图开关逻辑

文件：

```text
VSLoader/MainWindow.xaml.cs
```

当前地图按钮和地图快捷键最终都调用：

```csharp
private void ToggleFactoryMapWindow()
```

其中关闭地图逻辑大致为：

```csharp
SaveFactoryMapStateToSession();
_isFactoryMapOpen = false;
_runtimeLayoutState.WasFactoryMapOpen = false;
_factoryMapWindow.Close();
_factoryMapWindow = null;
```

该逻辑应作为用户主动关闭地图的统一语义。

### 4.3 地图窗口 Closed 回调

创建地图窗口时当前已有：

```csharp
_factoryMapWindow.Closed += (_, _) =>
{
    SaveFactoryMapStateToSession();
    _factoryMapWindow = null;
};
```

该回调只清理 `_factoryMapWindow`，但如果用户直接点地图窗口右上角 X，可能没有同步：

```csharp
_isFactoryMapOpen = false;
_runtimeLayoutState.WasFactoryMapOpen = false;
```

本次需要一起修正。

## 5. 推荐设计

### 5.1 新增统一关闭方法

建议在 `MainWindow.xaml.cs` 中新增：

```csharp
private void CloseFactoryMapByUserAction()
```

职责：

1. 保存地图窗口当前状态。
2. 标记地图不再打开。
3. 更新运行期布局状态。
4. 关闭地图窗口。
5. 清空 `_factoryMapWindow`。

建议逻辑：

```csharp
private void CloseFactoryMapByUserAction()
{
    if (_factoryMapWindow is null)
    {
        _isFactoryMapOpen = false;
        _runtimeLayoutState.WasFactoryMapOpen = false;
        return;
    }

    SaveFactoryMapStateToSession();
    _isFactoryMapOpen = false;
    _runtimeLayoutState.WasFactoryMapOpen = false;
    _factoryMapWindow.Close();
    _factoryMapWindow = null;
}
```

注意：

- 需要避免 `Closed` 事件重复保存或重复置空导致异常。
- 可以通过判断 `_factoryMapWindow is null` 保持幂等。

### 5.2 ToggleFactoryMapWindow 复用统一关闭

当前：

```csharp
if (_factoryMapWindow is { IsVisible: true })
{
    ...
}
```

建议改为：

```csharp
if (_factoryMapWindow is { IsVisible: true })
{
    CloseFactoryMapByUserAction();
    return;
}
```

这样主界面地图按钮关闭和地图快捷键关闭都会走统一逻辑。

### 5.3 地图窗口最小化转关闭

创建地图窗口后订阅：

```csharp
_factoryMapWindow.StateChanged += FactoryMapWindow_StateChanged;
```

新增：

```csharp
private void FactoryMapWindow_StateChanged(object? sender, EventArgs e)
{
    if (_factoryMapWindow is null || _factoryMapWindow.WindowState != WindowState.Minimized)
    {
        return;
    }

    CloseFactoryMapByUserAction();
}
```

行为：

- 用户点击地图窗口最小化按钮。
- WPF 进入 `WindowState.Minimized`。
- 立即拦截并关闭地图。
- 用户不会看到屏幕底部小窗口长期存在。

### 5.4 地图窗口关闭按钮同步状态

当前 `Closed` 回调需要同步地图打开状态：

```csharp
_isFactoryMapOpen = false;
_runtimeLayoutState.WasFactoryMapOpen = false;
```

建议：

```csharp
_factoryMapWindow.Closed += (_, _) =>
{
    SaveFactoryMapStateToSession();
    _isFactoryMapOpen = false;
    _runtimeLayoutState.WasFactoryMapOpen = false;
    _factoryMapWindow = null;
};
```

这样用户点地图窗口右上角 X，也不会留下内部状态不一致。

### 5.5 退出流程不应误判为用户关闭

当前程序退出时已有：

```csharp
CloseFactoryMapForExit()
```

该方法应保留独立语义：

- 程序退出时关闭地图。
- 不需要模拟用户关闭。
- 可继续设置 `_isFactoryMapOpen = false` 和 `WasFactoryMapOpen = false`。

如果 `CloseFactoryMapForExit()` 与 `Closed` 回调重复设置状态，应确保无副作用。

### 5.6 主窗口最小化仍隐藏地图

当前主窗口最小化：

```csharp
MainWindow_StateChanged
```

会调用：

```csharp
HideFactoryMapWindow();
```

该行为不变。

区别：

```text
主窗口最小化：地图跟随隐藏，保留“之前打开过”的运行期意图。
地图窗口自己最小化：等同用户主动关闭地图，不保留打开意图。
```

这是两个不同语义，不要混淆。

## 6. 边界规则

### 6.1 地图窗口最小化

用户点击地图窗口最小化：

```text
保存地图状态
关闭地图窗口
地图按钮状态视为关闭
下次点击地图按钮重新打开
```

### 6.2 地图窗口关闭按钮 X

用户点击地图窗口 X：

```text
保存地图状态
关闭地图窗口
地图按钮状态视为关闭
下次点击地图按钮重新打开
```

### 6.3 主界面地图按钮

用户点击主界面地图按钮：

- 地图未打开：打开地图。
- 地图已打开：调用 `CloseFactoryMapByUserAction()`。

### 6.4 地图快捷键

用户按地图快捷键：

- 地图未打开：打开地图。
- 地图已打开：调用 `CloseFactoryMapByUserAction()`。

### 6.5 主窗口最小化

主窗口最小化或隐藏到托盘：

- 继续调用 `HideFactoryMapWindow()`。
- 不改变本次需求。
- 不等同用户主动关闭地图。

### 6.6 程序退出

程序真正退出：

- 继续调用 `CloseFactoryMapForExit()`。
- 不需要走用户关闭入口。
- 需要保持无异常、无残留窗口。

## 7. 测试建议

### 7.1 纯逻辑测试

建议新增或扩展：

```text
VSLoader.Tests/MainWindowFactoryMapCloseTests.cs
```

新增静态判断方法便于测试：

```csharp
internal static bool ShouldCloseFactoryMapOnStateChanged(WindowState state)
{
    return state == WindowState.Minimized;
}
```

测试：

- `WindowState.Minimized` 返回 true。
- `WindowState.Normal` 返回 false。
- `WindowState.Maximized` 返回 false。

### 7.2 状态同步测试

如果已有主窗口状态方法可测，建议覆盖：

- 用户主动关闭后 `_isFactoryMapOpen` 应为 false。
- 用户主动关闭后 `WasFactoryMapOpen` 应为 false。
- 地图窗口 Closed 回调后状态应同步为 false。

如果难以直接测试私有字段，至少测试新增的静态判断方法，并依靠构建回归。

### 7.3 回归验证

必须运行：

```powershell
dotnet test .\VSLoader.sln -c Debug --no-restore
dotnet build .\VSLoader.sln -c Debug --no-restore
```

## 8. 手动验证清单

1. 打开主窗口。
2. 点击“地图”按钮，地图窗口打开。
3. 点击地图窗口最小化按钮。
4. 地图窗口应直接关闭，不应在屏幕底部出现小窗口。
5. 再次点击“地图”按钮，地图应能重新打开。
6. 打开地图后点击地图窗口右上角 X。
7. 再次点击“地图”按钮，地图应能重新打开。
8. 打开地图后按地图快捷键 `M`，地图关闭。
9. 再按 `M`，地图重新打开。
10. 主窗口最小化到托盘时，地图仍按原逻辑隐藏。
11. 托盘恢复主窗口后，地图恢复逻辑不被破坏。
12. 程序退出时无残留地图窗口。

## 9. 验收标准

满足以下条件即完成：

- 地图窗口点击最小化后不会显示屏幕底部小窗口。
- 地图窗口最小化等同关闭地图。
- 地图窗口右上角 X 关闭后，内部地图打开状态同步为关闭。
- 主界面地图按钮、地图快捷键、地图窗口最小化、地图窗口 X 的用户关闭语义一致。
- 主窗口最小化到托盘逻辑不受影响。
- 程序退出清理逻辑不受影响。
- 地图窗口位置、大小、缩放状态仍按现有逻辑保存。
- `dotnet test .\VSLoader.sln -c Debug --no-restore` 通过。
- `dotnet build .\VSLoader.sln -c Debug --no-restore` 通过。
