# v102 工厂地图画布与节点区分度优化编码规格说明

## 1. 背景

当前工厂地图窗口中：

- 地图画布背景为非常浅的 `#F8FAFC`
- 设备节点背景为纯白色
- 节点边框为浅灰蓝 `#CBD5E1`

这套颜色很干净，但画布和节点都偏白，区分度不够。节点较多或缩放后，容易出现视觉上“白成一片”的感觉，用户反馈容易晃眼。

本次优化目标是提高画布和节点的视觉分离感，让节点更自然地从背景中浮出来，但不引入花哨装饰，不影响地图性能。

## 2. 目标

优化工厂地图视觉层级：

- 地图画布背景略微降亮，改为更明确的浅灰蓝。
- 设备节点保持白色背景。
- 设备节点默认边框略微加深。
- 选中态、高亮态、连线模式起点态保持现有蓝色语义。
- 不新增阴影。
- 不新增网格背景。
- 不改变地图拖拽、缩放、选中、连线、右键菜单等交互逻辑。

## 3. 设计原则

### 3.1 优先靠颜色层级，不靠装饰

本次不使用明显阴影、渐变、网格、纹理等装饰。

原因：

- 地图节点数量可能较多，阴影会增加视觉噪音。
- 网格可能和工序连线抢视觉注意力。
- 该窗口是工具型界面，应保持清爽、稳定、可读。

### 3.2 节点仍然是信息主体

视觉重点应在：

- 节点名称
- 原始机型名
- 节点之间的连线
- 当前选中/高亮节点

背景只负责提供承托，不应该变成视觉主体。

### 3.3 最小修改

只调整现有颜色常量和刷子使用位置。

不调整：

- 节点尺寸
- 字体
- 行间距
- 布局算法
- 缩放算法
- 连线算法
- 保存格式

## 4. 影响范围

主要修改：

- `VSLoader/Views/FactoryMapWindow.xaml`
- `VSLoader/Views/FactoryMapWindow.xaml.cs`

不修改：

- 地图 JSON 数据结构
- 地图节点坐标
- 节点拖拽与多选逻辑
- 连线编辑逻辑
- 主界面列表样式
- 其他弹窗或窗口

## 5. 当前代码定位

### 5.1 地图背景

当前 XAML：

```xml
<Grid x:Name="MapViewport"
      Background="#F8FAFC">
```

以及：

```xml
<Canvas x:Name="MapCanvas"
        Background="#F8FAFC">
```

这两个背景色应保持一致。

### 5.2 节点默认视觉

当前节点创建时：

```csharp
Background = WpfBrushes.White,
BorderBrush = new SolidColorBrush(WpfColor.FromRgb(203, 213, 225)),
BorderThickness = new Thickness(1),
```

当前普通状态：

```csharp
private static void ApplyDeviceNormalVisual(Border border)
{
    border.Background = WpfBrushes.White;
    border.BorderBrush = new SolidColorBrush(WpfColor.FromRgb(203, 213, 225));
    border.BorderThickness = new Thickness(1);
}
```

### 5.3 选中/高亮状态

当前选中和高亮状态：

```csharp
Background = #EFF6FF
BorderBrush = #3B82F6
BorderThickness = 2
```

这部分不需要改。

### 5.4 连线

当前连线颜色：

```csharp
Stroke = new SolidColorBrush(WpfColor.FromRgb(148, 163, 184))
```

该颜色已经是中性灰蓝，暂不强制修改。

## 6. 推荐配色

### 6.1 地图画布背景

推荐改为：

```text
#EEF2F7
```

理由：

- 比 `#F8FAFC` 更明确地区分白色节点。
- 仍然非常浅，不会压过节点和文字。
- 偏灰蓝，和现有系统色相协调。

备选：

```text
#F1F5F9
```

如果觉得 `#EEF2F7` 稍重，可以用该备选。

本次推荐使用 `#EEF2F7`。

### 6.2 节点默认背景

保持：

```text
White
```

理由：

- 节点是信息卡片，白色利于阅读。
- 背景降亮后，白色节点会自然突出。

### 6.3 节点默认边框

推荐从：

```text
#CBD5E1
```

改为：

```text
#A8B3C3
```

理由：

- 比原边框更清晰。
- 比 `#94A3B8` 更柔和，不会显得节点过重。
- 与现有连线 `#94A3B8` 有层级差异。

### 6.4 选中/高亮状态

保持现状：

```text
背景 #EFF6FF
边框 #3B82F6
```

理由：

- 用户已经熟悉蓝色选中语义。
- 不扩大改动范围。

## 7. 实现要求

### 7.1 统一画布背景

将 `MapViewport` 和 `MapCanvas` 的背景都改为：

```xml
Background="#EEF2F7"
```

避免缩放、拖拽或画布边界处出现两种背景色。

### 7.2 统一节点默认边框

将节点创建时的默认边框和 `ApplyDeviceNormalVisual()` 中的边框都改为：

```csharp
new SolidColorBrush(WpfColor.FromRgb(168, 179, 195))
```

对应十六进制：

```text
#A8B3C3
```

确保：

- 新创建节点时颜色正确。
- 从选中态恢复普通态时颜色也正确。

### 7.3 不加阴影

不要添加：

```csharp
DropShadowEffect
```

也不要添加 XAML 阴影。

### 7.4 不加网格

不要新增：

- 网格线
- 背景纹理
- 额外 Canvas 图层

本次只处理颜色层级。

## 8. 行为要求

改动后：

- 地图打开速度不受影响。
- 鼠标滚轮缩放不受影响。
- 空白区域拖拽平移不受影响。
- 节点拖拽不受影响。
- 多选节点不受影响。
- 连线编辑不受影响。
- 右键菜单不受影响。
- 选中节点和左侧快捷项联动高亮不受影响。

## 9. 测试建议

该改动主要是视觉颜色调整，建议：

1. 构建验证：

```powershell
dotnet build .\VSLoader.sln -c Debug --no-restore
```

2. 回归测试：

```powershell
dotnet test .\VSLoader.sln -c Debug --no-restore
```

3. 手动验证：

- 打开地图窗口，确认画布背景不再接近纯白。
- 普通节点白色背景和画布背景有明显但柔和的区分。
- 节点文字仍然清晰。
- 选中态蓝色仍然明显。
- 连线没有被背景吞掉。
- 缩放和平移时背景一致，不出现白色边缘。

## 10. 验收标准

满足以下条件即完成：

- 地图背景不再与白色节点过度接近。
- 普通节点边界比之前更清晰。
- 整体仍然保持轻量、干净、工具型界面风格。
- 不新增阴影或网格。
- 不影响地图交互逻辑。
- `dotnet test .\VSLoader.sln -c Debug --no-restore` 通过。
- `dotnet build .\VSLoader.sln -c Debug --no-restore` 通过。

