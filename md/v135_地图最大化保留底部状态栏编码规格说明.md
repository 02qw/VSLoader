# v135 地图最大化保留底部状态栏编码规格说明

## 1. 背景

当前工厂地图窗口左下角有一行状态信息，用于显示：

```text
设备数量
连线数量
视口大小
缩放比例
地图边界
```

窗口化时，左下角状态栏可以正常显示。

但是用户反馈：

```text
地图窗口最大化后，左下角状态栏信息会丢失或被遮挡。
窗口化时状态栏又能显示。
```

截图表现为：

```text
窗口化：底部状态栏在窗口内正常显示。
最大化：底部状态栏区域被屏幕底部任务栏/窗口边界遮住，看起来像信息丢失。
```

本次需求是：

```text
无论地图窗口最大化还是窗口化，左下角状态信息都必须保留并可见。
```

## 2. 当前代码现状

### 2.1 状态栏布局

文件：

```text
VSLoader/Views/FactoryMapWindow.xaml
```

当前状态栏位于地图窗口的 `DockPanel.Bottom`：

```xml
<Border DockPanel.Dock="Bottom"
        Padding="10,7"
        Background="{StaticResource ModernSurfaceBrush}"
        BorderBrush="{StaticResource ModernSurfaceBorderBrush}"
        BorderThickness="0,1,0,0">
    <TextBlock x:Name="StatusText"
               Foreground="#64748B"
               FontFamily="Microsoft YaHei UI"
               FontSize="12"
               Text="地图未加载" />
</Border>
```

这个布局本身是合理的：

```text
顶部工具栏 DockPanel.Dock="Top"
底部状态栏 DockPanel.Dock="Bottom"
中间地图视口占用剩余空间
```

因此状态栏不应该因为最大化而从布局中消失。

### 2.2 状态文本刷新逻辑

文件：

```text
VSLoader/Views/FactoryMapWindow.xaml.cs
```

状态文本通过：

```csharp
private void RefreshStatusText()
```

刷新。

格式化方法：

```csharp
internal static string FormatDebugStatusText(
    string baseText,
    double viewportWidth,
    double viewportHeight,
    double scale,
    string bounds)
```

已有测试：

```text
VSLoader.Tests/FactoryMapStatusTextTests.cs
```

说明状态文本本身没有丢失。

### 2.3 自定义窗口外壳

文件：

```text
VSLoader/Views/FactoryMapWindow.xaml
```

地图窗口使用无系统边框窗口：

```xml
WindowStyle="None"
```

并使用：

```xml
<shell:WindowChrome CaptionHeight="44"
                    CornerRadius="0"
                    GlassFrameThickness="0"
                    ResizeBorderThickness="6"
                    UseAeroCaptionButtons="False" />
```

最大化按钮逻辑来自通用标题栏：

文件：

```text
VSLoader/Views/Controls/ModernTitleBar.xaml.cs
```

当前最大化逻辑：

```csharp
window.WindowState = window.WindowState == WindowState.Maximized
    ? WindowState.Normal
    : WindowState.Maximized;
```

问题点：

```text
无边框自定义 WindowChrome 窗口在最大化时，容易覆盖到整块屏幕区域。
如果最大化区域没有正确限制到当前屏幕 WorkingArea，底部内容会被任务栏遮住。
```

因此本问题的根因不是 `StatusText` 没刷新，而是：

```text
最大化后的窗口可视区域没有避开 Windows 任务栏。
```

## 3. 总体目标

### 3.1 用户目标

用户最大化地图窗口后：

```text
左下角状态栏仍然显示在任务栏上方。
状态栏内容和窗口化状态一致。
```

用户从最大化恢复窗口化后：

```text
窗口回到正常可拖动/可缩放状态。
底部状态栏仍然可见。
```

### 3.2 技术目标

1. 地图窗口最大化时使用当前屏幕的工作区 `WorkingArea`。
2. 最大化后不要覆盖 Windows 任务栏。
3. 支持任务栏在底部、顶部、左侧、右侧。
4. 支持多显示器，以地图窗口当前所在屏幕为准。
5. 不改变地图内部布局结构。
6. 不改变状态栏文本生成逻辑。
7. 不影响窗口化时的拖动、缩放和位置持久化。

## 4. 设计原则

### 4.1 修窗口边界，不修状态栏布局

状态栏当前在 `DockPanel.Dock="Bottom"`，布局语义正确。

本次不应该通过以下方式绕过：

```text
给状态栏加固定 MarginBottom
给地图窗口加固定 Padding
给底部状态栏写死高度补偿
```

原因：

```text
任务栏高度、位置、多屏 DPI 都可能变化。
固定补偿很容易在另一台电脑上出问题。
```

正确方向是：

```text
最大化窗口本身应该限制到屏幕 WorkingArea。
```

### 4.2 只处理 FactoryMapWindow

本次用户反馈只针对地图窗口。

不改：

```text
MainWindow
SettingsWindow
WorkspaceSelectorWindow
MessageDialogWindow
ShortcutEditWindow
BatchImportWindow
```

如果后续发现其他窗口也有同类最大化遮挡问题，再抽公共能力。

### 4.3 不改变地图视图状态

最大化工作区约束只影响：

```text
Window.Left
Window.Top
Window.Width
Window.Height
MaxWidth
MaxHeight
```

不直接修改：

```text
fitScale
userScale
mapOffsetX
mapOffsetY
FactoryMapView
```

地图视图刷新仍由现有 `SizeChanged` 和 `RefreshStatusText()` 触发。

## 5. 详细实现方案

### 5.1 在 FactoryMapWindow 监听 StateChanged

文件：

```text
VSLoader/Views/FactoryMapWindow.xaml.cs
```

在构造函数中新增：

```csharp
StateChanged += FactoryMapWindow_StateChanged;
```

新增方法：

```csharp
private void FactoryMapWindow_StateChanged(object? sender, EventArgs e)
{
    if (WindowState == WindowState.Maximized)
    {
        ApplyMaximizedWorkingArea();
        return;
    }

    ClearMaximizedWorkingAreaConstraint();
}
```

说明：

```text
进入最大化时，将窗口限制到工作区。
恢复窗口化时，清除最大化约束，避免用户后续拖动/缩放被 MaxWidth/MaxHeight 限制。
```

### 5.2 获取当前窗口所在屏幕工作区

文件：

```text
VSLoader/Views/FactoryMapWindow.xaml.cs
```

需要引入：

```csharp
using System.Windows.Interop;
using WinForms = System.Windows.Forms;
```

实现：

```csharp
private WinForms.Rectangle GetCurrentScreenWorkingArea()
{
    var handle = new WindowInteropHelper(this).Handle;
    return handle == IntPtr.Zero
        ? WinForms.Screen.PrimaryScreen.WorkingArea
        : WinForms.Screen.FromHandle(handle).WorkingArea;
}
```

注意：

```text
System.Windows.Forms.Rectangle 单位是物理像素。
WPF Window.Left/Top/Width/Height 使用 DIP。
需要做 DPI 换算。
```

### 5.3 像素到 WPF DIP 换算

文件：

```text
VSLoader/Views/FactoryMapWindow.xaml.cs
```

新增方法：

```csharp
private Rect ConvertScreenBoundsToDip(WinForms.Rectangle bounds)
{
    var source = PresentationSource.FromVisual(this);
    if (source?.CompositionTarget is null)
    {
        return new Rect(bounds.Left, bounds.Top, bounds.Width, bounds.Height);
    }

    var transform = source.CompositionTarget.TransformFromDevice;
    var topLeft = transform.Transform(new Point(bounds.Left, bounds.Top));
    var bottomRight = transform.Transform(new Point(bounds.Right, bounds.Bottom));
    return new Rect(topLeft, bottomRight);
}
```

说明：

```text
这保证高 DPI、多屏 DPI 环境下，工作区尺寸不会换算错误。
```

### 5.4 应用最大化工作区

文件：

```text
VSLoader/Views/FactoryMapWindow.xaml.cs
```

新增：

```csharp
private void ApplyMaximizedWorkingArea()
{
    var workingArea = ConvertScreenBoundsToDip(GetCurrentScreenWorkingArea());

    MaxWidth = workingArea.Width;
    MaxHeight = workingArea.Height;
    Left = workingArea.Left;
    Top = workingArea.Top;
    Width = workingArea.Width;
    Height = workingArea.Height;
}
```

说明：

```text
将最大化窗口限制在当前屏幕工作区。
任务栏无论在哪个方向，WorkingArea 都会自动避开。
```

### 5.5 清除最大化约束

文件：

```text
VSLoader/Views/FactoryMapWindow.xaml.cs
```

新增：

```csharp
private void ClearMaximizedWorkingAreaConstraint()
{
    MaxWidth = double.PositiveInfinity;
    MaxHeight = double.PositiveInfinity;
}
```

注意：

```text
不要在这里改 Left/Top/Width/Height。
恢复窗口化的尺寸应交给 WPF 自己的 RestoreBounds / WindowState 机制。
```

### 5.6 避免 StateChanged 重入

设置 `Left/Top/Width/Height` 一般不会再次触发 `StateChanged`，但为稳妥可以加防重入字段：

```csharp
private bool isApplyingMaximizedWorkingArea;
```

实现：

```csharp
private void ApplyMaximizedWorkingArea()
{
    if (isApplyingMaximizedWorkingArea)
    {
        return;
    }

    isApplyingMaximizedWorkingArea = true;
    try
    {
        ...
    }
    finally
    {
        isApplyingMaximizedWorkingArea = false;
    }
}
```

本字段只放在 `FactoryMapWindow` 内部。

## 6. 测试设计

### 6.1 静态测试：地图窗口监听 StateChanged

文件：

```text
VSLoader.Tests/FactoryMapWindowMaximizeTests.cs
```

读取：

```text
VSLoader/Views/FactoryMapWindow.xaml.cs
```

断言：

```text
构造函数中订阅 StateChanged。
存在 FactoryMapWindow_StateChanged。
最大化时调用 ApplyMaximizedWorkingArea。
非最大化时调用 ClearMaximizedWorkingAreaConstraint。
```

示例：

```csharp
[Fact]
public void Factory_map_window_constrains_maximized_state_to_working_area()
{
    var code = File.ReadAllText(TestProjectPaths.GetProjectFilePath(
        "VSLoader",
        "Views",
        "FactoryMapWindow.xaml.cs"));

    Assert.Contains("StateChanged += FactoryMapWindow_StateChanged", code);
    Assert.Contains("ApplyMaximizedWorkingArea", code);
    Assert.Contains("ClearMaximizedWorkingAreaConstraint", code);
    Assert.Contains("Screen.FromHandle", code);
    Assert.Contains("WorkingArea", code);
}
```

### 6.2 静态测试：不通过状态栏固定补偿解决

文件：

```text
VSLoader.Tests/FactoryMapWindowMaximizeTests.cs
```

读取：

```text
VSLoader/Views/FactoryMapWindow.xaml
```

断言：

```text
StatusText 仍然位于 DockPanel.Dock="Bottom"。
不要出现针对 StatusText 的固定 MarginBottom 或 PaddingBottom 补偿。
```

可以只做正向断言：

```csharp
Assert.Contains("DockPanel.Dock=\"Bottom\"", xaml);
Assert.Contains("x:Name=\"StatusText\"", xaml);
```

### 6.3 保留现有状态文本测试

继续运行已有测试：

```text
VSLoader.Tests/FactoryMapStatusTextTests.cs
```

确保本次不影响状态文本格式。

## 7. 验证命令

### 7.1 定向测试

```powershell
dotnet test .\VSLoader.Tests\VSLoader.Tests.csproj -c Debug --no-restore --filter "FullyQualifiedName~FactoryMapWindowMaximizeTests|FullyQualifiedName~FactoryMapStatusTextTests"
```

### 7.2 地图窗口相关测试

```powershell
dotnet test .\VSLoader.Tests\VSLoader.Tests.csproj -c Debug --no-restore --filter "FullyQualifiedName~FactoryMapWindowTaskbarTests|FullyQualifiedName~ModernWindowChromeUsageTests|FullyQualifiedName~FactoryMapWindowTitleBarTests"
```

### 7.3 Debug 构建

```powershell
dotnet build .\VSLoader.sln -c Debug --no-restore
```

如果 Debug 输出目录被正在运行的 VSLoader 占用，先关闭程序后重试。

## 8. 手动验证流程

实现后手动验证：

```text
1. 打开 VSLoader。
2. 打开工厂地图。
3. 确认窗口化状态下左下角状态栏可见。
4. 点击地图窗口最大化按钮。
5. 确认左下角状态栏仍然显示在任务栏上方。
6. 点击还原按钮恢复窗口化。
7. 确认左下角状态栏仍然可见。
```

继续验证：

```text
1. 将任务栏放在屏幕底部。
2. 最大化地图窗口。
3. 状态栏不能被底部任务栏遮挡。
```

如果方便，继续验证：

```text
1. 将任务栏移动到左侧/顶部/右侧。
2. 最大化地图窗口。
3. 地图窗口不能覆盖任务栏。
4. 左下角状态栏仍在窗口可见区域内。
```

多屏验证：

```text
1. 将地图窗口拖到第二屏。
2. 点击最大化。
3. 地图窗口应最大化到第二屏工作区。
4. 左下角状态栏仍然可见。
```

## 9. 边界情况清单

实现完成后必须逐条确认：

```text
1. 窗口化时状态栏显示。
2. 最大化时状态栏显示。
3. 最大化时窗口不覆盖 Windows 任务栏。
4. 还原窗口化后不被 MaxWidth/MaxHeight 限制。
5. 任务栏在底部时正常。
6. 任务栏在顶部/左侧/右侧时正常。
7. 多屏时按当前地图窗口所在屏幕的 WorkingArea 最大化。
8. 高 DPI 下窗口尺寸换算不明显偏移。
9. 状态栏文字格式不变。
10. 地图内部缩放、拖动、节点显示不受影响。
11. 地图窗口关闭后 Alt + X 恢复尺寸逻辑不受影响。
```

## 10. 不做的事情

本次不做：

```text
1. 不修改 StatusText 文案。
2. 不修改 RefreshStatusText 逻辑。
3. 不修改地图节点、连线、缩放算法。
4. 不改变地图窗口标题栏按钮视觉样式。
5. 不抽公共最大化服务到所有窗口。
6. 不新增窗口状态持久化字段。
```

## 11. 预期效果

完成后：

```text
地图窗口最大化时，会使用当前屏幕工作区。
窗口底部不会被任务栏遮住。
左下角状态栏在最大化和窗口化两种状态下都可见。
```

最终用户感受：

```text
地图最大化后仍然像正常产品窗口一样保留底部信息栏。
不会再出现窗口化有状态栏、最大化状态栏丢失的割裂体验。
```
