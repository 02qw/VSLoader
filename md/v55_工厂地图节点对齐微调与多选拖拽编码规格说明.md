# v55 工厂地图节点对齐微调与多选拖拽编码规格说明

## 1. 需求背景

当前工厂地图已经支持：

```text
1. 节点拖拽调整位置。
2. 节点位置持久化保存。
3. 节点之间手动连线。
4. 图文件导入导出。
```

现在地图编辑时有两个体验问题：

```text
1. 人手拖拽节点时容易产生细微偏移，导致节点不齐、连线视觉上有瑕疵。
2. 需要移动一组设备时，只能一个节点一个节点拖动，效率低，也容易破坏相对位置。
```

因此需要增加：

```text
1. 节点位置对齐和键盘微调能力。
2. 多选节点能力。
3. 多选后一组节点整体拖动能力。
```

## 2. 需求目标

本次目标：

```text
让工厂地图编辑更精确、更稳定、更适合批量调整布局。
```

完成后用户可以：

1. 拖动节点结束后，节点自动吸附到网格，减少小数坐标和轻微抖动。
2. 选中节点后，用方向键微调位置。
3. 点击“多选节点”按钮进入多选模式。
4. 在空白区域按住鼠标左键拖出蓝色矩形框。
5. 被蓝色矩形覆盖的节点一起被选中。
6. 拖动任意已选节点时，所有已选节点作为整体移动。
7. 整体移动结束后保存所有选中节点的新位置。
8. 再次点击“多选节点”按钮退出多选模式并清空选择。

## 3. 非目标范围

本次不实现：

```text
1. 自动流程布局。
2. 智能避让连线。
3. 节点自动按工序排序。
4. 多选后的复制、删除、分组。
5. 框选区域跨窗口拖拽。
6. 多选集合持久化。
7. 复杂吸附参考线。
8. 撤销/重做。
```

本次只处理：

```text
节点编辑精度 + 多选移动。
```

## 4. 总体设计

地图编辑工具区建议变为：

```text
[锁定/编辑] [连线] [多选节点] [导入图文件] [导出图文件]    状态说明文本
```

模式关系：

```text
1. 连线模式和多选节点模式互斥。
2. 开启连线模式时，自动退出多选节点模式。
3. 开启多选节点模式时，自动退出连线模式。
4. 锁定浏览模式下，不能开启连线模式，也不能开启多选节点模式。
5. 退出编辑模式时，同时退出连线模式和多选节点模式，并清空选择。
```

## 5. 网格吸附规则

为了解决拖拽后节点细微偏移问题，采用轻量网格吸附方案。

### 5.1 网格大小

定义常量：

```csharp
private const double SnapGridSize = 10;
```

### 5.2 吸附时机

以下时机必须执行网格吸附：

```text
1. 单节点拖拽结束。
2. 多选节点整体拖拽结束。
3. 键盘方向键移动后。
```

### 5.3 吸附算法

```csharp
private static double SnapToGrid(double value)
{
    return Math.Round(value / SnapGridSize) * SnapGridSize;
}
```

坐标约束：

```csharp
device.X = Math.Max(0, SnapToGrid(device.X));
device.Y = Math.Max(0, SnapToGrid(device.Y));
```

说明：

```text
不采用“自动吸附到其他节点”的模糊算法。
原因是该算法可能让节点被吸到用户不想要的位置。
网格吸附更稳定、可预期、实现轻量。
```

## 6. 键盘微调规则

### 6.1 触发条件

方向键微调只在以下条件下生效：

```text
1. 地图窗口处于编辑模式。
2. 当前存在已选中的节点。
3. 焦点在地图窗口或地图画布内。
```

### 6.2 移动步长

建议：

```text
方向键：移动 10px。
Ctrl + 方向键：移动 50px。
Shift + 方向键：移动 1px。
```

说明：

```text
默认方向键使用 10px，和网格吸附一致。
Shift 提供精细调整。
Ctrl 提供快速移动。
```

### 6.3 作用对象

```text
如果只选中一个节点，则移动该节点。
如果多选模式中选中多个节点，则整体移动所有选中节点。
```

### 6.4 保存策略

方向键移动后：

```text
1. 立即刷新连线。
2. 立即保存布局。
```

考虑到用户可能连续按方向键，为避免过度复杂，本次可以先采用立即保存。

如果后续出现性能问题，再增加延迟保存。

## 7. 多选节点模式

### 7.1 顶部按钮

新增按钮：

```text
多选节点
```

按钮行为：

```text
点击一次：进入多选节点模式。
再次点击：退出多选节点模式，清空已选节点。
```

锁定模式下点击：

```text
提示：请先切换到编辑模式后再进行多选。
```

### 7.2 多选状态字段

建议在 `FactoryMapWindow.xaml.cs` 中新增：

```csharp
private bool isMultiSelectMode;
private bool isSelectingNodes;
private bool isDraggingSelectedNodes;
private readonly HashSet<FactoryMapDeviceViewNode> selectedDevices = [];
private readonly Dictionary<FactoryMapDeviceViewNode, Border> elementByDevice = [];
private readonly Dictionary<FactoryMapDeviceViewNode, WpfPoint> multiDragStartPositions = [];
private Rectangle? selectionRectangle;
private WpfPoint selectionStartPoint;
```

说明：

```text
isMultiSelectMode：
    是否处于多选节点模式。

isSelectingNodes：
    是否正在拖动蓝色框选区域。

isDraggingSelectedNodes：
    是否正在整体拖动已选节点。

selectedDevices：
    当前选中的节点集合。

elementByDevice：
    从节点数据反查 UI 元素，方便批量更新视觉状态。

multiDragStartPositions：
    整体拖动开始时记录每个节点的原始坐标。

selectionRectangle：
    当前显示的蓝色半透明框选矩形。
```

## 8. 框选交互

### 8.1 开始框选

多选模式下，用户在地图空白区域按下鼠标左键：

```text
1. 记录 selectionStartPoint。
2. 创建蓝色半透明矩形 selectionRectangle。
3. 添加到 MapCanvas。
4. CaptureMouse。
5. 进入 isSelectingNodes = true。
```

注意：

```text
点击节点本身不开始框选。
点击空白区域才开始框选。
```

### 8.2 框选中

鼠标移动时：

```text
1. 根据起点和当前鼠标位置计算矩形区域。
2. 更新 selectionRectangle 的 Canvas.Left / Canvas.Top / Width / Height。
3. 实时计算被矩形覆盖的节点。
4. 覆盖到的节点显示选中视觉。
```

覆盖判断：

```text
节点矩形和框选矩形有交集，就认为被选中。
不要求节点完全被框住。
```

推荐算法：

```csharp
private static bool RectIntersects(WpfRect first, WpfRect second)
{
    return first.IntersectsWith(second);
}
```

### 8.3 结束框选

鼠标松开时：

```text
1. 删除 selectionRectangle。
2. ReleaseMouseCapture。
3. isSelectingNodes = false。
4. 保留 selectedDevices。
5. 状态栏提示：已选中 N 个节点。
```

如果没有框中任何节点：

```text
清空选择。
状态栏提示：未选中节点。
```

## 9. 多选整体拖动

### 9.1 开始整体拖动

多选模式下，如果用户按下的是已选中的节点：

```text
1. 不启动单节点拖拽。
2. 进入 isDraggingSelectedNodes = true。
3. 记录 lastDragPoint。
4. 记录 selectedDevices 中每个节点的原始坐标到 multiDragStartPositions。
5. CaptureMouse。
```

### 9.2 拖动中

鼠标移动时：

```text
1. 计算当前鼠标点和拖动起点的 delta。
2. 对 selectedDevices 中每个节点应用：
   X = 原始 X + delta.X / totalScale
   Y = 原始 Y + delta.Y / totalScale
3. 坐标最小为 0。
4. 更新每个节点对应 Border 的 Canvas.Left / Canvas.Top。
5. 实时刷新连线。
```

为了避免每一帧完全重建所有节点，可接受的 MVP 做法：

```text
拖动中只更新节点 Border 位置。
拖动结束后调用 RenderCurrentMap(resetView: false) 重建连线。
```

如果希望拖动中连线也跟随，可后续优化。

### 9.3 结束整体拖动

鼠标松开时：

```text
1. 对所有 selectedDevices 执行 SnapToGrid。
2. RenderCurrentMap(resetView: false)。
3. 保存布局。
4. ReleaseMouseCapture。
5. isDraggingSelectedNodes = false。
6. 保留选中状态。
7. 状态栏提示：已移动 N 个节点，地图布局已保存。
```

## 10. 单节点拖拽改造

现有单节点拖拽逻辑需要增加：

```text
1. 拖拽结束后 SnapToGrid。
2. 拖拽结束后保持该节点为当前选中节点。
3. 如果当前处于多选模式且点击的是已选节点，则进入整体拖动，而不是单节点拖动。
```

单节点拖拽结束：

```csharp
activeDevice.X = Math.Max(0, SnapToGrid(activeDevice.X));
activeDevice.Y = Math.Max(0, SnapToGrid(activeDevice.Y));
```

## 11. 选中视觉

### 11.1 单选/多选节点样式

普通节点：

```text
白底
灰色边框
1px 边框
```

选中节点：

```text
浅蓝底
蓝色边框
2px 边框
```

连线起点节点可继续使用更强蓝色样式。

如果节点同时是多选选中和连线起点：

```text
连线起点样式优先。
```

### 11.2 框选矩形样式

```text
Fill: 半透明蓝色
Stroke: 蓝色
StrokeThickness: 1
IsHitTestVisible: false
```

示例：

```csharp
Fill = new SolidColorBrush(Color.FromArgb(35, 37, 99, 235))
Stroke = new SolidColorBrush(Color.FromRgb(37, 99, 235))
```

## 12. 模式互斥规则

### 12.1 开启连线模式

```text
1. 如果当前是多选模式，退出多选模式。
2. 清空 selectedDevices。
3. 删除框选矩形。
4. 再进入连线模式。
```

### 12.2 开启多选模式

```text
1. 如果当前是连线模式，退出连线模式。
2. 清空 pendingConnectionStart。
3. 再进入多选模式。
```

### 12.3 退出编辑模式

```text
1. 退出连线模式。
2. 退出多选模式。
3. 清空 pendingConnectionStart。
4. 清空 selectedDevices。
5. 删除 selectionRectangle。
```

## 13. 与缩放/平移的关系

多选模式下：

```text
1. 鼠标滚轮缩放仍然可用。
2. 空白区域左键拖动默认用于框选，不再用于平移。
3. 如需平移，可以先退出多选模式，或用滚轮缩放后再调整。
```

锁定浏览模式下：

```text
原有空白拖动平移逻辑保持不变。
```

编辑模式但非多选模式下：

```text
原有空白拖动平移逻辑保持不变。
```

## 14. 坐标换算

鼠标点来自视口坐标，需要换算成地图坐标。

推荐新增方法：

```csharp
private WpfPoint ViewportPointToMapPoint(WpfPoint point)
{
    var scale = GetTotalScale();
    if (scale <= 0)
    {
        return new WpfPoint(0, 0);
    }

    return new WpfPoint(
        (point.X - mapOffsetX) / scale,
        (point.Y - mapOffsetY) / scale);
}
```

框选矩形应该使用地图坐标绘制在 `MapCanvas` 上。

## 15. 预计修改文件

预计修改：

```text
VSLoader/Views/FactoryMapWindow.xaml
VSLoader/Views/FactoryMapWindow.xaml.cs
```

可能新增测试：

```text
VSLoader.Tests/FactoryMapSnapTests.cs
```

如需测试纯算法，建议把网格吸附和矩形交集逻辑抽成 internal 静态类：

```text
VSLoader/Models/FactoryMapEditMath.cs
```

## 16. 自动化测试建议

WPF 鼠标拖拽不强求自动化测试。

建议测试纯算法：

### 16.1 SnapToGrid

```text
输入 327.42，期望 330。
输入 324.2，期望 320。
输入 -3，最终坐标约束后为 0。
```

### 16.2 RectIntersects

```text
框选矩形和节点矩形有交集，返回 true。
框选矩形和节点矩形完全分离，返回 false。
```

### 16.3 多选移动 delta

如果抽出算法：

```text
节点 A: 100, 100
节点 B: 200, 100
delta: 35, 15

拖动结束并吸附 10px 网格后：
A: 140, 120
B: 240, 120
```

## 17. 手工验收

### 17.1 单节点吸附

1. 打开地图。
2. 进入编辑模式。
3. 拖动某个节点到任意位置。
4. 松开鼠标。
5. 观察节点坐标应吸附到规整位置。
6. 连线视觉应比之前更整齐。

### 17.2 键盘微调

1. 进入编辑模式。
2. 点击选中一个节点。
3. 按方向键。
4. 节点按 10px 移动。
5. 按 Shift + 方向键。
6. 节点按 1px 移动。
7. 按 Ctrl + 方向键。
8. 节点按 50px 移动。

### 17.3 多选框选

1. 进入编辑模式。
2. 点击“多选节点”。
3. 在空白区域拖动鼠标。
4. 出现蓝色半透明矩形。
5. 被覆盖的节点显示选中态。
6. 松开鼠标后选中态保留。

### 17.4 多选整体拖动

1. 框选多个节点。
2. 按住其中一个已选节点拖动。
3. 所有已选节点一起移动。
4. 松开鼠标后位置保存。
5. 关闭并重新打开地图，位置仍保留。

### 17.5 模式互斥

1. 开启连线模式。
2. 再点击“多选节点”。
3. 连线模式应退出。
4. 开启多选模式。
5. 再点击“连线”。
6. 多选模式应退出。

### 17.6 浏览模式不受影响

1. 退出编辑模式。
2. 滚轮缩放仍可用。
3. 空白拖动画布平移仍可用。
4. 点击节点仍能选中主界面快捷项。

## 18. 构建验证

执行：

```powershell
cd C:\Users\shee_\OneDrive\Desktop\VSLoader
dotnet test .\VSLoader.sln -p:UseSharedCompilation=false
dotnet build .\VSLoader.sln -p:UseSharedCompilation=false
```

如果 `VSLoader.exe` 正在运行导致文件占用：

```powershell
Get-Process -Name VSLoader -ErrorAction SilentlyContinue | Stop-Process -Force
```

## 19. 验收标准

最终必须满足：

```text
1. 拖动节点结束后自动吸附网格。
2. 选中节点后方向键可以微调。
3. 多选节点按钮可进入/退出多选模式。
4. 多选模式下可以拖出蓝色矩形框。
5. 被矩形覆盖的节点会一起选中。
6. 已选节点可以整体拖动。
7. 整体拖动后位置保存。
8. 连线模式和多选模式互斥。
9. 原有连线、导入导出、缩放、平移功能不回退。
10. dotnet test 通过。
11. dotnet build 0 错误。
```
