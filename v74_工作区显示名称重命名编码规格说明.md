# v74 工作区显示名称重命名编码规格说明

## 1. 文档目的

本文件用于指导编程 Agent 在现有 VSLoader 多工作区功能上增加：

```text
选择工作区窗口中的“重命名”功能。
```

用户确认的关键边界：

```text
只修改工作区显示名称，不重命名磁盘文件夹。
```

也就是说：

```text
Workspaces\Default
Workspaces\workspace
Workspaces\workspace-2
```

这些目录名和目录路径不变。  
本次只更新配置中的名称字段。

## 2. 当前功能基础

当前已经存在工作区底座和选择窗口：

```text
VSLoader\Models\AppSettings.cs
VSLoader\Models\WorkspaceInfo.cs
VSLoader\Models\WorkspaceMetadata.cs
VSLoader\Models\WorkspaceContext.cs
VSLoader\Models\Services\AppSettingsService.cs
VSLoader\Models\Services\WorkspaceService.cs
VSLoader\ViewModels\WorkspaceSelectorViewModel.cs
VSLoader\ViewModels\WorkspaceListItemViewModel.cs
VSLoader\ViewModels\WorkspaceNameDialogViewModel.cs
VSLoader\Views\WorkspaceSelectorWindow.xaml
VSLoader\Views\WorkspaceSelectorWindow.xaml.cs
VSLoader\Views\WorkspaceNameDialog.xaml
VSLoader\Views\WorkspaceNameDialog.xaml.cs
```

当前选择窗口已有：

```text
1. 工作区列表。
2. 新建工作区。
3. 打开工作区。
4. 打开工作区文件夹。
5. 取消。
```

当前工作区名称显示来源：

```text
app-settings.json -> Workspaces[].Name
workspace.json -> Name
MainWindow title -> WorkspaceContext.Name
WorkspaceSelectorWindow list -> WorkspaceInfo.Name
```

## 3. 需求目标

完成后用户应该可以：

```text
1. 在“选择工作区”窗口选中某个工作区。
2. 点击“重命名”按钮。
3. 弹出名称输入窗口，默认填入当前工作区名称。
4. 输入新名称并确认。
5. 程序校验名称合法性。
6. 更新 app-settings.json 中对应工作区的 Name。
7. 更新该工作区目录下 workspace.json 中的 Name。
8. 刷新选择窗口列表。
9. 如果重命名的是当前正在打开的工作区，则主窗口标题同步更新。
10. 工作区实际文件夹路径不变。
```

## 4. 非目标范围

本阶段不实现：

```text
1. 不重命名工作区目录。
2. 不移动任何工作区文件。
3. 不修改快捷项、地图、AdminUI、WebUI 数据。
4. 不实现工作区删除。
5. 不实现工作区复制。
6. 不实现工作区导入导出。
7. 不实现批量重命名。
8. 不实现工作区图标。
```

## 5. 数据修改范围

### 5.1 必须修改

程序级配置：

```text
%AppData%\VSLoader\app-settings.json
```

更新：

```json
{
  "Workspaces": [
    {
      "Id": "default",
      "Name": "新的显示名称",
      "Path": "C:\\Users\\xxx\\AppData\\Roaming\\VSLoader\\Workspaces\\Default"
    }
  ]
}
```

工作区元数据：

```text
%AppData%\VSLoader\Workspaces\<WorkspaceFolder>\workspace.json
```

更新：

```json
{
  "Id": "default",
  "Name": "新的显示名称",
  "CreatedAt": "...",
  "UpdatedAt": "..."
}
```

### 5.2 不能修改

不能修改：

```text
WorkspaceInfo.Path
WorkspaceContext.RootPath
工作区实际文件夹名
config.json 中的快捷项数据
window-layout.json
factory-map.layout.json
UIdownload
```

示例：

```text
旧显示名：默认工作区
新显示名：产线A
路径仍然是：%AppData%\VSLoader\Workspaces\Default
```

这是正确行为。

## 6. UI 改造

### 6.1 选择工作区窗口增加按钮

修改文件：

```text
VSLoader\Views\WorkspaceSelectorWindow.xaml
```

在底部按钮区增加：

```text
[重命名]
```

推荐按钮顺序：

```text
[新建工作区] [重命名] [打开工作区文件夹]        [取消] [打开]
```

原因：

```text
“新建”和“重命名”都属于工作区管理操作，应该放在左侧同组。
```

按钮状态：

```text
未选中工作区时禁用。
选中工作区时启用。
```

绑定命令：

```text
RenameWorkspaceCommand
```

### 6.2 重命名弹窗

可以复用现有：

```text
WorkspaceNameDialog
WorkspaceNameDialogViewModel
```

但需要支持：

```text
1. 窗口标题可变：新建工作区 / 重命名工作区。
2. 输入框默认值可设置为旧名称。
3. 确认按钮文字可变：创建 / 保存。
```

推荐最小改造：

```text
WorkspaceNameDialogViewModel 增加 WindowTitle 和 ConfirmButtonText。
```

建议属性：

```csharp
public string WindowTitle { get; }

public string ConfirmButtonText { get; }
```

构造函数：

```csharp
public WorkspaceNameDialogViewModel(
    string windowTitle = "新建工作区",
    string confirmButtonText = "创建",
    string workspaceName = "")
```

XAML 修改：

```xml
Title="{Binding WindowTitle}"
...
Content="{Binding ConfirmButtonText}"
```

重命名时：

```csharp
var nameViewModel = new WorkspaceNameDialogViewModel(
    "重命名工作区",
    "保存",
    selectedWorkspace.Name);
```

## 7. ViewModel 改造

### 7.1 WorkspaceSelectorViewModel

修改文件：

```text
VSLoader\ViewModels\WorkspaceSelectorViewModel.cs
```

新增事件：

```csharp
public event Action? RequestRenameWorkspace;
```

新增命令：

```text
StartRenameWorkspaceCommand
```

CanExecute：

```text
SelectedWorkspace is not null
```

逻辑：

```csharp
[RelayCommand(CanExecute = nameof(HasSelectedWorkspace))]
private void StartRenameWorkspace()
{
    RequestRenameWorkspace?.Invoke();
}
```

新增公开方法：

```csharp
public SaveResult RenameSelectedWorkspace(string newName)
```

行为：

```text
1. 如果 SelectedWorkspace 为空，返回失败。
2. 校验 newName。
3. 去首尾空格。
4. 如果新名称和当前名称相同，返回成功，不写文件。
5. 检查是否和其他工作区名称重复，忽略大小写。
6. 修改 appSettings.Workspaces 中对应 WorkspaceInfo.Name。
7. 修改 WorkspaceInfo.UpdatedAt。
8. 修改工作区 workspace.json 中的 Name 和 UpdatedAt。
9. 保存 app-settings.json。
10. 刷新 Workspaces 列表。
11. 重新选中被重命名的工作区。
```

重复名称判断：

```text
排除当前工作区自身。
```

也就是：

```csharp
appSettings.Workspaces.Any(workspace =>
    !string.Equals(workspace.Id, selected.Id, StringComparison.OrdinalIgnoreCase)
    && string.Equals(workspace.Name.Trim(), trimmedName, StringComparison.OrdinalIgnoreCase))
```

### 7.2 校验逻辑复用

当前 `WorkspaceSelectorViewModel` 已经有：

```csharp
private static SaveResult ValidateWorkspaceName(string name)
```

重命名必须复用这套校验规则。

规则：

```text
1. 名称不能为空。
2. 名称去首尾空格。
3. 不能包含 \ / : * ? " < > |
4. 长度不超过 60。
5. 不能和其他工作区名称重复。
```

## 8. WorkspaceService 改造建议

推荐在 `WorkspaceService` 中新增专门方法，避免 ViewModel 直接手写 workspace.json。

修改文件：

```text
VSLoader\Models\Services\WorkspaceService.cs
```

新增方法：

```csharp
public SaveResult RenameWorkspace(WorkspaceInfo workspace, string newName)
```

职责：

```text
1. 只修改 WorkspaceInfo.Name。
2. 只修改 workspace.json 中 Name。
3. 不修改 workspace.Path。
4. 不移动目录。
```

建议逻辑：

```csharp
public SaveResult RenameWorkspace(WorkspaceInfo workspace, string newName)
{
    try
    {
        if (!IsWorkspaceUsable(workspace))
        {
            return SaveResult.Fail("工作区不可用或文件夹不存在。");
        }

        var trimmedName = newName.Trim();
        workspace.Name = trimmedName;
        workspace.UpdatedAt = DateTime.Now;

        var metadataPath = Path.Combine(workspace.Path, "workspace.json");
        WorkspaceMetadata metadata;
        if (File.Exists(metadataPath))
        {
            metadata = JsonSerializer.Deserialize<WorkspaceMetadata>(File.ReadAllText(metadataPath), jsonOptions)
                ?? new WorkspaceMetadata();
        }
        else
        {
            metadata = new WorkspaceMetadata();
        }

        metadata.Id = workspace.Id;
        metadata.Name = trimmedName;
        if (metadata.CreatedAt == default)
        {
            metadata.CreatedAt = workspace.CreatedAt == default ? DateTime.Now : workspace.CreatedAt;
        }
        metadata.UpdatedAt = workspace.UpdatedAt;

        File.WriteAllText(metadataPath, JsonSerializer.Serialize(metadata, jsonOptions));
        return SaveResult.Ok();
    }
    catch (Exception ex)
    {
        return SaveResult.Fail(ex.Message);
    }
}
```

说明：

```text
app-settings.json 的保存仍由 WorkspaceSelectorViewModel 调用 AppSettingsService.Save 完成。
WorkspaceService 只负责单个工作区目录内部的 workspace.json。
```

## 9. MainWindow 标题同步

场景：

```text
用户已经打开主窗口。
点击“工作区”按钮。
在选择窗口里选中当前工作区并重命名。
取消选择窗口或继续使用当前工作区。
```

期望：

```text
主窗口标题也同步变成新名称。
```

### 9.1 推荐实现方式

在 `App.SwitchWorkspace(MainWindow currentWindow)` 中，选择窗口关闭后，如果没有切换工作区，也要允许刷新当前主窗口标题。

可选方案：

```text
方案 A：WorkspaceSelectorViewModel 暴露 HasWorkspaceRenamed。
方案 B：MainWindow 每次从选择窗口返回后根据当前 WorkspaceInfo 刷新标题。
```

推荐方案 B，简单稳定。

MainWindow 新增方法：

```csharp
internal void RefreshWorkspaceDisplayName(string workspaceName)
{
    _workspaceContext.Name = workspaceName; // 如果 WorkspaceContext 当前 init-only，则不能这样做
    Title = FormatWindowTitleWithWorkspace(
        FormatWindowTitle(Assembly.GetExecutingAssembly().GetName().Version),
        workspaceName);
}
```

但是当前 `WorkspaceContext.Name` 是 `init`，不能直接修改。  
因此更推荐：

```text
不要修改 _workspaceContext，只刷新标题即可。
```

新增：

```csharp
internal void RefreshWorkspaceTitle(string workspaceName)
{
    Title = FormatWindowTitleWithWorkspace(
        FormatWindowTitle(Assembly.GetExecutingAssembly().GetName().Version),
        workspaceName);
}
```

App 中在 `ShowWorkspaceSelector` 返回后：

```csharp
var currentInfo = appSettings.Workspaces.FirstOrDefault(w => w.Id == currentWindow.WorkspaceId);
if (currentInfo is not null)
{
    currentWindow.RefreshWorkspaceTitle(currentInfo.Name);
}
```

如果用户重命名的是非当前工作区，不影响当前标题。

如果用户重命名的是当前工作区，标题会更新。

### 9.2 是否更新 WorkspaceContext.Name

本阶段可以不更新 `_workspaceContext.Name`。  
原因：

```text
当前运行期主要使用 WorkspaceContext.Id 和 RootPath。
标题显示可以单独刷新。
```

后续如果有更多地方需要当前工作区名称，可以再引入运行期 CurrentWorkspaceInfo 或可变 WorkspaceRuntimeContext。

## 10. WorkspaceSelectorWindow code-behind 改造

修改文件：

```text
VSLoader\Views\WorkspaceSelectorWindow.xaml.cs
```

当前已有：

```csharp
viewModel.RequestCreateWorkspace += ShowCreateWorkspaceDialog;
```

新增：

```csharp
viewModel.RequestRenameWorkspace += ShowRenameWorkspaceDialog;
```

新增方法：

```csharp
private void ShowRenameWorkspaceDialog()
{
    if (DataContext is not WorkspaceSelectorViewModel viewModel
        || viewModel.SelectedWorkspace is null)
    {
        return;
    }

    var nameViewModel = new WorkspaceNameDialogViewModel(
        "重命名工作区",
        "保存",
        viewModel.SelectedWorkspace.Name);

    var dialog = new WorkspaceNameDialog(nameViewModel)
    {
        Owner = this
    };

    if (dialog.ShowDialog() != true)
    {
        return;
    }

    var result = viewModel.RenameSelectedWorkspace(nameViewModel.WorkspaceName);
    if (!result.Success)
    {
        MessageBox.Show(this, result.ErrorMessage ?? "重命名工作区失败。", "VSLoader", ...);
    }
}
```

## 11. 测试要求

### 11.1 WorkspaceServiceTests

新增测试：

```text
1. RenameWorkspace_updates_workspace_info_name_without_changing_path
2. RenameWorkspace_updates_workspace_json_name
3. RenameWorkspace_returns_failure_when_workspace_directory_missing
```

必须验证：

```text
workspace.Path 不变。
workspace.json 中 Name 更新。
```

### 11.2 WorkspaceSelectorViewModelTests

新增测试：

```text
1. RenameSelectedWorkspace_updates_app_settings_name
2. RenameSelectedWorkspace_saves_app_settings
3. RenameSelectedWorkspace_updates_workspace_metadata
4. RenameSelectedWorkspace_rejects_duplicate_name_excluding_current_workspace
5. RenameSelectedWorkspace_allows_same_name_for_current_workspace
6. RenameSelectedWorkspace_rejects_invalid_name
7. RenameSelectedWorkspace_keeps_selected_workspace_after_rename
```

### 11.3 MainWindow / App 相关测试

如果当前测试不方便覆盖 WPF App 切换流程，可以补一个纯方法测试：

```text
MainWindowTitleTests:
FormatWindowTitleWithWorkspace 使用新名称显示。
```

主窗口标题实际刷新作为手工验收。

## 12. 手工验收场景

### 场景一：启动选择窗口重命名

操作：

```text
1. 启动程序。
2. 在选择工作区窗口选中“默认工作区”。
3. 点击“重命名”。
4. 输入“产线A”。
5. 保存。
```

预期：

```text
1. 列表中名称变为“产线A”。
2. app-settings.json 中对应 Workspaces[].Name 变为“产线A”。
3. Workspaces\Default\workspace.json 中 Name 变为“产线A”。
4. Workspaces\Default 文件夹名不变。
```

### 场景二：重命名后打开

操作：

```text
1. 选中重命名后的工作区。
2. 点击“打开”。
```

预期：

```text
主窗口标题显示：VSLoader vX.Y.Z - 产线A
```

### 场景三：主窗口中重命名当前工作区

操作：

```text
1. 已打开主窗口。
2. 点击“工作区”。
3. 选中当前工作区。
4. 点击“重命名”。
5. 输入新名称。
6. 保存。
7. 点击取消回到主窗口。
```

预期：

```text
主窗口不关闭。
主窗口标题刷新为新名称。
```

### 场景四：重复名称

操作：

```text
1. 有两个工作区：产线A、产线B。
2. 选中产线B。
3. 重命名为产线A。
```

预期：

```text
提示工作区名称已存在。
不保存。
列表不变化。
```

### 场景五：非法名称

操作：

```text
输入 Line:A 或空字符串。
```

预期：

```text
提示名称非法。
不保存。
```

## 13. 风险点

### 13.1 错误地重命名文件夹

风险：

```text
移动工作区目录会导致 RootPath、UIdownload、地图文件、布局文件路径全部受影响。
```

规避：

```text
RenameWorkspace 绝不能调用 Directory.Move。
只修改 JSON 中的 Name。
```

### 13.2 app-settings 和 workspace.json 不一致

风险：

```text
选择窗口显示一个名字，workspace.json 是另一个名字。
```

规避：

```text
重命名时同时更新两个文件。
如果 workspace.json 更新失败，则不要保存 app-settings.json。
```

推荐顺序：

```text
1. 调 WorkspaceService.RenameWorkspace 更新 workspace.json 和内存 WorkspaceInfo。
2. 成功后保存 app-settings.json。
3. 如果保存 app-settings.json 失败，提示用户。
```

### 13.3 当前主窗口标题不刷新

风险：

```text
用户重命名当前工作区后仍看到旧标题。
```

规避：

```text
工作区选择窗口关闭后，App.SwitchWorkspace 刷新当前主窗口标题。
```

### 13.4 重命名当前选中项后选择丢失

风险：

```text
列表刷新后 SelectedWorkspace 变 null。
```

规避：

```text
刷新列表后按 WorkspaceId 重新选中原工作区。
```

## 14. 验收标准

本需求完成必须满足：

```text
1. 选择工作区窗口有“重命名”按钮。
2. 未选中工作区时重命名不可用。
3. 点击重命名弹出输入窗口。
4. 输入窗口默认填入旧名称。
5. 保存后列表名称立即刷新。
6. app-settings.json 中名称更新。
7. workspace.json 中名称更新。
8. 工作区目录名不变。
9. 重复名称会提示并阻止保存。
10. 非法名称会提示并阻止保存。
11. 主窗口中重命名当前工作区后标题刷新。
12. dotnet build 通过。
13. dotnet test 通过。
14. 至少手工走通“启动选择窗口重命名 -> 打开主窗口”用户链路。
```

## 15. 推荐执行命令

实现前停止运行中的程序：

```powershell
Get-Process -Name VSLoader -ErrorAction SilentlyContinue | Stop-Process -Force
```

构建：

```powershell
dotnet build C:\Users\shee_\OneDrive\Desktop\VSLoader\VSLoader.sln -p:UseSharedCompilation=false
```

测试：

```powershell
dotnet test C:\Users\shee_\OneDrive\Desktop\VSLoader\VSLoader.sln -p:UseSharedCompilation=false
```

## 16. 推荐提交信息

```text
feat: add workspace display name rename
```
