# v109 全应用现代化视觉语言与核心窗口重构编码规格说明

## 1. 背景

当前 VSLoader 已经完成了部分窗口的现代化视觉优化：

- `WorkspaceSelectorWindow` 已完成工作区选择窗口现代化重构。
- `FactoryMapWindow` 已完成地图画布背景与节点区分度优化。
- `MessageDialogWindow` 已完成无系统边框自定义弹窗外壳。

但整体应用仍存在视觉不统一的问题：

- 主窗口顶部工具区仍有大量默认按钮观感。
- 主窗口背景、公告条、工具区、列表容器之间的层级还不够统一。
- 设置窗口仍大量使用传统 `GroupBox`，视觉偏旧。
- 批量新增识别窗口仍是传统表单 + 表格布局，按钮和输入框缺少统一样式。
- 快捷项编辑弹窗、工作区命名弹窗等小弹窗仍接近默认 WPF 样式。
- 工作区相关弹窗和工作区选择窗口的现代化风格还没有完全打通。
- 地图窗口顶部工具栏、按钮、状态区还没有和主程序统一。

本次目标是在不改变业务逻辑的前提下，建立一套轻量、统一、现代的 VSLoader 视觉语言，并优先覆盖核心窗口和常用弹窗。

## 2. 目标

本次实现目标：

- 建立统一的浅色现代化视觉语言。
- 复用 v108 工作区选择窗口的设计方向。
- 主窗口、批量新增识别窗口、设置窗口、小弹窗、工作区弹窗使用一致的控件风格。
- 保持工具软件的清爽、稳定、克制，不做营销页式设计。
- 优化按钮、输入框、面板、表格、公告条、忙碌遮罩、弹窗的视觉层级。
- 保证所有现有功能、绑定、命令、交互逻辑不变。
- 不引入第三方 UI 库。

## 3. 非目标

本次不做：

- 不改变快捷项数据结构。
- 不改变工作区数据结构。
- 不改变窗口布局配置文件结构，除非只是读取现有布局配置。
- 不改变批量新增识别算法。
- 不改变自动获取连接、更新检测、软件更新等业务逻辑。
- 不改变地图节点布局和连线数据结构。
- 不改变右键菜单功能项顺序。
- 不新增动画系统。
- 不引入 MahApps、MaterialDesignThemes 等第三方 UI 框架。
- 不做深色模式。
- 不做大面积渐变、毛玻璃、装饰光斑、营销式 hero 页面。

## 4. 影响范围

### 4.1 主窗口

文件：

```text
VSLoader/MainWindow.xaml
```

重点区域：

- 根背景。
- 状态公告条。
- 更新提醒公告条。
- 更新失败公告条。
- 搜索框和计数显示。
- 顶部第二行工具按钮。
- 主列表外层容器。
- 主列表表头、行、选中态。
- 主窗口忙碌遮罩。

### 4.2 批量新增识别窗口

文件：

```text
VSLoader/Views/BatchImportWindow.xaml
```

重点区域：

- 路径输入区。
- 选择文件夹 / 选择 CSV 按钮。
- 扫描预览按钮。
- 统计信息显示。
- 预览 DataGrid。
- 确认新增 / 取消按钮。
- 扫描忙碌遮罩。

### 4.3 设置窗口

文件：

```text
VSLoader/Views/SettingsWindow.xaml
```

重点区域：

- 根背景和滚动区域。
- `GroupBox` 视觉替换为现代分区面板。
- VSCode、AdminUI、AdminUI 密码、WebUI、软件更新、更新检测、地图快捷键、程序快捷键等配置分区。
- 输入框、密码框、只读快捷键框、浏览按钮、录制按钮、清除按钮。
- 底部保存 / 取消按钮。

注意：

- 保留 `PreviewMouseWheel="SettingsInput_PreviewMouseWheel"`，不能破坏输入控件滚轮转交外层滚动的修复。

### 4.4 通用提示弹窗

文件：

```text
VSLoader/Views/MessageDialogWindow.xaml
VSLoader/Views/MessageDialogWindow.xaml.cs
VSLoader/Models/Services/DialogService.cs
```

重点区域：

- 自定义标题栏。
- 内容区图标与文字。
- 底部按钮区。
- 确认、错误、提示三类视觉。

原则：

- 继续使用现有 `DialogService`。
- 不改变弹窗返回值语义。
- 不改变 Owner 和居中逻辑。

### 4.5 工作区弹窗

必须包含，不能遗漏。

文件：

```text
VSLoader/Views/WorkspaceNameDialog.xaml
VSLoader/Views/WorkspaceNameDialog.xaml.cs
VSLoader/Views/WorkspaceSelectorWindow.xaml.cs
```

覆盖场景：

- 新建工作区名称输入弹窗。
- 重命名工作区显示名称输入弹窗。
- 删除工作区确认弹窗。
- 工作区操作错误提示。

要求：

- `WorkspaceNameDialog` 视觉必须和 `WorkspaceSelectorWindow` 保持同一风格。
- 如果工作区删除确认或错误提示仍走系统 `MessageBox.Show`，本阶段应评估是否替换为 `DialogService` 或 `MessageDialogWindow`。
- 替换时必须保持行为语义不变：确认删除才删除，取消不删除。
- 不改变“重命名只改显示名称，不改真实文件夹名”的规则。
- 不改变“不允许删除当前工作区”和“不允许删除最后一个工作区”的规则。

### 4.6 快捷项编辑弹窗

文件：

```text
VSLoader/Views/ShortcutEditWindow.xaml
VSLoader/Views/ShortcutEditWindow.xaml.cs
```

重点区域：

- 名称、路径、备注等输入区域。
- 保存 / 取消按钮。
- 弹窗背景、边框、间距。

原则：

- 不改变字段绑定。
- 不改变校验逻辑。
- 不改变打开文件夹、浏览文件夹等交互。

### 4.7 地图窗口补充优化

文件：

```text
VSLoader/Views/FactoryMapWindow.xaml
VSLoader/Views/FactoryMapWindow.xaml.cs
```

重点区域：

- 顶部工具栏。
- 地图按钮。
- 自动获取连接按钮。
- 导入 / 导出图文件按钮。
- 编辑锁定按钮。
- 多选按钮。
- 状态显示。
- 地图窗口忙碌遮罩。

原则：

- 不改变地图画布坐标、缩放、拖拽、节点选择、连线编辑逻辑。
- 不改变浏览模式 / 编辑模式权限控制。

## 5. 统一视觉语言

### 5.1 色彩规范

建议沿用 v108 风格：

```text
应用背景：#F6F9FF
主面板背景：#FFFFFF
主面板边框：#E3EAF3
弱分隔线：#E8EEF6

主文字：#172033
正文文字：#334155
次级文字：#64748B
弱文字：#94A3B8

普通按钮边框：#D7E0EC
普通按钮背景：#FFFFFF
普通按钮 hover：#F4F8FF
普通按钮文字：#172033

主按钮背景：#2563EB
主按钮 hover：#1D4ED8
主按钮文字：#FFFFFF

危险文字：#DC2626
危险 hover：#FEF2F2
危险边框：#FECACA

选中背景：#EAF3FF
选中边框：#B8D4FF
选中强调：#2563EB

成功提示背景：#ECFDF5
成功提示边框：#A7F3D0
成功提示文字：#065F46

警告提示背景：#FFF7D6
警告提示边框：#E5B84A
警告提示文字：#6B4E00

错误提示背景：#FEE2E2
错误提示边框：#FCA5A5
错误提示文字：#7F1D1D
```

### 5.2 字体规范

统一使用：

```text
Microsoft YaHei UI
```

推荐字号：

```text
窗口标题：20-24
分区标题：15-16
普通正文：13-14
辅助说明：12-13
表格表头：13
表格内容：13-14
按钮文字：13
```

### 5.3 圆角规范

```text
外层主面板：8-10
普通按钮：6
输入框：6
列表项 / 卡片：8
弹窗外壳：8
公告条：8
```

不要使用过度胶囊圆角。

### 5.4 间距规范

```text
窗口外边距：16-22
面板内边距：14-18
控件垂直间距：8-12
按钮间距：8-10
表格单元格左右内边距：12
```

## 6. 共享样式实现方案

### 6.1 推荐新增资源字典

建议新增：

```text
VSLoader/Styles/ModernTheme.xaml
```

并在：

```text
VSLoader/App.xaml
```

中合并资源：

```xml
<ResourceDictionary.MergedDictionaries>
    <ResourceDictionary Source="Styles/ModernTheme.xaml" />
</ResourceDictionary.MergedDictionaries>
```

如果当前 `App.xaml` 已有转换器和资源，必须合并保留，不能覆盖。

### 6.2 建议定义的样式 Key

按钮：

```text
ModernButtonStyle
ModernPrimaryButtonStyle
ModernDangerButtonStyle
ModernIconButtonStyle
ModernToolbarButtonStyle
```

输入：

```text
ModernTextBoxStyle
ModernPasswordBoxStyle
ModernReadOnlyTextBoxStyle
ModernComboBoxStyle
ModernCheckBoxStyle
```

面板：

```text
ModernWindowRootStyle
ModernSurfaceBorderStyle
ModernSectionBorderStyle
ModernNoticeBorderStyle
ModernBusyPanelStyle
```

表格：

```text
ModernDataGridStyle
ModernDataGridColumnHeaderStyle
ModernDataGridCellStyle
ModernDataGridRowStyle
```

弹窗：

```text
ModernDialogButtonStyle
ModernDialogPrimaryButtonStyle
ModernDialogDangerButtonStyle
```

### 6.3 样式边界

第一阶段不建议全局覆盖所有 `Button`、`TextBox` 默认样式。

推荐使用显式 Key：

```xml
Style="{StaticResource ModernButtonStyle}"
```

原因：

- 降低误伤系统控件和特殊模板的风险。
- 便于逐个窗口迁移。
- 避免影响现有右键菜单、DataGrid 特殊模板、地图节点按钮。

## 7. 分阶段实现计划

### 7.1 第一阶段：共享视觉基础

实现：

- 新增 `ModernTheme.xaml`。
- 定义常用颜色 Brush。
- 定义按钮、输入框、面板、DataGrid、忙碌遮罩基础样式。
- `App.xaml` 合并资源字典。

验证：

- 程序能编译。
- 现有窗口不引用新样式时不受影响。

### 7.2 第二阶段：主窗口重构

修改：

```text
VSLoader/MainWindow.xaml
```

内容：

- 根背景改为统一浅蓝白。
- 公告条使用统一提示样式。
- 顶部搜索框使用现代输入框样式。
- 设置、工作区、新增、更新软件、检测更新、导入导出、批量新增、自动获取连接、地图、AdminUI、打开、编辑、删除按钮统一样式。
- 删除按钮使用危险样式。
- 主列表外层面板使用统一白色面板。
- DataGrid 样式接入统一资源，但保留现有列宽、排序、选中态、虚拟化配置。
- 主窗口忙碌遮罩使用统一忙碌面板样式。

不得修改：

- `Command` 绑定。
- `Click` 事件。
- `SelectedItem` 绑定。
- `Sorting` 事件。
- 列宽持久化逻辑。
- 右键菜单功能项。

### 7.3 第三阶段：通用弹窗与工作区弹窗

修改：

```text
VSLoader/Views/MessageDialogWindow.xaml
VSLoader/Views/WorkspaceNameDialog.xaml
VSLoader/Views/WorkspaceSelectorWindow.xaml.cs
```

内容：

- `MessageDialogWindow` 改成和现代主题一致的标题栏、内容区、按钮区。
- `WorkspaceNameDialog` 改成自定义现代弹窗视觉，或至少使用统一背景、输入框、按钮、间距。
- 工作区新建 / 重命名弹窗要和工作区选择窗口视觉一致。
- 工作区删除确认、错误提示如果仍是系统 `MessageBox.Show`，应替换为现有自定义弹窗服务。

注意：

- 工作区弹窗是本次必须覆盖项，不能遗漏。
- 替换系统 `MessageBox` 时必须保留确认语义，不能默认执行危险操作。
- 弹窗必须继续基于 VSLoader 窗口居中，而不是屏幕中心。

### 7.4 第四阶段：批量新增识别窗口

修改：

```text
VSLoader/Views/BatchImportWindow.xaml
```

内容：

- 根背景统一为浅蓝白。
- 顶部路径输入区放入白色面板。
- 标签、输入框、浏览按钮统一样式。
- 扫描预览作为主按钮。
- 统计信息改为轻量状态文本或小型状态块。
- 表格使用统一 DataGrid 样式。
- 确认新增使用主按钮，取消使用普通按钮。
- 忙碌遮罩使用统一样式。

不得修改：

- `ScanPreviewCommand`
- `ConfirmImportCommand`
- `CancelCommand`
- 预览排序逻辑。
- 网络预检查逻辑。
- 忙碌状态绑定。

### 7.5 第五阶段：设置窗口

修改：

```text
VSLoader/Views/SettingsWindow.xaml
```

内容：

- 根背景统一。
- 用现代分区 `Border` 替代传统 `GroupBox` 视觉。
- 每个配置分区包含标题和内容。
- 输入框、密码框、只读框、浏览按钮统一样式。
- 保存按钮为主按钮，取消为普通按钮。
- 长内容滚动保持稳定。

必须保留：

- `SettingsScrollViewer`
- `SettingsInput_PreviewMouseWheel`
- 所有绑定路径。
- 所有 Browse / Record / Clear 命令。
- 密码框 `PasswordChanged` 逻辑。

### 7.6 第六阶段：快捷项编辑弹窗

修改：

```text
VSLoader/Views/ShortcutEditWindow.xaml
```

内容：

- 根背景和弹窗面板现代化。
- 表单标签与输入框间距统一。
- 保存按钮主按钮化。
- 取消按钮普通化。

不得修改：

- 字段绑定。
- 校验逻辑。
- 文件夹选择逻辑。

### 7.7 第七阶段：地图窗口工具栏补齐

修改：

```text
VSLoader/Views/FactoryMapWindow.xaml
```

内容：

- 顶部工具栏背景、按钮、状态文字统一。
- 编辑锁定、多选、导入导出、自动获取连接按钮统一样式。
- 地图忙碌遮罩统一。

不得修改：

- 地图画布缩放拖拽。
- 节点编辑。
- 连线编辑。
- 浏览/编辑模式权限。

## 8. 工作区弹窗专项要求

### 8.1 新建 / 重命名工作区弹窗

当前文件：

```text
VSLoader/Views/WorkspaceNameDialog.xaml
```

当前问题：

- 使用系统窗口边框。
- 内部是普通 Grid + 默认 TextBox/Button。
- 和现代化后的 `WorkspaceSelectorWindow` 风格不一致。

目标：

- 背景、输入框、按钮、提示文字全部统一。
- 标题清晰。
- 输入框聚焦态明确。
- 创建 / 保存按钮为主按钮。
- 取消按钮为普通按钮。
- 保留非法字符提示。

### 8.2 删除工作区确认

当前需检查：

```text
VSLoader/Views/WorkspaceSelectorWindow.xaml.cs
```

如果仍使用：

```csharp
MessageBox.Show(...)
```

建议替换为：

```csharp
DialogService.Confirm(...)
```

或同等自定义弹窗。

要求：

- 文案要明确“彻底删除工作区文件夹”。
- 取消或关闭弹窗不能删除。
- 删除失败时显示可追溯错误信息。

### 8.3 工作区错误提示

包括：

- 创建失败。
- 重命名失败。
- 删除失败。
- 打开文件夹失败。
- 打开工作区失败。

应尽量使用统一自定义弹窗，不再露出系统默认 MessageBox。

如果某些错误暂时不替换，必须在实现说明中注明原因。

## 9. 业务逻辑保护清单

实现时必须逐项确认：

- 主窗口所有按钮命令不变。
- 主窗口搜索、筛选、计数显示不变。
- 主窗口快捷项选择、高亮、右键菜单不变。
- 主窗口列宽调整和持久化不变。
- 批量新增扫描、排序、勾选、导入不变。
- 设置页保存、取消、浏览、录制快捷键不变。
- 设置页输入框滚轮不吞外层滚动。
- 弹窗 Owner 和居中策略不变。
- 工作区新建、重命名、删除、打开逻辑不变。
- 地图窗口打开、关闭、最小化等同关闭、快捷键开关不变。
- 地图节点选择联动主列表不变。
- 自动更新和更新检测逻辑不变。

## 10. 测试建议

### 10.1 必跑命令

```powershell
dotnet test .\VSLoader.sln -c Debug --no-restore
dotnet build .\VSLoader.sln -c Debug --no-restore
```

### 10.2 推荐新增或调整测试

如果实现中新增了共享样式资源字典，建议增加资源加载测试：

```text
VSLoader.Tests/ModernThemeResourceTests.cs
```

测试目标：

- `ModernTheme.xaml` 能被加载。
- 关键 Style Key 存在。
- 不影响 `App.xaml` 原有转换器资源。

如果替换了工作区系统 MessageBox，建议增加或补充工作区窗口交互测试，至少验证命令入口仍存在，不改变 ViewModel 规则。

## 11. 手动验证清单

### 11.1 主窗口

1. 启动程序进入主窗口。
2. 顶部工具区按钮视觉统一。
3. 搜索框可输入，筛选正常。
4. 快捷项数量 `[x / y]` 正常显示。
5. 主列表列宽可调整并持久化。
6. 双击快捷项正常打开 VSCode。
7. 右键菜单功能正常。
8. 自动获取连接忙碌遮罩正常居中。
9. 更新提醒和更新失败提示视觉清晰。

### 11.2 工作区弹窗

1. 打开工作区选择窗口。
2. 点击新建工作区，弹窗视觉与工作区选择窗口一致。
3. 输入非法字符，提示逻辑正常。
4. 重命名工作区只改显示名称。
5. 删除工作区弹出统一确认弹窗。
6. 取消删除不会删除。
7. 确认删除后仍遵守不可删除当前工作区、不可删除最后一个工作区规则。
8. 工作区操作失败时不出现系统默认丑弹窗。

### 11.3 批量新增识别

1. 打开批量新增识别窗口。
2. 路径输入和浏览按钮正常。
3. 点击扫描预览显示统一忙碌遮罩。
4. 扫描结果表格正常显示。
5. 勾选、取消勾选正常。
6. 确认新增和取消正常。

### 11.4 设置窗口

1. 打开设置窗口。
2. 所有配置项可正常显示。
3. 鼠标位于 TextBox / PasswordBox 上时滚轮仍能滚动外层页面。
4. 浏览按钮正常。
5. 快捷键录制和清除正常。
6. 保存和取消正常。

### 11.5 弹窗

1. 普通提示弹窗视觉统一。
2. 错误弹窗视觉统一。
3. 确认弹窗视觉统一。
4. 弹窗相对 VSLoader 当前窗口居中。
5. 关闭按钮不会误触发确认。

### 11.6 地图窗口

1. 地图窗口顶部工具栏视觉统一。
2. 地图按钮和主窗口按钮风格一致。
3. 自动获取连接按钮可用。
4. 地图忙碌遮罩显示在地图窗口内。
5. 编辑/浏览模式权限不变。

## 12. 实现风险与规避

### 12.1 全局样式误伤

风险：

- 全局覆盖 `Button` / `TextBox` 可能影响地图节点按钮、右键菜单、DataGrid 内部控件。

规避：

- 第一阶段使用显式 Key 样式。
- 不默认覆盖所有控件。

### 12.2 工作区弹窗逻辑变化

风险：

- 将 `MessageBox.Show` 替换为自定义弹窗时，确认结果语义写反。

规避：

- 删除类操作必须以 `Confirm == true` 作为唯一执行条件。
- 关闭弹窗、Esc、取消都视为不确认。

### 12.3 设置页滚轮回归

风险：

- 重构 XAML 后漏掉 `PreviewMouseWheel`，导致输入框上滚轮无法滚动页面。

规避：

- 所有 `TextBox`、`PasswordBox`、只读快捷键输入框保留 `PreviewMouseWheel="SettingsInput_PreviewMouseWheel"`。

### 12.4 DataGrid 列宽持久化回归

风险：

- 主列表 DataGrid 样式重构影响列宽恢复和保存。

规避：

- 不改列定义绑定和事件。
- 不改 `CanUserResizeColumns`。
- 不改现有列名和顺序。

### 12.5 忙碌遮罩 Owner 归属回归

风险：

- 主窗口、地图窗口、批量新增窗口遮罩混用，导致遮罩显示在错误窗口。

规避：

- 每个窗口保留自己的遮罩绑定。
- 只改视觉，不抽走状态绑定。

## 13. 验收标准

满足以下条件即完成：

- 新增统一现代主题资源。
- 主窗口视觉与 v108 工作区选择窗口风格统一。
- 批量新增识别窗口视觉统一。
- 设置窗口视觉统一，且滚轮逻辑不回归。
- 通用提示弹窗视觉统一。
- 工作区新建、重命名、删除确认、错误提示弹窗纳入统一视觉，不遗漏。
- 快捷项编辑弹窗视觉统一。
- 地图窗口工具栏视觉统一。
- 所有业务逻辑保持不变。
- 不引入第三方 UI 框架。
- `dotnet test .\VSLoader.sln -c Debug --no-restore` 通过。
- `dotnet build .\VSLoader.sln -c Debug --no-restore` 通过。

