# v27 剪贴板写入重试与健壮性优化编码规格说明

## 1. 需求背景

v8 已实现 AdminUI 密码剪贴板功能：

```text
点击 AdminUI -> 成功打开 .jnlp -> 将配置好的 AdminUI 密码写入系统剪贴板
```

当前实现中，剪贴板写入使用：

```csharp
System.Windows.Clipboard.SetText(password);
```

该调用只执行一次。

用户反馈曾出现一次低概率问题：

```text
AdminUI 已打开，但写入剪贴板失败
```

后续无法稳定复现。

这类问题通常与 Windows 系统剪贴板被其他进程瞬时占用有关，例如：

- 输入法
- 浏览器
- 微信/聊天工具
- 截图工具
- 远程桌面
- Java/Swing 客户端启动过程
- 其他正在访问剪贴板的后台程序

剪贴板是系统级共享资源，如果某一瞬间被其他程序打开占用，WPF 的 `Clipboard.SetText` 可能抛出异常。

本次 v27 需求目标不是改变功能，而是提升剪贴板写入的健壮性，尽量避免偶发失败直接影响用户体验。

## 2. 需求目标

本次开发目标：

1. 保持现有 AdminUI 功能流程不变。
2. 保持点击 `AdminUI` 成功打开 `.jnlp` 后复制密码到剪贴板。
3. 将剪贴板写入从“单次尝试”改为“短间隔多次重试”。
4. 剪贴板短暂被占用时，程序应自动重试。
5. 多次重试仍失败时，才显示错误提示。
6. 剪贴板写入成功后，仍使用主界面状态栏临时提示。
7. 不新增复杂 UI。
8. 不改变 AdminUI 密码保存方式。
9. 不改变 `.jnlp` 打开逻辑。

## 3. 修改范围

主要新增文件：

```text
VSLoader\Models\Services\ClipboardService.cs
```

主要修改文件：

```text
VSLoader\ViewModels\MainViewModel.cs
```

原则上不需要修改：

- `AdminUiService`
- `PasswordProtectionService`
- `AdminUiConfig`
- `SettingsViewModel`
- `SettingsWindow`
- `DialogService`
- 配置文件结构
- 主窗口 XAML 布局

## 4. 当前逻辑

当前 `MainViewModel.OpenAdminUi()` 中的逻辑大致如下：

```csharp
var result = _adminUiService.OpenAdminUi(SelectedShortcut, _config.AdminUi);
if (!result.Success)
{
    _dialogService.ShowError(result.ErrorMessage ?? "打开 AdminUI 失败。");
    return;
}

var password = _passwordProtectionService.Unprotect(_config.AdminUi.ProtectedPassword);
if (string.IsNullOrEmpty(password))
{
    ShowTemporaryStatusMessage("AdminUI 已打开，但未配置 AdminUI 密码。");
    return;
}

try
{
    System.Windows.Clipboard.SetText(password);
    ShowTemporaryStatusMessage("AdminUI 已打开，密码已复制到剪贴板。");
}
catch (Exception ex)
{
    _dialogService.ShowError($"AdminUI 已打开，但写入剪贴板失败：{ex.Message}");
}
```

问题点：

```text
Clipboard.SetText(password)
```

只调用一次。

如果当时系统剪贴板被短暂占用，就会立即进入失败提示。

## 5. 推荐实现方案

新增独立服务：

```text
ClipboardService
```

职责：

- 统一封装剪贴板写入。
- 提供带重试的写入方法。
- 避免剪贴板异常处理散落在 ViewModel 中。
- 方便以后扩展“自动清空剪贴板”等功能。

## 6. ClipboardService 设计

新增文件：

```text
VSLoader\Models\Services\ClipboardService.cs
```

命名空间保持与现有服务一致：

```csharp
namespace VSLoader.Services;
```

推荐方法：

```csharp
public sealed class ClipboardService
{
    public async Task<SaveResult> SetTextWithRetryAsync(
        string text,
        int maxAttempts = 5,
        int delayMilliseconds = 120)
    {
        ...
    }
}
```

返回值复用现有：

```text
SaveResult
```

成功：

```csharp
return SaveResult.Ok();
```

失败：

```csharp
return SaveResult.Fail("...");
```

## 7. 剪贴板写入方式

推荐将：

```csharp
Clipboard.SetText(text);
```

替换为：

```csharp
Clipboard.SetDataObject(text, true);
```

说明：

- `SetDataObject(text, true)` 表示将数据放入剪贴板，并要求数据在应用退出后仍可保留。
- 该方式通常比简单 `SetText` 更适合作为最终剪贴板写入。
- 即便使用 `SetDataObject`，仍可能因剪贴板瞬时占用而失败，所以仍需要重试。

## 8. 重试策略

推荐参数：

```text
最大尝试次数：5 次
每次间隔：120 毫秒
```

总等待时间约：

```text
480 毫秒
```

该时间对用户几乎无感，但可以显著降低瞬时占用导致的失败概率。

推荐逻辑：

```csharp
public async Task<SaveResult> SetTextWithRetryAsync(
    string text,
    int maxAttempts = 5,
    int delayMilliseconds = 120)
{
    if (string.IsNullOrEmpty(text))
    {
        return SaveResult.Fail("剪贴板内容为空。");
    }

    Exception? lastException = null;

    for (var attempt = 1; attempt <= maxAttempts; attempt++)
    {
        try
        {
            Clipboard.SetDataObject(text, true);
            return SaveResult.Ok();
        }
        catch (Exception ex)
        {
            lastException = ex;

            if (attempt < maxAttempts)
            {
                await Task.Delay(delayMilliseconds);
            }
        }
    }

    return SaveResult.Fail(lastException?.Message ?? "未知错误");
}
```

注意：

- 该方法必须在 WPF UI 线程上调用。
- 不要在该方法中使用 `ConfigureAwait(false)`。
- `await Task.Delay(...)` 后应回到 WPF UI 上下文继续执行。
- 不要使用后台线程直接调用 WPF `Clipboard` API。

## 9. MainViewModel 修改

修改：

```text
VSLoader\ViewModels\MainViewModel.cs
```

新增字段：

```csharp
private readonly ClipboardService _clipboardService;
```

默认构造函数中新增：

```csharp
new ClipboardService()
```

完整构造函数中新增参数：

```csharp
ClipboardService clipboardService
```

并赋值：

```csharp
_clipboardService = clipboardService;
```

## 10. OpenAdminUi 改为异步命令

当前方法：

```csharp
[RelayCommand(CanExecute = nameof(HasSelectedShortcut))]
private void OpenAdminUi()
```

推荐改为：

```csharp
[RelayCommand(CanExecute = nameof(HasSelectedShortcut))]
private async Task OpenAdminUiAsync()
```

说明：

- CommunityToolkit.Mvvm 会生成 `OpenAdminUiCommand`。
- 主界面原有绑定 `OpenAdminUiCommand` 不需要修改。
- 改为异步后，可以自然等待剪贴板重试。

## 11. OpenAdminUi 推荐逻辑

推荐改造后逻辑：

```csharp
[RelayCommand(CanExecute = nameof(HasSelectedShortcut))]
private async Task OpenAdminUiAsync()
{
    if (SelectedShortcut is null)
    {
        return;
    }

    var result = _adminUiService.OpenAdminUi(SelectedShortcut, _config.AdminUi);
    if (!result.Success)
    {
        _dialogService.ShowError(result.ErrorMessage ?? "打开 AdminUI 失败。");
        return;
    }

    var password = _passwordProtectionService.Unprotect(_config.AdminUi.ProtectedPassword);
    if (string.IsNullOrEmpty(password))
    {
        ShowTemporaryStatusMessage("AdminUI 已打开，但未配置 AdminUI 密码。");
        return;
    }

    var clipboardResult = await _clipboardService.SetTextWithRetryAsync(password);
    if (clipboardResult.Success)
    {
        ShowTemporaryStatusMessage("AdminUI 已打开，密码已复制到剪贴板。");
        return;
    }

    _dialogService.ShowError($"AdminUI 已打开，但写入剪贴板失败：{clipboardResult.ErrorMessage}");
}
```

## 12. CanExecute 行为

本次不强制为 `OpenAdminUiAsync` 添加忙碌状态。

原因：

- 剪贴板重试总耗时很短。
- 不需要显示加载遮罩。
- 不需要屏蔽整个程序操作。

但仍保留现有 CanExecute：

```csharp
HasSelectedShortcut
```

也就是：

```text
必须选中快捷项，并且当前不是 IsBusy 状态。
```

## 13. 用户提示规则

### 13.1 写入成功

仍使用主界面状态栏临时提示：

```text
AdminUI 已打开，密码已复制到剪贴板。
```

### 13.2 未配置密码

仍使用主界面状态栏临时提示：

```text
AdminUI 已打开，但未配置 AdminUI 密码。
```

### 13.3 多次重试仍失败

使用现有自定义错误弹窗：

```text
AdminUI 已打开，但写入剪贴板失败：{错误信息}
```

不需要在每次重试失败时提示用户。

## 14. 不允许改变的内容

本次不允许改变：

- AdminUI 下载逻辑。
- `.jnlp` 文件匹配逻辑。
- `.jnlp` 打开逻辑。
- AdminUI 密码配置入口。
- AdminUI 密码加密存储方式。
- 配置文件字段。
- 主窗口布局。
- 设置窗口布局。
- 状态栏自动消失逻辑。
- 自定义弹窗组件。

## 15. 异常处理要求

`ClipboardService` 内部需要捕获写入剪贴板过程中的异常。

可直接捕获：

```csharp
catch (Exception ex)
```

原因：

- WPF 剪贴板 API 在不同系统环境下可能抛出不同异常。
- 这里属于边界服务，目标是保护主流程。
- 最终失败信息应返回给 ViewModel，由 ViewModel 统一提示。

注意：

- 不要吞掉最终错误。
- 不要在服务内部直接弹窗。
- 不要把密码写入日志。
- 不要把密码拼接到错误信息中。

## 16. 安全说明

本次不改变 v8 的安全边界。

仍然满足：

- 配置文件中不保存明文密码。
- 密码通过 Windows DPAPI 加密保存。
- 点击 AdminUI 后，明文密码会进入系统剪贴板。
- 程序不自动清空剪贴板。

本次不新增：

- 剪贴板自动清空。
- 剪贴板历史清理。
- 密码显示开关。
- 密码日志记录。

## 17. 验收标准

### 17.1 正常复制

操作：

1. 设置 AdminUI 密码。
2. 选中一个已有 `.jnlp` 的快捷项。
3. 点击 `AdminUI`。

期望：

- `.jnlp` 正常打开。
- 状态栏提示：

```text
AdminUI 已打开，密码已复制到剪贴板。
```

- 在任意输入框中 `Ctrl + V` 可以粘贴密码。

### 17.2 未配置密码

操作：

1. 清空 AdminUI 密码并保存。
2. 选中快捷项。
3. 点击 `AdminUI`。

期望：

- `.jnlp` 正常打开。
- 状态栏提示：

```text
AdminUI 已打开，但未配置 AdminUI 密码。
```

- 不弹出错误弹窗。

### 17.3 `.jnlp` 不存在

操作：

1. 删除某个快捷项对应的本地 `.jnlp`。
2. 点击 `AdminUI`。

期望：

- 不写入剪贴板。
- 提示：

```text
未找到对应 AdminUI 文件，请先点击“自动获取连接”。
```

### 17.4 剪贴板瞬时占用

测试方式可选：

- 快速连续复制其他内容。
- 使用其他程序频繁访问剪贴板。
- 或通过调试临时模拟前几次写入失败。

期望：

- 短暂失败时自动重试。
- 重试成功后只显示成功状态栏提示。
- 不向用户暴露中间失败。

### 17.5 持续失败

如果剪贴板持续不可写：

期望：

- 最多尝试 5 次。
- 最终显示错误弹窗：

```text
AdminUI 已打开，但写入剪贴板失败：{错误信息}
```

### 17.6 编译验收

执行：

```powershell
dotnet build .\VSLoader.sln
```

期望：

```text
0 warnings
0 errors
```

如果提示 `VSLoader.exe` 被占用，说明程序正在运行，应先关闭 VSLoader 后重新构建。

## 18. 建议实施步骤

1. 新增 `VSLoader\Models\Services\ClipboardService.cs`。
2. 在 `ClipboardService` 中实现 `SetTextWithRetryAsync`。
3. 修改 `MainViewModel` 构造函数，注入 `ClipboardService`。
4. 将 `OpenAdminUi` 改为 `OpenAdminUiAsync`。
5. 将原 `Clipboard.SetText(password)` 替换为 `_clipboardService.SetTextWithRetryAsync(password)`。
6. 保留原有成功、未配置密码、失败提示文案。
7. 编译项目。
8. 手动验证 AdminUI 打开与密码复制。

## 19. 明确不做的事情

本次不做：

- 自动填充 SwingUI 密码框。
- 判断 SwingUI 是否加载完成。
- 延迟到 SwingUI 加载完成后再写入剪贴板。
- 自动清空剪贴板。
- 修改 AdminUI 密码配置 UI。
- 修改 AdminUI 下载逻辑。
- 修改 `.jnlp` 命名和匹配逻辑。
- 记录明文密码日志。
