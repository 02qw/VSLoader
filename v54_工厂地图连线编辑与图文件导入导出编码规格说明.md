# v54 工厂地图连线编辑与图文件导入导出编码规格说明

## 1. 需求背景

当前 VSLoader 已经具备工厂地图窗口：

```text
1. 设备快捷项可以在地图中显示为方框节点。
2. 节点位置可以通过编辑模式拖拽调整。
3. 节点位置会持久化到本地地图布局 JSON。
4. 点击地图节点可以反向选中主界面快捷项。
```

现在需要补齐地图的流程表达能力：

```text
1. 用户可以在地图上手动给设备节点之间画连线。
2. 同一个节点可以连接多个设备，支持一对多、多对一和链式流程。
3. 画完线后，节点位置和连线关系都要保存成一份图文件。
4. 地图窗口上方增加导入图文件功能，允许导入别人配置好的地图信息。
5. 建议同时增加导出图文件功能，方便把当前地图分享给别人。
```

## 2. 需求目标

本次实现目标：

```text
在工厂地图窗口中增加连线编辑能力，并把节点位置、连线关系完整持久化为 JSON 图文件，同时支持导入和导出图文件。
```

完成后用户可以：

1. 打开地图窗口。
2. 进入编辑模式。
3. 点击“连线”按钮进入连线模式。
4. 依次点击两个设备节点，生成一条有方向的流程连线。
5. 同一个节点可以连接多个其他节点。
6. 右键点击已有连线可以删除连线。
7. 导出当前地图为 `.json` 文件。
8. 导入别人导出的 `.json` 图文件，恢复匹配到的节点位置和连线。

## 3. 非目标范围

本次不实现：

```text
1. 自动布局算法。
2. 自动避让连线。
3. 连线文字标签。
4. 连线颜色自定义。
5. 拖拽连线端点。
6. 复杂贝塞尔曲线编辑。
7. 地图缩略图。
8. 多画布、多地图方案。
9. 数据库或云同步。
```

本次保持轻量化：

```text
节点仍是方框按钮。
连线仍是折线。
保存文件仍是 JSON。
```

## 4. 交互设计

### 4.1 地图顶部按钮

在 `FactoryMapWindow` 顶部工具区增加按钮：

```text
连线
导入图文件
导出图文件
```

建议布局顺序：

```text
[锁定/编辑] [连线] [导入图文件] [导出图文件]    状态说明文本
```

按钮含义：

```text
锁定/编辑：
    继续沿用现有逻辑。
    锁定状态只能浏览、缩放、平移、点击节点选中快捷项。
    编辑状态可以拖拽节点位置。

连线：
    只在编辑状态下生效。
    点击后进入或退出连线模式。

导入图文件：
    选择一个 `.json` 图文件并导入。

导出图文件：
    选择保存位置，把当前地图导出为 `.json` 文件。
```

### 4.2 连线模式

连线模式必须依赖编辑模式。

规则：

```text
如果当前是锁定浏览模式，点击“连线”按钮时提示：
请先切换到编辑模式后再进行连线。

如果当前是编辑模式，点击“连线”按钮：
进入连线模式。

再次点击“连线”按钮：
退出连线模式，并清空当前已选择的起点。
```

连线流程：

```text
1. 用户点击“连线”按钮。
2. 状态栏提示：连线模式：请选择起点设备。
3. 用户点击第一个设备节点，作为起点。
4. 起点设备显示选中样式。
5. 状态栏提示：已选择起点：xxx，请选择终点设备。
6. 用户点击第二个设备节点，作为终点。
7. 程序校验通过后新增连线。
8. 新连线立即显示在地图中。
9. 新连线立即保存到本地布局文件。
10. 连线模式保持开启，继续等待用户选择下一个起点。
```

说明：

```text
完成一条线后，清空起点，不自动退出连线模式。
这样用户可以连续画多条线。
```

### 4.3 支持同节点多连线

必须支持：

```text
A -> B
A -> C
A -> D
```

也必须支持：

```text
A -> D
B -> D
C -> D
```

也必须支持：

```text
A -> B -> C -> D
```

底层数据结构是 `Edges` 列表，同一个 `From` 或同一个 `To` 可以出现多次。

只禁止：

```text
1. A -> A 自连接。
2. A -> B 重复同向连线。
```

默认允许：

```text
A -> B
B -> A
```

原因：

```text
它们是两条方向不同的线，表达意义不同。
```

### 4.4 取消起点

连线模式下：

```text
1. 如果已经选择起点，再点击空白区域，取消当前起点。
2. 如果再次点击同一个起点，也取消当前起点。
3. 取消后状态栏提示：已取消起点，请重新选择起点设备。
```

### 4.5 删除连线

支持删除已有连线。

交互：

```text
右键点击地图中的连线，弹出菜单：
删除连线
```

点击后：

```text
1. 从 currentMap.Edges 中移除这条连线。
2. 重新渲染地图。
3. 立即保存布局文件。
4. 状态栏提示：连线已删除。
```

不需要弹确认框。

原因：

```text
连线是轻量编辑对象，误删后可以重新连。
确认框会降低连续编辑效率。
```

## 5. 图文件数据结构

当前地图持久化结构已有：

```csharp
FactoryMapLayoutConfig
FactoryMapCanvas
FactoryMapDeviceNode
FactoryMapDeviceEdge
```

继续沿用该结构。

推荐 JSON 示例：

```json
{
  "version": 3,
  "canvas": {
    "width": 1600,
    "height": 900
  },
  "devices": [
    {
      "key": "\\\\192.168.15.69\\instances\\5534_TATP012",
      "name": "自动点锡机_012",
      "x": 120,
      "y": 80
    },
    {
      "key": "\\\\192.168.15.69\\instances\\5534_TSDE001",
      "name": "SPI_001",
      "x": 360,
      "y": 80
    }
  ],
  "edges": [
    {
      "from": "\\\\192.168.15.69\\instances\\5534_TATP012",
      "to": "\\\\192.168.15.69\\instances\\5534_TSDE001"
    }
  ]
}
```

字段说明：

```text
version：
    图文件版本号。本次建议升级为 3。

canvas：
    画布大小。

devices：
    节点位置列表。

devices.key：
    设备唯一匹配键，使用快捷项 TargetPath。

devices.name：
    导出时保存的显示名称，仅作为参考。
    导入后显示名称仍以当前程序快捷项名称为准。

devices.x / devices.y：
    节点在画布中的坐标。

edges：
    连线列表。

edges.from：
    起点设备 key。

edges.to：
    终点设备 key。
```

## 6. 持久化规则

本地默认地图布局文件仍使用：

```text
%AppData%\VSLoader\factory-map.layout.json
```

以下操作必须触发保存：

```text
1. 拖拽节点完成。
2. 新增连线成功。
3. 删除连线成功。
4. 导入图文件成功。
```

保存逻辑继续通过：

```csharp
FactoryMapLayoutService.Save(...)
```

## 7. 导出图文件规则

导出按钮行为：

```text
1. 打开保存文件对话框。
2. 默认文件名：factory-map.layout.json。
3. 文件类型：JSON 图文件 (*.json)。
4. 用户选择路径后，把当前 currentMap 序列化保存到该路径。
5. 导出成功后状态栏提示：图文件已导出。
6. 导出失败时使用自定义轻量弹窗提示错误。
```

导出内容：

```text
导出的文件应包含当前地图的全部 nodes/devices、edges、canvas。
```

注意：

```text
导出不改变本地默认布局文件。
```

## 8. 导入图文件规则

导入按钮行为：

```text
1. 打开文件选择对话框。
2. 只允许选择 `.json` 文件。
3. 读取并反序列化为 FactoryMapLayoutConfig。
4. 用导入文件里的 devices.key 匹配当前程序快捷项 TargetPath。
5. 匹配成功的节点应用导入文件中的 x/y。
6. 匹配失败的节点跳过。
7. 导入文件中的连线只有 from/to 都能匹配到当前快捷项时才保留。
8. 导入成功后覆盖当前本地默认地图布局文件。
9. 重新渲染地图。
10. 状态栏显示导入统计。
```

导入统计建议：

```text
图文件已导入：应用节点 48 个，保留连线 35 条，跳过节点 5 个，跳过连线 3 条。
```

### 8.1 匹配规则

匹配键：

```text
TargetPath
```

规范化：

```text
Trim()
忽略大小写
```

不做：

```text
路径存在性校验
路径模糊匹配
只按文件夹名匹配
按名称匹配
```

原因：

```text
名称可能变化，文件夹名可能重复，TargetPath 是当前项目里最稳定的快捷项身份标识。
```

### 8.2 导入后的显示名称

导入文件中的 `devices.name` 不覆盖当前快捷项名称。

原因：

```text
快捷项名称由当前程序配置和规则生成维护。
图文件只负责位置和连线。
```

### 8.3 导入异常

必须处理：

```text
1. 文件不存在。
2. 文件不是合法 JSON。
3. JSON 结构为空。
4. devices 或 edges 为 null。
5. 没有任何节点能匹配当前快捷项。
6. 保存本地布局失败。
```

异常提示使用项目现有自定义轻量弹窗体系。

## 9. 渲染规则

### 9.1 连线视觉

连线继续使用折线：

```text
起点节点右侧中心 -> 中间横向折点 -> 中间竖向折点 -> 终点节点左侧中心
```

建议增加箭头：

```text
连线末端加一个小箭头，表示方向。
```

如果箭头实现复杂，可以先用折线不带箭头，但代码结构应保留后续添加箭头的空间。

### 9.2 连线点击区域

Polyline 本身较细，不容易右键命中。

建议实现：

```text
1. 可见线：StrokeThickness = 2。
2. 命中线：同路径透明 Polyline，StrokeThickness = 10，Opacity = 0。
3. 右键菜单绑定在命中线上。
```

这样用户右键删除连线更容易操作。

### 9.3 连线层级

绘制顺序：

```text
1. 先画连线。
2. 再画设备节点。
```

保证设备节点显示在线上方。

## 10. 代码结构建议

### 10.1 预计修改文件

```text
VSLoader/Views/FactoryMapWindow.xaml
VSLoader/Views/FactoryMapWindow.xaml.cs
VSLoader/Models/Services/FactoryMapLayoutService.cs
VSLoader/Models/FactoryMapLayoutConfig.cs
VSLoader/Models/FactoryMapDeviceEdge.cs
```

### 10.2 可能新增模型

如有必要，可以新增：

```text
VSLoader/Models/FactoryMapImportResult.cs
VSLoader/Models/FactoryMapExportResult.cs
```

但不要过度设计。

### 10.3 建议新增服务方法

在 `FactoryMapLayoutService` 中增加：

```csharp
public FactoryMapLayoutLoadResult Import(string sourcePath, IEnumerable<ShortcutItem> shortcuts)
```

职责：

```text
读取外部图文件，按当前快捷项过滤并生成 FactoryMapDeviceViewData。
```

或者拆成：

```csharp
public FactoryMapLayoutLoadResult LoadFromFile(string layoutPath, IEnumerable<ShortcutItem> shortcuts)
public FactoryMapLayoutSaveResult SaveToFile(string layoutPath, FactoryMapDeviceViewData map)
```

推荐第二种：

```text
SaveToFile 可同时服务默认保存和导出。
LoadFromFile 可同时服务默认读取和导入。
```

## 11. FactoryMapWindow 状态字段建议

新增字段：

```csharp
private bool isConnectMode;
private FactoryMapDeviceViewNode? pendingConnectionStart;
private Border? pendingConnectionStartElement;
```

含义：

```text
isConnectMode：
    当前是否处于连线模式。

pendingConnectionStart：
    已选择但尚未完成连线的起点设备。

pendingConnectionStartElement：
    起点设备对应 UI 元素，用于显示选中样式。
```

## 12. 连线新增算法

伪代码：

```csharp
private void HandleDeviceClickInConnectMode(FactoryMapDeviceViewNode clickedDevice)
{
    if (pendingConnectionStart is null)
    {
        pendingConnectionStart = clickedDevice;
        HighlightConnectionStart(clickedDevice);
        SetStatusText($"已选择起点：{clickedDevice.Name}，请选择终点设备。");
        return;
    }

    if (IsSameDevice(pendingConnectionStart, clickedDevice))
    {
        ClearPendingConnectionStart();
        SetStatusText("已取消起点，请重新选择起点设备。");
        return;
    }

    if (EdgeExists(pendingConnectionStart.Key, clickedDevice.Key))
    {
        ClearPendingConnectionStart();
        SetStatusText("这条连线已经存在，已跳过。");
        return;
    }

    currentMap.Edges.Add(new FactoryMapDeviceEdgeViewData
    {
        From = pendingConnectionStart,
        To = clickedDevice
    });

    ClearPendingConnectionStart();
    RenderCurrentMap(resetView: false);
    saveLayout(currentMap);
    SetStatusText("连线已新增，请继续选择起点设备。");
}
```

重复判断：

```csharp
private bool EdgeExists(string fromKey, string toKey)
{
    return currentMap?.Edges.Any(edge =>
        string.Equals(edge.From.Key?.Trim(), fromKey?.Trim(), StringComparison.OrdinalIgnoreCase)
        && string.Equals(edge.To.Key?.Trim(), toKey?.Trim(), StringComparison.OrdinalIgnoreCase)) == true;
}
```

## 13. 删除连线算法

给每条连线绑定源数据对象：

```csharp
polyline.Tag = edge;
hitPolyline.Tag = edge;
```

右键菜单点击：

```csharp
private void DeleteEdge_Click(object sender, RoutedEventArgs e)
{
    if (sender is not MenuItem { Tag: FactoryMapDeviceEdgeViewData edge } || currentMap is null)
    {
        return;
    }

    currentMap.Edges.Remove(edge);
    RenderCurrentMap(resetView: false);
    saveLayout(currentMap);
    SetStatusText("连线已删除。");
}
```

## 14. 导入导出对话框

可以使用 WPF 标准对话框：

```csharp
Microsoft.Win32.OpenFileDialog
Microsoft.Win32.SaveFileDialog
```

导入过滤：

```text
JSON 图文件 (*.json)|*.json|所有文件 (*.*)|*.*
```

导出过滤：

```text
JSON 图文件 (*.json)|*.json|所有文件 (*.*)|*.*
```

默认文件名：

```text
factory-map.layout.json
```

## 15. 自动化测试建议

重点测试服务层，不测试 WPF 鼠标事件。

建议新增或扩展：

```text
VSLoader.Tests/FactoryMapLayoutServiceTests.cs
```

测试 1：同一个节点允许一对多连线

```text
输入 layout 中有：
A -> B
A -> C

当前快捷项包含 A/B/C。

期望：
Edges.Count == 2。
同一个 From 可以出现两次。
```

测试 2：导入时跳过不存在节点的连线

```text
输入 layout 中有：
A -> B
A -> Missing

当前快捷项只包含 A/B。

期望：
只保留 A -> B。
跳过 A -> Missing。
```

测试 3：保存后可以重新读取连线

```text
构造含 devices + edges 的 map。
SaveToFile。
LoadFromFile。

期望：
节点位置保留。
连线关系保留。
```

测试 4：重复边去重

```text
输入 layout 中重复出现：
A -> B
A -> B

期望：
导入后只保留一条。
```

## 16. 手工验收

### 16.1 新增连线

1. 打开 VSLoader。
2. 点击地图。
3. 点击编辑。
4. 点击连线。
5. 点击设备 A。
6. 点击设备 B。
7. 确认出现 A -> B 连线。
8. 关闭并重新打开地图，确认连线仍存在。

### 16.2 一对多

1. 进入连线模式。
2. 添加：

```text
A -> B
A -> C
A -> D
```

3. 确认三条线都能显示。

### 16.3 多对一

1. 添加：

```text
B -> A
C -> A
D -> A
```

2. 确认三条线都能显示。

### 16.4 重复连线

1. 添加 A -> B。
2. 再次添加 A -> B。
3. 期望不新增重复线，并提示已存在。

### 16.5 自连接

1. 点击 A 作为起点。
2. 再点击 A。
3. 期望取消起点，不新增 A -> A。

### 16.6 删除连线

1. 右键点击已有连线。
2. 点击删除连线。
3. 确认线消失。
4. 关闭并重新打开地图，确认线仍然不存在。

### 16.7 导出图文件

1. 点击导出图文件。
2. 保存到桌面。
3. 打开 JSON，确认包含 devices 和 edges。

### 16.8 导入图文件

1. 修改或删除本地地图布局。
2. 点击导入图文件。
3. 选择刚导出的 JSON。
4. 确认节点位置和连线恢复。

## 17. 构建验证

执行：

```powershell
cd C:\Users\shee_\OneDrive\Desktop\VSLoader
dotnet test .\VSLoader.sln -p:UseSharedCompilation=false
dotnet build .\VSLoader.sln -p:UseSharedCompilation=false
```

如果 `VSLoader.exe` 正在运行导致文件占用：

```powershell
Get-Process -Name VSLoader -ErrorAction SilentlyContinue | Stop-Process -Force
```

## 18. 验收标准

最终必须满足：

```text
1. 地图窗口可以进入连线模式。
2. 两次点击设备可以创建一条有方向连线。
3. 同一个节点可以连接多个设备。
4. 自连接被禁止。
5. 重复同向连线被禁止。
6. 右键连线可以删除。
7. 节点位置和连线都能保存到本地 JSON。
8. 可以导出图文件。
9. 可以导入图文件。
10. 导入时只应用当前快捷项能匹配到的节点和连线。
11. 导入文件中的名称不覆盖当前快捷项名称。
12. 现有缩放、平移、拖拽节点、点击节点选中快捷项功能不回退。
13. dotnet test 通过。
14. dotnet build 0 错误。
```
