# v91 更新器展示更新内容与确认启动编码规格说明

## 1. 背景

当前 VSLoader 已经具备自研自动更新能力：

1. 主程序点击“更新软件”。
2. 主程序启动 `VSLoader.Updater.exe`。
3. 主程序退出。
4. updater 读取 `manifest.json`，复制更新包、校验 SHA256、解压、替换文件。
5. 更新成功后 updater 自动启动新版 `VSLoader.exe`。
6. updater 自动关闭。

现在存在两个体验问题：

1. updater 界面没有展示 `manifest.json` 中的 `releaseNotes`，用户不知道本次更新改了什么。
2. 更新完成后会自动启动 VSLoader，用户没有确认动作，流程感不够清晰。

因此 v91 需要让 updater 在更新过程中展示本次更新内容，并在更新完成后等待用户确认，用户点击确认后才启动新版 VSLoader。

## 2. 需求目标

### 2.1 用户目标

用户点击更新后，updater 窗口需要做到：

1. 更新过程中展示本次更新内容。
2. 保留原来的进度条。
3. 保留原来的实时滚动详情日志。
4. 更新完成后弹出/显示确认信息。
5. 确认信息中也展示本次更新内容。
6. 用户点击确认后，才启动更新好的 `VSLoader.exe`。

### 2.2 技术目标

1. `releaseNotes` 来源统一为 `manifest.json` 的 `releaseNotes` 字段。
2. 不改变更新包复制、SHA256 校验、解压、替换、失败回滚逻辑。
3. 不改变主程序启动 updater 的基本流程。
4. 不影响错误状态下的“打开日志目录”和“关闭”按钮。
5. 保持 updater 窗口轻量，不引入复杂依赖。

## 3. 当前代码结构

### 3.1 updater 窗口

文件：

```text
VSLoader.Updater\MainWindow.xaml
VSLoader.Updater\MainWindow.xaml.cs
```

当前 UI 结构：

1. 标题：`正在更新 VSLoader`
2. 进度条：绑定 `ProgressValue`
3. 状态文字：绑定 `StatusText`
4. 滚动详情列表：绑定 `DetailLines`
5. 错误按钮区：`打开日志目录`、`关闭`

当前成功流程：

```csharp
SetProgress(95, "正在启动新版程序...");
Process.Start(... VSLoader.exe ...);
SetProgress(100, "更新完成。");
await Task.Delay(500);
isUpdating = false;
Close();
```

问题：

1. 成功后立即启动主程序。
2. 没有等待用户确认。
3. 没有展示 `releaseNotes`。

### 3.2 manifest 模型

文件：

```text
VSLoader.Updater\Services\SoftwareUpdateManifest.cs
```

当前已存在字段：

```csharp
public string ReleaseNotes { get; set; } = string.Empty;
```

说明：

该字段已经能从 manifest 中反序列化出来，本次不需要新增 manifest 字段。

### 3.3 updater 更新服务

文件：

```text
VSLoader.Updater\Services\UpdaterUpdateService.cs
```

当前 `RunAsync` 会读取 manifest：

```csharp
var manifest = await LoadManifestAsync(options.ManifestPath, cancellationToken);
```

但只使用：

1. `Version`
2. `PackageFile`
3. `Sha256`

没有把 `ReleaseNotes` 传回窗口。

## 4. manifest 格式约定

示例：

```json
{
  "version": "2.2.0",
  "packageFile": "VSLoader_2.2.0_win-x64.zip",
  "sha256": "xxx",
  "releaseNotes": "1. 优化更新器界面\n2. 修复配置导入问题\n3. 调整更新完成确认流程"
}
```

要求：

1. `releaseNotes` 可为空。
2. `releaseNotes` 支持多行文本。
3. `releaseNotes` 不参与版本判断。
4. `releaseNotes` 不参与 SHA256 校验。
5. `releaseNotes` 读取失败时不单独失败，manifest 整体读取失败仍走现有失败流程。

## 5. UI 行为规格

### 5.1 更新过程中展示更新内容

updater 主窗口增加“本次更新内容”区域。

推荐位置：

```text
标题
进度条
状态文字
本次更新内容
滚动详情日志
底部按钮
```

展示要求：

1. 标题文案：`本次更新内容`
2. 内容绑定 `ReleaseNotesText`
3. 支持换行。
4. 内容过长时可滚动。
5. 不遮挡进度条和详情日志。
6. 空内容时显示：

```text
本次更新未填写更新说明。
```

视觉建议：

1. 使用浅白或极浅灰背景。
2. 边框使用现有 updater 的 `#E5E7EB` 风格。
3. 字体使用 `Microsoft YaHei UI`。
4. 不使用复杂富文本。

### 5.2 更新完成确认

更新成功后，updater 不再自动启动 VSLoader。

更新完成后显示确认弹窗，弹窗内容包括：

1. 标题：`更新完成`
2. 说明：`VSLoader 已更新完成，点击确认后启动新版程序。`
3. 本次更新内容：展示 `ReleaseNotesText`
4. 按钮：`确认`

用户点击确认后：

1. 启动新版 `VSLoader.exe`。
2. updater 关闭。

如果用户关闭确认弹窗：

推荐按“确认”处理，即启动新版 VSLoader 并关闭 updater。

原因：

更新已经完成，确认弹窗只是启动前确认，不应让用户停在无反馈状态。

### 5.3 完成状态下窗口按钮

除确认弹窗外，也建议在 updater 主窗口底部显示一个按钮：

```text
启动 VSLoader
```

但为了避免重复交互，本次采用更简单明确的方案：

1. 更新完成后弹出确认弹窗。
2. 用户确认后启动。
3. 主窗口不额外保留启动按钮。

如果后续希望让用户先阅读主窗口内容再启动，可以另起版本改成“主窗口按钮确认”。

## 6. 推荐实现方案

### 6.1 UpdaterUpdateResult 增加 ReleaseNotes

当前结果类型位于：

```text
VSLoader.Updater\Services\UpdaterUpdateService.cs
```

或同文件底部的 `UpdaterUpdateResult` 定义处。

新增字段：

```csharp
public string ReleaseNotes { get; init; } = string.Empty;
```

成功返回时携带：

```csharp
return UpdaterUpdateResult.Ok("更新完成。", restartMainApp: true, manifest.ReleaseNotes);
```

无更新返回时也可携带：

```csharp
return UpdaterUpdateResult.Ok("当前已是最新版本。", restartMainApp: false, manifest.ReleaseNotes);
```

失败返回可以不携带 releaseNotes，除非 manifest 已读取成功但后续步骤失败。

最小实现可以只保证成功结果携带 releaseNotes。

### 6.2 主窗口增加 ReleaseNotesText 属性

文件：

```text
VSLoader.Updater\MainWindow.xaml.cs
```

新增属性：

```csharp
private string releaseNotesText = "正在读取更新内容...";

public string ReleaseNotesText
{
    get => releaseNotesText;
    private set
    {
        releaseNotesText = NormalizeReleaseNotes(value);
        OnPropertyChanged(nameof(ReleaseNotesText));
    }
}
```

兜底方法：

```csharp
private static string NormalizeReleaseNotes(string? value)
{
    return string.IsNullOrWhiteSpace(value)
        ? "本次更新未填写更新说明。"
        : value.Trim();
}
```

### 6.3 读取 manifest 后尽早展示 releaseNotes

有两种可选路径：

#### 方案 A：服务结果返回 releaseNotes

优点：

1. 改动少。
2. 数据流清晰。
3. 不需要新参数。

缺点：

1. releaseNotes 只有更新完成后才一定能显示。

#### 方案 B：progress 支持 releaseNotes

为 `UpdaterProgress` 增加：

```csharp
public string ReleaseNotes { get; init; } = string.Empty;
```

读取 manifest 后立即上报：

```csharp
progress?.Report(new UpdaterProgress
{
    Value = 5,
    Message = "正在读取更新信息...",
    ReleaseNotes = manifest.ReleaseNotes
});
```

优点：

1. 更新一开始就能看到 releaseNotes。

缺点：

1. 需要改 `UpdaterProgress`。
2. 窗口 progress 处理需要额外判断。

推荐采用方案 B。

原因：

用户希望“updater 更新界面展示本次更新内容”，这意味着更新过程中就应该展示，而不是等更新结束才出现。

### 6.4 更新完成后确认启动

修改 `MainWindow.xaml.cs` 中 `RunFullUpdateAsync` 成功分支。

当前逻辑：

```csharp
SetProgress(95, "正在启动新版程序...");
Process.Start(...);
SetProgress(100, "更新完成。");
await Task.Delay(500);
isUpdating = false;
Close();
```

改为：

```csharp
SetProgress(100, "更新完成，等待确认启动新版程序。");
isUpdating = false;
ShowUpdateCompletedConfirmation(options);
```

确认方法：

```csharp
private void ShowUpdateCompletedConfirmation(UpdaterOptions options)
{
    var dialog = new UpdateCompletedDialog(ReleaseNotesText)
    {
        Owner = this,
        WindowStartupLocation = WindowStartupLocation.CenterOwner
    };

    dialog.ShowDialog();
    StartMainApp(options);
    Close();
}
```

为保持轻量，可不新建复杂 MVVM，仅新建一个简单 WPF Window。

## 7. 完成确认弹窗规格

新增文件：

```text
VSLoader.Updater\UpdateCompletedDialog.xaml
VSLoader.Updater\UpdateCompletedDialog.xaml.cs
```

UI 内容：

1. 标题：`更新完成`
2. 说明文字：`VSLoader 已更新完成，点击确认后启动新版程序。`
3. `releaseNotes` 展示区。
4. 底部确认按钮。

弹窗尺寸建议：

```text
Width = 520
Height = 360
ResizeMode = NoResize
WindowStartupLocation = CenterOwner
```

按钮：

```text
确认
```

按钮行为：

```csharp
DialogResult = true;
Close();
```

关闭窗口行为：

如果用户点右上角关闭，也视为确认。

实现方式：

```csharp
protected override void OnClosing(CancelEventArgs e)
{
    DialogResult ??= true;
    base.OnClosing(e);
}
```

如果 WPF `DialogResult` 设置时机不方便，可以不依赖返回值，`ShowDialog()` 后直接启动主程序。

## 8. 非 update 模式兼容

updater 当前存在两种模式：

1. `update`：updater 自己读取 manifest、复制包、校验、解压、替换。
2. `apply`：只应用已经准备好的 staging 目录。

本次需求主要作用于 `update` 模式。

对于 `apply` 模式：

1. 没有 manifestPath。
2. 无法稳定读取 releaseNotes。
3. 可以保持原有自动启动行为，避免扩大改动面。

如果后续需要 apply 模式也展示 releaseNotes，应通过参数传入 releaseNotes 或 manifestPath，另起版本处理。

## 9. 错误处理

### 9.1 manifest 读取失败

保持现有逻辑：

```text
manifest 读取失败：xxx
```

此时没有 releaseNotes 可展示，更新失败界面照旧。

### 9.2 releaseNotes 为空

不报错，显示：

```text
本次更新未填写更新说明。
```

### 9.3 启动新版程序失败

用户点击确认后，如果 `Process.Start` 失败：

1. 不应静默关闭 updater。
2. 显示错误：

```text
新版程序启动失败：xxx
```

3. 保留 updater 窗口，并显示“打开日志目录/关闭”或可复用错误状态按钮。

最小实现：

```csharp
try
{
    StartMainApp(options);
    Close();
}
catch (Exception ex)
{
    ShowError($"新版程序启动失败：{ex.Message}");
}
```

### 9.4 更新完成后禁止误判为“正在更新”

更新完成等待用户确认时：

```csharp
isUpdating = false;
```

这样用户关闭 updater 不会再弹出：

```text
正在更新，暂时不能关闭。
```

但如果用户关闭完成确认弹窗，仍应按确认处理并启动新版程序。

## 10. 测试要求

### 10.1 UpdaterUpdateServiceTests

如果当前已有相关测试，新增或修改：

1. manifest 包含 `releaseNotes` 时，`RunAsync` 成功结果携带该内容。
2. manifest `releaseNotes` 为空时不影响更新成功。
3. manifest 无效时仍返回失败，不因为 releaseNotes 逻辑吞掉错误。

### 10.2 UpdaterProgress 测试

如果采用推荐方案 B：

1. 读取 manifest 后 progress 能收到 `ReleaseNotes`。
2. UI progress 处理遇到空 releaseNotes 不报错。

### 10.3 MainWindow 行为测试

当前项目可能没有 WPF 窗口 UI 自动化测试，可使用可测试方法拆分：

1. `NormalizeReleaseNotes(null)` 返回兜底文案。
2. `NormalizeReleaseNotes("  abc  ")` 返回 `abc`。
3. `StartMainApp` 失败时进入错误状态。

如果不方便直接测试 UI，可至少保证服务层测试覆盖 releaseNotes 数据流。

## 11. 手工验证

### 11.1 releaseNotes 展示

准备 manifest：

```json
{
  "version": "9.9.9",
  "packageFile": "VSLoader_9.9.9_win-x64.zip",
  "sha256": "正确sha",
  "releaseNotes": "1. 新增更新内容展示\n2. 更新完成后手动确认启动"
}
```

点击主程序“更新软件”后：

1. updater 窗口打开。
2. 主程序退出。
3. updater 窗口显示“本次更新内容”。
4. 内容显示 manifest 中的 releaseNotes。
5. 进度条继续变化。
6. 详情日志继续滚动。

### 11.2 更新完成确认

更新完成后：

1. 不自动启动 VSLoader。
2. 弹出“更新完成”确认弹窗。
3. 弹窗内展示 releaseNotes。
4. 用户点击确认后启动新版 VSLoader。
5. updater 关闭。

### 11.3 空 releaseNotes

manifest 中：

```json
"releaseNotes": ""
```

期望：

1. updater 主界面显示兜底文案。
2. 完成确认弹窗也显示兜底文案。
3. 更新流程不失败。

### 11.4 失败场景

故意让 SHA256 错误：

1. updater 显示失败信息。
2. 不出现完成确认弹窗。
3. 不启动 VSLoader。
4. 错误按钮仍可用。

## 12. 验收标准

满足以下条件视为完成：

1. updater 更新过程中显示 manifest 的 `releaseNotes`。
2. releaseNotes 支持多行文本。
3. releaseNotes 为空时有友好兜底文案。
4. 进度条不受影响。
5. 实时滚动详情日志不受影响。
6. 更新失败回滚逻辑不受影响。
7. 更新成功后不自动启动 VSLoader。
8. 更新完成确认弹窗展示 releaseNotes。
9. 用户点击确认后启动新版 VSLoader。
10. 启动新版失败时显示错误，不静默关闭。
11. `dotnet test .\VSLoader.sln -c Debug` 通过。
12. `dotnet build .\VSLoader.sln -c Debug` 通过。

