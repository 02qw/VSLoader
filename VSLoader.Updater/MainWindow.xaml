<Window x:Class="VSLoader.Updater.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="正在更新 VSLoader"
        Width="560"
        Height="390"
        MinWidth="520"
        MinHeight="430"
        ResizeMode="NoResize"
        ShowInTaskbar="True"
        Topmost="True"
        WindowStartupLocation="CenterScreen"
        Icon="pack://application:,,,/Assets/tomato.ico">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
    </Window.Resources>

    <Grid Margin="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   FontFamily="Microsoft YaHei UI"
                   FontSize="18"
                   FontWeight="SemiBold"
                   Foreground="#111827"
                   Text="正在更新 VSLoader" />

        <ProgressBar Grid.Row="1"
                     Height="12"
                     Margin="0,18,0,12"
                     Maximum="100"
                     Value="{Binding ProgressValue, Mode=OneWay}" />

        <TextBlock Grid.Row="2"
                   FontFamily="Microsoft YaHei UI"
                   FontSize="13"
                   Foreground="#374151"
                   Text="{Binding StatusText}"
                   TextWrapping="Wrap" />

        <GroupBox Grid.Row="3"
                  Margin="0,14,0,0"
                  Padding="10"
                  Header="本次更新内容"
                  Visibility="{Binding ShowReleaseNotes, Converter={StaticResource BooleanToVisibilityConverter}}">
            <ScrollViewer MaxHeight="82"
                          VerticalScrollBarVisibility="Auto">
                <TextBlock FontFamily="Microsoft YaHei UI"
                           FontSize="12"
                           Foreground="#374151"
                           Text="{Binding ReleaseNotesText}"
                           TextWrapping="Wrap" />
            </ScrollViewer>
        </GroupBox>

        <Border Grid.Row="4"
                Margin="0,14,0,14"
                BorderBrush="#E5E7EB"
                BorderThickness="1"
                Background="#FAFAFA">
            <ListBox x:Name="DetailList"
                     BorderThickness="0"
                     Background="Transparent"
                     FontFamily="Consolas, Microsoft YaHei UI"
                     FontSize="12"
                     ItemsSource="{Binding DetailLines}" />
        </Border>

        <StackPanel Grid.Row="5"
                    HorizontalAlignment="Right"
                    Orientation="Horizontal">
            <Button Width="120"
                    Height="32"
                    Margin="0,0,8,0"
                    Command="{Binding OpenErrorLogDirectoryCommand}"
                    Content="打开日志目录"
                    Visibility="{Binding HasError, Converter={StaticResource BooleanToVisibilityConverter}}" />
            <Button Width="80"
                    Height="32"
                    Command="{Binding CloseCommand}"
                    Content="关闭"
                    Visibility="{Binding HasError, Converter={StaticResource BooleanToVisibilityConverter}}" />
        </StackPanel>
    </Grid>
</Window>
