# v119 地图最小化后地图按钮直接恢复编码规格说明

## 1. 背景

v113 之后，工厂地图窗口已经从主窗口附属窗口调整为独立任务栏窗口。

当前主界面第二行存在“地图”按钮：

```text
VSLoader/MainWindow.xaml
```

点击后会调用：

```text
VSLoader/MainWindow.xaml.cs
FactoryMapButton_Click -> ToggleFactoryMapWindow()
```

现有逻辑中，地图按钮承担“打开/关闭地图”的切换功能。

## 2. 问题描述

当前用户操作链路：

1. 点击主界面“地图”按钮，打开地图窗口。
2. 在地图窗口上点击最小化。
3. 回到主界面，再点击“地图”按钮。

当前错误表现：

```text
第一次点击：关闭了已经最小化的地图窗口
第二次点击：重新创建并打开地图窗口
```

用户期望表现：

```text
第一次点击：直接把最小化的地图窗口恢复出来
```

也就是说：

地图窗口已经存在但处于最小化状态时，主界面地图按钮不应该执行关闭，而应该执行恢复。

## 3. 根因分析

当前 `ToggleFactoryMapWindow()` 的核心判断只看 `_isFactoryMapOpen`：

```csharp
private void ToggleFactoryMapWindow()
{
    if (_isFactoryMapOpen)
    {
        CloseFactoryMapByUserAction();
        return;
    }

    _isFactoryMapOpen = true;
    _isFactoryMapHiddenByMainWindow = false;
    _runtimeLayoutState.WasFactoryMapOpen = true;
    ShowFactoryMapIfNeeded();
}
```

这里的问题是：

```text
_isFactoryMapOpen 表示“用户意图上地图是打开的”
```

但代码把它当成：

```text
地图当前正在前台显示，可以被关闭
```

地图窗口被用户最小化后：

```text
_isFactoryMapOpen == true
_factoryMapWindow != null
_factoryMapWindow.WindowState == WindowState.Minimized
```

因此再次点击地图按钮会进入关闭分支，导致用户必须点两次才能重新打开地图。

此外，`ShowFactoryMapIfNeeded()` 中还有一段逻辑：

```csharp
if (_factoryMapWindow.WindowState == WindowState.Minimized && !shouldRestoreFromMainWindowHide)
{
    return;
}
```

这段逻辑原本用于尊重“用户自己最小化地图窗口”的状态，避免主窗口状态变化时强行恢复地图。

这段逻辑本身可以保留，但不能阻止“用户主动点击地图按钮”恢复地图。

## 4. 目标

本次目标：

- 地图窗口已最小化时，点击主界面“地图”按钮应直接恢复地图窗口。
- 地图窗口已最小化时，按地图快捷键也应直接恢复地图窗口。
- 地图窗口正常显示时，点击主界面“地图”按钮仍然关闭地图。
- 地图窗口未打开时，点击主界面“地图”按钮仍然打开地图。
- 保留主窗口最小化/隐藏时地图跟随隐藏的既有逻辑。
- 保留用户手动最小化地图后，主窗口普通状态变化不强行恢复地图的既有逻辑。

## 5. 非目标

本次不做以下事项：

- 不重构地图窗口整体生命周期。
- 不改地图窗口标题栏 UI。
- 不改地图窗口最小化按钮样式。
- 不改地图节点、连线、缩放、拖拽逻辑。
- 不改地图布局持久化格式。
- 不改主窗口快捷键打开/隐藏主程序的逻辑。
- 不改地图快捷键配置项。
- 不修改工作区、设置页、批量新增识别等无关模块。

## 6. 推荐方案

### 6.1 将地图按钮逻辑调整为三态判断

地图按钮和地图快捷键应共同复用 `ToggleFactoryMapWindow()`。

`ToggleFactoryMapWindow()` 应从当前二态逻辑：

```text
未打开 -> 打开
已打开 -> 关闭
```

调整为三态逻辑：

```text
未打开 -> 打开
已打开且正常显示 -> 关闭
已打开但最小化 -> 恢复
```

### 6.2 新增纯判断方法便于测试

在 `MainWindow.xaml.cs` 中新增内部静态方法：

```csharp
internal static bool ShouldRestoreMinimizedFactoryMapOnToggle(
    bool isFactoryMapOpen,
    bool hasFactoryMapWindow,
    WindowState factoryMapWindowState)
{
    return isFactoryMapOpen
        && hasFactoryMapWindow
        && factoryMapWindowState == WindowState.Minimized;
}
```

该方法只做状态判断，不直接操作窗口。

### 6.3 新增恢复地图窗口方法

在 `MainWindow.xaml.cs` 中新增实例方法：

```csharp
private void RestoreMinimizedFactoryMapWindow()
{
    if (_factoryMapWindow is null)
    {
        ShowFactoryMapIfNeeded();
        return;
    }

    _isFactoryMapOpen = true;
    _isFactoryMapHiddenByMainWindow = false;
    _runtimeLayoutState.WasFactoryMapOpen = true;

    _factoryMapWindow.Show();
    _factoryMapWindow.WindowState = WindowState.Normal;
    _factoryMapWindow.Activate();
    RefreshFactoryMap();
}
```

说明：

- `Show()` 用于确保窗口可见。
- `WindowState = WindowState.Normal` 用于从任务栏恢复。
- `Activate()` 用于把地图窗口带到前台。
- `RefreshFactoryMap()` 保证地图数据与当前快捷项保持同步。
- 不调用 `PositionFactoryMapWindow()`，避免恢复最小化窗口时把用户原来的窗口位置重算掉。

### 6.4 调整 ToggleFactoryMapWindow

调整后的逻辑建议为：

```csharp
private void ToggleFactoryMapWindow()
{
    if (ShouldRestoreMinimizedFactoryMapOnToggle(
            _isFactoryMapOpen,
            _factoryMapWindow is not null,
            _factoryMapWindow?.WindowState ?? WindowState.Normal))
    {
        RestoreMinimizedFactoryMapWindow();
        return;
    }

    if (_isFactoryMapOpen)
    {
        CloseFactoryMapByUserAction();
        return;
    }

    _isFactoryMapOpen = true;
    _isFactoryMapHiddenByMainWindow = false;
    _runtimeLayoutState.WasFactoryMapOpen = true;
    ShowFactoryMapIfNeeded();
}
```

## 7. 边界情况

### 7.1 地图正常显示

状态：

```text
_isFactoryMapOpen == true
_factoryMapWindow.WindowState == WindowState.Normal
```

点击地图按钮：

```text
关闭地图
```

### 7.2 地图最大化显示

状态：

```text
_isFactoryMapOpen == true
_factoryMapWindow.WindowState == WindowState.Maximized
```

点击地图按钮：

```text
关闭地图
```

本次不改变最大化窗口的语义。

### 7.3 地图被用户最小化

状态：

```text
_isFactoryMapOpen == true
_factoryMapWindow.WindowState == WindowState.Minimized
```

点击地图按钮：

```text
恢复地图窗口
```

### 7.4 主窗口最小化导致地图隐藏

主窗口最小化时仍执行：

```text
HideFactoryMapWindow()
```

并设置：

```text
_isFactoryMapHiddenByMainWindow = true
```

主窗口恢复时仍由 `ShowFactoryMapIfNeeded()` 处理。

本次不改变这条链路。

### 7.5 用户手动最小化地图后，主窗口状态变化

用户手动最小化地图后，如果只是主窗口发生普通状态变化，不应自动恢复地图。

`ShowFactoryMapIfNeeded()` 中的以下逻辑可以保留：

```csharp
if (_factoryMapWindow.WindowState == WindowState.Minimized && !shouldRestoreFromMainWindowHide)
{
    return;
}
```

用户主动点击地图按钮或地图快捷键时，才恢复最小化地图。

## 8. 测试要求

### 8.1 新增状态判断单元测试

修改：

```text
VSLoader.Tests/MainWindowFactoryMapCloseTests.cs
```

新增测试：

```csharp
[Theory]
[InlineData(true, true, WindowState.Minimized, true)]
[InlineData(true, true, WindowState.Normal, false)]
[InlineData(true, true, WindowState.Maximized, false)]
[InlineData(false, true, WindowState.Minimized, false)]
[InlineData(true, false, WindowState.Minimized, false)]
public void ShouldRestoreMinimizedFactoryMapOnToggle_only_restores_existing_minimized_open_map(
    bool isFactoryMapOpen,
    bool hasFactoryMapWindow,
    WindowState factoryMapWindowState,
    bool expected)
{
    Assert.Equal(expected, MainWindow.ShouldRestoreMinimizedFactoryMapOnToggle(
        isFactoryMapOpen,
        hasFactoryMapWindow,
        factoryMapWindowState));
}
```

### 8.2 回归测试

保留现有测试：

```text
ShouldRestoreFactoryMapWithMainWindow_requires_open_visible_and_not_minimized
ShouldCloseFactoryMapOnStateChanged_never_closes_from_window_state
```

这两个测试用于保证：

- 主窗口恢复逻辑不被误改。
- 地图不再因为窗口状态变化被自动关闭。

## 9. 验收标准

满足以下条件即完成：

- 地图窗口未打开时，点击地图按钮能打开地图。
- 地图窗口正常显示时，点击地图按钮能关闭地图。
- 地图窗口最大化显示时，点击地图按钮能关闭地图。
- 地图窗口被用户最小化后，点击地图按钮能直接恢复地图，不需要点第二次。
- 地图窗口被用户最小化后，按地图快捷键能直接恢复地图，不需要按第二次。
- 主窗口最小化/隐藏时，地图仍跟随隐藏。
- 主窗口恢复时，不强行恢复用户手动最小化的地图。
- 地图恢复时不重算窗口位置，不打乱用户上次的地图窗口位置。
- `dotnet test .\VSLoader.Tests\VSLoader.Tests.csproj -c Debug --no-restore` 通过。
- `dotnet build .\VSLoader.sln -c Debug --no-restore` 通过。

## 10. 风险与注意事项

### 10.1 不要把最小化直接等同于关闭

地图现在是独立任务栏窗口。

用户点击最小化，语义是：

```text
暂时收起窗口
```

不是：

```text
关闭地图功能
```

因此 `_isFactoryMapOpen` 不应在地图最小化时被置为 `false`。

### 10.2 不要在恢复最小化时重新定位地图

恢复最小化窗口时应尊重 Windows 自己保存的恢复位置。

不要调用：

```csharp
PositionFactoryMapWindow(useSessionBounds: true)
```

否则可能导致用户原本拖好的地图窗口位置被重算。

### 10.3 不要破坏主窗口联动

主窗口快捷键隐藏主程序时，地图仍应跟着隐藏。

该逻辑依赖：

```text
HideFactoryMapWindow()
_isFactoryMapHiddenByMainWindow
ShowFactoryMapIfNeeded()
```

本次只修复用户主动点击地图按钮/地图快捷键时的最小化恢复，不重构这条联动链路。
