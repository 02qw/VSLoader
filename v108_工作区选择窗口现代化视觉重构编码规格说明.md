# v108 工作区选择窗口现代化视觉重构编码规格说明

## 1. 背景

当前工作区选择窗口功能已经完整：

- 展示工作区列表。
- 双击打开工作区。
- 新建工作区。
- 重命名工作区显示名称。
- 删除工作区。
- 打开工作区文件夹。
- 右键菜单支持打开、重命名、打开文件夹、删除。

但当前 UI 观感偏旧：

- 整体大面积纯白，层级不明显。
- 列表像普通表格行，不像可选择的“工作区对象”。
- 按钮接近 WPF 默认样式，缺少主次关系。
- 主操作“打开”不够突出。
- 配色偏灰，整体缺少 Google / Apple 类产品常见的清爽明亮感。

本次希望在不改变核心业务逻辑的前提下，重构工作区选择窗口的 UI 界面与配色。

## 2. 目标

对工作区选择窗口进行现代化视觉重构：

- 借鉴 Google / Apple 类产品的浅色、干净、层级明确的设计语言。
- 保持工具软件的克制，不做夸张装饰。
- 使用浅蓝白背景提升空气感。
- 使用白色面板承载工作区列表。
- 工作区列表项改成轻量卡片式视觉。
- 选中态更清晰。
- hover 态更柔和。
- “打开”按钮突出为主按钮。
- “删除”按钮体现轻危险态。
- 右键菜单功能和现有样式保持可用。
- 不改变任何业务逻辑。

## 3. 非目标

本次不做：

- 不改变 `WorkspaceSelectorViewModel` 的业务逻辑。
- 不改变工作区创建、重命名、删除规则。
- 不改变工作区真实文件夹英文命名策略。
- 不改变“重命名只改显示名称”的规则。
- 不改变“不允许删除最后一个工作区”的规则。
- 不改变“不允许删除当前正在使用工作区”的规则。
- 不改变右键菜单功能项顺序。
- 不新增搜索、排序、筛选功能。
- 不新增动画。
- 不做毛玻璃、渐变背景、大面积品牌色背景。
- 不改主窗口 UI。

## 4. 当前代码定位

### 4.1 主 UI 文件

文件：

```text
VSLoader/Views/WorkspaceSelectorWindow.xaml
```

当前结构：

```text
Window
└── Grid Margin=16 Background=White
    ├── 顶部标题 StackPanel
    ├── Border 包裹 ListBox
    └── 底部按钮区
```

这是本次主要修改文件。

### 4.2 交互代码

文件：

```text
VSLoader/Views/WorkspaceSelectorWindow.xaml.cs
```

当前负责：

- 双击打开工作区。
- 右键选中工作区。
- 创建右键菜单。
- 弹出新建/重命名/删除确认窗口。

本次原则上不修改业务交互逻辑。

如果需要调整右键菜单视觉，只能保持现有菜单功能项不变。

### 4.3 ViewModel

文件：

```text
VSLoader/ViewModels/WorkspaceSelectorViewModel.cs
VSLoader/ViewModels/WorkspaceListItemViewModel.cs
```

本次不改 ViewModel。

UI 继续绑定：

```text
Workspaces
SelectedWorkspace
StartCreateWorkspaceCommand
StartRenameWorkspaceCommand
StartDeleteWorkspaceCommand
OpenWorkspaceFolderCommand
CancelCommand
OpenSelectedWorkspaceCommand
```

## 5. 推荐视觉方向

采用：

```text
浅蓝白背景 + 白色面板 + 轻卡片列表项 + 明确主按钮
```

整体应更接近：

- Apple 设置类窗口的清爽白色层级。
- Google Workspace 的轻量蓝白选中态。

但不要像官网 landing page，不做装饰性大标题和大图。

## 6. 配色规范

推荐使用以下色值：

```text
窗口背景：#F6F9FF
主面板背景：#FFFFFF
主面板边框：#E3EAF3
分隔弱线：#E8EEF6

普通列表项背景：#FFFFFF
列表项 hover：#F4F8FF
列表项选中：#EAF3FF
列表项选中边框：#B8D4FF
选中指示条：#2563EB

主文字：#172033
次级文字：#64748B
弱文字：#94A3B8

普通按钮边框：#D7E0EC
普通按钮 hover：#F4F8FF
主按钮背景：#2563EB
主按钮 hover：#1D4ED8
主按钮文字：#FFFFFF
危险文字：#DC2626
危险 hover：#FEF2F2
```

说明：

- 不使用大面积深色。
- 不使用高饱和渐变。
- 不使用一整片蓝色背景。
- 保持白色列表和浅蓝白背景的层级对比。

## 7. 布局设计

### 7.1 窗口尺寸

当前：

```xml
Height="540"
Width="760"
MinHeight="480"
MinWidth="700"
```

可以保持不变。

如果视觉上需要更多呼吸感，可将：

```xml
Height="560"
Width="780"
```

但第一阶段建议保持当前尺寸，降低布局回归风险。

### 7.2 根布局

从：

```xml
<Grid Margin="16" Background="White">
```

调整为：

```xml
<Grid Background="#F6F9FF">
```

内部增加一个带 Padding 的主容器，例如：

```xml
<Grid Margin="22">
```

理由：

- 根背景负责页面底色。
- 内部 Margin 负责内容留白。
- 避免纯白窗口没有层级。

### 7.3 顶部标题区

保留标题：

```text
VSLoader 工作区
```

副标题建议改为：

```text
选择一个工作区继续，或新建独立工作区管理不同产线配置。
```

样式建议：

```text
标题字号：24
标题字重：SemiBold
标题颜色：#172033
副标题字号：13
副标题颜色：#64748B
```

### 7.4 列表面板

列表外层 Border：

```text
Background：#FFFFFF
BorderBrush：#E3EAF3
CornerRadius：10
Padding：8
```

注意：

- 虽然卡片圆角建议不超过 8，但这是外层面板，允许稍大一点。
- 列表项卡片自身圆角控制在 8。

### 7.5 列表项

当前列表项是全宽表格行加底部分割线。

建议改成轻卡片：

```text
Margin：0,0,0,8
Padding：14,12
CornerRadius：8
BorderThickness：1
普通 BorderBrush：Transparent 或 #EEF3FA
```

内容保持三层：

1. 工作区名称 + 状态标签。
2. 工作区路径。
3. 创建 / 更新时间。

不改变绑定字段。

### 7.6 状态标签

当前 `StatusBadge` 为白底灰边。

建议：

```text
Background：#F8FAFC
BorderBrush：#E2E8F0
Foreground：#64748B
CornerRadius：999 不建议
CornerRadius：6
Padding：8,2
```

不要使用胶囊大圆角，保持克制。

### 7.7 选中态

选中项：

```text
Background：#EAF3FF
BorderBrush：#B8D4FF
SelectedIndicator：#2563EB
```

左侧指示条建议：

```text
Width：3
CornerRadius：2
```

### 7.8 Hover 态

hover 项：

```text
Background：#F4F8FF
BorderBrush：#D7E8FF
```

如果同时选中，应以选中态优先。

### 7.9 底部按钮区

底部仍分左右：

左侧管理按钮：

```text
新建工作区
重命名
删除
打开工作区文件夹
```

右侧确认按钮：

```text
取消
打开
```

视觉规则：

- “打开”为主按钮：蓝底白字。
- “取消”为普通按钮。
- “删除”为轻危险按钮：白底、红色文字、hover 浅红。
- 其他为普通按钮。

## 8. 控件样式实现建议

为了避免每个按钮重复写大量属性，建议在 `WorkspaceSelectorWindow.xaml` 内部定义局部资源：

```xml
<Window.Resources>
    <Style x:Key="WorkspaceButtonStyle" TargetType="Button">...</Style>
    <Style x:Key="PrimaryWorkspaceButtonStyle" TargetType="Button" BasedOn="{StaticResource WorkspaceButtonStyle}">...</Style>
    <Style x:Key="DangerWorkspaceButtonStyle" TargetType="Button" BasedOn="{StaticResource WorkspaceButtonStyle}">...</Style>
</Window.Resources>
```

要求：

- Button 高度保持 36。
- 字号 13。
- 边框 1。
- 圆角建议 6。
- hover / disabled 状态清楚。

如果 WPF 默认 Button Template 难以控制圆角和 hover，可以在局部 Style 中重写 `ControlTemplate`。

不要引入第三方 UI 库。

## 9. 右键菜单

当前右键菜单在：

```text
WorkspaceSelectorWindow.xaml.cs
```

功能顺序：

```text
打开
重命名
打开工作区文件夹
删除
```

本次保持不变。

现有菜单样式已经接近主界面纯白右键菜单，可暂时不改。

如果要微调，只能微调 hover 色为：

```text
#F4F8FF
```

不要改变功能项和命令绑定。

## 10. 业务逻辑保护

必须保持以下行为不变：

- 双击工作区打开。
- 选中工作区后点击“打开”打开。
- 新建工作区仍通过原命令。
- 重命名只修改显示名称，不改真实文件夹名。
- 删除工作区仍弹确认。
- 删除工作区仍彻底删除文件夹。
- 不允许删除当前使用中的工作区。
- 不允许删除最后一个工作区。
- 右键菜单功能保持一致。
- 选择窗口关闭/取消逻辑保持一致。

## 11. 测试建议

本次主要为 XAML 视觉重构，业务逻辑不变。

仍需要运行现有测试，确保绑定和命令未破坏：

```powershell
dotnet test .\VSLoader.sln -c Debug --no-restore
dotnet build .\VSLoader.sln -c Debug --no-restore
```

重点关注已有测试：

```text
WorkspaceSelectorViewModelTests
WorkspaceListItemViewModelTests
WorkspaceServiceTests
```

如果重构中新增可测试辅助逻辑，再补测试。

## 12. 手动验证清单

1. 启动程序，进入选择工作区窗口。
2. 窗口整体背景应为浅蓝白，不再是纯白生硬页面。
3. 工作区列表应显示为白色面板。
4. 工作区项应像轻卡片，不再像普通表格行。
5. hover 态柔和。
6. 选中态清晰，有蓝色指示条。
7. 工作区名称、路径、时间信息仍完整展示。
8. 状态标签仍正常显示/隐藏。
9. “打开”按钮明显是主操作。
10. “删除”按钮有轻危险视觉。
11. 双击工作区仍能打开。
12. 右键菜单仍能打开，并且功能顺序不变。
13. 新建、重命名、删除、打开文件夹、取消、打开功能全部正常。

## 13. 验收标准

满足以下条件即完成：

- 工作区选择窗口视觉明显更现代、清爽。
- 背景、面板、列表项层级清楚。
- 主操作“打开”突出。
- 删除操作有轻危险态。
- 工作区列表项 hover / selected 状态清晰。
- 不改变任何业务逻辑。
- 不引入第三方 UI 库。
- `dotnet test .\VSLoader.sln -c Debug --no-restore` 通过。
- `dotnet build .\VSLoader.sln -c Debug --no-restore` 通过。
