# v100 现代按钮焦点态去虚线与可访问性替代编码规格说明

## 1. 背景

当前主界面、地图窗口、设置窗口、批量新增窗口等多个页面的按钮，在获得键盘焦点或鼠标点击后，会出现一圈 WPF 默认的虚线焦点框。

该虚线框不是业务状态，也不是“选中”“正在处理”等程序语义，而是 WPF 控件默认的：

```xml
FocusVisualStyle
```

现有现代化按钮样式已经自定义了按钮背景、边框、圆角、悬停态和按下态，但没有显式替换默认焦点视觉，因此 WPF 仍会叠加默认虚线框。

典型影响位置包括：

- 主界面第二行工具按钮，例如“地图”“检测更新”“自动获取连接”
- 设置窗口按钮
- 批量新增识别窗口按钮
- 地图窗口顶部工具栏按钮
- 各类确认弹窗按钮

这些虚线框会破坏当前现代化 UI 的整体观感，因此需要统一替换。

## 2. 目标

本次目标是：

- 去除现代按钮上的默认虚线焦点框。
- 不取消按钮本身的焦点能力。
- 使用更现代、更克制的边框或背景变化表达焦点状态。
- 保留键盘操作可访问性，用户通过 Tab 切换焦点时仍能知道当前焦点在哪个按钮。
- 修改集中在公共样式层，避免逐个页面、逐个按钮打补丁。

## 3. 非目标

本次不做以下事项：

- 不修改按钮点击命令或业务逻辑。
- 不调整按钮尺寸、布局、文案。
- 不重构窗口结构。
- 不修改 DataGrid 行选中态。
- 不修改 TextBox、PasswordBox 等输入控件焦点态，除非发现同样由本次按钮样式直接影响。
- 不引入新的 UI 库或第三方控件。

## 4. 现状分析

公共按钮样式集中在：

```text
VSLoader/Styles/ModernTheme.xaml
```

当前主要按钮样式包括：

```xml
ModernButtonStyle
ModernPrimaryButtonStyle
ModernDangerButtonStyle
ModernToolbarButtonStyle
ModernIconButtonStyle
```

其中：

- `ModernButtonStyle` 是普通按钮基础样式。
- `ModernPrimaryButtonStyle` 基于普通按钮，但重写了 `ControlTemplate`。
- `ModernDangerButtonStyle` 基于普通按钮，但重写了 `ControlTemplate`。
- `ModernToolbarButtonStyle` 基于普通按钮，未重写模板。
- `ModernIconButtonStyle` 是独立图标按钮样式。

现有 `ModernButtonStyle` 没有配置：

```xml
<Setter Property="FocusVisualStyle" Value="{x:Null}" />
```

所以默认虚线焦点框仍然会出现。

另外，项目里已有部分控件已经关闭了默认焦点虚线，例如：

```xml
ModernWindowChrome.xaml
ModernDataGridCellStyle
WorkspaceSelectorWindow.xaml 中的 ListBoxItem 样式
```

这说明项目当前的视觉方向已经倾向于“关闭默认虚线，改用自定义现代状态”。

## 5. 推荐设计

### 5.1 总体策略

在公共按钮样式中统一关闭 WPF 默认虚线焦点框：

```xml
<Setter Property="FocusVisualStyle" Value="{x:Null}" />
```

然后在按钮模板内部增加焦点触发器：

```xml
<Trigger Property="IsKeyboardFocused" Value="True">
    ...
</Trigger>
```

或者根据实际模板需要使用：

```xml
<Trigger Property="IsKeyboardFocusWithin" Value="True">
    ...
</Trigger>
```

按钮焦点态不再使用虚线，而是使用边框色和轻微背景色变化表达。

### 5.2 普通按钮焦点态

作用对象：

```xml
ModernButtonStyle
```

建议效果：

- 边框色改为 `ModernPrimaryBrush`
- 背景使用非常浅的蓝白色，例如 `#F8FBFF`
- 文字颜色不变
- 不增加阴影，避免按钮跳动或过度抢眼

示例方向：

```xml
<Trigger Property="IsKeyboardFocused" Value="True">
    <Setter TargetName="Root" Property="BorderBrush" Value="{StaticResource ModernPrimaryBrush}" />
    <Setter TargetName="Root" Property="Background" Value="#F8FBFF" />
</Trigger>
```

### 5.3 主按钮焦点态

作用对象：

```xml
ModernPrimaryButtonStyle
```

由于该样式重写了模板，不能只依赖基础样式的模板触发器，需要在自己的模板里补充焦点态。

建议效果：

- 保持蓝色主按钮背景。
- 边框改为较亮的浅蓝色，例如 `#BFDBFE`。
- 不改变文字色。
- 不叠加虚线。

### 5.4 危险按钮焦点态

作用对象：

```xml
ModernDangerButtonStyle
```

由于该样式也重写了模板，需要单独补充焦点态。

建议效果：

- 边框改为浅红色，例如 `#FCA5A5`。
- 背景可以使用轻微浅红，例如 `#FFF7F7`。
- 不改变文字色。
- 不叠加虚线。

### 5.5 工具栏按钮焦点态

作用对象：

```xml
ModernToolbarButtonStyle
```

该样式基于 `ModernButtonStyle`，未重写模板，因此可以继承普通按钮的焦点态。

本次不需要单独改它。

### 5.6 图标按钮焦点态

作用对象：

```xml
ModernIconButtonStyle
```

当前图标按钮没有自定义模板，主要用于标题栏、公告关闭按钮等小按钮。

本次至少应增加：

```xml
<Setter Property="FocusVisualStyle" Value="{x:Null}" />
```

如果实现时发现图标按钮仍然需要明显焦点态，可增加轻量模板：

- 背景透明
- 鼠标悬停浅灰或浅蓝
- 键盘焦点时显示浅蓝背景或浅蓝边框

但为了最小修改，优先只关闭默认虚线，不扩大图标按钮模板重构范围。

## 6. 影响范围

主要修改：

```text
VSLoader/Styles/ModernTheme.xaml
```

建议新增或调整测试：

```text
VSLoader.Tests/ModernButtonFocusVisualTests.cs
```

或放入已有现代主题资源测试文件中。

不修改：

- `MainWindow.xaml` 中各个按钮绑定
- `FactoryMapWindow.xaml` 的地图业务逻辑
- `SettingsWindow.xaml` 的配置项逻辑
- `BatchImportWindow.xaml` 的扫描导入逻辑
- `MessageDialogWindow.xaml` 的弹窗命令逻辑

## 7. 编码要求

### 7.1 ModernButtonStyle

在 `ModernButtonStyle` 中增加：

```xml
<Setter Property="FocusVisualStyle" Value="{x:Null}" />
```

并在模板触发器中增加焦点态。

焦点态触发器建议放在 `IsMouseOver`、`IsPressed`、`IsEnabled` 触发器附近。

需要注意禁用态优先级：

- 按钮禁用时，不应显示蓝色焦点态。
- 如果触发器顺序会影响最终效果，应确保 `IsEnabled=False` 的视觉最终覆盖焦点态。

### 7.2 ModernPrimaryButtonStyle

由于 `ModernPrimaryButtonStyle` 重写了 `ControlTemplate`，应在该模板内单独增加焦点态触发器。

要求：

- 不改变主按钮默认颜色。
- 不改变悬停和按下态原有逻辑。
- 焦点态只作为键盘焦点提示。

### 7.3 ModernDangerButtonStyle

由于 `ModernDangerButtonStyle` 重写了 `ControlTemplate`，应在该模板内单独增加焦点态触发器。

要求：

- 不改变危险按钮的语义颜色。
- 焦点态使用红色体系的浅色边框或浅背景。
- 不引入虚线。

### 7.4 ModernIconButtonStyle

增加：

```xml
<Setter Property="FocusVisualStyle" Value="{x:Null}" />
```

如果不新增模板，则不强制增加复杂焦点态。

后续如果发现图标按钮键盘焦点不够明显，再单独做一个图标按钮焦点态版本。

## 8. 测试建议

建议增加轻量 XAML 文本测试，避免 UI 自动化成本过高。

测试内容：

1. `ModernButtonStyle` 包含 `FocusVisualStyle` 且值为 `{x:Null}`。
2. `ModernIconButtonStyle` 包含 `FocusVisualStyle` 且值为 `{x:Null}`。
3. `ModernTheme.xaml` 中存在 `IsKeyboardFocused` 或 `IsKeyboardFocusWithin` 焦点触发器。
4. `ModernPrimaryButtonStyle` 和 `ModernDangerButtonStyle` 的模板中存在焦点态处理。

示例测试方向：

```csharp
var xaml = File.ReadAllText(TestProjectPaths.GetProjectFilePath("VSLoader", "Styles", "ModernTheme.xaml"));

Assert.Contains("FocusVisualStyle", xaml);
Assert.Contains("{x:Null}", xaml);
Assert.Contains("IsKeyboardFocused", xaml);
```

如果已有主题资源测试文件，应优先扩展已有测试，不重复创建过多测试类。

## 9. 验收标准

满足以下条件即完成：

- 主界面“地图”等按钮点击后不再出现默认虚线框。
- 设置窗口、批量新增窗口、地图窗口工具栏按钮不再出现默认虚线框。
- 主按钮、普通按钮、危险按钮仍有清晰但克制的焦点视觉。
- 键盘 Tab 切换按钮时，用户仍能看出当前焦点按钮。
- 按钮禁用态不被焦点态错误覆盖。
- 业务逻辑、按钮命令、窗口交互均不受影响。
- `dotnet test .\VSLoader.sln -c Debug --no-restore` 通过。
- `dotnet build .\VSLoader.sln -c Debug --no-restore` 通过。

## 10. 风险与边界

### 10.1 可访问性风险

如果只关闭 `FocusVisualStyle`，但不增加替代焦点态，会影响键盘用户识别当前焦点。

因此本次必须同时做：

- 关闭默认虚线
- 增加现代焦点态

### 10.2 模板继承风险

`ModernPrimaryButtonStyle` 和 `ModernDangerButtonStyle` 虽然基于 `ModernButtonStyle`，但它们重写了 `ControlTemplate`。

因此基础模板里的焦点触发器不会自动作用到它们自己的模板内部。

实现时必须分别确认普通按钮、主按钮、危险按钮三类按钮都有合理焦点态。

### 10.3 图标按钮风险

`ModernIconButtonStyle` 当前没有完整模板，关闭虚线后可能导致键盘焦点不明显。

本次先按最小修改处理。如果实际验收发现图标按钮需要明显键盘焦点，再追加轻量模板，不和本次普通按钮焦点态混在一起扩大范围。

