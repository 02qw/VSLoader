# v67 主列表新增原始模块名列编码规格说明

## 1. 需求背景

当前 VSLoader 主界面快捷项列表显示三列：

```text
名称 | 备注 | 更新时间
```

用户希望在主界面增加一列，用于显示快捷项的“原始模块名”。  
该列放在“备注”和“更新时间”之间。

目标显示结构：

```text
名称 | 备注 | 原始模块名 | 更新时间
```

这里的“原始模块名”指批量新增识别过程中，从目标文件夹下：

```text
META-INF\ZAM-DEPLOY.xml
```

中读取出的模块名，例如：

```xml
description="Application for eap-sic-Jutze-3D-AOI"
```

提取结果：

```text
eap-sic-Jutze-3D-AOI
```

该值不是：

```text
1. CSV 中的中文 DisplayName。
2. 文件夹名中的 TSSM / TRSM / TAOI 等设备类型编码。
3. 目标文件夹名称。
```

## 2. 需求目标

完成后：

```text
1. 主界面快捷项列表新增“原始模块名”列。
2. 新列位于“备注”和“更新时间”之间。
3. 批量新增识别时，将解析到的 ZAM 原始模块名保存到快捷项数据中。
4. 配置保存到 %AppData%\VSLoader\config.json 后，下次启动仍能显示原始模块名。
5. 旧配置中没有原始模块名字段的快捷项，显示为空白，不影响启动。
6. 搜索框支持搜索原始模块名。
7. 点击“原始模块名”列头可以排序，并显示现有三态排序反馈。
8. 不在主界面列表刷新时实时读取网络路径下的 ZAM-DEPLOY.xml。
```

## 3. 非目标范围

本次不实现：

```text
1. 主界面实时扫描每个快捷项目录并读取 ZAM-DEPLOY.xml。
2. 手动编辑快捷项时提供原始模块名输入框。
3. 批量修复所有旧快捷项的原始模块名。
4. 单独增加“刷新原始模块名”按钮。
5. 修改 CSV 文件格式。
6. 修改批量新增名称生成算法。
7. 修改地图节点显示逻辑。
8. 修改 AdminUI/WebUI/VSCode 打开逻辑。
```

说明：

```text
本次只做字段持久化、批量导入写入、主列表显示、搜索和排序。
```

## 4. 设计原则

### 4.1 不实时读取网络文件

不要在主界面 DataGrid 显示时实时读取：

```text
快捷项.TargetPath\META-INF\ZAM-DEPLOY.xml
```

原因：

```text
1. 快捷项可能很多。
2. 目标路径可能是网络路径。
3. 网络路径不可达会导致界面卡顿。
4. 主界面列表刷新频繁，实时读文件会影响体验。
```

正确方案：

```text
批量新增识别阶段已经读取过 ZAM-DEPLOY.xml。
在这个阶段把原始模块名保存到 ShortcutItem。
主界面只读取 ShortcutItem.SourceModuleName 字段。
```

### 4.2 兼容旧配置

旧版 `config.json` 中的快捷项没有 `SourceModuleName` 字段。

新增字段必须默认空字符串：

```csharp
public string SourceModuleName { get; set; } = string.Empty;
```

旧配置反序列化后：

```text
SourceModuleName = ""
```

主界面显示为空白即可。

## 5. 涉及文件

主要修改：

```text
C:\Users\shee_\OneDrive\Desktop\VSLoader\VSLoader\Models\ShortcutItem.cs
C:\Users\shee_\OneDrive\Desktop\VSLoader\VSLoader\Models\BatchImportPreviewItem.cs
C:\Users\shee_\OneDrive\Desktop\VSLoader\VSLoader\Models\Services\BatchImportService.cs
C:\Users\shee_\OneDrive\Desktop\VSLoader\VSLoader\ViewModels\MainViewModel.cs
C:\Users\shee_\OneDrive\Desktop\VSLoader\VSLoader\Models\ShortcutSortField.cs
C:\Users\shee_\OneDrive\Desktop\VSLoader\VSLoader\Models\Services\ShortcutSortService.cs
C:\Users\shee_\OneDrive\Desktop\VSLoader\VSLoader\MainWindow.xaml
C:\Users\shee_\OneDrive\Desktop\VSLoader\VSLoader\MainWindow.xaml.cs
```

建议新增或修改测试：

```text
C:\Users\shee_\OneDrive\Desktop\VSLoader\VSLoader.Tests\BatchImportServiceSimpleModuleMapTests.cs
C:\Users\shee_\OneDrive\Desktop\VSLoader\VSLoader.Tests\ShortcutSearchServiceTests.cs
C:\Users\shee_\OneDrive\Desktop\VSLoader\VSLoader.Tests\ShortcutSortServiceTests.cs
```

如果当前不存在 `ShortcutSortServiceTests.cs`，可以新增。

## 6. 数据模型修改

### 6.1 ShortcutItem 新增字段

文件：

```text
C:\Users\shee_\OneDrive\Desktop\VSLoader\VSLoader\Models\ShortcutItem.cs
```

当前：

```csharp
public sealed class ShortcutItem
{
    public string Name { get; set; } = string.Empty;

    public string TargetPath { get; set; } = string.Empty;

    public string Description { get; set; } = string.Empty;

    public DateTime CreatedAt { get; set; }

    public DateTime UpdatedAt { get; set; }
}
```

修改为：

```csharp
public sealed class ShortcutItem
{
    public string Name { get; set; } = string.Empty;

    public string TargetPath { get; set; } = string.Empty;

    public string Description { get; set; } = string.Empty;

    public string SourceModuleName { get; set; } = string.Empty;

    public DateTime CreatedAt { get; set; }

    public DateTime UpdatedAt { get; set; }
}
```

`Clone()` 方法也要复制：

```csharp
SourceModuleName = SourceModuleName,
```

### 6.2 BatchImportPreviewItem 新增字段

文件：

```text
C:\Users\shee_\OneDrive\Desktop\VSLoader\VSLoader\Models\BatchImportPreviewItem.cs
```

新增：

```csharp
public string SourceModuleName { get; set; } = string.Empty;
```

用途：

```text
批量扫描预览阶段暂存从 ZAM-DEPLOY.xml 中解析出的原始模块名。
最终新增或更新快捷项时写入 ShortcutItem.SourceModuleName。
```

## 7. 批量识别逻辑修改

文件：

```text
C:\Users\shee_\OneDrive\Desktop\VSLoader\VSLoader\Models\Services\BatchImportService.cs
```

### 7.1 简化模块映射模式

当前简单模块映射模式中：

```csharp
var simpleResult = CreateSimpleModuleMapName(rules, folderName, directory);
```

`SimpleModuleMapResult` 已经包含：

```csharp
string ModuleName
```

该值就是 ZAM 原始模块名。

需要在所有成功生成预览项的地方设置：

```csharp
SourceModuleName = simpleResult.ModuleName
```

包括：

```text
1. 可新增
2. 可更新
3. 可清理
4. 已跳过但目标路径无变化
5. 名称重复
6. 规则错误但已经读到模块名的场景
```

其中核心至少要覆盖：

```text
可新增 / 可更新 / 可清理 / 已跳过无变化
```

因为这些会影响后续保存或展示。

### 7.2 复杂规则模式

当前复杂规则模式：

```csharp
var matchResult = FindMatchingRule(rules, folderName, directory);
```

`FindMatchingRule` 内部在规则包含 `ModulePattern` 时也会调用：

```csharp
TryEnsureModuleName(...)
```

但当前 `RuleMatchResult` 没有把 `moduleName` 返回出来。

需要扩展 `RuleMatchResult`，增加：

```csharp
string ModuleName
```

当规则匹配成功且读取到 ZAM 模块名时返回：

```csharp
return new RuleMatchResult(rule, match, null, StatusSkipped, moduleName ?? string.Empty);
```

当规则没有 `ModulePattern`，没有读取模块名时：

```csharp
ModuleName = string.Empty
```

预览项中设置：

```csharp
SourceModuleName = matchResult.ModuleName
```

说明：

```text
复杂规则模式只有使用 ModulePattern 时才有原始模块名。
不强制为了显示这一列去额外读取 ZAM 文件。
```

## 8. 创建和更新快捷项

### 8.1 CreateShortcuts

当前：

```csharp
new ShortcutItem
{
    Name = item.GeneratedName.Trim(),
    TargetPath = item.TargetPath.Trim(),
    Description = $"批量新增：{item.FolderName}",
    CreatedAt = now,
    UpdatedAt = now
}
```

修改为：

```csharp
new ShortcutItem
{
    Name = item.GeneratedName.Trim(),
    TargetPath = item.TargetPath.Trim(),
    Description = $"批量新增：{item.FolderName}",
    SourceModuleName = item.SourceModuleName.Trim(),
    CreatedAt = now,
    UpdatedAt = now
}
```

### 8.2 CreateApplyItems

同样要在 `Shortcut = new ShortcutItem` 中设置：

```csharp
SourceModuleName = item.SourceModuleName.Trim(),
```

### 8.3 MainViewModel 应用更新

文件：

```text
C:\Users\shee_\OneDrive\Desktop\VSLoader\VSLoader\ViewModels\MainViewModel.cs
```

当前批量更新已有快捷项时，应已有类似逻辑：

```csharp
existingShortcut.Name = applyItem.Shortcut.Name;
existingShortcut.TargetPath = applyItem.Shortcut.TargetPath;
existingShortcut.Description = applyItem.Shortcut.Description;
existingShortcut.UpdatedAt = applyItem.Shortcut.UpdatedAt;
```

需要新增：

```csharp
existingShortcut.SourceModuleName = applyItem.Shortcut.SourceModuleName;
```

### 8.4 HasBatchImportChanges

当前判断是否有变化：

```csharp
return !string.Equals(existingShortcut.Name.Trim(), generatedName.Trim(), StringComparison.OrdinalIgnoreCase)
    || !string.Equals(existingShortcut.Description.Trim(), generatedDescription.Trim(), StringComparison.OrdinalIgnoreCase)
    || !string.Equals(NormalizePathKey(existingShortcut.TargetPath), NormalizePathKey(generatedTargetPath), StringComparison.OrdinalIgnoreCase);
```

需要新增参数：

```csharp
string generatedSourceModuleName
```

并加入比较：

```csharp
|| !string.Equals(existingShortcut.SourceModuleName.Trim(), generatedSourceModuleName.Trim(), StringComparison.OrdinalIgnoreCase)
```

这样旧数据没有原始模块名时，重新批量扫描会显示“可更新”，从而把原始模块名补进去。

## 9. 主列表 UI 修改

文件：

```text
C:\Users\shee_\OneDrive\Desktop\VSLoader\VSLoader\MainWindow.xaml
```

当前列：

```xml
<DataGridTextColumn Width="5*" Header="名称" Binding="{Binding Name}" SortMemberPath="Name" />
<DataGridTextColumn Width="8*" Header="备注" Binding="{Binding Description}" SortMemberPath="Description" />
<DataGridTextColumn Width="5*" Header="更新时间" Binding="{Binding UpdatedAt, StringFormat=yyyy-MM-dd HH:mm:ss}" SortMemberPath="UpdatedAt" />
```

建议修改为：

```xml
<DataGridTextColumn Width="4*"
                    Binding="{Binding Name}"
                    Header="名称"
                    SortMemberPath="Name" />

<DataGridTextColumn Width="5*"
                    Binding="{Binding Description}"
                    Header="备注"
                    SortMemberPath="Description" />

<DataGridTextColumn Width="5*"
                    Binding="{Binding SourceModuleName}"
                    Header="原始模块名"
                    SortMemberPath="SourceModuleName" />

<DataGridTextColumn Width="4*"
                    Binding="{Binding UpdatedAt, StringFormat=yyyy-MM-dd HH:mm:ss}"
                    Header="更新时间"
                    SortMemberPath="UpdatedAt" />
```

说明：

```text
名称和更新时间保持紧凑。
备注和原始模块名都可能较长，给相对更多空间。
列宽比例为 4 : 5 : 5 : 4，整体视觉更均衡。
```

新列 `ElementStyle` 应参考其他列：

```text
1. VerticalAlignment = Center
2. Padding 与现有列一致
3. TextTrimming = CharacterEllipsis
4. ToolTip 可以绑定 SourceModuleName，便于查看完整模块名
```

## 10. 排序修改

### 10.1 ShortcutSortField

文件：

```text
C:\Users\shee_\OneDrive\Desktop\VSLoader\VSLoader\Models\ShortcutSortField.cs
```

新增：

```csharp
SourceModuleName
```

### 10.2 ShortcutSortService

文件：

```text
C:\Users\shee_\OneDrive\Desktop\VSLoader\VSLoader\Models\Services\ShortcutSortService.cs
```

新增排序逻辑：

```csharp
ShortcutSortField.SourceModuleName => string.Compare(
    x.SourceModuleName,
    y.SourceModuleName,
    StringComparison.CurrentCultureIgnoreCase)
```

### 10.3 MainWindow.xaml.cs

文件：

```text
C:\Users\shee_\OneDrive\Desktop\VSLoader\VSLoader\MainWindow.xaml.cs
```

`SortHeaderTitles` 增加：

```csharp
["SourceModuleName"] = "原始模块名",
```

`TryGetSortField` 增加：

```csharp
"SourceModuleName" => ShortcutSortField.SourceModuleName,
```

允许排序字段增加：

```csharp
return sortMemberPath is "Name" or "Description" or "SourceModuleName" or "UpdatedAt";
```

## 11. 搜索修改

文件：

```text
C:\Users\shee_\OneDrive\Desktop\VSLoader\VSLoader\ViewModels\MainViewModel.cs
```

当前搜索大概率包含：

```csharp
shortcut.Name
shortcut.TargetPath
shortcut.Description
```

需要加入：

```csharp
|| _shortcutSearchService.IsTextMatch(shortcut.SourceModuleName, keyword)
```

这样用户输入：

```text
Jutze
AOI
eap-sic
```

都可以匹配到对应快捷项。

## 12. 手动编辑快捷项的处理

本次不在 `ShortcutEditWindow` 中增加原始模块名输入框。

原因：

```text
原始模块名是从 ZAM-DEPLOY.xml 解析出的机器项目模块标识。
它不是用户备注，也不建议手动随意编辑。
```

如果用户手动编辑快捷项名称、路径、备注：

```text
SourceModuleName 保持原值不变。
```

如果用户手动新增快捷项：

```text
SourceModuleName 默认为空。
```

## 13. 配置兼容性

新增字段后，保存出来的 `config.json` 示例：

```json
{
  "Name": "矩子3D-AOI_001",
  "TargetPath": "\\\\192.168.15.69\\instances\\5534_TAOI001",
  "Description": "批量新增：5534_TAOI001",
  "SourceModuleName": "eap-sic-Jutze-3D-AOI",
  "CreatedAt": "2026-06-12T10:00:00",
  "UpdatedAt": "2026-06-12T10:00:00"
}
```

旧配置没有 `SourceModuleName` 时：

```text
程序正常启动。
主列表原始模块名列为空白。
```

## 14. 测试方案

### 14.1 编译验证

执行：

```powershell
cd C:\Users\shee_\OneDrive\Desktop\VSLoader
dotnet build .\VSLoader.sln -p:UseSharedCompilation=false
```

预期：

```text
Build succeeded.
0 Error(s)
```

### 14.2 单元测试验证

执行：

```powershell
cd C:\Users\shee_\OneDrive\Desktop\VSLoader
dotnet test .\VSLoader.sln -p:UseSharedCompilation=false
```

预期：

```text
所有现有测试通过。
```

### 14.3 批量新增写入原始模块名

准备目录：

```text
测试父目录
└── 5534_TAOI001
    └── META-INF
        └── ZAM-DEPLOY.xml
```

`ZAM-DEPLOY.xml` 中包含：

```xml
description="Application for eap-sic-Jutze-3D-AOI"
```

使用简单模块映射 CSV：

```csv
ModuleName,DisplayName
eap-sic-Jutze-3D-AOI,矩子3D-AOI
```

预期：

```text
1. 扫描预览生成名称：矩子3D-AOI_001。
2. 应用新增后，config.json 中该快捷项包含 SourceModuleName = eap-sic-Jutze-3D-AOI。
3. 主界面“原始模块名”列显示 eap-sic-Jutze-3D-AOI。
```

### 14.4 更新旧快捷项补原始模块名

场景：

```text
已有快捷项 TargetPath 相同，但 SourceModuleName 为空。
```

重新批量扫描。

预期：

```text
1. 预览状态为“可更新”。
2. 应用后该快捷项 SourceModuleName 被补充。
3. 名称、备注、路径无变化时也能因为 SourceModuleName 变化而更新。
```

### 14.5 搜索原始模块名

输入搜索关键词：

```text
Jutze
AOI
eap-sic
```

预期：

```text
包含 eap-sic-Jutze-3D-AOI 的快捷项被筛选出来。
```

### 14.6 原始模块名排序

步骤：

```text
1. 点击“原始模块名”列头。
2. 再次点击。
3. 第三次点击。
```

预期：

```text
1. 第一次升序，列头显示向上箭头。
2. 第二次降序，列头显示向下箭头。
3. 第三次恢复默认排序，箭头消失。
```

### 14.7 旧配置兼容

准备一个没有 `SourceModuleName` 字段的 `config.json`。

预期：

```text
程序正常启动。
主列表原始模块名列为空白。
不会抛出配置读取错误。
```

## 15. 风险点

### 15.1 主界面卡顿

风险：

```text
如果主界面显示时实时读取 ZAM 文件，会导致网络路径卡顿。
```

规避：

```text
只读取持久化字段 SourceModuleName。
不在 DataGrid Binding 或搜索中访问文件系统。
```

### 15.2 旧数据大量为空

风险：

```text
历史快捷项没有 SourceModuleName，看起来新列为空。
```

处理：

```text
这是兼容旧数据的正常表现。
用户后续重新批量扫描并应用更新后，会逐步补齐。
```

### 15.3 复杂规则模式没有模块名

风险：

```text
老式 MatchType/Pattern CSV 没有 ModulePattern 时，不会读取 ZAM 模块名。
```

处理：

```text
SourceModuleName 为空。
本次不为了显示列而额外读取所有 ZAM 文件。
```

## 16. 验收标准

本需求完成必须满足：

```text
1. 主界面显示四列：名称、备注、原始模块名、更新时间。
2. 原始模块名列位于备注和更新时间之间。
3. 批量新增识别能把 ZAM 原始模块名保存到 ShortcutItem.SourceModuleName。
4. config.json 中能持久化 SourceModuleName。
5. 旧配置没有该字段时程序正常启动。
6. 搜索框支持搜索 SourceModuleName。
7. 原始模块名列支持三态排序。
8. 主界面显示不实时读取网络文件。
9. dotnet build 通过。
10. dotnet test 通过。
```

## 17. 建议提交信息

```text
feat: show source module name in shortcut list
```
