# v1 批量新增识别功能需求说明

## 1. 需求背景

当前 VSLoader 已支持手动新增快捷项。用户需要逐个填写快捷项名称、目标路径和备注。

在实际使用中，目标文件夹通常集中存放在同一个父级目录下，并且文件夹名称具有一定规律，例如：

```text
3134_TSSP001
5924_TSSP002
8812_TSSM001
8864_TSSM002
8914_TSSM003
11314_TSSM004
```

如果逐个手动新增，会比较繁琐。因此需要增加“批量新增识别”功能，让程序自动扫描目标父级目录下的子文件夹，并按照用户提供的 CSV 匹配规则生成快捷项。

## 2. 功能入口

在主窗口顶部按钮区域，“新增”按钮旁边增加一个按钮：

```text
批量新增识别
```

按钮位置建议：

```text
新增 | 批量新增识别 | 编辑 | 删除 | 打开 | 设置
```

点击后打开“批量新增识别”窗口。

## 3. 核心流程

用户操作流程：

1. 点击主界面的“批量新增识别”按钮。
2. 在窗口中输入或选择目标父级文件夹路径。
3. 选择 CSV 匹配规则文件。
4. 点击“扫描预览”。
5. 程序读取目标父级文件夹下的一级子文件夹。
6. 程序根据 CSV 规则匹配每个子文件夹名称。
7. 匹配成功的子文件夹生成待新增快捷项。
8. 未匹配任何规则的子文件夹跳过。
9. 生成名称与已有快捷项重复的项目跳过并提示。
10. 用户在预览列表中确认待新增项目。
11. 用户点击“确认新增”后，程序批量写入快捷项并保存到配置文件。

## 4. “没有匹配到规则”的含义

“没有匹配到规则”指的是：目标父级目录下存在某个子文件夹，但这个子文件夹的名称不符合 CSV 文件里的任何一条规则。

例如目标父级目录中有这些子文件夹：

```text
8812_TSSM001
8864_TSSM002
3365_TRSM005
12001_TCVF009
```

如果 CSV 规则只有：

```csv
MatchType,Pattern,DisplayName,NameTemplate
Contains,TSSM,xxx,{DisplayName}_{FolderName}
```

那么：

```text
8812_TSSM001  -> 命中 TSSM 规则
8864_TSSM002  -> 命中 TSSM 规则
3365_TRSM005  -> 没有命中任何规则
12001_TCVF009 -> 没有命中任何规则
```

本需求中，未匹配任何规则的文件夹处理方式为：

```text
跳过，不自动新增。
```

预览界面可以显示“已跳过数量”，并允许用户查看跳过原因，但不会把这些文件夹写入快捷项。

## 5. CSV 规则文件格式

### 5.1 文件类型

规则文件使用 CSV 文件。

推荐文件名：

```text
batch-rules.csv
```

CSV 可以用 Excel、WPS 表格、记事本、VSCode 编辑。

### 5.2 CSV 表头

CSV 第一行必须是表头，固定为：

```csv
MatchType,Pattern,DisplayName,NameTemplate
```

四列含义：

| 列名 | 是否必填 | 说明 |
| --- | --- | --- |
| `MatchType` | 是 | 匹配方式，v1 支持 `Contains` |
| `Pattern` | 是 | 用来匹配文件夹名称的关键字 |
| `DisplayName` | 是 | 该规则对应的显示名称 |
| `NameTemplate` | 是 | 生成快捷项名称的模板 |

### 5.3 v1 支持的匹配方式

v1 只支持一种匹配方式：

```text
Contains
```

含义是：只要文件夹名称中包含 `Pattern`，就认为命中该规则。

例如：

```csv
MatchType,Pattern,DisplayName,NameTemplate
Contains,TSSM,xxx,{DisplayName}_{FolderName}
```

那么以下文件夹都会命中：

```text
8812_TSSM001
8864_TSSM002
8914_TSSM003
```

### 5.4 名称模板变量

v1 支持两个模板变量：

| 变量 | 含义 |
| --- | --- |
| `{DisplayName}` | CSV 中配置的显示名称 |
| `{FolderName}` | 实际扫描到的子文件夹名称 |

本需求确认采用的名称生成规则为：

```text
xxx_原文件夹名
```

对应 CSV 写法：

```csv
MatchType,Pattern,DisplayName,NameTemplate
Contains,TSSM,xxx,{DisplayName}_{FolderName}
```

示例：

| 文件夹名 | 命中规则 | 生成快捷项名称 |
| --- | --- | --- |
| `8812_TSSM001` | `TSSM` | `xxx_8812_TSSM001` |
| `8864_TSSM002` | `TSSM` | `xxx_8864_TSSM002` |
| `8914_TSSM003` | `TSSM` | `xxx_8914_TSSM003` |

## 6. 用户如何编写 CSV

### 6.1 最简单示例

如果用户只想识别 `TSSM` 文件夹，并把它们统一命名为 `xxx_原文件夹名`，CSV 文件内容如下：

```csv
MatchType,Pattern,DisplayName,NameTemplate
Contains,TSSM,xxx,{DisplayName}_{FolderName}
```

保存为：

```text
batch-rules.csv
```

### 6.2 多种规则示例

如果要识别多个类型，可以每种类型写一行：

```csv
MatchType,Pattern,DisplayName,NameTemplate
Contains,TSSM,xxx,{DisplayName}_{FolderName}
Contains,TSSP,yyy,{DisplayName}_{FolderName}
Contains,TRSM,zzz,{DisplayName}_{FolderName}
Contains,TPSM,aaa,{DisplayName}_{FolderName}
```

示例结果：

| 文件夹名 | 命中 Pattern | DisplayName | 生成快捷项名称 |
| --- | --- | --- | --- |
| `8812_TSSM001` | `TSSM` | `xxx` | `xxx_8812_TSSM001` |
| `3134_TSSP001` | `TSSP` | `yyy` | `yyy_3134_TSSP001` |
| `3365_TRSM005` | `TRSM` | `zzz` | `zzz_3365_TRSM005` |
| `3556_TPSM002` | `TPSM` | `aaa` | `aaa_3556_TPSM002` |

### 6.3 用 Excel 编写 CSV 的方式

用户可以新建 Excel 表格，第一行填写：

```text
MatchType | Pattern | DisplayName | NameTemplate
```

从第二行开始填写规则：

```text
Contains | TSSM | xxx | {DisplayName}_{FolderName}
Contains | TSSP | yyy | {DisplayName}_{FolderName}
Contains | TRSM | zzz | {DisplayName}_{FolderName}
```

填写完成后，另存为：

```text
CSV UTF-8（逗号分隔） (*.csv)
```

注意：

- 不要修改表头名称。
- 不要删除 `NameTemplate` 中的大括号。
- `MatchType` v1 只写 `Contains`。
- `Pattern` 建议填写文件夹名称中的稳定关键字，例如 `TSSM`、`TSSP`。
- `DisplayName` 可以填写中文或英文，例如 `测试站`、`xxx`。

## 7. 匹配规则执行方式

程序读取目标父级目录下的一级子文件夹。

只扫描一级目录，不递归扫描子目录。

示例：

```text
目标父级目录
├─ 8812_TSSM001
├─ 8864_TSSM002
└─ 子目录A
   └─ 更深层目录
```

程序只扫描：

```text
8812_TSSM001
8864_TSSM002
子目录A
```

不扫描：

```text
更深层目录
```

匹配规则：

- 从 CSV 第一条规则开始向下匹配。
- 一个文件夹命中第一条规则后，不再继续匹配后续规则。
- 匹配时忽略大小写。
- 如果没有命中任何规则，跳过该文件夹。

## 8. 预览确认

点击“扫描预览”后，程序必须展示预览列表。

预览列表建议显示：

| 列名 | 说明 |
| --- | --- |
| 状态 | 可新增、已跳过、名称重复、规则错误 |
| 生成名称 | 根据规则生成的快捷项名称 |
| 目标路径 | 父级路径 + 子文件夹名 |
| 文件夹名 | 原始子文件夹名称 |
| 命中规则 | 命中的 `Pattern` |
| 说明 | 跳过或错误原因 |

用户点击“确认新增”时，只新增状态为“可新增”的项目。

## 9. 重名处理

当前 VSLoader 已规定快捷项名称不可重复。

批量新增时也遵守该规则。

如果生成的快捷项名称与已有快捷项重复：

```text
跳过并提示。
```

如果同一批次内生成了重复名称：

```text
跳过重复项并提示。
```

程序不自动追加序号，不自动改名。

## 10. 数据保存

用户点击“确认新增”后：

- 程序将可新增项目加入当前快捷项列表。
- 每个快捷项的 `TargetPath` 为目标父级目录和子文件夹名拼接后的完整路径。
- 每个快捷项的 `CreatedAt` 使用当前本地时间。
- 每个快捷项的 `UpdatedAt` 使用当前本地时间。
- 保存到原有配置文件：

```text
%AppData%\VSLoader\config.json
```

## 11. 界面建议

新增窗口名称：

```text
批量新增识别
```

窗口包含：

- 目标父级文件夹路径输入框。
- “选择文件夹”按钮。
- CSV 规则文件路径输入框。
- “选择 CSV”按钮。
- “扫描预览”按钮。
- 预览列表。
- “确认新增”按钮。
- “取消”按钮。

状态区域显示：

- 扫描到的文件夹数量。
- 可新增数量。
- 跳过数量。
- 重名数量。
- 规则错误数量。

## 12. 异常处理

| 场景 | 处理方式 |
| --- | --- |
| 目标父级路径为空 | 提示用户填写目标父级路径 |
| 目标父级路径不存在 | 提示目标路径不存在或不可访问 |
| CSV 路径为空 | 提示用户选择 CSV 规则文件 |
| CSV 文件不存在 | 提示 CSV 文件不存在 |
| CSV 表头不正确 | 提示 CSV 表头必须是 `MatchType,Pattern,DisplayName,NameTemplate` |
| CSV 有空字段 | 跳过该规则并提示规则错误 |
| `MatchType` 不是 `Contains` | 跳过该规则并提示 v1 只支持 `Contains` |
| 文件夹未匹配任何规则 | 跳过 |
| 生成名称重复 | 跳过并提示 |

## 13. 验收标准

- 主窗口“新增”按钮旁边出现“批量新增识别”按钮。
- 点击按钮后打开批量新增识别窗口。
- 用户可以选择目标父级文件夹。
- 用户可以选择 CSV 规则文件。
- CSV 按 `Contains,TSSM,xxx,{DisplayName}_{FolderName}` 配置后，可以识别 `8812_TSSM001` 等文件夹。
- 识别结果生成名称为 `xxx_8812_TSSM001`。
- 未匹配任何规则的文件夹不会新增。
- 重名项目不会新增，并在预览中提示。
- 用户确认后，可新增项目写入快捷项列表。
- 重启程序后，批量新增的快捷项仍然存在。

