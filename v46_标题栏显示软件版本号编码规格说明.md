# v46 标题栏显示软件版本号编码规格说明

## 1. 需求背景

当前 VSLoader 窗口标题栏显示：

```text
VSLoader
```

用户希望在软件左上角图标和名称后面显示当前软件版本号，便于区分安装包版本、测试版本和更新后的版本。

当前打包脚本：

```text
build-installer.ps1
```

已经通过参数指定版本号：

```powershell
[string]$Version = "1.7.4"
```

并在发布时写入 .NET 程序版本信息：

```powershell
-p:Version=$Version
-p:AssemblyVersion=$Version
-p:FileVersion=$Version
```

因此标题栏版本号应从程序集版本信息读取，而不是在 XAML 中硬编码。

## 2. 需求目标

程序启动后，窗口标题栏显示：

```text
VSLoader v1.7.4
```

如果打包时版本号改为：

```powershell
.\build-installer.ps1 -Version 1.7.5
```

则发布后的程序标题栏应自动显示：

```text
VSLoader v1.7.5
```

## 3. 非目标范围

本次不实现：

1. 不新增设置项。
2. 不允许用户在界面手动修改版本号。
3. 不改安装包版本规则。
4. 不改托盘名称。
5. 不改 exe 文件名。
6. 不做关于页面。
7. 不把版本号写入配置文件。

## 4. 当前代码现状

主窗口文件：

```text
VSLoader/MainWindow.xaml
```

当前标题写死为：

```xml
Title="VSLoader"
```

打包脚本：

```text
build-installer.ps1
```

当前发布命令已写入：

```powershell
-p:Version=$Version
-p:AssemblyVersion=$Version
-p:FileVersion=$Version
```

因此程序内部可以读取到发布版本。

## 5. 推荐实现方案

推荐在：

```text
VSLoader/MainWindow.xaml.cs
```

中设置窗口标题。

实现方式：

1. 保留 XAML 中的默认 `Title="VSLoader"`，作为设计时和兜底标题。
2. 在 `MainWindow` 构造函数 `InitializeComponent()` 之后调用方法：

```csharp
Title = BuildWindowTitle();
```

3. `BuildWindowTitle()` 从当前程序集读取版本号。

推荐读取：

```csharp
Assembly.GetExecutingAssembly().GetName().Version
```

并格式化为：

```text
主版本.次版本.修订版本
```

例如：

```text
1.7.4
```

不要显示第四段 revision，避免出现：

```text
VSLoader v1.7.4.0
```

## 6. 标题格式

固定格式：

```text
VSLoader v{Major}.{Minor}.{Build}
```

示例：

```text
VSLoader v1.7.4
```

如果无法读取有效版本号，兜底：

```text
VSLoader
```

## 7. 版本读取规则

新增辅助方法：

```csharp
private static string BuildWindowTitle()
```

内部逻辑：

```csharp
var version = Assembly.GetExecutingAssembly().GetName().Version;
if (version is null)
{
    return "VSLoader";
}

return $"VSLoader v{version.Major}.{version.Minor}.{version.Build}";
```

注意：

如果 `version.Build < 0`，则只显示：

```text
VSLoader v{Major}.{Minor}
```

但当前打包脚本传入的版本号如 `1.7.4`，Build 正常存在。

## 8. Debug 运行效果

如果直接在 Debug 下运行，版本号可能来自项目默认版本。

可接受显示：

```text
VSLoader v1.0.0
```

或者当前项目编译得到的默认版本。

只要发布时能跟随 `build-installer.ps1 -Version` 即可。

## 9. 与打包脚本的关系

`build-installer.ps1` 当前已经传递：

```powershell
-p:Version=$Version
-p:AssemblyVersion=$Version
-p:FileVersion=$Version
```

本次原则上不需要修改脚本。

如后续发现读取的是 `1.0.0` 而不是脚本版本，可以改为读取：

```csharp
FileVersionInfo.GetVersionInfo(Environment.ProcessPath!).ProductVersion
```

但本次优先使用程序集版本，改动更小。

## 10. 测试建议

如果要做单元测试，建议把标题生成逻辑抽为可测试方法，例如：

```csharp
internal static string FormatWindowTitle(Version? version)
```

测试：

```text
Version(1, 7, 4) -> VSLoader v1.7.4
Version(1, 7) -> VSLoader v1.7
null -> VSLoader
```

但如果不想暴露内部方法，本次可以只做构建验证和手工验证。

推荐最小测试：

```text
新增 MainWindowTitleTests，测试 FormatWindowTitle。
```

## 11. 预期改动文件

预计改动：

- `VSLoader/MainWindow.xaml.cs`

可能改动：

- `VSLoader/MainWindow.xaml`
- `VSLoader.Tests/MainWindowTitleTests.cs`

不预计改动：

- `build-installer.ps1`

## 12. 验收场景

### 12.1 Debug 启动

运行程序后，标题栏显示：

```text
VSLoader v{当前程序集版本}
```

### 12.2 打包版本

执行：

```powershell
.\build-installer.ps1 -Version 1.7.5
```

安装或运行发布目录中的程序后，标题栏显示：

```text
VSLoader v1.7.5
```

### 12.3 版本号变化

版本号从 `1.7.5` 改到 `1.7.6` 后重新打包，标题栏应自动变化。

## 13. 构建与测试验证

实现完成后执行：

```powershell
cd C:\Users\shee_\OneDrive\Desktop\VSLoader
dotnet test .\VSLoader.sln -p:UseSharedCompilation=false
dotnet build .\VSLoader.sln -p:UseSharedCompilation=false
```

如果 `VSLoader.exe` 被占用，先执行：

```powershell
Get-Process -Name VSLoader -ErrorAction SilentlyContinue | Stop-Process -Force
```

最终要求：

- 测试全部通过。
- 构建 `0 个错误`。
- 不引入新的无关警告。

## 14. 最终效果

窗口左上角标题栏从：

```text
VSLoader
```

变为：

```text
VSLoader v1.7.4
```

版本号跟随 `build-installer.ps1` 中发布时指定的 `$Version`，后续更新版本时不需要再手动修改界面代码。
