# v29 托盘菜单样式统一优化编码规格说明

## 1. 需求背景

v28 已实现 VSLoader 最小化到系统托盘功能。

当前托盘图标右键菜单使用：

```csharp
System.Windows.Forms.ContextMenuStrip
System.Windows.Forms.ToolStripMenuItem
```

菜单功能正常，但视觉样式与主列表快捷项右键菜单不一致。

主列表快捷项右键菜单是 WPF `ContextMenu`，样式特点为：

- 白色背景
- 无左侧图标预留空白
- 浅灰边框
- 菜单项文字左对齐
- 菜单项有合适 padding
- 鼠标悬停时为浅蓝背景

用户希望托盘区右键菜单可以模仿主列表快捷项右键菜单的视觉效果。

## 2. 需求目标

本次开发目标：

1. 保持 v28 托盘功能不变。
2. 保持托盘右键菜单仍然包含 `显示 VSLoader` 和 `退出`。
3. 优化托盘右键菜单视觉，使其接近主列表快捷项右键菜单。
4. 托盘菜单背景为纯白。
5. 去掉或压缩 WinForms 菜单默认左侧图标空白。
6. 菜单项 hover 背景使用浅蓝色。
7. 菜单字体使用接近主界面的中文 UI 字体。
8. 不引入复杂自绘 WPF 浮窗。
9. 不影响托盘图标、隐藏窗口、快捷键唤起和退出功能。

## 3. 技术路线

继续使用：

```csharp
System.Windows.Forms.ContextMenuStrip
```

不改成 WPF 自绘菜单。

原因：

- `NotifyIcon` 原生适配 WinForms 菜单。
- WinForms 菜单稳定、轻量。
- WPF 自绘托盘菜单需要额外处理托盘图标坐标、多屏幕、DPI、失焦关闭等问题，复杂度较高。

本次采用轻量优化：

```text
ContextMenuStrip + ToolStripProfessionalRenderer 自定义颜色 + 菜单项尺寸/边距调整
```

## 4. 参考样式

主列表快捷项右键菜单位于：

```text
VSLoader\MainWindow.xaml
```

参考样式片段：

```xml
<ContextMenu Padding="0"
             Background="White"
             BorderBrush="#D1D5DB"
             BorderThickness="1">
```

菜单项参考：

```xml
<Setter Property="MinWidth" Value="130" />
<Setter Property="Padding" Value="14,8" />
<Setter Property="Foreground" Value="#111827" />
```

悬停色：

```xml
#E5F1FB
```

本次托盘菜单应尽量靠近该视觉效果。

## 5. 修改范围

主要修改文件：

```text
VSLoader\MainWindow.xaml.cs
```

原则上不需要修改：

- `MainWindow.xaml`
- `App.xaml`
- `App.xaml.cs`
- `GlobalHotkeyService`
- `MainViewModel`
- `VSLoader.csproj`
- 配置文件结构

## 6. 推荐实现方式

在 `MainWindow.xaml.cs` 中新增一个私有颜色表类：

```csharp
private sealed class TrayMenuColorTable : WinForms.ProfessionalColorTable
{
    public override System.Drawing.Color ToolStripDropDownBackground => System.Drawing.Color.White;
    public override System.Drawing.Color ImageMarginGradientBegin => System.Drawing.Color.White;
    public override System.Drawing.Color ImageMarginGradientMiddle => System.Drawing.Color.White;
    public override System.Drawing.Color ImageMarginGradientEnd => System.Drawing.Color.White;
    public override System.Drawing.Color MenuItemSelected => System.Drawing.Color.FromArgb(229, 241, 251);
    public override System.Drawing.Color MenuItemSelectedGradientBegin => System.Drawing.Color.FromArgb(229, 241, 251);
    public override System.Drawing.Color MenuItemSelectedGradientEnd => System.Drawing.Color.FromArgb(229, 241, 251);
    public override System.Drawing.Color MenuBorder => System.Drawing.Color.FromArgb(209, 213, 219);
    public override System.Drawing.Color MenuItemBorder => System.Drawing.Color.FromArgb(229, 241, 251);
}
```

在创建托盘菜单时设置：

```csharp
menu.Renderer = new WinForms.ToolStripProfessionalRenderer(new TrayMenuColorTable());
```

## 7. ContextMenuStrip 样式要求

在 `CreateTrayMenu()` 中设置菜单整体属性：

```csharp
var menu = new WinForms.ContextMenuStrip
{
    BackColor = System.Drawing.Color.White,
    ForeColor = System.Drawing.Color.FromArgb(17, 24, 39),
    Font = new System.Drawing.Font("Microsoft YaHei UI", 10F),
    Padding = new WinForms.Padding(0),
    ShowImageMargin = false,
    ShowCheckMargin = false,
    Renderer = new WinForms.ToolStripProfessionalRenderer(new TrayMenuColorTable())
};
```

说明：

- `ShowImageMargin = false` 用于去掉左侧图标预留空白。
- `ShowCheckMargin = false` 用于去掉勾选列空白。
- `Padding = 0` 让菜单边缘更接近 WPF 菜单。
- 字体使用 `Microsoft YaHei UI`，与主窗口更一致。

## 8. ToolStripMenuItem 样式要求

新增辅助方法：

```csharp
private static WinForms.ToolStripMenuItem CreateTrayMenuItem(string text, EventHandler onClick)
{
    var item = new WinForms.ToolStripMenuItem(text)
    {
        AutoSize = false,
        Width = 130,
        Height = 36,
        Padding = new WinForms.Padding(14, 0, 14, 0),
        Margin = new WinForms.Padding(0)
    };

    item.Click += onClick;
    return item;
}
```

然后在 `CreateTrayMenu()` 中使用：

```csharp
var showItem = CreateTrayMenuItem("显示 VSLoader", (_, _) => RestoreAndActivate());
var exitItem = CreateTrayMenuItem("退出", (_, _) => ExitApplication());
```

目标效果：

- 菜单项宽度接近快捷项右键菜单。
- 菜单项高度舒适。
- 文本没有明显左侧空白。
- 两个菜单项垂直排列紧凑清晰。

## 9. 边框样式说明

WinForms `ContextMenuStrip` 的边框可控性不如 WPF `ContextMenu`。

本次通过：

```csharp
ProfessionalColorTable.MenuBorder
```

尽量设置边框颜色为：

```text
#D1D5DB
```

不要求做到像 WPF 控件那样完全一致。

验收标准以视觉接近、无明显灰色块、无左侧空白为主。

## 10. 不推荐方案

本次不推荐改为 WPF 自绘托盘菜单。

不推荐原因：

- 需要定位托盘图标屏幕坐标。
- 需要处理多显示器。
- 需要处理 Windows 缩放 DPI。
- 需要处理菜单失焦自动关闭。
- 需要处理任务栏在上下左右不同位置。
- 复杂度明显高于当前需求。

## 11. 不允许改变的功能

本次不允许改变：

- 点击 `X` 隐藏到托盘。
- 托盘右键菜单功能项。
- 双击托盘图标显示窗口。
- 托盘菜单 `显示 VSLoader` 功能。
- 托盘菜单 `退出` 功能。
- 全局快捷键监听。
- 隐藏到托盘后快捷键恢复窗口。
- 应用图标资源。

## 12. 验收标准

### 12.1 菜单背景

操作：

1. 启动 VSLoader。
2. 右键托盘图标。

期望：

- 托盘菜单背景为纯白。
- 不出现突兀灰色块。

### 12.2 左侧空白

期望：

- 菜单项左侧不再出现明显图标预留空白。
- 文本位置接近主列表快捷项右键菜单。

### 12.3 悬停状态

操作：

1. 鼠标移动到 `显示 VSLoader` 或 `退出`。

期望：

- 当前项背景显示浅蓝色。
- 悬停效果与主列表右键菜单风格接近。

### 12.4 菜单尺寸

期望：

- 菜单宽度约为 130 像素。
- 菜单项高度约为 36 像素。
- 视觉上比系统默认托盘菜单更接近应用内右键菜单。

### 12.5 功能不回归

操作：

1. 点击 `显示 VSLoader`。
2. 双击托盘图标。
3. 点击 `退出`。
4. 隐藏到托盘后按全局快捷键。

期望：

- 所有 v28 功能保持正常。

### 12.6 编译验收

执行：

```powershell
dotnet build .\VSLoader.sln
```

期望：

```text
0 warnings
0 errors
```

如果提示 `VSLoader.exe` 被占用，说明程序正在运行，应先关闭 VSLoader 后重新构建。

## 13. 建议实施步骤

1. 打开 `VSLoader\MainWindow.xaml.cs`。
2. 找到 `CreateTrayMenu()`。
3. 为 `ContextMenuStrip` 增加 `BackColor`、`ForeColor`、`Font`、`Padding`、`ShowImageMargin`、`ShowCheckMargin`、`Renderer`。
4. 新增 `TrayMenuColorTable` 私有类。
5. 新增 `CreateTrayMenuItem()` 辅助方法。
6. 用辅助方法创建 `显示 VSLoader` 和 `退出` 两个菜单项。
7. 编译项目。
8. 手动右键托盘图标检查视觉效果。
9. 验证 `显示 VSLoader`、双击托盘、`退出`、全局快捷键均未回归。

## 14. 明确不做的事情

本次不做：

- WPF 自绘托盘弹出菜单。
- 托盘左键单击行为。
- 托盘气泡通知。
- 托盘菜单图标。
- 托盘菜单分割线。
- 托盘菜单圆角。
- 托盘菜单阴影。
- 菜单动画。
