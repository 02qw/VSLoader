# v13 顶部按钮均匀分布与列表列宽优化编码规格说明

## 1. 需求背景

v11 将顶部工具区拆成两行，解决了搜索框和大量按钮挤在一行的问题。

v12 将第二行按钮从左右两端分散改成左对齐紧凑分组，解决了中间大留白割裂的问题。

但 v12 后仍存在新的视觉问题：

- 第二行按钮整体堆在左侧。
- 第二行右侧仍有一大片空白区域。
- 按钮没有充分利用第二行可用空间。
- 下方列表列与列之间存在无意义的大空白，尤其备注列使用 `*` 后会吃掉过多剩余空间。

本次 v13 需求希望进一步优化空间分配：

1. 顶部第二行按钮在可用区域中更均匀分布。
2. 下方列表列宽更克制，减少无意义大片空白。
3. 保持信息完整可见，不牺牲快捷项列表的大局观。

## 2. 核心设计目标

本次优化不是简单地把界面“填满”，而是让空间分配更有秩序。

目标：

- 第二行按钮不要全部挤在左侧。
- 第二行按钮也不要左右两端割裂。
- 按钮应在第二行横向区域中均匀分布。
- 列表中名称、备注、更新时间之间距离应更合理。
- 每一行快捷项仍然是单行展示。
- 不遮挡下方快捷项信息。
- 不增加顶部工具区高度。
- 不改变任何业务逻辑。

## 3. 修改范围

主要修改文件：

- `VSLoader\MainWindow.xaml`

原则上不需要修改：

- `VSLoader\ViewModels\MainViewModel.cs`
- `VSLoader\MainWindow.xaml.cs`
- 服务层代码
- 模型层代码

本次属于布局和列宽优化。

## 4. 顶部第二行按钮布局设计

### 4.1 当前问题

当前第二行类似：

```text
[新增] [批量新增识别] [自动获取连接]    [AdminUI] [打开] [编辑] [删除]                          空白
```

右侧空白面积过大，视觉上不平衡。

### 4.2 目标效果

期望第二行更接近：

```text
[新增]       [批量新增识别]       [自动获取连接]       [AdminUI]       [打开]       [编辑]       [删除]
```

按钮之间不是紧贴，也不是被推到两端，而是在第二行可用宽度中均匀展开。

### 4.3 推荐技术方案：UniformGrid

推荐将第二行按钮区域改为 `UniformGrid`：

```xml
<UniformGrid Grid.Row="1"
             Margin="0,10,0,0"
             Columns="7">
    ...
</UniformGrid>
```

每个按钮占一个等宽槽位，按钮本身在槽位内居中或拉伸。

推荐按钮在槽位内拉伸：

```xml
HorizontalAlignment="Stretch"
Margin="0,0,10,0"
```

最后一个按钮可不设置右侧 margin，避免行尾多余空白。

### 4.4 按钮顺序

从左到右保持：

```text
新增
批量新增识别
自动获取连接
AdminUI
打开
编辑
删除
```

### 4.5 按钮高度

保持：

```text
Height = 36
```

不增加顶部工具区高度。

### 4.6 按钮宽度策略

不再使用固定按钮宽度作为主要布局依据。

建议：

- 移除或弱化 `Width="76"`、`Width="112"` 等固定宽度。
- 让按钮在等宽槽位内拉伸。
- 使用 `MinWidth` 保证短按钮不会过窄。

推荐：

```xml
MinWidth="76"
Height="36"
HorizontalAlignment="Stretch"
```

对于中文较长按钮，例如：

```text
批量新增识别
自动获取连接
```

可以设置：

```xml
MinWidth="112"
```

但如果使用 `UniformGrid Columns="7"`，通常每个槽位宽度足够，不需要单独固定 `Width`。

## 5. 列表列宽优化设计

### 5.1 当前问题

当前列表列宽大致为：

```text
名称：340
备注：*
更新时间：180
```

当窗口宽度较大时，备注列会吃掉所有剩余空间，导致：

- 名称列与备注列之间距离显得大。
- 备注列与更新时间列之间距离显得大。
- 更新时间列被推得很远。
- 整体像被横向拉开。

### 5.2 目标效果

列表应该尽量完整显示内容，同时避免列与列之间出现无意义的大块空白。

期望：

```text
名称              备注                         更新时间
```

而不是：

```text
名称                         备注                                      更新时间
```

### 5.3 推荐方案：使用比例列宽

建议将列宽从固定 + 单个 `*` 改为比例分配：

```xml
名称：2*
备注：3*
更新时间：1.2*
```

但 WPF `DataGridTextColumn.Width` 常用写法可使用：

```xml
Width="2*"
Width="3*"
Width="180"
```

综合稳定性和可读性，本项目推荐：

```text
名称：2*
备注：3*
更新时间：180
```

这样：

- 名称列会随窗口适度伸缩。
- 备注列仍然比名称列宽，但不会无限吃掉所有意义空间。
- 更新时间保持固定宽度，便于右对齐。

### 5.4 可选方案：固定列 + 最大备注宽度

如果后续发现比例列宽效果仍不理想，可考虑：

```text
名称：300
备注：*
更新时间：180
```

同时控制窗口默认宽度。

但本次优先采用比例列宽。

### 5.5 文本溢出处理

保持当前列文本裁剪策略：

```xml
TextTrimming="CharacterEllipsis"
```

这样当内容过长时，不会撑开布局。

## 6. 第一行保持不变

第一行仍然是：

```text
[搜索框........................................] [设置]
```

不调整：

- 搜索框绑定。
- 设置按钮位置。
- 设置按钮命令。
- 第一行高度。

## 7. 窗口尺寸策略

保持 v11 设置：

```xml
Width="1040"
MinWidth="860"
```

本次暂不修改窗口默认尺寸。

如果实现后 `UniformGrid` 在 `MinWidth=860` 下导致长按钮文本拥挤，再考虑将 `MinWidth` 轻微提升到 `900`。

但本次优先不动窗口尺寸。

## 8. 功能绑定要求

所有按钮必须保持现有命令绑定：

| 按钮 | 命令 |
|---|---|
| 新增 | `AddShortcutCommand` |
| 批量新增识别 | `OpenBatchImportCommand` |
| 自动获取连接 | `DownloadAdminUiLinksCommand` |
| AdminUI | `OpenAdminUiCommand` |
| 打开 | `OpenShortcutCommand` |
| 编辑 | `EditShortcutCommand` |
| 删除 | `DeleteShortcutCommand` |
| 设置 | `OpenSettingsCommand` |

搜索框继续保持：

```xml
Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
```

## 9. 不允许改变的行为

本次不允许改变：

- 顶部工具区行数。
- 第一行搜索框和设置按钮布局。
- 下方快捷项单行展示。
- 快捷项列表排序逻辑。
- 搜索逻辑。
- 双击打开 VSCode。
- 右键菜单。
- AdminUI 打开逻辑。
- AdminUI 密码复制逻辑。
- 自动获取连接逻辑。
- 设置窗口逻辑。

## 10. 性能要求

本次只调整 XAML 布局，不应影响列表性能。

必须保持：

- `EnableRowVirtualization="True"`
- `EnableColumnVirtualization="True"`
- `VirtualizingPanel.IsVirtualizing="True"`
- `VirtualizingPanel.VirtualizationMode="Recycling"`

不要新增：

- 每行动画。
- 每行阴影。
- 复杂卡片模板。
- 图片或图标列。

## 11. 验收标准

### 11.1 顶部按钮视觉验收

启动应用后：

- 第二行按钮不再全部堆在左侧。
- 第二行右侧不再出现突兀大片空白。
- 七个按钮在第二行区域中较均匀分布。
- 按钮文字完整可见。
- 顶部仍然只有两行。
- 下方列表没有被遮挡。

### 11.2 列表列宽验收

启动应用后：

- 名称、备注、更新时间三列之间距离更自然。
- 备注列不会制造大片无意义空白。
- 更新时间仍然完整显示。
- 快捷项仍然单行显示。
- 长文本使用省略号，不撑破布局。

### 11.3 功能验收

以下功能必须正常：

1. 搜索框输入时列表实时过滤。
2. `新增` 按钮可用。
3. `批量新增识别` 按钮可用。
4. `自动获取连接` 按钮可用。
5. `AdminUI` 按钮可用。
6. `打开` 按钮可用。
7. `编辑` 按钮可用。
8. `删除` 按钮可用。
9. `设置` 按钮可用。
10. 双击快捷项仍可打开 VSCode。
11. 右键菜单仍可正常显示和执行。

### 11.4 编译验收

执行：

```powershell
dotnet build .\VSLoader.sln
```

期望：

```text
0 warnings
0 errors
```

如果提示 `VSLoader.exe` 被占用，说明程序正在运行，应先关闭 VSLoader 后重新构建。

## 12. 建议实施步骤

1. 打开 `VSLoader\MainWindow.xaml`。
2. 找到顶部第二行按钮区域。
3. 将外层第二行布局改为 `UniformGrid Columns="7"`。
4. 移除第二行按钮的固定 `Width`，改用 `MinWidth` 和 `HorizontalAlignment="Stretch"`。
5. 保持按钮高度为 `36`。
6. 调整按钮间距，避免行尾多余空白。
7. 将列表列宽调整为：
   - 名称：`2*`
   - 备注：`3*`
   - 更新时间：`180`
8. 编译项目。
9. 手动检查顶部按钮和列表列宽效果。

## 13. 明确不做的事情

本次不做：

- 隐藏按钮。
- 增加更多菜单。
- 改成图标按钮。
- 改变窗口默认尺寸。
- 增加第三行工具栏。
- 改变下方列表行高。
- 改变快捷项数据模型。
- 修改业务逻辑。
