# v80 工作区真实目录英文命名编码规格说明

## 1. 文档目的

本文件用于指导编程 Agent 修改 VSLoader 的工作区创建逻辑，使新增工作区的真实文件夹名固定使用英文安全命名。

核心目标：

```text
工作区真实目录名只使用英文、数字，不使用用户输入的中文显示名。
```

用户看到的工作区名称仍然可以是中文，并且重命名时只修改显示名称，不修改真实目录名。

## 2. 背景说明

当前 VSLoader 已经支持多工作区。

工作区数据保存位置：

```text
%AppData%\VSLoader\Workspaces\<工作区真实目录名>
```

每个工作区目录中保存：

```text
config.json
workspace.json
window-layout.json
factory-map.layout.json
UIdownload
```

当前项目已经实现：

```text
1. 工作区选择
2. 新增工作区
3. 重命名工作区显示名称
4. 删除工作区
5. 工作区级配置隔离
```

但是当前新增工作区时，真实目录名与工作区 Id 可能从用户输入的显示名称生成。  
如果用户输入中文工作区名，真实路径中可能出现中文或由中文过滤后生成不稳定目录名。

由于 AdminUI / JNLP / 外部启动链路可能受路径字符影响，工作区真实路径应尽量稳定、英文、安全。

## 3. 当前代码现状

主要相关文件：

```text
VSLoader\Models\Services\WorkspaceService.cs
VSLoader\Models\WorkspaceInfo.cs
VSLoader\ViewModels\WorkspaceSelectorViewModel.cs
VSLoader.Tests\WorkspaceServiceTests.cs
VSLoader.Tests\WorkspaceSelectorViewModelTests.cs
```

当前 `WorkspaceService.CreateWorkspace` 大致逻辑：

```csharp
var name = string.IsNullOrWhiteSpace(displayName) ? "新工作区" : displayName.Trim();
var id = CreateWorkspaceId(name, settings.Workspaces);

var info = new WorkspaceInfo
{
    Id = id,
    Name = name,
    Path = Path.Combine(WorkspacesDirectory, id),
};
```

当前 `CreateWorkspaceId` 会从显示名称中提取：

```csharp
char.IsLetterOrDigit(ch) || ch is '-' or '_'
```

这导致真实目录名与用户输入显示名称耦合。

## 4. 需求目标

完成后程序应满足：

```text
1. default 工作区保持现有目录，不做迁移。
2. 新增工作区的真实目录名自动使用 work1、work2、work3 这类英文名。
3. 新增工作区的 Id 与真实目录名保持一致，例如 work1。
4. 用户输入的工作区名称只作为显示名称保存到 WorkspaceInfo.Name。
5. 重命名工作区只修改显示名称，不修改 Id，不修改 Path，不重命名文件夹。
6. 工作区选择窗口显示中文显示名，不显示或不依赖真实目录名。
7. 删除工作区时仍删除对应真实目录文件夹。
8. 打开工作区时仍按 WorkspaceInfo.Path 进入对应真实目录。
9. 已存在的旧工作区不自动迁移、不自动改名，避免数据丢失风险。
10. 如果 work1 已存在，则自动找下一个可用编号。
```

## 5. 命名规则

### 5.1 默认工作区

现有默认工作区保持不变。

当前代码中默认值：

```csharp
private const string DefaultWorkspaceId = "default";
private const string DefaultWorkspaceDirectoryName = "Default";
```

本次不强制迁移已经存在的默认目录。

如果当前代码默认目录是：

```text
Default
```

可以保持现状。

如果后续想统一改成小写：

```text
default
```

应另开迁移需求，本次不做。

### 5.2 新增工作区

新增工作区真实目录名统一格式：

```text
work1
work2
work3
...
```

不使用：

```text
work 1
work-1
工作区A
产线一
用户输入名称
```

推荐规则：

```text
从 1 开始递增。
如果 app-settings.json 中已有 Id=work1 或 Workspaces\work1 文件夹已存在，则跳过 work1。
继续尝试 work2、work3，直到找到可用值。
```

### 5.3 显示名称

用户输入：

```text
一号线
新线测试
67线
```

只保存为：

```csharp
WorkspaceInfo.Name
```

不参与真实目录名生成。

示例：

```json
{
  "Id": "work1",
  "Name": "一号线",
  "Path": "%AppData%\\VSLoader\\Workspaces\\work1"
}
```

## 6. 非目标范围

本阶段不实现：

```text
1. 不迁移已有中文目录工作区。
2. 不自动重命名已有工作区文件夹。
3. 不修改 default 工作区真实目录。
4. 不改工作区选择窗口整体 UI。
5. 不修改工作区删除规则。
6. 不修改工作区重命名弹窗交互。
7. 不修改快捷项、地图、AdminUI、WebUI 的业务数据结构。
8. 不新增导入导出工作区功能。
9. 不实现用户自定义真实目录名。
```

## 7. 详细技术方案

### 7.1 修改 WorkspaceService.CreateWorkspace

目标文件：

```text
VSLoader\Models\Services\WorkspaceService.cs
```

当前：

```csharp
var id = CreateWorkspaceId(name, settings.Workspaces);
```

应调整为：

```csharp
var id = CreateNextEnglishWorkspaceId(settings.Workspaces);
```

其中：

```text
name 继续来自用户输入，作为显示名。
id 不再来自 name。
Path 继续使用 Path.Combine(WorkspacesDirectory, id)。
```

### 7.2 新增英文 Id 生成方法

建议在 `WorkspaceService` 中新增私有方法：

```csharp
private string CreateNextEnglishWorkspaceId(IReadOnlyCollection<WorkspaceInfo> existingWorkspaces)
```

推荐逻辑：

```csharp
private string CreateNextEnglishWorkspaceId(IReadOnlyCollection<WorkspaceInfo> existingWorkspaces)
{
    var index = 1;
    while (true)
    {
        var id = $"work{index}";
        var path = Path.Combine(WorkspacesDirectory, id);
        var idExists = existingWorkspaces.Any(workspace =>
            string.Equals(workspace.Id, id, StringComparison.OrdinalIgnoreCase));
        var pathExists = Directory.Exists(path);

        if (!idExists && !pathExists)
        {
            return id;
        }

        index++;
    }
}
```

说明：

```text
同时检查 app-settings.json 中的 Id 和磁盘文件夹，可以避免残留文件夹冲突。
```

### 7.3 保留旧方法或删除旧方法

当前 `CreateWorkspaceId` 如果只被 `CreateWorkspace` 使用，可以删除。

如果仍有其他测试或逻辑依赖，可以保留但不再用于新增工作区。

要求：

```text
新增工作区不能再调用基于 displayName 的 CreateWorkspaceId。
```

### 7.4 RenameWorkspace 不变

当前重命名逻辑已经只改：

```text
workspace.Name
workspace.json 中的 Name
UpdatedAt
```

不改：

```text
workspace.Id
workspace.Path
真实目录名
```

本次应保持该行为。

如有测试，需明确断言：

```text
重命名后 Id 不变。
重命名后 Path 不变。
重命名后 Directory.Exists(oldPath) 仍为 true。
```

## 8. 数据示例

### 8.1 新增中文显示名工作区

用户输入：

```text
六七线
```

保存到程序区配置：

```json
{
  "Id": "work1",
  "Name": "六七线",
  "Path": "C:\\Users\\shee_\\AppData\\Roaming\\VSLoader\\Workspaces\\work1"
}
```

真实目录：

```text
%AppData%\VSLoader\Workspaces\work1
```

### 8.2 重命名

重命名前：

```json
{
  "Id": "work1",
  "Name": "六七线",
  "Path": "%AppData%\\VSLoader\\Workspaces\\work1"
}
```

重命名为：

```text
新六七线
```

重命名后：

```json
{
  "Id": "work1",
  "Name": "新六七线",
  "Path": "%AppData%\\VSLoader\\Workspaces\\work1"
}
```

真实目录仍然是：

```text
%AppData%\VSLoader\Workspaces\work1
```

## 9. 兼容性处理

### 9.1 已有工作区

已有工作区不自动迁移。

如果用户之前已经有：

```text
%AppData%\VSLoader\Workspaces\一号线
%AppData%\VSLoader\Workspaces\workspace
%AppData%\VSLoader\Workspaces\abc
```

本次程序启动后：

```text
继续按 app-settings.json 中的 Path 使用。
不自动改名。
不自动移动文件夹。
```

原因：

```text
工作区目录内包含配置、地图、下载文件、布局等数据，自动迁移风险较高。
```

### 9.2 新增工作区

从本版本开始，所有新增工作区才使用：

```text
work1、work2、work3...
```

### 9.3 残留目录冲突

如果磁盘中已经存在：

```text
Workspaces\work1
```

即使 app-settings.json 里没有登记 `work1`，新增工作区也应跳过它，创建：

```text
work2
```

避免覆盖用户残留数据。

## 10. 测试要求

### 10.1 WorkspaceService 测试

建议修改或新增：

```text
VSLoader.Tests\WorkspaceServiceTests.cs
```

新增测试一：

```text
CreateWorkspace_uses_english_work_id_instead_of_display_name
```

输入：

```text
displayName = "六七线"
```

预期：

```text
context.Id == "work1"
context.Name == "六七线"
context.RootPath 以 Workspaces\work1 结尾
Directory.Exists(context.RootPath) == true
```

新增测试二：

```text
CreateWorkspace_skips_existing_work_ids
```

准备：

```text
settings.Workspaces 中已有 Id=work1
```

输入：

```text
displayName = "另一条线"
```

预期：

```text
新工作区 Id == "work2"
```

新增测试三：

```text
CreateWorkspace_skips_existing_work_directories
```

准备：

```text
磁盘中预先创建 Workspaces\work1
settings.Workspaces 为空或不包含 work1
```

输入：

```text
displayName = "新线"
```

预期：

```text
新工作区 Id == "work2"
```

新增测试四：

```text
RenameWorkspace_keeps_id_and_path_unchanged
```

准备：

```text
创建 work1 工作区
重命名为 "中文显示名"
```

预期：

```text
workspace.Id 仍为 work1
workspace.Path 仍指向 Workspaces\work1
真实目录没有被重命名
workspace.Name 更新为中文显示名
workspace.json 中 Name 更新为中文显示名
```

### 10.2 WorkspaceSelectorViewModel 测试

如果已有新增工作区测试，需更新断言。

重点断言：

```text
CreateWorkspace("六七线") 成功后：
SelectedWorkspace.Id == "work1"
SelectedWorkspace.Name == "六七线"
```

不要再断言 Id 来源于显示名称。

## 11. 手工验收

### 场景一：新增中文工作区

操作：

```text
1. 打开工作区选择窗口。
2. 点击新增。
3. 输入：六七线。
4. 创建成功。
5. 打开工作区文件夹。
```

预期：

```text
界面显示：六七线。
真实文件夹路径：%AppData%\VSLoader\Workspaces\work1 或下一个可用 workN。
路径中不包含“六七线”。
```

### 场景二：重命名

操作：

```text
1. 选择 work1 对应的工作区。
2. 点击重命名。
3. 改名为：新六七线。
4. 打开工作区文件夹。
```

预期：

```text
界面显示：新六七线。
真实文件夹仍然是 work1。
没有新建“新六七线”文件夹。
没有丢失 config.json、地图文件、UIdownload。
```

### 场景三：连续新增

操作：

```text
连续新增三个工作区：
一号线
二号线
三号线
```

预期：

```text
真实目录依次为：
work1
work2
work3

界面显示依次为：
一号线
二号线
三号线
```

### 场景四：已有目录冲突

操作：

```text
手动在 Workspaces 下创建 work1 文件夹。
然后在程序中新增工作区。
```

预期：

```text
程序创建 work2。
不会覆盖已有 work1。
```

## 12. 风险点

### 12.1 误迁移已有工作区

风险：

```text
自动改已有工作区目录可能导致路径失效、地图丢失、下载文件找不到。
```

规避：

```text
本次只影响新增工作区。
已有工作区保持原 Path。
```

### 12.2 重命名时误改文件夹

风险：

```text
用户只是想改显示名，却导致真实目录变化。
```

规避：

```text
RenameWorkspace 只改 Name，不改 Id，不改 Path。
```

### 12.3 work1 残留目录被覆盖

风险：

```text
磁盘中已有 work1，但 app-settings.json 没有登记，新增时覆盖旧数据。
```

规避：

```text
生成 workN 时必须同时检查 existingWorkspaces 和 Directory.Exists。
```

## 13. 验收标准

本需求完成必须满足：

```text
1. 新增中文工作区时，真实目录名不包含中文。
2. 新增工作区真实目录名格式为 workN。
3. workN 从 1 开始寻找第一个可用编号。
4. 用户看到的显示名称仍然是输入的中文名。
5. 重命名只改显示名称，不改真实目录。
6. 已有工作区不自动迁移。
7. 删除工作区仍删除 WorkspaceInfo.Path 指向的真实目录。
8. dotnet build 通过。
9. dotnet test 通过。
```

## 14. 推荐执行命令

实现前停止运行中的程序：

```powershell
Get-Process -Name VSLoader -ErrorAction SilentlyContinue | Stop-Process -Force
```

构建：

```powershell
dotnet build C:\Users\shee_\OneDrive\Desktop\VSLoader\VSLoader.sln -p:UseSharedCompilation=false
```

测试：

```powershell
dotnet test C:\Users\shee_\OneDrive\Desktop\VSLoader\VSLoader.sln -p:UseSharedCompilation=false
```

## 15. 推荐提交信息

```text
fix: use english workspace directory names
```
