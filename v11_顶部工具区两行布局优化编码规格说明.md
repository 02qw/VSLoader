# v11 顶部工具区两行布局优化编码规格说明

## 1. 需求背景

当前 VSLoader 主窗口顶部区域将搜索框和所有操作按钮放在同一行。

由于按钮数量较多，按钮区占用了大量横向空间，导致：

- 主窗口默认宽度较大。
- 搜索框被拉得很长。
- 下方快捷项列表跟随窗口横向铺开。
- 整体界面显得过宽、松散，不够紧凑。

本次 v11 需求希望重新设计顶部工具区：  
将搜索区和操作按钮区拆成两行，以降低横向空间压力，同时保持所有按钮可见。

## 2. 需求目标

本次优化目标：

1. 顶部工具区从“一行布局”改为“两行布局”。
2. 第一行放搜索框和设置按钮。
3. 第二行放快捷项操作按钮。
4. 所有现有功能按钮必须保留，不隐藏到更多菜单。
5. 主窗口默认宽度和最小宽度适当降低。
6. 不改变任何业务逻辑和按钮绑定命令。

## 3. 修改范围

主要修改文件：

- `VSLoader\MainWindow.xaml`

原则上不需要修改：

- `VSLoader\ViewModels\MainViewModel.cs`
- `VSLoader\MainWindow.xaml.cs`
- 服务层和模型层代码

本次属于布局优化，不是功能开发。

## 4. 当前问题来源

当前窗口配置：

```xml
Width="1280"
MinWidth="1120"
```

顶部工具区当前结构类似：

```text
[搜索框                                  ] [新增] [批量新增识别] [自动获取连接] [AdminUI] [编辑] [删除] [打开] [设置]
```

按钮数量较多，并且按钮区使用固定宽度按钮 + `Auto` 列，导致窗口横向压力较大。

## 5. 新布局设计

### 5.1 整体结构

顶部工具区改为两行：

```text
第一行：
[搜索快捷项...                                      ] [设置]

第二行：
[新增] [批量新增识别] [自动获取连接]        [AdminUI] [打开] [编辑] [删除]
```

### 5.2 第一行：搜索 + 设置

第一行只放：

- 搜索输入框
- 设置按钮

布局规则：

- 搜索框占据剩余空间。
- 设置按钮固定宽度，位于最右侧。
- 设置按钮作为全局配置入口，不放在第二行快捷项操作区。

推荐结构：

```xml
<Grid Grid.Row="0">
    <Grid.ColumnDefinitions>
        <ColumnDefinition Width="*" />
        <ColumnDefinition Width="Auto" />
    </Grid.ColumnDefinitions>

    <TextBox Grid.Column="0" ... />
    <Button Grid.Column="1" Content="设置" ... />
</Grid>
```

### 5.3 第二行：操作按钮分组

第二行放快捷项操作按钮，并分为左右两组。

左侧：创建和批处理类操作

```text
[新增] [批量新增识别] [自动获取连接]
```

右侧：选中项操作

```text
[AdminUI] [打开] [编辑] [删除]
```

推荐结构：

```xml
<Grid Grid.Row="1" Margin="0,10,0,0">
    <Grid.ColumnDefinitions>
        <ColumnDefinition Width="Auto" />
        <ColumnDefinition Width="*" />
        <ColumnDefinition Width="Auto" />
    </Grid.ColumnDefinitions>

    <StackPanel Grid.Column="0" Orientation="Horizontal">
        ...
    </StackPanel>

    <StackPanel Grid.Column="2" Orientation="Horizontal">
        ...
    </StackPanel>
</Grid>
```

中间 `*` 列用于自然拉开左右分组。

## 6. 按钮顺序要求

### 6.1 第一行

从左到右：

```text
搜索框
设置
```

### 6.2 第二行左侧

从左到右：

```text
新增
批量新增识别
自动获取连接
```

### 6.3 第二行右侧

从左到右：

```text
AdminUI
打开
编辑
删除
```

注意：

- 当前旧布局中 `编辑`、`删除` 在 `打开` 前面。
- 新布局建议将高频动作 `打开` 放在 `AdminUI` 后面，`编辑`、`删除` 放后面。
- 如果实现时希望完全保持旧顺序，也可以保留 `AdminUI`、`编辑`、`删除`、`打开`，但推荐使用本规格的新顺序。

## 7. 窗口尺寸建议

当前：

```xml
Width="1280"
MinWidth="1120"
```

建议调整为：

```xml
Width="1040"
MinWidth="860"
```

理由：

- 顶部工具区拆成两行后，横向空间压力降低。
- `860` 的最小宽度仍能容纳第二行左右按钮组。
- 默认 `1040` 更接近工具软件的自然宽度。

窗口高度可暂时保持：

```xml
Height="620"
MinHeight="520"
```

如实现后顶部两行导致列表可视区域明显不足，可再微调高度，但本次优先不修改高度。

## 8. 列表列宽建议

当前列表列宽：

```xml
名称：360
备注：*
更新时间：190
```

在窗口默认宽度降低后，建议轻微调整：

```text
名称：340
备注：*
更新时间：180
```

要求：

- 仍然不显示目标路径列。
- 仍然保持单行一项。
- 备注列继续使用 `*` 吃剩余空间。

## 9. 顶部工具区视觉细节

建议：

- 搜索框高度保持 `36`。
- 按钮高度统一为 `36`。
- 第一行和第二行之间间距 `10`。
- 按钮之间间距保持 `8`。
- 第二行左右两组之间通过中间 `*` 空间自然分隔。
- 不增加额外说明文字。
- 不添加图标，除非后续单独设计。

## 10. 功能绑定要求

所有按钮必须保持原有命令绑定：

| 按钮 | 命令 |
|---|---|
| 新增 | `AddShortcutCommand` |
| 批量新增识别 | `OpenBatchImportCommand` |
| 自动获取连接 | `DownloadAdminUiLinksCommand` |
| AdminUI | `OpenAdminUiCommand` |
| 打开 | `OpenShortcutCommand` |
| 编辑 | `EditShortcutCommand` |
| 删除 | `DeleteShortcutCommand` |
| 设置 | `OpenSettingsCommand` |

搜索框必须保持：

```xml
Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
```

## 11. 不允许改变的行为

本次不允许改变：

- 搜索逻辑。
- 快捷项列表排序逻辑。
- 双击打开 VSCode 逻辑。
- 右键菜单逻辑。
- AdminUI 打开逻辑。
- 自动获取连接逻辑。
- 设置窗口逻辑。
- 忙碌遮罩逻辑。

本次只修改布局和必要的尺寸。

## 12. 验收标准

### 12.1 视觉验收

启动应用后：

- 窗口默认不再显得过宽。
- 搜索框不再和所有按钮挤在同一行。
- 顶部第一行是搜索框 + 设置。
- 顶部第二行是操作按钮，左右分组清晰。
- 下方列表仍然有足够空间展示快捷项。

### 12.2 功能验收

以下功能必须正常：

1. 搜索框输入时列表实时过滤。
2. 点击 `新增` 可打开新增窗口。
3. 点击 `批量新增识别` 可打开批量识别窗口。
4. 点击 `自动获取连接` 可执行 AdminUI 下载逻辑。
5. 选中快捷项后点击 `AdminUI` 可打开对应 `.jnlp`。
6. 选中快捷项后点击 `打开` 可用 VSCode 打开路径。
7. 选中快捷项后点击 `编辑` 可编辑。
8. 选中快捷项后点击 `删除` 可删除。
9. 点击 `设置` 可打开设置窗口。
10. 右键菜单和双击功能不受影响。

### 12.3 编译验收

执行：

```powershell
dotnet build .\VSLoader.sln
```

期望：

```text
0 warnings
0 errors
```

如果提示 `VSLoader.exe` 被占用，说明程序正在运行，应先关闭 VSLoader 后重新构建。

## 13. 建议实施步骤

1. 打开 `VSLoader\MainWindow.xaml`。
2. 将窗口 `Width` 改为 `1040`。
3. 将窗口 `MinWidth` 改为 `860`。
4. 找到当前顶部搜索框和按钮所在的 `Grid DockPanel.Dock="Top"`。
5. 将该 Grid 改成两行布局。
6. 第一行放搜索框和设置按钮。
7. 第二行放左右两组操作按钮。
8. 将 `设置` 按钮从旧按钮组中移到第一行右侧。
9. 调整列表列宽为 `340 / * / 180`。
10. 编译并人工验证按钮功能。

## 14. 明确不做的事情

本次不做：

- 隐藏按钮。
- 增加更多菜单。
- 增加图标按钮。
- 改成 Ribbon。
- 改成侧边栏。
- 改变快捷项列表行样式。
- 改变业务命令和 ViewModel。
