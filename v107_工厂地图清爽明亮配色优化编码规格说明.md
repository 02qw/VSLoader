# v107 工厂地图清爽明亮配色优化编码规格说明

## 1. 背景

v102 中为了让地图背景和节点区分开，已将地图画布背景调整为偏灰蓝色：

```text
地图背景：#EEF2F7
小网格：#E5EAF1
主网格：#D7DEE8
节点边框：#A8B3C3
```

该方案解决了“白色节点和背景分不清”的问题，但实际观感偏灰、偏冷、偏沉，用户反馈为：

```text
土灰土灰的，看着灰蒙蒙的没有生气
```

本次希望借鉴 Google / Apple 类产品中更清爽、明亮、干净的视觉倾向，在不破坏现有地图可读性和编辑功能的前提下优化地图配色。

## 2. 目标

本次只做地图视觉配色的最小优化：

- 去掉当前地图背景的土灰感。
- 保持地图背景与白色节点有明确区分。
- 保持网格轻量，不干扰节点和连线阅读。
- 保持编辑模式网格仍可作为对齐辅助。
- 保持浏览模式没有网格的现有规则。
- 不改变地图布局、缩放、拖拽、连线、节点选中等交互逻辑。
- 不改变地图 JSON 数据结构。

## 3. 非目标

本次不做：

- 不新增渐变背景。
- 不新增装饰图案、光斑、毛玻璃、阴影背景。
- 不改变节点尺寸、圆角、字体、换行逻辑。
- 不改变连线算法。
- 不改变节点选中态蓝色。
- 不改变按钮状态色。
- 不新增主题配置。
- 不做深色模式。

## 4. 当前代码定位

### 4.1 地图窗口 XAML

文件：

```text
VSLoader/Views/FactoryMapWindow.xaml
```

当前地图区域：

```xml
<Grid x:Name="MapViewport"
      Background="#EEF2F7">
```

以及：

```xml
<Canvas x:Name="MapCanvas"
        Background="#EEF2F7">
```

本次需要同步修改 `MapViewport` 和 `MapCanvas` 的背景色，避免视口边缘和画布本体颜色不一致。

### 4.2 网格线颜色

文件：

```text
VSLoader/Views/FactoryMapWindow.xaml.cs
```

当前网格颜色：

```csharp
var minorBrush = new SolidColorBrush(WpfColor.FromRgb(229, 234, 241));
var majorBrush = new SolidColorBrush(WpfColor.FromRgb(215, 222, 232));
```

对应：

```text
小网格：#E5EAF1
主网格：#D7DEE8
```

### 4.3 节点边框颜色

文件：

```text
VSLoader/Views/FactoryMapWindow.xaml.cs
```

当前节点边框创建时：

```csharp
BorderBrush = new SolidColorBrush(WpfColor.FromRgb(168, 179, 195))
```

当前普通状态：

```csharp
border.BorderBrush = new SolidColorBrush(WpfColor.FromRgb(168, 179, 195));
```

对应：

```text
#A8B3C3
```

## 5. 推荐配色

本次采用“明亮冷白蓝”方案。

目标观感：

- 画布像浅蓝白纸面，有空气感。
- 网格轻，不抢视觉。
- 节点仍是主体。
- 连线和选中态不被削弱。

推荐色值：

```text
地图背景：#F7FAFF
小网格：#ECF3FF
主网格：#DCEAFF
节点边框：#B7C6D8
```

### 5.1 地图背景

从：

```text
#EEF2F7
```

改为：

```text
#F7FAFF
```

理由：

- 比当前背景更亮、更轻。
- 带一点蓝，不是纯白。
- 白色节点仍能与背景区分。
- 视觉上更接近 Google / Apple 常见的清爽浅色工作区。

### 5.2 小网格

从：

```text
#E5EAF1
```

改为：

```text
#ECF3FF
```

理由：

- 小网格只作为编辑辅助，不应像表格线一样压住画面。
- 新颜色更轻、更蓝白，减少灰脏感。

### 5.3 主网格

从：

```text
#D7DEE8
```

改为：

```text
#DCEAFF
```

理由：

- 主网格仍比小网格明显。
- 不应强到和工序连线混淆。

### 5.4 节点边框

从：

```text
#A8B3C3
```

改为：

```text
#B7C6D8
```

理由：

- 当前边框偏灰重。
- 新颜色更柔和、更偏蓝，和新背景更协调。
- 节点仍能从背景中分离出来。

## 6. 保持不变的颜色

本次暂时不调整：

### 6.1 连线颜色

当前：

```text
#94A3B8
```

保持不变。

理由：

- 新背景变亮后，当前连线对比度会自然提升。
- 连线是工序关系主体之一，先不动可以降低回归风险。

### 6.2 节点选中态

当前：

```text
选中背景：#EFF6FF
选中边框：#3B82F6
```

保持不变。

理由：

- 当前蓝色选中态清晰。
- 与新浅蓝白背景兼容。

### 6.3 按钮状态色

编辑、锁定、连线、多选按钮状态色保持不变。

理由：

- 本次优化聚焦地图画布，不扩大范围。

## 7. 实现要求

### 7.1 修改 XAML 背景

修改：

```text
VSLoader/Views/FactoryMapWindow.xaml
```

将：

```xml
Background="#EEF2F7"
```

替换为：

```xml
Background="#F7FAFF"
```

仅限：

- `MapViewport`
- `MapCanvas`

不要修改窗口外层 `DockPanel Background="#F6F7F9"`，该颜色属于地图窗口整体外壳背景，不在本次范围。

### 7.2 修改网格颜色

修改：

```text
VSLoader/Views/FactoryMapWindow.xaml.cs
```

将：

```csharp
var minorBrush = new SolidColorBrush(WpfColor.FromRgb(229, 234, 241));
var majorBrush = new SolidColorBrush(WpfColor.FromRgb(215, 222, 232));
```

改为：

```csharp
var minorBrush = new SolidColorBrush(WpfColor.FromRgb(236, 243, 255));
var majorBrush = new SolidColorBrush(WpfColor.FromRgb(220, 234, 255));
```

### 7.3 修改普通节点边框

修改：

```text
VSLoader/Views/FactoryMapWindow.xaml.cs
```

将普通节点边框：

```csharp
WpfColor.FromRgb(168, 179, 195)
```

改为：

```csharp
WpfColor.FromRgb(183, 198, 216)
```

需要覆盖两处：

- 创建节点 `Border` 时的 `BorderBrush`。
- `ApplyDeviceNormalVisual(...)` 中的 `BorderBrush`。

不要修改：

- `ApplyDeviceSelectedVisual`
- `ApplyDeviceHighlightedVisual`
- `ApplyDeviceConnectionStartVisual`

## 8. 测试建议

本次属于视觉色值优化，逻辑风险低。

建议新增或扩展轻量测试时，可优先测试色值常量。但当前色值散落在 XAML 和窗口代码中，不建议为了测试强行重构。

本次必须执行构建回归：

```powershell
dotnet test .\VSLoader.sln -c Debug --no-restore
dotnet build .\VSLoader.sln -c Debug --no-restore
```

如果后续地图视觉色值继续频繁调整，再考虑抽取：

```csharp
FactoryMapPalette
```

作为统一色板。

## 9. 手动验证清单

1. 打开主窗口。
2. 点击“地图”按钮打开地图。
3. 浏览模式下地图背景应更明亮、更清爽，不再灰蒙蒙。
4. 白色节点仍能从背景中清楚分离。
5. 节点边框不应过重。
6. 连线仍清晰可见。
7. 切换到编辑模式，网格应轻量可见。
8. 网格不应压过节点和连线。
9. 节点选中态蓝色仍明显。
10. 地图缩放、拖拽、节点编辑、连线功能不受影响。

## 10. 验收标准

满足以下条件即完成：

- 地图背景由 `#EEF2F7` 调整为 `#F7FAFF`。
- 小网格由 `#E5EAF1` 调整为 `#ECF3FF`。
- 主网格由 `#D7DEE8` 调整为 `#DCEAFF`。
- 普通节点边框由 `#A8B3C3` 调整为 `#B7C6D8`。
- 地图整体观感更明亮清爽，不再明显灰蒙蒙。
- 节点与背景仍有清楚区分。
- 连线、选中态、按钮状态不被修改。
- 交互逻辑不变。
- `dotnet test .\VSLoader.sln -c Debug --no-restore` 通过。
- `dotnet build .\VSLoader.sln -c Debug --no-restore` 通过。
