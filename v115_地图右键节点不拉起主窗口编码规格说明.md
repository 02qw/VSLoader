# v115 地图右键节点不拉起主窗口编码规格说明

## 1. 背景

v113 将工厂地图从主窗口附属窗口重构为独立窗口，使地图具备独立任务栏窗口、独立最小化等能力。

重构后出现一个新的交互问题：

用户只在地图窗口中操作，例如右键某个地图节点打开节点右键菜单时，主窗口会突然显示到前台。

这与原本正确的用户体验不一致。地图窗口已经独立后，用户在地图中操作时，屏幕上应该只保持地图窗口和地图右键菜单，不应因为选中节点而把主窗口弹出来。

## 2. 现象

复现路径：

1. 打开 VSLoader 主窗口。
2. 打开工厂地图窗口。
3. 通过快捷键或窗口操作让主窗口隐藏、最小化或位于后方。
4. 在地图窗口中右键某个节点。

当前结果：

- 地图节点右键菜单正常出现。
- 但主窗口也被拉到前台。

期望结果：

- 只显示地图窗口和节点右键菜单。
- 主窗口不被激活、不被显示到前台。
- 地图节点仍然可以同步选中对应快捷项的数据。

## 3. 根因分析

地图节点右键入口位于：

```text
VSLoader/Views/FactoryMapWindow.xaml.cs
```

当前右键节点时会先调用：

```csharp
selectShortcut(device.Shortcut);
```

对应主窗口回调位于：

```text
VSLoader/MainWindow.xaml.cs
```

调用链如下：

```csharp
Device_PreviewMouseRightButtonDown
    -> selectShortcut(device.Shortcut)
    -> MainWindow.SelectShortcutFromMap(shortcut)
    -> MainWindow.SelectShortcutInGrid(viewModel, shortcut)
```

当前 `SelectShortcutInGrid` 实现中包含：

```csharp
ShortcutsGrid.Focus();
```

这句代码会把键盘焦点切到主窗口里的 DataGrid。地图窗口独立后，这会导致 WPF/Windows 将主窗口激活到前台。

因此问题本质是：

```text
地图节点选择同步逻辑 = 数据选中 + 滚动定位 + 主窗口抢焦点
```

其中“主窗口抢焦点”在地图独立窗口场景下是不应该发生的。

## 4. 目标

本次目标：

- 地图中右键节点时，不再拉起主窗口。
- 地图中左键选择节点时，也不应因为同步左侧快捷项而抢主窗口焦点。
- 保留地图节点与主窗口快捷项的数据同步能力。
- 保留必要的筛选条件清理逻辑。
- 保留 `SelectedShortcut`、`ShortcutsGrid.SelectedItem`、`ScrollIntoView` 的同步。
- 只移除地图触发路径下的 `ShortcutsGrid.Focus()` 副作用。

## 5. 非目标

本次不做以下事项：

- 不修改地图窗口独立窗口架构。
- 不恢复地图窗口 `Owner = MainWindow` 的旧附属窗口模式。
- 不修改地图节点右键菜单内容。
- 不修改右键菜单功能执行逻辑，例如 VSCode、AdminUI、WebUI、编辑、删除。
- 不修改主窗口托盘显示/隐藏逻辑。
- 不修改全局快捷键逻辑。
- 不取消地图点击节点时左侧快捷项同步选中。

## 6. 推荐方案

### 6.1 将“选中快捷项”和“聚焦 DataGrid”拆开

将当前方法：

```csharp
private void SelectShortcutInGrid(MainViewModel viewModel, ShortcutItem shortcut)
```

调整为支持是否聚焦：

```csharp
private void SelectShortcutInGrid(
    MainViewModel viewModel,
    ShortcutItem shortcut,
    bool focusGrid)
```

内部逻辑：

```csharp
viewModel.SelectedShortcut = shortcut;
ShortcutsGrid.SelectedItem = shortcut;
ShortcutsGrid.ScrollIntoView(shortcut);

if (focusGrid)
{
    ShortcutsGrid.Focus();
}
```

### 6.2 地图触发路径不聚焦主窗口

`SelectShortcutFromMap` 是地图窗口触发的选择同步入口。

该入口中调用：

```csharp
SelectShortcutInGrid(viewModel, shortcut, focusGrid: false);
```

如果存在清空搜索条件后的异步选择：

```csharp
Dispatcher.BeginInvoke(...)
```

异步回调中也必须使用：

```csharp
SelectShortcutInGrid(viewModel, shortcut, focusGrid: false);
```

这样可以保留：

- 清空搜索条件
- 选中快捷项
- 滚动到对应项

但不会让主窗口获得焦点。

### 6.3 主窗口内部路径可保留聚焦

如果未来有主窗口内部按钮或主窗口内部操作需要主动聚焦 `ShortcutsGrid`，可以继续调用：

```csharp
SelectShortcutInGrid(viewModel, shortcut, focusGrid: true);
```

本次如果没有其他调用方，可以只在方法内部默认参数：

```csharp
bool focusGrid = false
```

但更推荐显式传参，避免以后误用。

## 7. 影响范围

主要修改：

```text
VSLoader/MainWindow.xaml.cs
```

可能新增或调整测试：

```text
VSLoader.Tests/MainWindowFactoryMapSelectionTests.cs
```

或扩展已有：

```text
VSLoader.Tests/MainWindowFactoryMapCloseTests.cs
VSLoader.Tests/FactoryMapShortcutActionTests.cs
```

不修改：

```text
VSLoader/Views/FactoryMapWindow.xaml.cs
VSLoader/Views/FactoryMapWindow.xaml
VSLoader/ViewModels/MainViewModel.cs
```

除非测试需要暴露纯函数判断。

## 8. 测试建议

UI 焦点行为较难直接通过单元测试完整验证，但可以新增轻量代码结构测试，防止回退。

建议测试点：

1. `MainWindow.xaml.cs` 中 `SelectShortcutInGrid` 支持 `focusGrid` 参数。
2. `SelectShortcutInGrid` 内只有在 `focusGrid` 为 true 时才调用 `ShortcutsGrid.Focus()`。
3. `SelectShortcutFromMap` 中调用 `SelectShortcutInGrid` 时传入 `focusGrid: false`。
4. 清空搜索条件后的 `Dispatcher.BeginInvoke` 回调中也传入 `focusGrid: false`。

如果已有 WPF UI 自动化测试能力，也可增加手动/自动验证：

- 主窗口隐藏或在后方时，地图右键节点后主窗口不变为 active。
- 地图节点仍然高亮。
- 主窗口快捷项仍然被同步选中。

## 9. 验收标准

满足以下条件即完成：

- 地图窗口中右键节点时，主窗口不会自动跳到前台。
- 地图窗口中左键选择节点时，主窗口不会自动跳到前台。
- 地图节点右键菜单仍正常显示。
- 地图节点右键菜单中的 VSCode、AdminUI、获取 AdminUI 连接、WebUI、编辑、删除功能仍可用。
- 地图节点与主窗口快捷项仍保持同步选中。
- 如果主窗口搜索框存在筛选条件，地图选中节点仍能清空筛选并同步选中对应快捷项。
- 不影响主窗口快捷键显示/隐藏逻辑。
- 不影响地图快捷键打开/关闭逻辑。
- `dotnet test .\VSLoader.Tests\VSLoader.Tests.csproj -c Debug --no-restore` 通过。
- `dotnet build .\VSLoader.sln -c Debug --no-restore` 通过。

## 10. 风险与边界

### 10.1 ScrollIntoView 是否会激活窗口

当前根因明确指向 `ShortcutsGrid.Focus()`。

`ScrollIntoView` 理论上只滚动 DataGrid 项，不应主动激活主窗口。

如果修复后仍出现主窗口弹出，应继续排查：

- `ShortcutsGrid.SelectedItem = shortcut`
- `ScrollIntoView(shortcut)`
- `SelectedShortcut` 变化后是否触发其他 UI 激活逻辑

但本次第一阶段只移除已确认的抢焦点调用。

### 10.2 右键菜单功能执行时是否允许主窗口出现

右键菜单本身不应拉起主窗口。

但用户点击菜单项后，有些功能可能本来就会打开其他窗口或弹窗：

- 编辑：可能打开编辑窗口。
- 删除：可能打开确认弹窗。
- 获取 AdminUI 连接：可能显示进度遮罩。

这些属于用户明确点击菜单项后的功能反馈，不属于“仅右键节点就弹主窗口”的问题。

本次只修复右键打开菜单阶段的主窗口意外弹出。

### 10.3 主窗口内部交互

不要全局删除 `ShortcutsGrid.Focus()`。

正确做法是让调用方决定是否需要焦点。

地图触发路径不需要焦点；主窗口内部未来如果需要焦点，可以显式传入 `focusGrid: true`。

