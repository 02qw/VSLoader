# v6 主列表排版与可读性优化编码规格说明

## 1. 文档目的

本文档用于指导编程 agent 或开发人员优化 VSLoader 主界面快捷项列表的排版和可读性。

本次需求只做 UI 展示优化，不修改任何业务逻辑。

## 2. 当前问题

当前主界面快捷项列表存在以下视觉问题：

- 行高偏小，文字和行分隔线挤在一起。
- 行间距过密，大量快捷项一起展示时阅读压力较大。
- 横向分隔线颜色过重，视觉上显得生硬。
- 默认字体和字号偏紧，不够适合大量中文名称浏览。
- 表头和内容区缺少层次感。
- 选中态使用系统默认样式，整体观感较原生。

## 3. 优化目标

优化后主列表应满足：

- 每一行高度更舒适。
- 中文名称阅读更清晰。
- 分隔线更轻，不抢视觉重点。
- 表头更清楚。
- 大量快捷项浏览时更容易区分行。
- 不影响现有排序、搜索、打开、AdminUI、批量新增等功能。

## 4. 修改范围

主要修改：

```text
VSLoader\MainWindow.xaml
```

不应修改：

```text
Models
Services
ViewModels
ConfigService
AdminUiService
BatchImportService
快捷项保存结构
排序逻辑
搜索逻辑
打开 VSCode 逻辑
AdminUI 逻辑
```

## 5. 主列表 DataGrid 优化规格

目标控件：

```xml
<DataGrid x:Name="ShortcutsGrid" ...>
```

### 5.1 行高

设置：

```xml
RowHeight="34"
```

说明：

- 当前行高过小。
- `34` 能让中文名称和时间显示更舒适。
- 不建议超过 `38`，否则数据密度会下降太多。

### 5.2 字体

设置：

```xml
FontFamily="Microsoft YaHei UI"
FontSize="14"
```

说明：

- `Microsoft YaHei UI` 更适合 Windows 中文界面。
- `14` 比默认字号更适合长时间阅读。

### 5.3 表头高度

设置：

```xml
ColumnHeaderHeight="36"
```

说明：

- 表头高度略高于普通行，形成层次。

### 5.4 分隔线

保留横向分隔线，但改成浅灰色。

设置：

```xml
GridLinesVisibility="Horizontal"
HorizontalGridLinesBrush="#E5E7EB"
```

说明：

- 不使用深色横线。
- 保留横线方便用户横向阅读。

### 5.5 行背景

设置交替行背景：

```xml
AlternatingRowBackground="#FAFAFA"
Background="White"
```

说明：

- 轻微区分相邻行。
- 不使用强烈色块。

### 5.6 表头样式

新增或设置 `ColumnHeaderStyle`。

推荐样式：

```xml
<DataGrid.ColumnHeaderStyle>
    <Style TargetType="DataGridColumnHeader">
        <Setter Property="Background" Value="#F3F4F6" />
        <Setter Property="Foreground" Value="#111827" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="Padding" Value="8,0" />
        <Setter Property="HorizontalContentAlignment" Value="Left" />
        <Setter Property="BorderBrush" Value="#E5E7EB" />
        <Setter Property="BorderThickness" Value="0,0,0,1" />
    </Style>
</DataGrid.ColumnHeaderStyle>
```

### 5.7 单元格样式

新增或设置 `CellStyle`。

推荐样式：

```xml
<DataGrid.CellStyle>
    <Style TargetType="DataGridCell">
        <Setter Property="Padding" Value="8,0" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
    </Style>
</DataGrid.CellStyle>
```

说明：

- 单元格文字左右留出空间。
- 文字垂直居中。
- 减少原生焦点框干扰。

### 5.8 行样式

新增或设置 `RowStyle`。

推荐样式：

```xml
<DataGrid.RowStyle>
    <Style TargetType="DataGridRow">
        <Setter Property="MinHeight" Value="34" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
    </Style>
</DataGrid.RowStyle>
```

## 6. 列宽优化

当前主列表展示三列：

- 名称
- 备注
- 更新时间

建议列宽：

```xml
<DataGridTextColumn Width="360"
                    Binding="{Binding Name}"
                    Header="名称" />
<DataGridTextColumn Width="*"
                    Binding="{Binding Description}"
                    Header="备注" />
<DataGridTextColumn Width="190"
                    Binding="{Binding UpdatedAt, StringFormat=yyyy-MM-dd HH:mm:ss}"
                    Header="更新时间" />
```

说明：

- 名称列从 `260` 增加到 `360`，适配较长中文名称。
- 备注列占用剩余空间。
- 更新时间列从 `180` 增加到 `190`，避免时间显示拥挤。

## 7. 按钮区域可选优化

本需求重点是主列表。

如果顺手优化按钮区域，必须保持小范围：

- 统一按钮高度为 `36`。
- 不改变按钮顺序。
- 不改变按钮命令绑定。
- 不改变按钮文字。

如果实现时风险较高，可以不改按钮。

## 8. 验收标准

- 主界面快捷项列表行高明显增加。
- 每行文字垂直居中。
- 名称、备注、更新时间列显示正常。
- 横向分隔线变为浅灰色。
- 表头背景为浅灰色。
- 表头文字略加粗。
- 列表使用 `Microsoft YaHei UI` 字体。
- 列表字号为 `14`。
- 名称列更宽，长中文名称不再显得拥挤。
- 交替行背景生效。
- 双击快捷项仍能打开 VSCode。
- 选中快捷项后 AdminUI 仍能使用。
- 搜索、排序、批量新增、自动获取连接不受影响。
- `dotnet build .\VSLoader.sln` 必须通过。

