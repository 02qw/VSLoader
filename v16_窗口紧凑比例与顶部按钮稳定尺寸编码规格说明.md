# v16 窗口紧凑比例与顶部按钮稳定尺寸编码规格说明

## 1. 需求背景

当前 VSLoader 顶部第二行按钮在 v13 中使用 `UniformGrid Columns="7"` 均匀分布。

该方案解决了右侧大空白问题，但在窗口缩小时暴露出新问题：

- 按钮槽位被压缩。
- 按钮存在 `MinWidth` 和 `Margin`，可能超过槽位宽度。
- 例如 `自动获取连接` 按钮右边框会被裁切或看起来消失。

用户进一步明确了新的视觉目标：

- 无论用户放大还是缩小窗口，按钮尺寸和样式都应保持稳定。
- 按钮不要随窗口被拉伸或压扁。
- 程序默认打开时不应过度横向拉长。
- 默认窗口比例应更紧凑，接近用户截图中较舒服的状态。

本次 v16 需求需要从“按钮均匀铺满整行”调整为“按钮尺寸稳定 + 窗口默认比例更紧凑”。

## 2. 需求目标

本次优化目标：

1. 顶部第二行按钮尺寸固定，不随窗口缩放变形。
2. 窗口缩小时按钮不被裁切、不丢边框。
3. 窗口放大时按钮不被过度拉宽。
4. 默认窗口尺寸更接近紧凑工具窗口，而不是横向过长。
5. 列表列宽配合紧凑窗口重新稳定分配。
6. 不改变任何业务逻辑。

## 3. 修改范围

主要修改文件：

- `VSLoader\MainWindow.xaml`

原则上不需要修改：

- `VSLoader\ViewModels\MainViewModel.cs`
- `VSLoader\MainWindow.xaml.cs`
- 服务层代码
- 模型层代码

本次属于窗口尺寸和 XAML 布局优化。

## 4. 当前问题来源

当前第二行按钮区域类似：

```xml
<UniformGrid Grid.Row="1"
             Columns="7">
    <Button MinWidth="112"
            Margin="0,0,10,0"
            HorizontalAlignment="Stretch" />
</UniformGrid>
```

当窗口宽度缩小时：

```text
UniformGrid 单个槽位宽度 < 按钮 MinWidth + Margin
```

按钮视觉区域就可能被裁切，表现为右边框消失或按钮内容拥挤。

## 5. 新设计原则

本次从 v13 的“均匀铺满”调整为：

```text
按钮自身尺寸稳定，窗口尺寸更克制，布局自然排列。
```

原则：

- 按钮宽度固定。
- 按钮高度固定。
- 不使用 `UniformGrid` 强制拉伸按钮。
- 不让按钮随窗口放大而变宽。
- 不让按钮随窗口缩小而被压扁。
- 正常窗口宽度下按钮保持一行。
- 极端缩小时可允许按钮换行作为安全兜底，但默认和最小宽度应尽量避免换行。

## 6. 推荐实现方案

### 6.1 第二行按钮容器改为 WrapPanel

将第二行从：

```xml
<UniformGrid Columns="7">
```

改为：

```xml
<WrapPanel Grid.Row="1"
           Margin="0,10,0,0"
           Orientation="Horizontal">
```

理由：

- `WrapPanel` 不会强制拉伸按钮。
- 按钮保持固定尺寸。
- 窗口变窄时不会硬挤裁切按钮。
- 极端情况下可以自然换行，优先保证按钮完整。

### 6.2 按钮固定宽度

推荐按钮宽度：

| 按钮 | Width |
|---|---:|
| 新增 | `96` |
| 批量新增识别 | `132` |
| 自动获取连接 | `132` |
| AdminUI | `104` |
| 打开 | `96` |
| 编辑 | `96` |
| 删除 | `96` |

统一高度：

```text
Height = 36
```

按钮间距：

```xml
Margin="0,0,10,8"
```

最后一个按钮也可以保留该 Margin，WrapPanel 下影响较小。

### 6.3 按钮不使用 Stretch

移除：

```xml
HorizontalAlignment="Stretch"
MinWidth="..."
```

使用：

```xml
Width="..."
Height="36"
```

## 7. 窗口尺寸调整

当前 v11 后窗口大致为：

```xml
Width="1040"
Height="620"
MinWidth="860"
MinHeight="520"
```

本次建议调整为：

```xml
Width="960"
Height="720"
MinWidth="900"
MinHeight="560"
```

理由：

- `960 x 720` 比原先更接近紧凑工具窗口。
- 不再横向过长。
- 高度增加后，顶部两行工具区不会明显压缩列表可视区域。
- `MinWidth=900` 可以尽量保证第二行按钮在正常情况下仍保持一行显示。

如果实际运行发现 `MinWidth=900` 下按钮仍换行，可微调为：

```xml
MinWidth="940"
```

但本规格优先使用：

```xml
MinWidth="900"
```

## 8. 列表列宽调整

v13 当前列宽：

```text
名称：2*
备注：3*
更新时间：180
```

在紧凑窗口下，建议改为更稳定的关键列宽：

```text
名称：280
备注：*
更新时间：180
```

理由：

- 名称列保持稳定识别区域。
- 更新时间保持稳定显示。
- 窗口缩放主要影响备注列。
- 避免名称和备注因比例缩放产生大幅波动。

对应 XAML：

```xml
<DataGridTextColumn Width="280" ... />
<DataGridTextColumn Width="*" ... />
<DataGridTextColumn Width="180" ... />
```

继续保留文本省略：

```xml
TextTrimming="CharacterEllipsis"
```

## 9. 保持现有两行顶部结构

第一行保持：

```text
[搜索框] [设置]
```

第二行保持：

```text
[新增] [批量新增识别] [自动获取连接] [AdminUI] [打开] [编辑] [删除]
```

不新增第三行，不隐藏按钮，不增加更多菜单。

## 10. 不允许改变的内容

本次不允许改变：

- 按钮命令绑定。
- 搜索框绑定。
- 顶部工具区行数。
- 右键菜单。
- 双击打开逻辑。
- AdminUI 逻辑。
- 自动获取连接逻辑。
- 密码复制逻辑。
- 主列表排序逻辑。
- 数据模型和配置文件结构。

## 11. 功能绑定要求

所有按钮必须保持现有命令绑定：

| 按钮 | 命令 |
|---|---|
| 新增 | `AddShortcutCommand` |
| 批量新增识别 | `OpenBatchImportCommand` |
| 自动获取连接 | `DownloadAdminUiLinksCommand` |
| AdminUI | `OpenAdminUiCommand` |
| 打开 | `OpenShortcutCommand` |
| 编辑 | `EditShortcutCommand` |
| 删除 | `DeleteShortcutCommand` |
| 设置 | `OpenSettingsCommand` |

## 12. 验收标准

### 12.1 按钮视觉验收

启动应用后：

- 第二行按钮尺寸稳定。
- 按钮不被拉伸。
- 按钮不被压扁。
- `自动获取连接` 右边框完整显示。
- 窗口放大后按钮尺寸仍保持固定。
- 窗口缩小时按钮不会被裁切。

### 12.2 窗口比例验收

启动应用后：

- 窗口不再横向过长。
- 默认窗口比例更接近紧凑工具窗口。
- 下方快捷项列表仍有足够展示空间。

### 12.3 列表验收

- 名称列宽稳定。
- 更新时间列宽稳定。
- 备注列根据剩余空间伸缩。
- 每行快捷项仍然单行展示。

### 12.4 功能验收

以下功能必须正常：

1. 搜索过滤。
2. 新增。
3. 批量新增识别。
4. 自动获取连接。
5. AdminUI。
6. 打开。
7. 编辑。
8. 删除。
9. 设置。
10. 双击打开。
11. 右键菜单。

### 12.5 编译验收

执行：

```powershell
dotnet build .\VSLoader.sln
```

期望：

```text
0 warnings
0 errors
```

如果提示 `VSLoader.exe` 被占用，说明程序正在运行，应先关闭 VSLoader 后重新构建。

## 13. 建议实施步骤

1. 打开 `VSLoader\MainWindow.xaml`。
2. 调整窗口尺寸为 `Width=960`、`Height=720`、`MinWidth=900`、`MinHeight=560`。
3. 找到顶部第二行按钮区域。
4. 将 `UniformGrid` 改为 `WrapPanel`。
5. 移除按钮 `MinWidth` 和 `HorizontalAlignment=Stretch`。
6. 为按钮设置固定 `Width` 和 `Height=36`。
7. 调整列表列宽为 `280 / * / 180`。
8. 编译并人工验证窗口缩放效果。

## 14. 明确不做的事情

本次不做：

- 列头排序反馈。
- 修改业务逻辑。
- 隐藏按钮。
- 增加图标。
- 增加更多菜单。
- 增加第三行工具栏。
