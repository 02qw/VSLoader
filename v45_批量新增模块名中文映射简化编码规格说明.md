# v45 批量新增模块名中文映射简化编码规格说明

## 1. 需求背景

v44 已经实现：

```text
批量新增识别可以读取目标文件夹下 META-INF\ZAM-DEPLOY.xml，
从 description="Application for xxx" 中提取模块名，
再通过 CSV 的 ModulePattern 映射 DisplayName。
```

但 v44 的 CSV 仍然偏复杂：

```csv
MatchType,Pattern,ModulePattern,DisplayName,NameTemplate
Regex,^(?<Code>\d+)_(?<Type>TAOI)(?<No>\d+)$,^eap-sic-Jutze-3D-AOI$,矩子3D-AOI,{DisplayName}_{No}
```

用户实际需要维护的只是：

```text
模块名 -> 中文快捷项名称
```

`No` 编号仍然可以由程序后台从文件夹名中自动提取，不需要用户在 CSV 中写正则。

## 2. 需求目标

新增一种更简单的 CSV 规则格式：

```csv
ModuleName,DisplayName
eap-sic-Jutze-3D-AOI,矩子3D-AOI
eap-sic-AMX-Sintering,AMX银烧结
eap-sic-KRE-CoverAssy,上盖组装
```

批量新增识别时：

1. 扫描目标父目录下的子文件夹。
2. 从子文件夹名中自动提取编号 `No`。
3. 读取子文件夹下：

```text
META-INF\ZAM-DEPLOY.xml
```

4. 从 XML 的 `description` 中提取模块名。
5. 使用简单 CSV 查找模块名对应的中文名。
6. 按固定规则生成快捷项名称：

```text
{DisplayName}_{No}
```

示例：

```text
文件夹名：12190_TAOI007
ZAM 模块名：eap-sic-Jutze-3D-AOI
CSV 映射：eap-sic-Jutze-3D-AOI -> 矩子3D-AOI
后台提取 No：007
最终快捷项名称：矩子3D-AOI_007
```

## 3. 非目标范围

本次不实现：

1. 不新增独立按钮。
2. 不改变批量新增识别窗口入口。
3. 不改变目标路径 `TargetPath`。
4. 不改变备注 `Description = 批量新增：{FolderName}`。
5. 不改变新增、更新、清理重复项的主流程。
6. 不要求用户在 CSV 中维护正则。
7. 不要求用户在 CSV 中维护 `{DisplayName}_{No}` 模板。
8. 不删除 v44 的旧复杂规则支持，除非后续明确要求。

## 4. 新 CSV 格式

### 4.1 推荐文件名

新增或替换示例文件：

```text
VSLoader/Config/batch-rules.v4.module-map.example.csv
```

说明：

```text
v3 仍保留为复杂规则示例。
v4 用于简单模块映射表。
```

如果用户希望继续使用带 v3 字样，也可以命名为：

```text
VSLoader/Config/batch-rules.v3.module-map.example.csv
```

但从功能语义上，推荐用 v4 区分简化版。

### 4.2 表头

固定表头：

```csv
ModuleName,DisplayName
```

字段含义：

| 字段 | 含义 |
| --- | --- |
| ModuleName | ZAM-DEPLOY.xml 中 `Application for ` 后提取到的模块名 |
| DisplayName | 最终快捷项名称中 `_No` 前面的中文名称 |

### 4.3 示例内容

```csv
ModuleName,DisplayName
eap-sic-Jutze-3D-AOI,矩子3D-AOI
eap-sic-Mirtec-AOI,美陆AOI
eap-sic-AMX-Sintering,AMX银烧结
eap-sic-KRE-CoverAssy,上盖组装
eap-sic-SMTK-FormicReflow,SMTK真空甲酸炉
```

## 5. No 编号后台提取规则

用户不再在 CSV 中维护文件夹名正则。

程序后台固定从文件夹名中提取 `No`。

当前目标文件夹通常类似：

```text
12190_TAOI007
5534_TATP012
3134_TSSP001
```

推荐后台规则：

```regex
^(?<Code>\d+)_(?<Type>[A-Za-z]+)(?<No>\d+)$
```

提取：

```text
No = 最后一段连续数字
```

示例：

| 文件夹名 | No |
| --- | --- |
| 12190_TAOI007 | 007 |
| 5534_TATP012 | 012 |
| 3134_TSSP001 | 001 |

如果文件夹名不符合规则：

```text
已跳过：文件夹名无法提取编号 No。
```

## 6. 模块名读取规则

沿用 v44 的 ZAM 读取逻辑。

读取路径：

```text
目标文件夹\META-INF\ZAM-DEPLOY.xml
```

从 XML 任意节点的 `description` 属性中查找：

```text
Application for 
```

提取其后的模块名。

示例：

```xml
<DeploymentDescriptor description="Application for eap-sic-Jutze-3D-AOI" />
```

提取：

```text
eap-sic-Jutze-3D-AOI
```

## 7. 匹配规则

简单映射模式下，不使用正则。

匹配方式：

```text
ModuleName 与 XML 中提取到的模块名 Trim 后进行不区分大小写的精确匹配。
```

示例：

```text
CSV ModuleName：eap-sic-Jutze-3D-AOI
XML ModuleName：eap-sic-Jutze-3D-AOI
匹配成功
```

不做：

```text
Contains
Regex
模糊匹配
```

原因：

1. 降低用户维护难度。
2. 避免错误匹配。
3. 模块名本身已经是稳定唯一标识。

## 8. 与 v44 复杂规则兼容

当前 `BatchImportService.LoadRules` 已支持：

```csv
MatchType,Pattern,ModulePattern,DisplayName,NameTemplate
```

本次需要扩展为同时支持：

```csv
ModuleName,DisplayName
```

建议识别规则：

1. 如果 CSV 表头包含 `ModuleName` 和 `DisplayName`，且不包含 `MatchType` / `Pattern`：
   - 使用简单模块映射模式。
2. 如果 CSV 表头包含 `MatchType`、`Pattern`、`DisplayName`、`NameTemplate`：
   - 使用旧规则 / v44 复杂规则模式。
3. 如果表头两种格式都不满足：
   - 返回表头错误。

## 9. 模型设计

当前模型：

```text
VSLoader/Models/BatchImportRule.cs
```

已有字段：

```csharp
MatchType
Pattern
ModulePattern
DisplayName
NameTemplate
SortIndex
```

建议最小扩展：

```csharp
public string ModuleName { get; set; } = string.Empty;

public bool IsSimpleModuleMapRule =>
    !string.IsNullOrWhiteSpace(ModuleName);
```

对于简单映射 CSV，读取后自动补齐：

```csharp
MatchType = "ModuleMap"
Pattern = string.Empty
ModuleName = CSV ModuleName
DisplayName = CSV DisplayName
NameTemplate = "{DisplayName}_{No}"
```

## 10. BuildPreview 逻辑调整

当前 v44 流程：

```text
文件夹名匹配 Pattern
读取 ZAM 模块名
匹配 ModulePattern
GenerateName
```

本次新增简单映射流程：

```text
如果 rules 中是简单模块映射规则：
    1. 从 folderName 后台提取 No
    2. 读取 ZAM 模块名
    3. 根据 ModuleName 精确查找 DisplayName
    4. generatedName = DisplayName + "_" + No
否则：
    沿用 v44 复杂规则流程
```

建议新增辅助方法：

```csharp
private static bool IsSimpleModuleMapMode(IReadOnlyList<BatchImportRule> rules)
```

判断：

```text
rules.Count > 0 且所有规则都有 ModuleName
```

不建议同一个 CSV 混用简单模式和复杂模式。

如果发现混用：

```text
CSV 规则错误：不能混用 ModuleName 简化映射格式和 MatchType 复杂规则格式。
```

## 11. GenerateName 规则

简单映射模式下，不再使用用户 CSV 中的 `NameTemplate`。

固定：

```text
{DisplayName}_{No}
```

代码可以直接生成：

```csharp
var generatedName = $"{rule.DisplayName.Trim()}_{no}";
```

也可以沿用内部模板：

```csharp
NameTemplate = "{DisplayName}_{No}"
```

推荐直接生成，逻辑更清晰。

## 12. 错误与跳过策略

### 12.1 文件夹名无法提取 No

```text
Status = 已跳过
Message = 文件夹名无法提取编号 No。
```

### 12.2 ZAM 文件不存在

```text
Status = 已跳过
Message = 未找到 META-INF\ZAM-DEPLOY.xml，无法读取模块名。
```

### 12.3 XML 没有模块描述

```text
Status = 已跳过
Message = ZAM-DEPLOY.xml 中未找到 Application for 模块描述。
```

### 12.4 XML 解析失败

```text
Status = 规则错误
Message = ZAM-DEPLOY.xml 解析失败：{错误信息}
```

### 12.5 模块名未在 CSV 中配置

```text
Status = 已跳过
Message = 模块名未在 CSV 中配置：{ModuleName}
```

## 13. 与现有功能关系

### 13.1 批量新增预览

保持原预览流程。

新生成的名称仍然进入：

```text
名称重复判断
路径更新判断
历史重复清理判断
```

### 13.2 排序

简单映射模式下仍然设置：

```text
SortNo = 后台提取的 No
SortName = GeneratedName
```

排序效果不变。

### 13.3 备注

保持：

```text
批量新增：{FolderName}
```

### 13.4 旧 CSV

旧复杂规则 CSV 应继续可用。

这样用户可以在过渡期选择：

```text
复杂规则 CSV
简单模块映射 CSV
```

## 14. CSV 示例文件

新增：

```text
VSLoader/Config/batch-rules.v4.module-map.example.csv
```

初始内容可由当前 v3 模块规则提取为：

```csv
ModuleName,DisplayName
eap-sic-AMX-Sintering,AMX银烧结
eap-sic-Quick-Sintering,快速银烧结
eap-sic-HB-VerticalReflow,浩宝氮气垂直炉
eap-sic-JT-VerticalReflow,劲拓垂直炉
eap-sic-JT-SPI,SPI
eap-sic-Desen-Printing,印刷机
eap-sic-Desen-SolderDispensing,自动点锡机
eap-sic-Jutze-3D-AOI,矩子3D-AOI
eap-sic-Mirtec-AOI,美陆AOI
eap-sic-HIKRAYIN-3D-XRay,3D-X-RAY
eap-sic-KRE-Sintering-NG-Unloader,银烧结NG抓取装置
eap-sic-KRE-HotBonder-CarrierChanger,一次焊键合载盘切换机
eap-sic-KRE-3D-X-Ray-NG-Unloader,3D-X-RAY-NG下料机
eap-sic-KRE-Sintering-CarrierChanger,烧结载具切换机
eap-sic-KRE-WeldAssy,二次焊接组装机
eap-sic-KRE-WeldDisassy,二次焊接拆卸机
eap-sic-KRE-Gluing-CarrierChanger,灌胶载盘切换机
eap-sic-KRE-CoverAssy,上盖组装
eap-sic-KRE-DBC-Loader,DBC上料机
eap-sic-KRE-AirBlower,键合吹扫
eap-sic-KRE-Screw,外壳组装-打螺丝机
eap-sic-KRE-ShellAssy,外壳组装-双工位点胶机
eap-sic-KRE-FirstTest,初测机
eap-sic-KRE-FinalTester,终测机
eap-sic-SBT-TermWelding,端子超声焊
eap-sic-SBT-PinWelding,PIN超声焊
eap-sic-SharpTop-Gluing,真空灌胶机
eap-sic-SMTK-FormicReflow,SMTK真空甲酸炉
eap-sic-SiliCool-HotBonder,热贴机
eap-sic-CJ-WireBonder,全自动键合机
eap-sic-KRE-Welding-CarrierChanger,焊接载具切换机
```

## 15. 测试要求

新增或扩展测试：

```text
VSLoader.Tests/BatchImportServiceSimpleModuleMapTests.cs
```

### 15.1 简单映射 CSV 可读取

输入：

```csv
ModuleName,DisplayName
eap-sic-Jutze-3D-AOI,矩子3D-AOI
```

预期：

```text
rule.ModuleName = eap-sic-Jutze-3D-AOI
rule.DisplayName = 矩子3D-AOI
```

### 15.2 根据模块映射和后台 No 生成名称

文件夹：

```text
12190_TAOI007
```

ZAM 模块：

```text
eap-sic-Jutze-3D-AOI
```

预期：

```text
GeneratedName = 矩子3D-AOI_007
```

### 15.3 No 不需要 CSV 正则

CSV 只有：

```csv
ModuleName,DisplayName
eap-sic-Jutze-3D-AOI,矩子3D-AOI
```

预期仍可生成：

```text
矩子3D-AOI_007
```

### 15.4 模块未配置时跳过

ZAM 模块：

```text
eap-sic-Unknown
```

预期：

```text
Status = 已跳过
Message 包含 模块名未在 CSV 中配置
```

### 15.5 文件夹名无法提取 No 时跳过

文件夹：

```text
InvalidFolder
```

预期：

```text
Status = 已跳过
Message 包含 文件夹名无法提取编号 No
```

### 15.6 旧复杂 CSV 仍可用

保留现有 v44 测试，确保没有破坏复杂规则。

## 16. 预期改动文件

预计改动：

- `VSLoader/Models/BatchImportRule.cs`
- `VSLoader/Models/Services/BatchImportService.cs`
- `VSLoader/Config/batch-rules.v4.module-map.example.csv`
- `VSLoader.Tests/BatchImportServiceSimpleModuleMapTests.cs`

可能改动：

- `VSLoader/Config/batch-rules.v3.zam.example.csv`

建议保留 v3 文件，作为复杂规则示例。

## 17. 构建与测试验证

实现完成后执行：

```powershell
cd C:\Users\shee_\OneDrive\Desktop\VSLoader
dotnet test .\VSLoader.sln -p:UseSharedCompilation=false
dotnet build .\VSLoader.sln -p:UseSharedCompilation=false
```

如果 `VSLoader.exe` 被占用，先执行：

```powershell
Get-Process -Name VSLoader -ErrorAction SilentlyContinue | Stop-Process -Force
```

最终要求：

- 测试全部通过。
- 构建 `0 个错误`。
- 不引入新的无关警告。

## 18. 最终效果

用户后续只需要维护简单 CSV：

```csv
ModuleName,DisplayName
eap-sic-Jutze-3D-AOI,矩子3D-AOI
```

程序自动完成：

```text
读取 ZAM 模块名
查中文名
从文件夹名提取 No
拼成 DisplayName_No
```

最终：

```text
矩子3D-AOI_007
```

相比 v44，用户不再需要理解或维护：

```text
MatchType
Pattern
ModulePattern
NameTemplate
正则表达式
```
