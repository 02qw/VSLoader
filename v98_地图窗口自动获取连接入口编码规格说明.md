# v98 地图窗口自动获取连接入口编码规格说明

## 1. 背景

当前主界面已经有“自动获取连接”按钮，用于批量获取所有快捷项的 AdminUI `.jnlp` 连接文件。

用户在使用地图窗口时，如果发现某些节点尚未获取 AdminUI 连接，需要切回主界面点击“自动获取连接”，再回到地图窗口继续操作，流程不够顺。

因此需要在地图窗口顶部也增加一个“自动获取连接”按钮，作为同一功能的快捷入口。

## 2. 目标

在地图窗口顶部工具栏增加按钮：

```text
自动获取连接
```

点击后执行的效果必须和主界面的“自动获取连接”一致：

- 批量获取所有快捷项的 AdminUI 连接
- 使用现有 AdminUI 网络预检查
- 使用现有忙碌遮罩和进度展示
- 使用现有成功/失败提示
- 使用现有 `MainViewModel.DownloadAdminUiLinksCommand`

地图窗口不重新实现下载逻辑，只作为入口按钮。

## 3. 设计原则

### 3.1 复用主界面命令

当前主界面的自动获取连接逻辑已经集中在：

```csharp
MainViewModel.DownloadAdminUiLinksCommand
```

以及对应方法：

```csharp
DownloadAdminUiLinksAsync()
```

该逻辑已经包含：

- `IsBusy` 判断
- 无快捷项提示
- AdminUI 网络连接预检查
- 下载进度显示
- 下载结果统计
- 异常捕获
- Busy 状态清理

因此地图窗口按钮必须复用这套逻辑，不允许复制一份新的下载流程。

### 3.2 地图窗口只负责触发

`FactoryMapWindow` 只负责：

- 显示按钮
- 接收点击事件
- 通过委托通知 `MainWindow`

不直接依赖：

- `AdminUiService`
- `MainViewModel`
- `ConfigService`

这样可以避免地图窗口承担过多业务职责。

## 4. 影响范围

主要修改：

- `VSLoader/Views/FactoryMapWindow.xaml`
- `VSLoader/Views/FactoryMapWindow.xaml.cs`
- `VSLoader/MainWindow.xaml.cs`

可能新增或调整测试：

- 地图窗口动作相关测试
- 主窗口地图委托触发相关测试

不修改：

- `AdminUiService`
- `MainViewModel.DownloadAdminUiLinksAsync`
- 主界面“自动获取连接”按钮逻辑
- AdminUI 下载文件保存位置
- AdminUI 下载算法

## 5. UI 设计

### 5.1 按钮位置

按钮放在地图窗口顶部工具栏，和现有按钮同一行：

- 导入图文件
- 导出图文件
- 编辑锁
- 多选节点
- 连线
- 自动获取连接

建议放在“导出图文件”之后，编辑类按钮之前。

理由：

- “导入/导出/自动获取连接”都属于地图窗口上方的工具操作入口
- 不打断后续编辑模式按钮的逻辑分组

### 5.2 按钮文案

按钮文案：

```text
自动获取连接
```

和主界面保持一致，避免用户理解成本。

## 6. 代码设计

### 6.1 FactoryMapWindow 构造函数增加委托

当前 `FactoryMapWindow` 已经通过构造函数接收多个委托，例如：

- 选择快捷项
- 执行快捷项动作
- 保存地图布局
- 获取当前快捷项
- 获取地图布局路径
- 标记地图文件已使用

新增一个委托：

```csharp
private readonly Action downloadAdminUiLinks;
```

构造函数增加参数：

```csharp
Action downloadAdminUiLinks
```

按钮点击时调用：

```csharp
downloadAdminUiLinks();
```

### 6.2 MainWindow 传入主命令触发逻辑

`MainWindow` 创建 `FactoryMapWindow` 时传入：

```csharp
DownloadAdminUiLinksFromMap
```

新增方法：

```csharp
private async void DownloadAdminUiLinksFromMap()
{
    if (DataContext is not MainViewModel viewModel)
    {
        return;
    }

    if (viewModel.DownloadAdminUiLinksCommand.CanExecute(null))
    {
        await viewModel.DownloadAdminUiLinksCommand.ExecuteAsync(null);
    }
}
```

注意：

- 使用现有命令的 `CanExecute`
- 不直接调用私有方法
- 不复制下载逻辑

### 6.3 Busy 状态处理

主命令已有 `CanExecute = CanRunGlobalCommand`。

因此：

- 如果主界面正在忙，地图按钮点击后不会执行下载
- 不需要地图窗口自己维护 Busy 状态

如需进一步优化，可在地图按钮上根据外部状态禁用，但本次不强制。

### 6.4 异常处理

地图窗口按钮事件不吞业务异常。

异常应由 `MainViewModel.DownloadAdminUiLinksAsync()` 现有逻辑统一处理。

如果委托本身异常，可在 `MainWindow.DownloadAdminUiLinksFromMap()` 中捕获并显示错误，但一般不需要，因为命令内部已有保护。

建议保守实现：

```csharp
try
{
    await viewModel.DownloadAdminUiLinksCommand.ExecuteAsync(null);
}
catch (Exception ex)
{
    MessageBox.Show(this, $"自动获取连接失败：{ex.Message}", "VSLoader");
}
```

是否加这层由实现时结合现有 `DialogService` 可用性决定。

## 7. 行为要求

### 7.1 正常场景

用户在地图窗口点击“自动获取连接”：

- 主界面进入 Busy 状态
- 显示现有忙碌遮罩和进度
- 下载所有快捷项 AdminUI 连接
- 完成后显示现有结果提示

### 7.2 主程序忙碌中

如果主程序已经处于 Busy 状态：

- 点击地图按钮不应触发重复下载
- 不应导致崩溃或并发执行

### 7.3 地图窗口关闭或隐藏

地图窗口关闭/隐藏不影响下载逻辑。

因为下载由主窗口 ViewModel 执行。

### 7.4 无快捷项

复用主命令现有逻辑：

```text
当前没有快捷项可处理。
```

### 7.5 网络失败

复用主命令现有逻辑：

```text
网络连接失败，请检查 AdminUI BaseUrl、网络环境或 VPN。
```

## 8. 测试建议

建议覆盖：

1. `FactoryMapWindow` 点击按钮会调用传入委托。
2. `MainWindow` 地图按钮触发后会执行 `DownloadAdminUiLinksCommand`。
3. 当 `CanExecute` 为 false 时，不执行命令。
4. 主界面原有“自动获取连接”按钮行为不变。

如果 UI 事件测试成本较高，可优先测试委托方法或命令调用路径。

## 9. 验收标准

满足以下条件即完成：

- 地图窗口顶部出现“自动获取连接”按钮。
- 点击该按钮后，执行效果与主界面“自动获取连接”一致。
- 没有新增一套 AdminUI 下载逻辑。
- 主界面原按钮仍然可用。
- 忙碌状态下不会重复启动下载。
- `dotnet test .\VSLoader.sln -c Debug` 通过。
- `dotnet build .\VSLoader.sln -c Debug` 通过。

