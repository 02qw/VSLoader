# v122 主快捷键最小化前窗口层级快照编码规格说明

## 1. 背景

v121 已经把地图窗口和主窗口的恢复逻辑从“固定主窗口在前”改成了“按本次 session 中最后活跃窗口决定谁在前”。

但用户实际验证后发现，单纯记录最后活跃窗口仍然不够准确。

用户真正希望的是：

```text
主界面快捷键只负责隐藏/还原。
还原时应恢复隐藏/最小化之前那一刻的主窗口和地图窗口位置、大小、前后层级关系。
```

也就是说，主快捷键不是重新计算谁应该在前，而是还原“最小化前的状态快照”。

## 2. 问题描述

当前异常表现：

```text
1. 第一次按主快捷键恢复时，地图窗口在前。
2. 第二次按主快捷键隐藏/最小化程序。
3. 第三次按主快捷键恢复程序。
4. 实际结果：主窗口在上，地图窗口在下。
5. 期望结果：仍然恢复第二次隐藏/最小化之前的层级，地图窗口在上。
```

这个问题说明：

```text
“最后活跃窗口”会在隐藏/恢复过程中被 Activated 事件污染。
```

例如：

- 地图窗口隐藏时，主窗口可能被系统短暂激活。
- 主窗口恢复时，`Show()` 或 `Activate()` 可能触发 `MainWindow_Activated`。
- 如果恢复逻辑直接读取 `_lastActiveVsLoaderWindow`，就可能读到隐藏过程产生的新状态，而不是用户隐藏前的真实层级。

## 3. 根因分析

v121 的核心字段是：

```csharp
private VsLoaderForegroundWindow _lastActiveVsLoaderWindow = VsLoaderForegroundWindow.Main;
```

它表示：

```text
当前 session 中，最后一次被激活的是主窗口还是地图窗口。
```

但它不是稳定的“隐藏前快照”。

主快捷键隐藏流程大致为：

```text
保存主窗口位置
隐藏地图窗口
最小化或隐藏主窗口
```

在这些操作过程中，WPF/Windows 可能触发窗口激活事件，导致 `_lastActiveVsLoaderWindow` 被更新为 `Main`。

因此第三次恢复时如果继续用 `_lastActiveVsLoaderWindow`，就可能错误地把主窗口放到最前。

正确的数据应该是：

```text
执行主快捷键隐藏/最小化之前一刻的窗口层级快照。
```

## 4. 目标

本次目标：

- 主快捷键隐藏/最小化前，先记录当前主窗口和地图窗口的层级快照。
- 主快捷键恢复时，优先使用这份隐藏前快照决定最终谁在前。
- 即使隐藏/恢复过程中 `Activated` 事件改变了 `_lastActiveVsLoaderWindow`，也不能影响本次恢复层级。
- 每次再次隐藏/最小化时，都用当时的新层级覆盖旧快照。
- 继续保留 v121 的兜底规则：地图关闭、不可见、最小化时恢复主窗口。
- 不引入配置文件持久化。
- 不改变窗口大小、位置、地图缩放、地图视口等已有逻辑。

## 5. 非目标

本次不做以下事项：

- 不把层级快照写入 layout JSON。
- 不跨程序重启恢复主窗口/地图窗口前后层级。
- 不引入 Win32 Z-Order API。
- 不修改地图窗口节点、连线、右键菜单、缩放、滚动等功能。
- 不修改主窗口快捷项业务逻辑。
- 不修改地图快捷键打开/关闭地图的语义。
- 不调整窗口 UI 样式。

## 6. 推荐方案

### 6.1 保留最后活跃窗口记录

继续保留 v121 的状态：

```csharp
private VsLoaderForegroundWindow _lastActiveVsLoaderWindow = VsLoaderForegroundWindow.Main;
```

主窗口激活时：

```csharp
private void MainWindow_Activated(object? sender, EventArgs e)
{
    _lastActiveVsLoaderWindow = VsLoaderForegroundWindow.Main;
}
```

地图窗口激活时：

```csharp
private void FactoryMapWindow_Activated(object? sender, EventArgs e)
{
    _lastActiveVsLoaderWindow = VsLoaderForegroundWindow.FactoryMap;
}
```

这个字段继续代表用户实时操作中的最后活跃窗口。

### 6.2 新增隐藏前层级快照字段

在 `MainWindow.xaml.cs` 中新增：

```csharp
private VsLoaderForegroundWindow? _foregroundSnapshotBeforeMainHotkeyMinimize;
```

含义：

```text
主窗口被主快捷键、关闭到托盘、窗口最小化隐藏之前，主窗口和地图窗口谁在前。
```

使用可空类型的原因：

- 初始启动时还没有隐藏过。
- 没有快照时可以回退到 `_lastActiveVsLoaderWindow`。

### 6.3 统一捕获隐藏前快照

新增方法：

```csharp
private void CaptureForegroundSnapshotBeforeMainWindowHide()
{
    _foregroundSnapshotBeforeMainHotkeyMinimize = _lastActiveVsLoaderWindow;
}
```

所有会让主窗口和地图窗口一起隐藏/最小化的入口，在真正隐藏前调用该方法。

需要覆盖的入口：

```text
ToggleWindowFromHotkey() 中执行隐藏/最小化前
MainWindow_Closing() 中关闭到托盘前
MainWindow_StateChanged() 中主窗口进入 Minimized 前
```

示例：

```csharp
SaveMainWindowBoundsToSession();
CaptureForegroundSnapshotBeforeMainWindowHide();
HideFactoryMapWindow();
WindowState = WindowState.Minimized;
```

注意：

```text
必须先捕获快照，再隐藏地图窗口。
```

否则隐藏地图窗口本身可能改变激活状态。

### 6.4 恢复时优先读取快照

新增可测试静态方法：

```csharp
internal static VsLoaderForegroundWindow ResolveForegroundWindowForRestore(
    VsLoaderForegroundWindow? snapshotBeforeMinimize,
    VsLoaderForegroundWindow currentLastActiveWindow)
{
    return snapshotBeforeMinimize ?? currentLastActiveWindow;
}
```

恢复逻辑改为：

```csharp
private void RestoreAndActivate()
{
    Dispatcher.Invoke(() =>
    {
        var preferredForegroundWindow = ResolveForegroundWindowForRestore(
            _foregroundSnapshotBeforeMainHotkeyMinimize,
            _lastActiveVsLoaderWindow);

        RestoreMainWindowForHotkey();
        ShowFactoryMapIfNeeded();
        RestorePreferredForegroundWindow(preferredForegroundWindow);
    });
}
```

这样即使 `RestoreMainWindowForHotkey()` 中的 `Show()` 触发 `MainWindow_Activated`，也不会改变本次恢复使用的 `preferredForegroundWindow`。

### 6.5 不主动清空快照

恢复完成后不建议立刻清空：

```text
_foregroundSnapshotBeforeMainHotkeyMinimize = null;
```

原因：

- 用户可能在本次 session 中反复按主快捷键隐藏/恢复。
- 下一次隐藏前会重新调用 `CaptureForegroundSnapshotBeforeMainWindowHide()` 覆盖快照。
- 如果恢复后立刻清空，在某些托盘恢复或系统状态切换场景下可能退回到被污染的 `_lastActiveVsLoaderWindow`。

因此快照生命周期为：

```text
首次隐藏前生成
每次隐藏前覆盖
程序退出时自然释放
```

## 7. 边界情况

### 7.1 没有快照

如果程序启动后还没有经历过主快捷键隐藏/最小化：

```text
_foregroundSnapshotBeforeMainHotkeyMinimize == null
```

恢复逻辑应回退到：

```text
_lastActiveVsLoaderWindow
```

### 7.2 快照是地图，但地图已关闭

即使快照记录的是：

```text
FactoryMap
```

如果地图窗口已经关闭或为空：

```text
_factoryMapWindow == null
```

仍然必须兜底激活主窗口。

这部分继续由 v121 的判断方法处理：

```csharp
ShouldActivateFactoryMapAfterHotkeyRestore(...)
```

### 7.3 快照是地图，但地图被用户最小化

如果地图窗口是用户主动最小化到任务栏：

```text
WindowState == WindowState.Minimized
```

恢复主窗口时不应强行拉起地图窗口。

此时兜底激活主窗口。

### 7.4 隐藏过程中 Activated 事件触发

这是本次修复的关键边界。

即使隐藏过程中发生：

```text
MainWindow_Activated()
```

并把 `_lastActiveVsLoaderWindow` 改成 `Main`，恢复时仍然应使用隐藏前保存的快照。

### 7.5 下一次隐藏必须覆盖旧快照

用户可能改变了窗口层级：

```text
第一次隐藏前：地图在前
恢复后用户点击主窗口
第二次隐藏前：主窗口在前
```

第二次隐藏时必须覆盖旧快照。

下次恢复应主窗口在前。

## 8. 影响范围

主要修改：

```text
VSLoader/MainWindow.xaml.cs
```

测试修改：

```text
VSLoader.Tests/MainWindowFactoryMapCloseTests.cs
```

不修改：

```text
VSLoader/Views/FactoryMapWindow.xaml
VSLoader/ViewModels/MainViewModel.cs
VSLoader/Models/Services/*
VSLoader/Styles/*
workspace 配置文件
layout 配置文件
```

## 9. 编码要求

### 9.1 快照必须在隐藏前捕获

必须遵守顺序：

```text
保存窗口位置
捕获前台快照
隐藏地图窗口
隐藏或最小化主窗口
```

不能在 `HideFactoryMapWindow()` 后捕获快照。

### 9.2 恢复前先解析快照

`RestoreAndActivate()` 中必须先解析：

```csharp
var preferredForegroundWindow = ResolveForegroundWindowForRestore(...);
```

再执行：

```csharp
RestoreMainWindowForHotkey();
ShowFactoryMapIfNeeded();
RestorePreferredForegroundWindow(preferredForegroundWindow);
```

不要在 `Show()` 后再读取 `_lastActiveVsLoaderWindow`。

### 9.3 不写入配置文件

`_foregroundSnapshotBeforeMainHotkeyMinimize` 只是运行时状态。

不要写入：

```text
layout.json
appsettings.json
workspace 配置
```

### 9.4 不改变地图自身快捷键语义

地图快捷键仍然只负责打开/关闭地图。

本次只调整主窗口快捷键隐藏/恢复主窗口和地图窗口组合时的层级快照。

## 10. 测试要求

### 10.1 快照优先级测试

新增测试：

```csharp
[Fact]
public void ResolveForegroundWindowForRestore_prefers_snapshot_from_before_hotkey_minimize()
```

断言：

```text
snapshotBeforeMinimize = FactoryMap
currentLastActiveWindow = Main
结果为 FactoryMap
```

这个测试用于证明：

```text
隐藏过程中即使最后活跃窗口被改成 Main，恢复时仍然按隐藏前快照恢复地图在前。
```

### 10.2 无快照回退测试

新增测试：

```csharp
[Fact]
public void ResolveForegroundWindowForRestore_falls_back_to_current_last_active_when_snapshot_is_missing()
```

断言：

```text
snapshotBeforeMinimize = null
currentLastActiveWindow = Main
结果为 Main
```

以及：

```text
snapshotBeforeMinimize = null
currentLastActiveWindow = FactoryMap
结果为 FactoryMap
```

### 10.3 继续保留 v121 判断测试

继续保留：

```text
ShouldActivateFactoryMapAfterHotkeyRestore_only_activates_visible_non_minimized_last_active_map
```

确保：

- 快照是地图但地图关闭时，主窗口兜底。
- 快照是地图但地图不可见时，主窗口兜底。
- 快照是地图但地图最小化时，主窗口兜底。

### 10.4 人工验证

必须人工验证：

```text
场景 1：
地图窗口在前 -> 按主快捷键隐藏 -> 再按主快捷键恢复
期望：地图窗口仍在前。

场景 2：
地图窗口在前 -> 按主快捷键隐藏 -> 再按主快捷键恢复 -> 再按主快捷键隐藏 -> 再按主快捷键恢复
期望：每次恢复都是地图窗口在前。

场景 3：
地图窗口在前 -> 恢复后点击主窗口让主窗口在前 -> 按主快捷键隐藏 -> 再恢复
期望：主窗口在前。

场景 4：
地图窗口最小化到任务栏 -> 按主快捷键恢复主程序
期望：不强行拉起地图窗口。
```

### 10.5 构建验证

执行：

```powershell
dotnet test .\VSLoader.Tests\VSLoader.Tests.csproj -c Debug --no-restore
```

执行：

```powershell
dotnet build .\VSLoader.sln -c Debug --no-restore
```

如果 Debug 输出目录被正在运行的 VSLoader 锁定，可以临时使用：

```powershell
dotnet test .\VSLoader.Tests\VSLoader.Tests.csproj -c Debug --no-restore -p:BaseOutputPath=.\artifacts\test-output\
dotnet build .\VSLoader.sln -c Debug --no-restore -p:BaseOutputPath=.\artifacts\test-output\
```

但最终需要覆盖 Debug 目录时，必须先退出正在运行的 VSLoader。

## 11. 验收标准

满足以下条件即完成：

- 主快捷键隐藏/恢复不会丢失隐藏前的主窗口/地图窗口层级。
- 地图在前时，连续多次按主快捷键隐藏/恢复，地图都保持在前。
- 用户恢复后改成主窗口在前，再次隐藏/恢复时主窗口保持在前。
- 隐藏/恢复过程中的 `Activated` 事件不会污染本次恢复结果。
- 地图关闭、不可见、最小化时仍兜底主窗口。
- 不新增配置文件字段。
- 不改变窗口位置、大小、地图视口和缩放逻辑。
- 相关测试通过。
- Debug 构建通过。

## 12. 风险与注意事项

### 12.1 快照不是实时状态

`_foregroundSnapshotBeforeMainHotkeyMinimize` 表示隐藏前状态，不表示当前状态。

不要用它替代 `_lastActiveVsLoaderWindow`。

### 12.2 快照和可激活性要分开

快照只回答：

```text
用户隐藏前希望谁在前
```

是否真的能激活地图，仍由当前窗口状态决定：

```text
地图是否打开
地图窗口是否存在
地图是否可见
地图是否未最小化
```

### 12.3 不要清空快照导致回退污染状态

恢复后不要急着清空快照。

下一次隐藏前会覆盖它。

这样更符合用户对“连续隐藏/恢复”的直觉。
